# Research04 认识清单与清理快照

## 目标与判断

目标是回答直接神经估计的条件优势、训练域依赖和失效边界。当前证据足以支持条件点估计比较、低β外推反转、同中心扩宽局部代价及参数—寿命差异；不能分离目标/选点、逼近、优化和求解结构，也不能支持自动回退、区间覆盖、真实场景或大网络替代。

## lessons

- 旧 E09-2—11 是早期 raw/feature、分类、混合和范围探索；其“BP-feature工程首选”等表述已被当前协议替代。保留在 Git 标签中，不作为当前结论。
- 同宽平移同时改变真实β；只能描述窗口相对位置，不能解释为位置因果效应。
- 风险诊断 AUC 0.737/0.726/0.711，同时保留失效信号0.468/0.306；候选规则未独立确认。

## retained

当前工作树保留：v0.2正文/附录、9图及源数据清单、正式汇总CSV/manifest/报告/核验记录、13个代码入口与5个测试。主目录 `D:\weibull` 保留本项目正式传统参照和探索诊断逐样本文件。原同中心实验在 `C:\Users\36089\.codex\worktrees\6615\weibull\Research\04-直接NN参数估计\artifacts\training_domain_width_location_v1`，约296.9 MB，48模型、1,512,000行；只读盘点未迁移。

基础训练扫描/模型/逐样本与先行嵌套正式模型未在当前工作树或主目录定位；这限制端到端重跑，不影响已核验汇总的引用边界。

## removed

清理前工作树 Research04 为386文件、35,817,537 bytes；清理后为166文件、8,493,315 bytes。删除本项目活动目录的 `artifacts/smoke/`（12,043,570 bytes）及 `history/E09-旧直接估计范式比较/`（15,534,540 bytes）；旧 v0.1稿和其旧图不再作为当前入口，恢复而非继续维护。主目录仅删除已确认的本项目缓存（`__pycache__`）和两份未跟踪v0.2 PDF，共22文件、3,169,768 bytes；删除前逐文件备份。

## restore

- Git快照：标签 `research04-before-lean-20260908`，基点 `fc4f531f5a2dc4fd42ba90e175430e926a3e03bb`。
- tracked ZIP：`D:\weibull-backups\research04-20260908\research04-tracked-before.zip`，SHA256 `9b3fad4f38a8dc31e9c6a75aeea18e7d5c2eabe8884a60ef2b42642148c631a9`。
- 清理前清单：`D:\weibull-backups\research04-20260908\inventory-before.json`；主目录额外文件副本与清单：`main-extra-before/`、`main-extra-manifest.json`。
- 未删除的同中心原始封存保留原 worktree；删除 smoke/E09 后可从标签或ZIP恢复。

## checks

已执行 `python Research/04-直接NN参数估计/code/verify_manuscript_v02.py`（应为201项数值、37个本地链接），`python -m pytest Research/04-直接NN参数估计/tests -q -p no:cacheprovider`，以及 `git diff --check`。本轮不启动正式训练或图像重绘；删除后的最终文件数、占用、链接和测试结果在提交前更新。

## next

若只报告条件性能与边界，Research04可维持ACTIVE并停止扩展；若必须解释优势来源，只做一项对齐目标/选点和内部划分的有界对照。自动回退、区间覆盖、真实数据和网络容量扫描后置。
