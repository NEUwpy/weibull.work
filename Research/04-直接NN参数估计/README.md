# 直接神经估计与传统估计路线比较

> 成熟度：`ACTIVE`
> 关系角色：`INDEPENDENT`
> 当前阶段：传统方法同协议参照已补齐；训练域宽度与位置分离实验已预约

## 研究目的

本 Research 比较三参数 Weibull 小样本估计中的两类路线：一类由 MDM、WMLE、LSE 等方法保留既有统计结构，另一类由神经网络直接从观测样本输出参数。研究不以宣布某一方法普遍优越为目标，而是回答：直接神经估计在什么条件下能够取得优势，优势来自哪里；训练范围、测试位置和样本量改变后，优势是否仍然存在。当前工作止于比较与适用边界解释，不继续开发自动回退规则。

这项工作独立保存在 `Research/` 中。它可以为 Study01 和 Study02 提供方法参照，但不参与决定两篇论文的主结论，也不跨不同实验协议拼接方法排名。

## 核心科学问题

1. 在相同样本、相同参数域和相同评价指标下，直接神经估计与结构化估计方法的参数精度、稳定性、失败率和寿命分位点误差有何差异？
2. 网络对训练网格上的参数点、训练范围内未见参数点、邻近域外点和远域外点分别表现如何？
3. 训练参数域的宽度、位置、样本密度和样本量怎样影响域内精度与域外覆盖之间的权衡？
4. 直接神经估计的优势主要来自模型表达能力、训练分布提供的先验约束，还是输入表示与损失函数的选择？
5. 可观测样本形态能否解释部分高风险输入？已有诊断保留为探索材料，自动回退与部署阈值不属于当前必做工作。
6. 如果直接神经估计在多数域内条件下更准，为什么不能依靠更大的网络替代所有 Weibull 参数估计方法；这种替代需要哪些前提，传统方法还承担什么不可由模型容量自动解决的作用？

## 与既有工作的关系

杨小玉等关于 BPNN 直接估计三参数 Weibull 分布的工作提供了文献起点：它说明直接从次序样本估计三个参数是可行路线。现有文献和本项目早期实验主要关注给定训练域内的估计精度；本 Research 进一步把比较扩展到未见参数点、双侧域外、样本量、尺度和训练域设计，并把“何时有效、何时失效以及怎样使用”作为完整问题。既有高保真重构只保留为历史线索，文献方法与结论应回到完整原文核对。

## 研究框架与当前进度

| 模块 | 要回答的问题 | 当前证据 | 状态 |
|---|---|---|---|
| 文献与复现锚点 | 既有直接神经估计采用了什么输入、网络、训练域和评价方式 | 已有 Study02 前置研究 A 和旧 E09 重构材料 | 待取得或定位完整原文后更新审计 |
| 同协议总体比较 | Direct-P 与 MDM、WMLE、LSE 在相同测试面板上的 Bias、SD、RMSE、$J_1$、失败率和寿命分位点误差如何 | WMLE/LSE 已新增 252,000 行，和原三方法按 126,000 样本设计统一汇总；原三方法读取封存统计 | 已完成 |
| 域内与域外泛化 | 训练点、域内未见点、近域外和远域外是否出现不同规律 | 126,000 个共享测试样本和双侧 $\beta$ 外推已完成 | 已完成 |
| 样本量效应 | 小样本量改变后两类路线的相对优势是否稳定 | $n=7,10,15,20$ 分层结果已完成 | 已完成 |
| 训练域设计 | 训练范围扩大是否必然更好，范围宽度与位置各起什么作用 | 嵌套宽度、固定总量和固定单元密度已完成；同宽区间平移尚未完成 | 部分完成 |
| 尺度与输入表示 | 单位或整体尺度改变是否改变网络结论 | $\eta=1$ 至 $10^6$ 的比例尺度等变审计已完成 | 已完成 |
| 机制解释 | 为什么域内直接估计较强、低 $\beta$ 域外发生反转 | 已有训练先验、输入几何、形状参数敏感度和参数补偿证据 | 部分完成 |
| 风险诊断（探索） | 哪些样本形态与 Direct-P 高风险有关 | 已有变异系数、训练支持距离和最大值/均值诊断；保留原结果，不继续独立回退验证 | 本轮停止扩展 |
| 区间层评价 | 估计区间是否覆盖真值，区间有多宽 | 尚未开展，不用点误差替代覆盖评价 | 后置 |

## 已形成的主要认识

- 在当前对齐的训练域和测试协议内，Direct-P 在已见网格和域内未见点上均优于自适应 MDM，说明它不只是记忆离散训练点。
- 这种优势不是无条件的：低 $\beta$ 侧外推会发生反转，高 $\beta$ 侧在当前测试上限内仍保持优势，域外风险具有方向性。
- 扩大训练域能够改善远点覆盖，但会降低共同局部区间内的参数精度；增加训练样本密度没有消除这一代价，训练分布本身就是模型的一部分。
- 样本量增加时，两类路线都会改善，但结构化 MDM 的改善更快，直接神经估计的相对优势会收窄。
- 比例缩放下的尺度等变已经通过结构化输入输出设计实现；这不等于网络对任意测量条件变化都具有稳健性。
- 加入 WMLE/LSE 后，Direct-P 的域内优势仍然成立。当前 WMLE 实现有 9.615% 的样本未通过方程残差判据，LSE 失败率为 0%；比较同时报告失败率与成功估计的参数误差，不把求解实现的表现概括为所有同名方法的性能。
- 在 42,000 个连续参数测试样本上，样本变异系数、训练支持距离和最大值/均值对“Direct-P 劣于 MDM”的识别 AUC 分别为 0.737、0.726 和 0.711。以验证集第 90 百分位作描述性回退阈值时，样本变异系数规则在保留 82.1% Direct-P 覆盖的同时将总体 $J_1$ 从 0.4721 降至 0.4347；该阈值仍需新样本独立确认。

## 统一解释框架

### 同协议方法参照（2026-09-04）

下表为 J1，越小越好；训练域 beta=1.5..5.0，其余参数和样本量见协议。远域外合并了左右两侧，方向性仍应查分 beta 结果。

| 方法 | 已见网格 | 域内未见 | 近域外 | 远域外 |
|---|---:|---:|---:|---:|
| Direct-P | 0.3658 | 0.3532 | 0.4873 | 0.7587 |
| 自适应 MDM | 0.5696 | 0.5648 | 0.6188 | 0.6840 |
| 固定 MDM-0.1 | 0.6285 | 0.6202 | 0.6972 | 0.7585 |
| WMLE | 0.7731 | 0.7721 | 0.8219 | 1.0065 |
| LSE | 0.8793 | 0.8843 | 0.8595 | 0.8698 |

完整 Bias/SD/RMSE、失败率与 x0.90/0.95/0.99 统计见 [同协议比较报告](artifacts/traditional_aligned_v1/report.md)。Study01 旧参照采用同一方法入口，但样本设计与实现版本不同，旧数值保留在原位，不作为这张表的数据来源。

### 训练分布与点决策

设训练时采用的参数与样本分布为 $\pi_{\mathrm{train}}$，最终评价损失为 $L$。只观察样本 $X$ 时，理论上的最优点决策是

$$
a_L^*(X)=\arg\min_a\operatorname E_{\pi_{\mathrm{train}}}\!\left[L(a,\theta)\mid X\right].
$$

参数平方损失下，它退化为训练分布条件下的参数均值。这个表达把当前四类现象连在一起：

- **有限样本不可辨识：** 同一小样本形态可以与多组参数相容，网络只能在这些候选参数之间作条件折中。扩大网络可以减小函数逼近和优化误差，但不能补回样本没有提供的信息。
- **训练分布决定折中：** 改变训练参数域及其权重，会改变条件分布 $\pi_{\mathrm{train}}(\theta\mid X)$，因而也会改变最优点预测。现有训练域宽度实验已经给出这一效应的经验依据。
- **任务目标决定答案：** 改变 $L$，最优动作 $a_L^*(X)$ 也会改变。参数恢复、单个寿命点和整条寿命曲线不是同一个估计任务；Study02 的 P/Q/QCP 结果提供了直接例证。
- **点预测不等于不确定性：** $a_L^*(X)$ 只给出一个动作，条件分布的宽度和输入是否脱离训练支持需要另行评价。网络容量本身不会自动产生经过校准的风险提示。

因此，足够大的网络可以在稳定且匹配的应用域内取代日常点估计步骤，但不能仅凭容量取消训练域、任务目标和不确定性边界。传统方法的作用也不应表述为“任何情况下都更准”，而是提供不依赖神经网络训练支持范围的结构化求解、外部参照和必要时的回退路线。

## 下一步最小充分工作

1. **传统方法同协议比较已完成。** WMLE/LSE 在 126,000 个共享测试样本上完成，运行 22.8 分钟。沿用 Research04 的样本、指标与失败规则，16 项相关测试与独立数值检查通过。执行/分析入口为 `code/run_traditional_aligned.py`、`code/analyze_traditional_aligned.py`，产物为 `artifacts/traditional_aligned_v1/`；不加入 MLE。
2. **分离训练范围的宽度与位置。** 同中心扩宽采用 $[2.5,3.5]$、$[2,4]$、$[1,5]$、$[0.5,5.5]$；同宽平移采用 $[1.5,2.5]$、$[2.5,3.5]$、$[3.5,4.5]$。固定总训练量和固定单元密度分开比较，seed=42，复用 Direct-P；已安排本机北京时间 2026-09-05 00:30 在独立项目任务执行。详细协议见 `02-连续参数域与OOD正式实验计划-v0.1.md` §11。
3. **不继续回退规则。** 保留现有候选诊断，独立阈值验证和自动切换不是当前完成条件。
4. **区间覆盖评价后置。** 后续另行决定区间构造与比较方式；当前结论限定为点估计，不扩展成区间可靠性结论。
5. **收口研究表达。** 传统方法和训练域实验完成后，压缩为“适用区间—优势来源—失效边界”的结果与解释，并同步本目录稿件和组会第二部分。

## 完成条件

当本 Research 能够在同一实验协议下明确回答以下四句话时即可停止继续扩展：

1. 直接神经估计在哪些范围内优于结构化传统估计；
2. 这一优势主要为什么产生；
3. 它在什么条件下减弱或反转；
4. 为什么单纯增大网络不能取消训练分布与任务目标的影响；可观测风险诊断仅作补充，不要求交付自动选择或回退系统。

无需穷举所有网络结构、所有传统估计器或所有可能的域外扰动，也不为本 Research 新建通用实验平台。

## 已有证据与材料

- Study01 Direct-MLP/P3/P4 的代码、测试和封存产物保持在 Study01 原位；本目录仅提供研究入口，不复制正式 evidence。
- Study02 前置研究 A 保持嵌入 Study02；它是 Study02 路线冻结的支撑材料，不搬动 formal artifacts。
- `history/E09-旧直接估计范式比较/` 是旧 `docs/research/09两种神经网络直接估计/` 原字节迁入的完整实验包。该包存在单模型 seed、数据命名空间和文档结论不一致等边界，只能作为历史探索。
- 连续参数域与 OOD 的正式比较口径见 [`02-连续参数域与OOD正式实验计划-v0.1.md`](02-连续参数域与OOD正式实验计划-v0.1.md)。正式轮以当前 Study01 Mean-Normalized-MLP + MDM 为结构保留路线，并为 Direct 网络加入尺度等变输出。
- Study01 对齐泛化实验入口为 [`code/run_study01_aligned_generalization.py`](code/run_study01_aligned_generalization.py)。训练使用 Study01 的 48,000 样本设计和种子 42；独立测试包含已见网格、域内未见点、近域外和远域外共 126,000 个共享样本。Smoke 与正式结果分别写入 `artifacts/smoke/study01_aligned_generalization_v1/` 和 [`artifacts/study01_aligned_generalization_v1/`](artifacts/study01_aligned_generalization_v1/)。
- 正式结果已经完成：Direct-P 在已见网格与域内未见点的联合误差相对自适应 MDM 分别下降 35.8% 和 37.5%，对应的 $x_{0.95}$ RMSE 分别下降 14.9% 和 16.5%；低 $\beta$ 外推发生反转，高 $\beta$ 外推在测试上限 5.75 处仍保持优势。完整统计、配对置信区间和机制分解见 [`artifacts/study01_aligned_generalization_v1/analysis/report.md`](artifacts/study01_aligned_generalization_v1/analysis/report.md)。
- 样本量分层已经补齐：$n=7$ 至 20 时两类路线均改善，自适应 MDM 的改善更快，Direct-P 的域内相对优势由 41.3% 收窄到 28.3%。大 $\beta$ 机制诊断显示 MDM 的形状方向敏感度随 $1/\beta^2$ 衰减，且 $\eta$ 与 $\gamma$ 误差呈强负相关补偿。
- 训练域宽度实验已经补齐：Direct-P 分别在 $\beta\in[2,3]$、$[1.5,3.5]$ 和 $[1.5,5]$ 上训练，并共享 $0.75$ 至 5.75 的正式测试样本。固定总训练样本量时，宽域模型在共同区间 $[2,3]$ 的 $J_1$ 比窄域高 32.3%；固定每个参数单元的样本密度后，宽域使用 2.67 倍总训练量，增幅仍为 31.5%。训练域扩大因而牺牲局部参数精度、改善远点覆盖，主要代价落在 $\beta$ 恢复；$x_{0.95}$ 因参数补偿只小幅变化。完整结果见 [`artifacts/training_domain_width_v1/analysis/report.md`](artifacts/training_domain_width_v1/analysis/report.md)。
- 三个训练域的点类型已单独汇总：训练网格间距为 0.5，正式测试间距为 0.25，因此每个区间均覆盖训练点、域内非训练点、左右紧邻域外点和更远域外点。代表结果与解释见 [`artifacts/training_domain_width_v1/analysis/point_type_report.md`](artifacts/training_domain_width_v1/analysis/point_type_report.md)。
- 多尺度等变审计已经补齐：四个正式 Direct-P 模型不重新训练，将全部 126,000 个测试样本分别成对缩放到 $\eta=1,10,100,1000,10^4,10^6$，六个尺度的 $J_1=0.4705367$、$x_{0.95}$ RMSE=0.2060071、失败率=0.05476% 均保持不变；参数缩放还原后的最大数值差为 $9.1\times10^{-13}$。结果见 [`artifacts/scale_equivariance_v1/report.md`](artifacts/scale_equivariance_v1/report.md)。
- 正式中文稿件为 [`03-稿件-直接神经估计与结构保留路线比较-v0.1.md`](03-稿件-直接神经估计与结构保留路线比较-v0.1.md)，包含方法、结果、机制、图题、表题和复现入口。
- 结果分析与作图入口为 [`code/analyze_study01_aligned_generalization.py`](code/analyze_study01_aligned_generalization.py)；数值表保留为 CSV，图形同时输出 PNG 与 PDF。
- 训练域宽度实验与作图入口分别为 [`code/run_training_domain_width.py`](code/run_training_domain_width.py) 和 [`code/analyze_training_domain_width.py`](code/analyze_training_domain_width.py)；正式清单位于 [`artifacts/training_domain_width_v1/manifest.json`](artifacts/training_domain_width_v1/manifest.json)。

## 原位入口

- Study01 P3 配置：`../../Study/01-study-MDM最小偏移量优化研究/code/p3_config.py`
- Study01 P4 配置：`../../Study/01-study-MDM最小偏移量优化研究/code/p4_config.py`
- Study01 P4 封存产物：`../../Study/01-study-MDM最小偏移量优化研究/artifacts/formal/p4_formal_compare/`
- Study02 前置研究 A：`../../Study/02-study-NN参数估计与分位点目标研究/06-A-前置研究报告.md`

不同材料只有在数据、输入、目标、切分、指标和随机性口径一致时才能比较；当前入口不把它们合并成一个统一排行榜。

## 当前结论边界

- 当前正式轮固定一个模型种子 42，能够回答冻结训练协议下的域内插值、方向性外推和训练域宽度权衡，不代表网络初始化的总体稳定性。
- 尺度审计验证的是 $\eta,\gamma$ 同比例变化下的确定性尺度等变，不覆盖绝对测量噪声、截断阈值或传感器分辨率不随尺度同比例变化的情况。
- 当前三个训练域是嵌套区间，尚未系统平移同一宽度的区间；因此可以说明当前区间的宽度—覆盖权衡，但不能把宽度效应与区间位置效应完全分离。
- Bias、SD、RMSE 与 $x_{0.95}$ 指标已经报告；区间覆盖率尚未验证。
- 低 $\beta$ 侧的反转限定了当前 Direct-P 的适用边界；高 $\beta$ 侧未在本轮范围内观察到反转。本研究不据此声称已有可靠的 OOD 触发或自动回退规则。
