# Study01 前史

本目录保存形成当前 Study01 之前的完整研究包：

- `03-MDM最佳偏移量早期研究/`：旧参数化、旧网格和旧随机协议下的偏移量探索；
- `04-NN自适应偏移量早期整合路线/`：把早期指标、求根、偏移量和 NN 路线合并的大型历史包；
- `README-旧Study01-Research入口.md`：Study01 曾经使用的本地 Research 入口原文。

旧 04 内部状态文档互相矛盾，且多份脚本硬编码旧 `docs/research` 路径。本次将它作为一个不可拆分的历史包保存，不从中挑选片段拼成当前证据，也不修改旧脚本冒充原版本可复现。
