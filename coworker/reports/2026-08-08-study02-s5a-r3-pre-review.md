# Study02 S5A R3 Codex/Mentor 预审

- 审核分支：`codex/study02-paper-20260806`
- 审核 tip：`12e686b7b6192735a390edeac62ec229b67a0928`
- 审核范围：S5A R2 两项修订及论文/项目状态的一致性
- 结论：**REVISE（极窄文字与状态同步；不新增实验、不重训、不改 sealed 证据）**

## 已接受并关闭

1. `S5-001` 的核心修订已成立：正文已明确区分“由精确恒等式直接支持的结果空间机制”与“尚未检验的训练动力学原因”，不再把抵消现象直接写成训练因果。
2. `S5-003` 已成立：SPO/SPO+ 主张准确；Koenker–Bassett 只作类比；无检索支持的优先权主张已删除；两条无关引用填充已删除并重编号。
3. 引用双向检查为 11/11，`0 orphan / 0 unused`；`python -m pytest code/study02pq -q` 为 `72 passed`；`git diff --check` clean。

## 必须完成的最后一次极窄修订

### S5-005（blocking）：仍有少量证据边界表述过强

只改措辞，不改数字、图或实验：

1. `14-PQ-论文初稿.md` §3.2、§4.2、§4.5 中仍有“低敏感度区域 P 的准确参数已足够，Q 的欠定解略输”等因果式解释。现有证据只支持区域关联。统一改为：高敏感度区域对应更大的 Q 相对收益，低敏感度区域方向可转为 P 较优；原因未由本实验识别。
2. 摘要/Abstract/结论中的“收益集中于小样本”须限定为：在本文测试的 `n=7–20` 小样本范围内，相对改善在 `n=7–10` 较大。本文没有大样本对照，不能作跨小/大样本判断。
3. 摘要/Abstract/结论中的“在网络容量上稳健/成立”须限定为：在所测容量检查中方向一致；E2 仅 folds `{1,3}`，属于描述性检查，不得写成一般容量稳健性。
4. “任务对齐在显式可微目标函数上更高效”仍易被读成已证一般机制。改为证据级表述：在冻结合同内，Q 获得较低目标误差并呈现上述代数误差结构；显式可微仅是本实现能直接反向传播的条件。

中英文摘要、讨论、实践含义和结论应使用同一边界，不新增新的机制解释。

### S5-006（blocking）：权威状态文档已滞后

1. 根 `README.md` 当前仍把 Study02 写成 24-fit pilot、120-fit 正式实验尚未完成；须同步为 S1–S4 已完成并获审，S5A 正在预审修订，且保留“未投稿、未合并 main”。根 README 是项目唯一权威入口，这一漂移不能留到最终批准之后。
2. Study02 `README.md` 把初稿写成“13 条核验文献”，应改为 11 条，并同步当前 S5A R3 状态。
3. `08-PQ-论文研究蓝图与阶段清单.md` 的 S5A 记录仍写 13 条文献和首轮 checkpoint；补记 R2 tip `12e686b7` 与本次 R3 极窄 revise 状态。
4. `16-PQ-引用与完整性审计.md` 保留 R2 基线说明，同时补充当前审计 tip/修订状态，避免审计对象看起来仍停在 `033c68c7`。

## 修订后验证与回报

- 仅允许论文与上述状态/审计文档的必要文字修改；不新增实验、不重训、不改任何 sealed 数值、图、配置、代码或 Study01。
- 运行 `git diff --check`、零 orphan 脚本、`python -m pytest code/study02pq -q`。
- 报告 baseline/tip、changed files、逐条响应 S5-005/S5-006、测试结果、local==remote、工作树状态。
- 提交并推送后状态写 `READY FOR CODEX/MENTOR S5A R4 PRE-REVIEW`，不得自行 APPROVE，不启动模拟 reviewer。

## Mentor 判断

科学问题—方法—证据—判断主链已经完整，主实验、机制分解、可信性和边界证据无需扩张。本轮剩余问题不会改变 3.1% 主数值或边界实验结论，但会影响读者对因果性、样本量范围和容量稳健性的理解，因此应在 S5A 批准前一次性收口。完成后若复核无新问题，可结束 S5A 预审并回到用户 checkpoint，再决定是否进入模拟审稿阶段。
