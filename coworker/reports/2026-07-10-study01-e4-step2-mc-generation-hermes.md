# Study/01 E4 — Step 2 MC Generation Report

> Date: 2026-07-10
> Executor: Hermes (executor role)
> Plan: `coworker/plans/2026-07-10-study01-e4-staged-execution.md`
> Codex review: `coworker/reviews/2026-07-10-study01-e4-step1-preflight-codex.md` — APPROVE Step 2 with cleanup gate
> Status/Handoff: `Study/01-study-MDM最小偏移量优化研究/E4-validation-suite-状态交接.md`

---

## Verdict: APPROVE for Step 3 reference analysis

MC generation completed successfully. All row counts and combo coverage match expected values exactly. Zero failures. Output in correct directory. Sealed artifacts verified untouched. Ch1-Ch6/README modifications present in working tree are not attributable to Step 2 (MC script only writes CSVs).

---

## 1. Cleanup Action

**Executed:** Deleted misplaced untracked `Study/artifacts/` directory.

Command used:
```
rm -rf Study/artifacts/
```

Authorization: User dispatch included "Cleanup Study/artifacts approved".

Also removed 4 stale 0-byte chunk files that were accidentally committed in `8103587`:
```
rm -f "Study/01-study-.../artifacts/formal/E4_robustness/"chunk_*.csv
```
(These were artifacts from a previous aborted run that Codex's commit `8103587` inadvertently included.)

---

## 2. Command and Execution

**Command:**
```
cd /d/weibull && PYTHONUNBUFFERED=1 python "Study/01-study-MDM最小偏移量优化研究/code/run_E4_mc_generation.py"
```

**Exit code:** 0

**Start time:** ~09:15 (2026-07-10)
**End time:** ~11:10 (2026-07-10)
**Total elapsed:** 6919.8s (115.3 min) — per script output

**Worker breakdown (from script output):**
- Worker 0: 117000 rows (9 combos)
- Worker 1: 117000 rows (9 combos)
- Worker 2: 104000 rows (8 combos)
- Worker 3: 104000 rows (8 combos)
- Total: 442000 rows (34 combos × 500 repeats × 26 deltas)

---

## 3. Output Files

| File | Path | Size | Rows (data) |
|---|---|---|---|
| boundary_risk_curves.csv | `Study/01-study-.../artifacts/formal/E4_robustness/` | 35,533,656 bytes (33.9 MB) | 260,000 |
| offgrid_risk_curves.csv | `Study/01-study-.../artifacts/formal/E4_robustness/` | 25,194,298 bytes (24.0 MB) | 182,000 |

Chunk files (chunk_00.csv through chunk_03.csv) were automatically deleted by the merge step.

No manifest.json or run_log.txt was generated by `run_E4_mc_generation.py` (the script does not produce these — they are produced by `run_E4_formal_validation.py` in Step 3+).

---

## 4. Row Count Verification

### boundary_risk_curves.csv

| Check | Expected | Actual | Status |
|---|---|---|---|
| Data rows | 20 × 500 × 26 = 260,000 | 260,000 | PASS |
| Combo IDs | B01-B20 | B01-B20 (all 20 present) | PASS |
| R per combo | 500 | 500 (all 20 combos) | PASS |
| Delta values | 0.00, 0.02, ..., 0.50 (26 points) | All 26 present | PASS |
| Beta values | 1.2, 1.5, 2.0, 2.5, 4.0, 6.0 | All 6 present | PASS |
| Status field | All success | 260,000 success, 0 failure | PASS |
| Non-success rate | 0.0000 | 0.0000 | PASS |

### offgrid_risk_curves.csv

| Check | Expected | Actual | Status |
|---|---|---|---|
| Data rows | 14 × 500 × 26 = 182,000 | 182,000 | PASS |
| Combo IDs | O01-O14 | O01-O14 (all 14 present) | PASS |
| R per combo | 500 | 500 (all 14 combos) | PASS |
| Status field | All success | 182,000 success, 0 failure | PASS |
| Non-success rate | 0.0000 | 0.0000 | PASS |

---

## 5. Output Path Verification

Both files are in: `Study/01-study-MDM最小偏移量优化研究/artifacts/formal/E4_robustness/`

This matches the correct path resolved by `config.py` (`ARTIFACTS_DIR = Study/01.../artifacts/formal/`, `E4_OUTPUT_DIR = ARTIFACTS_DIR/E4_robustness/`).

The previously misplaced `Study/artifacts/` has been deleted and does not exist.

---

## 6. Safety Checks

| Check | Result |
|---|---|
| Sealed E1/E2/E3a/E3b artifacts | UNTOUCHED — `git diff HEAD -- artifacts/formal/{E1_baseline,E2_oracle_layers,E3_sample_adaptive,E3b_vector_mlp,shared_data}/` = EMPTY |
| Ch1-Ch6 / README / 00-05 | MC generation script does not write Markdown files. However, the working tree shows content modifications to some 00-05/draft files relative to HEAD. These modifications are NOT attributable to Step 2 (the MC script only writes CSVs to `E4_robustness/`). Their source cannot be determined from existing Git evidence. Step 2 did not cause them. |
| E4a/E4d executed? | NO — only MC generation script was run |
| Analysis script run? | NO |
| Ch7 written? | NO |

---

## 7. Operational Notes

- **First attempt failure:** The first background run (PID 35140) produced no output because the shell had silently drifted to the `main` branch, where the script path did not resolve. The process was killed and restarted on the correct branch (`study01-e4-validation`).
- **0-byte chunk files:** During the first ~15 minutes of the successful run, chunk files showed 0 bytes because Python's CSV writer buffers data internally and only flushes after each combo completes (~15-20 min/combo). This was initially mistaken for a stall.
- **Branch drift:** The shell unexpectedly switched to `main` at least twice during this session. This is a known hazard on this Windows setup. Future runs should verify `git branch --show-current` immediately before launching.
- **Commit `8103587`:** Codex committed the previously untracked scripts and 4 stale 0-byte chunk files. The chunk files were cleaned up before the successful run.

---

## 8. REVISE Fixes Applied

### Round 1 (first Codex REVISE)

1. **Post-hoc provenance** (Finding #1): Added `mc_generation_manifest.json` and `mc_generation_run_log.txt`. POST-HOC RECONSTRUCTED. Records HEAD=8103587, dirty workspace, seed, combos, R=500, delta grid, timestamps, SHA-256.

2. **--tracks argument** (Finding #2): `run_E4_formal_validation.py` accepts `--tracks e4b,e4c`.

3. **Fail-closed generation** (Finding #3): Worker returncode check, chunk existence/row count validation, combo coverage, duplicate key check, atomic write.

4. **Report/state corrections** (Finding #4): CRLF claim, stale handoff line.

### Round 2 (second Codex REVISE)

5. **Fail-closed exact row count** (Finding #1 R2): Chunk validation now checks exact row count = combos_per_worker × 500 × 26, and per-combo verification of exactly 500 repeats and 26 deltas. Deleting a row from a chunk now causes abort.

6. **--tracks output gate** (Finding #2 R2): E4d_skip_reason.md only written when e4d is requested but fails. split_report.csv only written when e4a is requested. Manifest output_files list is now dynamic — only files actually produced this run. Track-specific metadata filenames: `manifest_e4b_e4c.json`, `summary_e4b_e4c.json`, `run_log_e4b_e4c.txt` (not suite-level overrides). Manifest includes `tracks_requested` and `is_full_run` fields.

7. **Cost actual timing** (Finding #3 R2): E4b/E4c `elapsed_s` now uses `time.time()` before/after `evaluate_references()` instead of hardcoded 0.

8. **Document corrections** (Finding #4 R2): Status handoff L216 stale "待确认" line replaced with confirmed `--tracks e4b,e4c` command. Report L13 assertion corrected. run_log boundary bytes fixed 33533656→35533656.

---

## 9. Recommendation: APPROVE Step 3

Step 3 (E4b/E4c reference analysis) can proceed with:

```bash
python "Study/01-study-MDM最小偏移量优化研究/code/run_E4_formal_validation.py" --tracks e4b,e4c
```

This will produce only E4b/E4c reference, endpoint, near-optimal, and cost outputs with track-specific metadata files. E4a and E4d will not execute and will not produce any output files.
