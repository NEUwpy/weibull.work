# Task Plan — Study02 G3 D8 production wiring R1

Goal:

完成 G3 在 staged/formal 启动前缺失的 D8 生产接线：把已批准的 D7 selection trace/receipt 转换为确定、可审计、fail-closed 的 selected placeholder 与 deferred A-E3/A-E2 运行规格，并把 R5 批准的 checkpoint provenance rebuild 接入真实 pre-unseal 生产路径。交付实现、测试和执行者报告；本棒不运行 staged/formal，也不访问 test。

Known facts:

- 分支：`claude/study02-a-20260715`；Controller 基线：`ee7e52ff440b93a86151d6fbbc41d7f05cf6aee1`。
- Codex R5 已 APPROVE R4/D7 point-evidence provenance，审查见 `coworker/reviews/2026-07-18-study02-g3-selection-provenance-r5-codex.md`。
- `resolve_selected_placeholders` 与 `reconstruct_deferred_specs` 当前仍为 fail-closed 占位；D8 production orchestration 尚不存在。
- 冻结依赖顺序为 A-E1 -> A-E3 -> A-E2；test 仍为 `sealed`，A-E1 formal 未授权。
- 唯一用户脏文件是 `.claude/settings.local.json`，必须保留且不得暂存、改写或提交。
- Codex 是唯一 Controller/规划者/审查者；Claude Code 是唯一 executor。

Boundaries:

- Allowed:
  - 修改 Study02 D8 所需的 formal executor/runner/contracts/CLI 代码及直接相关测试。
  - 复用已冻结 matrix、selection trace v3、receipt v3、point-evidence provenance 与 predecessor manifest 合同。
  - 在临时目录或测试 fixture 中做合成/微型 wiring smoke；不得创建正式 run。
  - 更新本棒执行者报告与必要的 handoff 状态说明。
- Not allowed:
  - 不启动 staged A-E1、任何 formal training/validation、9d 或 G4。
  - 不读取、物化、试探或评估任何 module test 数据；`test_access_count` 必须保持 0。
  - 不修改冻结配置、矩阵、科学指标、fit cap、bootstrap、failure penalty、selection rule 或 artifact 公共 schema；如确实不可避免，停止并报告。
  - 不修改 Study01、main、凭证、本地设置或无关文件。
  - 不提交、不推送、不 self-approve；由 Codex 在审查 APPROVE 后精确暂存、提交和推送。

Executor autonomy:

- 选择与现有 `study02a` 模块边界一致的最小实现路径。
- 优先把 lineage 验证放在一个生产入口内，避免让调用者注入未经重建的 provenance 或自行拼接 predecessor。
- 可以重构内部重复逻辑，但不得改变已批准的序列化工件或冻结科学口径。

Required behavior:

- `selected_top_N` 与 `selected:<decision>` 只能从完整验证的 immutable selection trace/receipt 权威解析；缺失、重复、歧义、越界、非 winner、哈希或 module/run 不一致必须 fail-closed。
- deferred A-E3/A-E2 specs 必须从冻结矩阵和验证过的 predecessor 证据重建；A-E3 只接受 A-E1 predecessor，A-E2 只接受 A-E3 predecessor，错序、跨 run、陈旧 trace、缺失 receipt 或重复消费必须拒绝。
- 生产 pre-unseal 路径必须内部调用 `rebuild_selection_point_provenance` 并把结果交给 `build_pre_unseal_bundle`；不得把任意外部 `point_provenance_by_fit` 当作生产权威。
- 输出顺序、fit IDs、run IDs、predecessor hashes 与重复执行行为必须确定且可审计。
- 所有入口继续证明 `test_access_count=0`；本棒不得改变 test state。

Stop conditions:

- 冻结协议没有唯一决定某个 placeholder、decision grouping 或 predecessor 语义。
- 正确实现要求改变公共 schema、冻结科学口径或 fit cap。
- 发现活动 Study02 写入者/实验进程、工作区冲突或不可恢复环境问题。
- 其他实现缺陷由 executor 在本棒范围内修复，不因测试失败直接停止。

Verification:

- 为成功路径和失败路径新增/更新测试，至少覆盖 placeholder 解析、lineage/hash/module/run 绑定、错序 predecessor、缺失/歧义 selection、重复消费与 provenance 生产接线。
- 运行所有直接相关 formal selection/executor/runner/contracts/state/CLI 测试；报告精确命令、通过数、失败数和跳过项。
- 运行正确路径 `compileall` 与 `git diff --check`。
- 明确核对没有正式 run、没有 test 访问、没有冻结工件变更、没有 `.claude/settings.local.json` 进入 diff。

Report:

- 写入 `coworker/reports/2026-07-18-study02-g3-d8-r1-claude.md`。
- 包含 changed files、实现的数据/控制流、检查及精确结果、跳过项、阻塞、偏离和 `git status --short`。
- 完成后停止，等待 Codex 在 `coworker/reviews/2026-07-18-study02-g3-d8-r1-codex.md` 给出 APPROVE/REVISE/BLOCK。
