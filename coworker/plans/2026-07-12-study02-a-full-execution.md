# Task Plan

Goal:

持续完成 Study/02 前置研究 A，从 G1 推进到 G6，为全部 19 个研究问题形成可复现、边界明确的答案，并交付报告、代码、数据表和通过 Nature 论文图工作流验证的图像。

Known facts:

- 当前分支为 `codex/long-task-20260711`，起点为 `origin/main` 的 `155052b`。
- Study/02 的 19 个问题、实验协议框架和 G0-G6 计划已经建立。
- 当前工作树包含用户先前对 `08-更新日志.md` 的两处 v1.66 事实修正，必须保留。
- 用户授权在既定范围内自主推进；只有重大范围变化、新增付费资源、真实数据无法确定或必须由用户作出的学术选择才暂停。
- Nature 图像后端已确认为 Python。
- 当前任务持续目标已建立；30 分钟同任务 heartbeat loop 已启用。
- heartbeat automation id 为 `study02-a`，续接提示已统一使用规范大小写路径 `Study/02-study-NN参数估计与分位点目标研究/...`。
- 写代码前已读取根 `02-规则.md` 与 `python/studies/common/README.md`；MC 抽样、方法调用、指标和文件型实验优先复用统一流水线。

Boundaries:

- Allowed:
  - 审计本地文献与必要的权威外部文献。
  - 设计、实现、运行和验证 MC、NN、统计分析与真实数据实验。
  - 在 Study/02 目录内创建报告、代码、数据表、图像、manifest 和运行日志。
  - 按证据修订 `01-A`、`02-A`、`03-A` 及 Study/02 README。
  - 每个 G 阶段完成后更新 `08-更新日志.md`、提交当前分支并推送 `origin`。
- Not allowed:
  - 不扩展到删失或截断样本。
  - 不把计算速度发展为独立研究问题。
  - 不修改或重写 Study/01 已封存正式证据。
  - 不伪造文献、实验结果、真实数据真值或统计显著性。
  - 不使用 R 绘制、预览、导出或 QA 论文图。
  - 不自动引入付费 API、外部算力或私有数据上传。

Executor autonomy:

- 在当前研究边界内自主选择最小而充分的实现路径、pilot 规模、代码结构和验证方式。
- G2 formal 协议冻结前允许 pilot；formal 运行后不得无痕改变主指标、参数空间、切分或停止条件。
- 每个阶段都要把证据、偏离、失败和未支持结果写入对应报告，不只保留成功结果。
- 主比较允许参数路线和直接分位点路线在相同搜索资源预算下分别优化；相同输入和骨干只作补充受控比较。
- 续接前先读 `00-A-执行状态.md` 并取得单写者租约；存在有效租约、活动 PID 或活动实验时，heartbeat 只能监测，不得重复启动。

Execution phases:

## G0 执行基础设施与问题冻结

确认权威文件、持续目标、loop、deepwork 状态、单写者租约、交付规则和 Git 阶段规则。完成后更新日志、提交并推送。

## G1 文献与复现审计

核对既有 NN Weibull 参数估计研究，建立主张—方法—证据—缺口矩阵；选定复现对象并形成可执行复现合同。不得在缺失配置时假装精确复现。

## G2 实验协议冻结

冻结参数化、参数空间、样本量、训练规模、方法池、指标、切分、随机重复、输入表示、模型候选、真实数据、异常值与产物结构。建立协议快照、机器可读配置、配置哈希、冻结提交、独立 training/validation/calibration/test 四角色、模块级测试集一次启封、主要指标和停止条件预注册；calibration 只用于 A12 可信度校准，不参与模型选择；已启封 test 不得跨 G3-G5 回流调参。冻结 GitHub 单文件大小与分块/重建策略。计划与协议接受独立审查后才能进入 formal。

## G3 P0 基线实验

按依赖顺序回答 A1/A4 → A7/A8/A10/A17/A18 → A5/A6，并让 A9 多次训练稳定性贯穿所有入选 NN，形成可用于主研究的参数路线候选。每个模块先 pilot、后 formal，并保存逐样本结果与 manifest。

## G4 P1 比较与边界实验

回答 A2/A3/A13/A14/A15/A19，量化 NN 相对传统方法的优势、参数空间隐含先验、泛化与等变性边界。

## G5 P2 可信性与工程实验

回答 A11/A12/A16，形成真实数据、单次可信度和基础异常值敏感性证据。

## G6 综合与最终验证

形成问题—证据矩阵和完整研究报告；整理全部代码、数据表和图像。Nature Python-only 合同从首次正式绘图即执行，每幅图保存 Python 脚本、源数据、图像合同和视觉 QA；G6 汇总全量复现、统计谬误扫描和最终独立审查后收口。

Stop conditions:

- 暂停并请求用户：研究主题或范围必须改变；需要新增付费资源；真实数据存在多个会改变结论的不可判定选择；需要用户承担的学术价值判断。
- 不因实验失败、结果不显著或某路线不优而停止；如实记录并继续回答问题。
- G6 只有在 19 个问题逐项有可审计答案、交付物完整、复现与审查通过、所有阶段提交已推送后才完成。

Verification:

- 阶段级：计划/协议一致性、代码测试、pilot/formal 产物完整性、结果重算、随机性检查、数据泄漏检查、统计解释与失败审计。
- 图像级：结论合同、源数据追溯、Python-only 生成、SVG/PDF/TIFF 或协议规定格式、视觉 QA。
- Git 级：`git diff --check`、阶段相关测试、提交范围审计、远端分支确认。
- 运行级：追加式 `artifacts/run_ledger.jsonl` 记录 run ID、命令、时间、退出码、代码/配置哈希、种子、产物校验和、失败及替代关系。

Report:

- 每阶段在 `coworker/reports/` 写执行报告，列出改动文件、检查及精确结果、跳过项、阻塞和偏离。
- 每阶段在 `coworker/reviews/` 写 `APPROVE / REVISE / BLOCK` 审查。
- Study/02 最终报告位于 Study/02 目录，回答全部 19 个问题并链接正式证据。

Stage-close contract:

每个 G 阶段必须依次完成验证、阶段报告、oracle 审查、修订至 APPROVE、更新日志、精确暂存、提交、推送并把本地/远端 SHA 写回 `00-A-执行状态.md`。现有 `08-更新日志.md` 用户改动不得悄悄混入 Study/02 提交；优先独立提交，否则只分块暂存本阶段新增 hunk 并保留未暂存 diff 证据。
