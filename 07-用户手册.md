# 用户手册

> 本文档面向终端用户。模块定义的权威源为 `06-模块.md`；指标与图表/表格规范由结构化源维护（`metrics-spec.ts`、`charts-spec.ts`、`chart-registry.ts` 和共享实现），`/help/metrics` 与 `/help/charts` 是用户可读渲染视图。如有冲突，以权威源为准。

---

## 功能详解

### 威布尔计算器

计算器是平台的核心入口，用于对失效数据进行威布尔参数估计。

#### 数据输入

- **手动输入** — 在文本框中输入失效时间，以换行、逗号或空格分隔
- **从案例库调用** — 点击「从案例库选择」按钮，选取标准数据集

#### 方法选择

- **单方法** — 选择一种估计方法进行计算
- **多方法对比** — 勾选多种方法，结果以卡片形式叠加展示
- **MDM 偏移量** — 选择 MDM 时可在方法栏设置固定偏移量 δ；默认为 `0.10`，可选范围为 `0.00–0.50`，步长为 `0.02`。计算过程页的「位置参数梯度判据」与计算卡片使用同一个 δ，任一处修改都会同步到另一处并自动重算。切换“固定值 / AI优化”或修改固定 δ 时，参数栏持续显示当前 β、η、γ，重算完成后直接更新，不会先变为空白。
- **Windows 本地一键启动** — 双击项目根目录的 `Weibull本地启动器.exe`，启动器会自动启动后端与前端，待服务就绪后使用默认浏览器打开 `http://localhost:3000`。重复双击不会重复启动已经就绪的服务；启动失败时可查看 `logs/local-launcher-*.log`。

#### 模型选择

- **2 参数威布尔** — 估计形状参数 β 和尺度参数 η
- **3 参数威布尔** — 额外估计位置参数 γ（最小寿命）

#### 结果解读

- 参数栏始终显示 β、η、γ；尚未估计或结果被重置时显示当前默认/手动参数，估计成功后显示本次估计参数。

- **β（形状参数）** — 反映失效模式：β < 1 早期失效，β ≈ 1 随机失效，β > 1 耗损失效
- **η（尺度参数）** — 特征寿命，约 63.2% 的产品在该时间前失效
- **γ（位置参数）** — 最小寿命，失效不会发生在该时间之前
- **R²** — 拟合优度，越接近 1 表示拟合越好

### 参数估计方法

每种参数估计方法都有独立的详情页面，包含以下标签页：

- **原理文档** — 方法的数学原理、公式推导与适用条件
- **程序流程** — 公式与代码的对应关系，理解计算步骤
- **计算过程** — 展示计算中间量，观察参数如何收敛
- **结果分析** — 拟合优度评估、残差分析、置信区间
- **适用范围** — 不同参数条件下的系统性表现，基于蒙特卡洛模拟
- **可信性验证** — 复现论文结果，验证方法实现的正确性
- **方法对比** — 横向对比不同方法的表现（开发中）

目前已实现 11 种参数估计算法：MDM、MLE、MMLE、WMLE、LRE、LSE、MM、MPS、PWM、Bayesian、Grey GM(1,1)。

### 人工智能方法

- **过程量优化** — 方法保持不变，由 AI 为当前样本选择方法内部的过程量。MDM 已支持在计算卡片中切换“固定值 / AI优化”，AI 会预测 26 个候选偏移量的损失，选择预测损失最低的 δ，再执行标准 MDM 参数估计。
- **查看 AI 选择过程** — 在 MDM 卡片点击“查看AI选择过程”，可进入“人工智能方法 → 过程量优化 → MDM”。详情页会带入同一张计算卡片及当前样本、β/η/γ、偏移量模式和结果；修改卡片中的输入或模式后，选择曲线和结果会按同一状态更新。
- **与固定值比较** — 页面使用同一输入比较 AI 建议偏移量和固定 `δ=0.10`，同时给出两者的预测损失、β/η/γ 估计值，以及带 `+`/`−` 号的损失差异、预计准确度变化和参数差异。负的损失差异表示 AI 预测损失更低，正的预计准确度变化表示模型预期相对改善。
- **直接估计** — 保留从样本直接输出 Weibull 参数的原型入口。
- **自适应选择** — 用于未来在多种估计方法或策略之间作有边界的选择；当前不提供自动推荐。

MDM 的 AI 偏移量优化当前仅支持三参数完整失效样本，样本量为 `n=7、10、15、20`，候选范围为 `0.00–0.50`、步长 `0.02`。页面曲线是模型预测损失，不是当前样本可直接观测的真实误差；“预计准确度变化”也只是由两项预测损失换算的相对指标，不代表已知真参数下的实测准确率。

### 案例数据库

存储标准失效数据集，为计算和验证提供数据来源。

- 案例存储（单个案例 / 案例组）
- 多维度检索（行业、样本量、数据类型）
- 来源链接（关联到原始文献）
- 一键调用（跳转到计算器直接使用）

### 可靠性图书馆

管理与阅读学术文献，为方法和案例提供理论支撑。

- Markdown 文献阅读
- 双语切换（原文/翻译）
- 参考文献跳转
- 与方法/案例的双向链接
