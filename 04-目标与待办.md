# 目标与待办

> **更新时间**: 2026-06-26

<!--
书写规范（不渲染，仅供开发者参考）

目标层级（必遵）：

| 层级 | Markdown | 页面显示 | 说明 |
|------|----------|----------|------|
| 长期目标 | `## 标题：描述` | 一级分组 | 宏观愿景，冒号后为描述 |
| 中期目标 | `### 标题` | 二级分组 | 里程碑，归属于上层长期目标 |
| 短期目标 | `#### 标题` | 三级分组（隐藏） | 任务分类，仅用于逻辑归类，页面不显示标题 |
| 任务条目 | `- [标记] 内容` | 卡片 | 最小执行单元 |

层级推导原则：长期→中期→短期 必须逐级归属，不能出现与上层无关的孤立目标。

任务条目状态语法：

| 语法 | 含义 | 页面显示 |
|------|------|----------|
| `- [>]` | 聚焦中（当前正在做） | 绿色高亮卡片 + 「聚焦中」徽章 |
| `- [!]` | 下一个完成（即将开始） | 黄色边框卡片 + 「下一个」徽章 |
| `- [ ]` | 待完成 | 灰色卡片 + 「待完成」徽章 |
| `- [x/y]` | 计数进度（如 `[21/500]`） | 蓝色卡片 + 右侧显示 `21/500` |

计数型条目用于可量化的积累型工作（如文献库建设），x 为当前数量，y 为目标总数，两者均在 Markdown 中定义，页面自动计算比例渲染。

-->

## 智能算法推荐：根据样本特征匹配最优参数估计方法

### 建立方法评价体系

#### 指标规范文档
- [x] /help/metrics 页面已实现（可读规范，代码为权威源）
- [x] 指标定义、公式、适用场景已嵌入规范页
- [x] 权威源已迁移至代码：`src/lib/metrics.ts` + `python/studies/common/metrics.py`

#### 图表规范文档
- [x] /help/charts 页面已实现（可读规范，代码为权威源）
- [x] 图表类型、用途、配色规范已嵌入规范页
- [ ] 统一 ai/charts 与 shared/charts 的组件规范

#### 后端共享指标函数
- [x] `python/studies/common/metrics.py` 已创建
- [ ] 统一 compute_relative_mse（当前 7 处重复）
- [ ] 统一 compute_mae / compute_mre（当前 3 处重复）
- [ ] 统一 mse_of_delta（当前 4 处重复）
- [ ] 统一 evaluate_model 中的指标计算
- [ ] 替换各脚本中的重复实现，逐文件标记完成

#### 前端共享指标函数
- [x] `src/lib/metrics.ts` 已创建
- [ ] 统一 calcMAE / calcMRE / calcMSE / calcBias / calcStats
- [ ] 替换 DeltaAccuracyTab、ResultAnalysisLab、GenericStudyViewer、CompareResultsView 中的内联计算
- [ ] 逐组件标记完成

#### 评价数据格式统一
- [ ] 评价数据统一使用 CSV 格式（含当前使用 JSON 的 M3 统计数据）
- [ ] 统一 CSV 列名规范（n, beta_true, eta_true, gamma_true, est_beta, ...）

#### 横向对比
- [ ] 方法横向对比模块开发

#### 智能推荐
- [ ] 根据样本特征智能推荐最佳方法

---

## 人工智能方法优化：用AI优化参数估计过程与效果

### 模块 1：关系建立（MDM δ 优化）

#### 框架搭建
- [x] /ai 页面框架（总览 + 3 个子页面骨架）
- [x] 导航入口（顶部栏"人工智能方法"按钮）

#### 数据与模型
- [x] 训练数据生成脚本（generate_training_data.py）
- [x] 蒙特卡洛模拟生成训练数据（598 条）
- [x] PyTorch MLP 模型训练（n=5 MAE 0.037, n=10 MAE 0.020）

#### 系统集成
- [x] 后端 API：POST /ai/relationship/mdm
- [x] 前端在线使用 Tab（输入样本 → 输出最优 δ）

#### 已完成
- [x] M1-R1 直接学习路线（按 n 分别训练，MSE 0.027-0.034）
- [x] M1-R2 迭代逼近路线（公共模型，收敛率 74-80%）
- [x] 核心发现：最优 δ 替代固定 δ 提升精度 97-99%

> ⚠ 以上为旧原型完成记录。M1 当前状态为旧原型/待重构，不作为当前正式结论。当前状态见 `README.md`「当前状态快照」。

#### 待完善
- [ ] MSE-δ 曲线数学性质研究，优化搜索策略
- [ ] 可信性验证 Tab
- [ ] 方法对比 Tab

### 模块 3：直接估计（端到端 β,η,γ）

#### 已完成
- [x] 8 种预处理方案全部实验完成
- [x] B-1 统一模型（原始+掩码）为最优方案
- [x] γ 参数空间扩展 + 全部模型重新训练
- [x] 泛化验证（组内/插值/外推）
- [x] M1 vs M3 基准对比数据

> ⚠ 以上为原型完成记录。M3 当前状态为原型已实现，不作为当前正式结论。当前状态见 `README.md`「当前状态快照」。

#### 待完善
- [ ] AI vs 传统方法（MLE/MDM）深度对比

### 模块 2：优化求解（AI 辅助 MLE）

#### 待开发
- [ ] 确定 AI 辅助方式（初始值预测？优化策略？）

---

## RAG 智能问答：基于可靠性知识库的智能问答

### 知识库建设

#### 文献库完善
- [ ] 完善和校对文献库

#### 案例库完善
- [ ] 完善和校对案例数据库
- [ ] 建立案例与文献的完整关联

### 问答系统

#### RAG 问答实现
- [ ] 基于文献库的 RAG 智能问答

---

## 完善方法系统：各方法的完整展示与验证

### 方法内容完善

#### 原理文档与流程
- [ ] 完成所有算法的 Markdown 原理文档
- [ ] 完成所有 22 种算法的流程数据文件

#### 计算与分析
- [ ] 各方法的计算过程可视化
- [ ] 各方法的结果分析模块

### 验证与评估

#### 适用范围与可信性验证
- [ ] 各方法的适用范围蒙特卡洛分析
- [ ] 各方法的可信性验证（复现论文结果）

---

## 完善文献库与案例库：数据基础的建设与校对

### 文献库建设

#### 文献库完善
- [21/500] 持续补充中

### 案例库建设

#### 案例库完善
- [2/100] 持续补充中
