# 目标与待办

> **更新时间**: 2026-08-10

<!--
书写规范（不渲染，仅供开发者参考）

目标层级（必遵）：

| 层级 | Markdown | 页面显示 | 说明 |
|------|----------|----------|------|
| 长期目标 | `## 标题：描述` | 一级分组 | 宏观愿景，冒号后为描述 |
| 中期目标 | `### 标题` | 二级分组 | 里程碑，归属于上层长期目标 |
| 短期目标 | `#### 标题` | 三级分组（隐藏） | 任务分类，仅用于逻辑归类，页面不显示标题 |
| 任务条目 | `- [标记] 内容` | 卡片 | 最小执行单元 |

层级推导原则：长期→中期→短期 必须逐级归属，不能出现与上层无关的孤立目标。

任务条目状态语法：

| 语法 | 含义 | 页面显示 |
|------|------|----------|
| `- [>]` | 聚焦中（当前正在做） | 绿色高亮卡片 + 「聚焦中」徽章 |
| `- [!]` | 下一个完成（即将开始） | 黄色边框卡片 + 「下一个」徽章 |
| `- [ ]` | 待完成 | 灰色卡片 + 「待完成」徽章 |
| `- [x/y]` | 计数进度（如 `[21/500]`） | 蓝色卡片 + 右侧显示 `21/500` |

计数型条目用于可量化的积累型工作（如文献库建设），x 为当前数量，y 为目标总数，两者均在 Markdown 中定义，页面自动计算比例渲染。

-->

## 自适应估计策略：根据样本、任务与数据条件作出有边界的低风险决策

> 当前科学定义见 `Study/README.md` 和 `Study/研究规划v0.2.md`。旧 M1–M4 只是产品原型编号，不再决定研究顺序，也不承诺“自动推荐最佳方法”。

### 共享评价与数据基础

#### 指标与图表规范
- [x] /help/metrics 已实现，规范源为 `metrics-spec.ts`，实现源为 `src/lib/metrics.ts` + `python/studies/common/metrics.py`
- [x] /help/charts 已实现，读取 `charts-spec.ts` + `chart-registry.ts`
- [ ] 统一 ai/charts 与 shared/charts 的组件规范

#### 共享计算与数据合同
- [x] 后端共享指标模块 `python/studies/common/metrics.py` 已创建
- [ ] 统一 compute_relative_mse、compute_mae / compute_mre、mse_of_delta 和 evaluate_model 中的重复计算
- [ ] 统一前端 calcMAE / calcMRE / calcMSE / calcBias / calcStats，并替换组件内联实现
- [ ] 冻结跨 Study 比较所需的样本身份、CSV 列名、失败处理和任务损失合同

### 现有 Study 收口

#### Study01：MDM 内部偏移量选择
- [>] 保持 `delta` 为唯一主要决策变量，完成正文、附录和有限真实案例定位
- [x] 完成 opportunity、可执行策略、plug-in 负向机制与主要设计域边界证据

#### Study02：目标损失对齐
- [>] 完成远端现行成果的独立复核与 main 合并
- [x] 在冻结设计中完成 `P_equal` / `Q_param` 主效应、机制和风险转移分析

### 下一阶段 Research

#### 方法风险地形
- [x] 复核 `Research/05-传统估计方法横向比较/` 的协议、失败惩罚、尺度口径和旧包历史 seal
- [x] 完成 160-cell、48,000 共享样本的最佳固定、条件选择和 sample hindsight 比较
- [x] 条件选择空间成立：cell 选择较最佳固定 MDM-0.1 改善 6.07% [4.85%, 7.30%]，进入可辨识性研究

#### 可部署选择与简单参照
- [!] 只用当前样本可观测信息，先比较固定规则、少量等变统计量规则和可解释模型
- [ ] 只有简单规则不足且机会仍可辨识时，再检验复杂学习模型
- [ ] 用风险下降及其恢复的 oracle 比例评价策略，不以分类准确率代替估计收益
- [ ] 分析信息不足、近似平局、域外样本和错误选择的机制与边界

#### 风险校准与拒绝
- [ ] 校准“所选动作相对回退方案的预期收益”和不确定性
- [ ] 定义证据不足时的拒绝、回退和结果说明合同

### Study 升级与博士综合

#### Research 升级门槛
- [ ] 只有中心问题、主要决策变量、独立证据链、机制解释和边界同时成立，才建立新的 Study；暂不预编号

#### 最小充分组合
- [ ] 在共同合同下比较基线、各单模块和最终组合
- [ ] 只对出现明显非加性现象的模块增加交互实验，不做无问题驱动的全排列搜索

### 产品集成边界

#### AI 页面与 API
- [ ] 仅把通过上述证据门槛的策略接入 `/ai` 页面或生产 API
- [ ] 每次输出动作、预期收益、不确定性、适用域和回退方案，不只返回“最佳方法”
- [ ] 旧 M1/M3 页面、模型和数据只作迁移材料；清除旧 97–99% 等非正式结论文案

---

## RAG 智能问答：基于可靠性知识库的智能问答

### 知识库建设

#### 文献库完善
- [ ] 完善和校对文献库

#### 案例库完善
- [ ] 完善和校对案例数据库
- [ ] 建立案例与文献的完整关联

### 问答系统

#### RAG 问答实现
- [ ] 基于文献库的 RAG 智能问答

---

## 完善方法系统：各方法的完整展示与验证

### 方法内容完善

#### 原理文档与流程
- [ ] 完成所有算法的 Markdown 原理文档
- [ ] 完成所有 22 种算法的流程数据文件

#### 计算与分析
- [ ] 各方法的计算过程可视化
- [ ] 各方法的结果分析模块

### 验证与评估

#### 适用范围与可信性验证
- [ ] 各方法的适用范围蒙特卡洛分析
- [ ] 各方法的可信性验证（复现论文结果）

---

## 完善文献库与案例库：数据基础的建设与校对

### 文献库建设

#### 文献库完善
- [21/500] 持续补充中

### 案例库建设

#### 案例库完善
- [2/100] 持续补充中
