# 开发规则

本文档定义项目的开发规范，**写新代码之前必读**。

核心目标：最大化复用，最小化重复，避免屎山代码。

---

## 1. 核心原则

### 1.1 复用优先

```
已存在的、调试过的组件 = 可信赖的资产
不要重复造轮子
```

### 1.2 样板复制模式

添加新功能时，采用 **"复制-精简-复用-扩展"** 流程：

```
1. 复制成熟的样板
2. 删除不需要的部分
3. 复用通用组件
4. 添加特有逻辑
```

### 1.3 通用下沉原则

| 场景 | 处理方式 |
|------|----------|
| 1个方法使用 | 留在 `methods/{method}/` 内 |
| 2+方法共用 | 提取到 `shared/` |
| 全局通用 | 提取到 `lib/` |

### 1.4 各司其职，不冗余

每个部分有其分工，不赘述、冗余、分工不清。具体分工见 [01-结构.md](01-结构.md)。

---

## 2. 添加新方法

### 2.1 样板方法

**MDM 方法** (`src/components/methods/mdm/`) 是样板，包含：
- 5个Tab页面框架
- 可视化组件
- 可信性验证组件（原案例展示）
- 适用范围组件（原方法示例）

### 2.2 前端流程

```bash
# 1. 复制样板目录
cp -r src/components/methods/mdm src/components/methods/{新方法名}

# 2. 精简不需要的组件
# 删除该方法不需要的 case-studies、charts、visualizers

# 3. 复用通用组件
# 从 @/components/shared/charts 导入通用图表

# 4. 添加特有逻辑
# 只写该方法独有的组件
```

### 2.3 后端同步

1. 在 `python/methods/` 创建算法实现
2. 在 `python/main.py` 的 `method_map` 中注册
3. 在 `src/content/algorithms/` 添加文档
4. 在 `src/data/methods.json` 添加索引

---

## 3. 添加新案例

### 3.1 基础案例

在 `src/content/cases/` 新建 `.md` 文件，包含：
- `data_raw` (换行分隔的数据)
- 基础元数据 (title, industry)

### 3.2 可信性验证 (Verification，原案例展示)

可信性验证是预计算的论文复现验证，用于验证方法正确性。

> 详细规范见 [02-B-可信性验证规范.md](02-B-可信性验证规范.md)

---

## 4. 添加新适用范围 (Study，原方法示例)

适用范围是蒙特卡洛模拟的可视化分析。

> 详细规范见 [02-A-适用范围规范.md](02-A-适用范围规范.md)

---

## 5. 指标与图表规范

使用或新建指标、图表前，**必须先查阅系统规范**：

| 规范 | 位置 | 内容 |
|------|------|------|
| 指标规范 | `/help/metrics` | 所有评价指标的公式、含义、适用场景 |
| 图表规范 | `/help/charts` | 所有图表类型的用途、数据要求、配色规范 |

**规则**：
- 新增评价指标 → 必须先查指标规范，已有则直接复用共享函数，没有则先更新规范再实现
- 新增图表 → 必须先查图表规范，已有则复用组件，没有则先更新规范再实现
- 指标计算代码 → 必须调用共享函数（前端 `src/lib/metrics.ts`，后端 `python/studies/common/metrics.py`），禁止内联重复实现

### 5.1 图表设计模式

采用 **"高配版 + 功能开关"** 模式：

```tsx
interface ChartProps {
  data: DataType[]              // 数据（必须）

  // 功能开关（可选）
  interactive?: boolean         // 总开关，默认 true
  showSlider?: boolean          // 是否显示滑动条
  overlayMode?: boolean         // 多曲线叠加模式

  // 展示配置
  domain?: { x: [number, number]; y: [number, number] }
  referenceLines?: ReferenceLine[]
}
```

### 5.2 使用场景

| 场景 | 配置 |
|------|------|
| 计算过程 | `interactive={true}` |
| 可信性验证 | `interactive={false} overlayMode={true}` |
| 适用范围 | 纯渲染组件 + ChartCard |

### 5.3 添加前检查

```
□ 是否已有类似图表？
□ 横纵坐标是否相同？（相同则复用，用 props 控制差异）
□ 是否多个方法需要？（是则放 shared/charts/）
□ 是否方法独有？（是则放 methods/{method}/charts/）
```

---

## 6. 目录职责

| 需求 | 放哪里 |
|------|--------|
| 箱型图、热力图等通用图表 | `shared/charts/` |
| 方法专用图表 | `methods/{method}/charts/` |
| 方法专用可信性验证 | `methods/{method}/case-studies/`（详见 [02-B](02-B-可信性验证规范.md)） |
| 适用范围数据 | `public/studies/{method}/`（详见 [02-A](02-A-适用范围规范.md)） |
| 多方法共用工具函数 | `lib/` |

---

## 7. 代码风格

### 7.1 命名约定

| 类型 | 命名规则 | 示例 |
|------|----------|------|
| 组件文件 | PascalCase | `MDMVisualizer.tsx` |
| 工具函数 | camelCase | `generatePDFPoints()` |
| 常量 | UPPER_SNAKE | `MAX_ITERATIONS` |
| 目录 | lowercase | `case-studies/` |

### 7.2 导出规范

每个目录维护 `index.ts` 统一导出：

```tsx
// methods/mdm/index.ts
export { MDMVisualizer } from './visualizers/MDMVisualizer'
export { SigmaBetaChart } from './charts/SigmaBetaChart'
```

---

## 8. 说明文档编写规范

### 8.1 表里内容约定

说明文档同时面向开发者和用户，通过 **引用 (`>`)** 区分：

| 标记 | 含义 | 渲染行为 |
|------|------|----------|
| 普通文本 | 用户可见的表层内容 | 正常呈现 |
| `> 引用文本` | 开发者内部注释（里层） | 自动过滤，不呈现给用户 |

**规则**：
- 文档的元注释（如"本文档记录…"、版本号、内部交叉引用）放在引用中
- 功能描述、用法说明等用户关心的内容保持普通文本
- 写新内容时，想一下"用户需要看到这个吗？"，不需要就放引用里

### 8.2 大段内部内容的处理

对于整节都是内部架构细节的情况（如 01-结构.md 的职责边界、数据层等），不在文档中逐行标记引用，而是在渲染层做**节级过滤**——只提取用户需要的章节。

---

## 9. 禁止事项

| 禁止 | 原因 |
|------|------|
| 读取 `_archive/` 目录 | 归档废弃文件，无参考价值 |
| 硬编码数据在 TS/JS 文件 | 数据应放在 content/ 或 public/ |
| 为不同场景创建相似图表 | 应复用组件，用 props 控制 |
| 跳过文档直接写代码 | 必须先检查能否复用 |

---

## 10. 检查清单

### 新建文件前

```
□ 是否读过本文档？
□ 是否搜索过现有组件？
□ 是否确认无法复用？
□ 是否知道样板是谁？
□ 是否知道应该放在哪个目录？
□ 涉及指标？→ 查 /help/metrics 规范 + 调用共享函数
□ 涉及图表？→ 查 /help/charts 规范 + 复用已有组件
```

### 提交代码前

```
□ 是否有重复代码可以提取？
□ 通用组件是否放到了 shared/？
□ 是否更新了 index.ts 导出？
```
