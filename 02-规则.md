# 开发规则

本文档定义项目的开发规范，**写新代码之前必读**。

核心目标：最大化复用，最小化重复，避免屎山代码。

---

## 1. 核心原则

### 1.1 复用优先

```
已存在的、调试过的组件 = 可信赖的资产
不要重复造轮子
```

### 1.2 样板复制模式

添加新功能时，采用 **"复制-精简-复用-扩展"** 流程：

```
1. 复制成熟的样板
2. 删除不需要的部分
3. 复用通用组件
4. 添加特有逻辑
```

### 1.3 通用下沉原则

| 场景 | 处理方式 |
|------|----------|
| 1个方法使用 | 留在 `methods/{method}/` 内 |
| 2+方法共用 | 提取到 `shared/` |
| 全局通用 | 提取到 `lib/` |

### 1.4 各司其职，不冗余

每个部分有其分工，不赘述、冗余、分工不清。具体分工见 [01-结构.md](01-结构.md)。

---

## 2. 添加新方法

### 2.1 样板方法

**MDM 方法** (`src/components/methods/mdm/`) 是样板，包含：
- 5个Tab页面框架
- 可视化组件
- 案例展示组件
- 方法示例组件

### 2.2 前端流程

```bash
# 1. 复制样板目录
cp -r src/components/methods/mdm src/components/methods/{新方法名}

# 2. 精简不需要的组件
# 删除该方法不需要的 case-studies、charts、visualizers

# 3. 复用通用组件
# 从 @/components/shared/charts 导入通用图表

# 4. 添加特有逻辑
# 只写该方法独有的组件
```

### 2.3 后端同步

1. 在 `python/methods/` 创建算法实现
2. 在 `python/main.py` 的 `method_map` 中注册
3. 在 `src/content/algorithms/` 添加文档
4. 在 `src/data/methods.json` 添加索引

---

## 3. 添加新案例

### 3.1 基础案例

在 `src/content/cases/` 新建 `.md` 文件，包含：
- `data_raw` (换行分隔的数据)
- 基础元数据 (title, industry)

### 3.2 案例展示 (Case Study)

案例展示是预计算的参数研究可视化，避免每次访问都重新计算。

**数据位置**: `public/case-studies/{method}/caseX/`
- `config.md` - YAML 配置 + Markdown 描述（必需）
- `data.json` - 模拟数据

**添加新案例（3步）**:

```bash
# 1. 创建目录和配置
mkdir public/case-studies/mdm/case17
```

创建 `config.md`:
```yaml
---
id: "case-17"
name: "案例17: 标题"
description: "案例描述"
architecture: "case17"  # 特殊架构用 caseN，常规用 normal
csvFile: "/case-studies/mdm/case17/data.json"
defaults:
  beta: 2.0
  eta: 1000
  gamma: 1000
---
```

```bash
# 2. 创建数据文件
# Python 脚本: python/mdm/case_studies/case17/generate_data.py
# 输出: public/case-studies/mdm/case17/data.json
```

```tsx
// 3. 注册组件（仅特殊架构需要）
// 在 CaseStudyViewer.tsx 中添加 dynamic import
const Case17Viewer = dynamic(() => import('./mdm/case-studies/case17/Case17Viewer'))
```

**案例分类**:

| 类型 | architecture | 渲染方式 |
|------|-------------|----------|
| 常规案例 | `normal` | CaseStudyViewer 内置渲染 |
| Markdown | `markdown` | 纯文档展示 |
| 特殊案例 | `case5`, `case6`, ... | 专用组件 |

**注意**: 常规架构只需步骤 1-2，API 会自动发现。

---

## 4. 添加新方法示例 (Study)

方法示例是蒙特卡洛模拟的可视化分析。

**位置**: `src/components/methods/{method}/studies/`

**添加流程**:
1. 在 `methods/{method}/studies/` 创建 `{Method}StudyViewer.tsx`
2. 复制 MDMStudyViewer 作为模板
3. 修改方法特有参数和计算设置
4. 在 `page.tsx` 中注册 dynamic import

**通用部分**（所有威布尔方法共享）：
- 三参数：β, η, γ
- 样本量 n
- 蒙特卡洛 mcRuns
- 图表：箱型图、密度图、热力图

**方法特有部分**：
- MDM: 偏移量 δ、gammaSteps、betaBounds
- WMLE: 权重函数参数
- MLE: 基本无特有参数

---

## 5. 图表组件规范

### 5.1 设计模式

采用 **"高配版 + 功能开关"** 模式：

```tsx
interface ChartProps {
  data: DataType[]              // 数据（必须）

  // 功能开关（可选）
  interactive?: boolean         // 总开关，默认 true
  showSlider?: boolean          // 是否显示滑动条
  overlayMode?: boolean         // 多曲线叠加模式

  // 展示配置
  domain?: { x: [number, number]; y: [number, number] }
  referenceLines?: ReferenceLine[]
}
```

### 5.2 使用场景

| 场景 | 配置 |
|------|------|
| 计算过程 | `interactive={true}` |
| 案例展示 | `interactive={false} overlayMode={true}` |
| 方法示例 | 纯渲染组件 + ChartCard |

### 5.3 添加前检查

```
□ 是否已有类似图表？
□ 横纵坐标是否相同？（相同则复用，用 props 控制差异）
□ 是否多个方法需要？（是则放 shared/charts/）
□ 是否方法独有？（是则放 methods/{method}/charts/）
```

---

## 6. 目录职责

| 需求 | 放哪里 |
|------|--------|
| 箱型图、热力图等通用图表 | `shared/charts/` |
| 方法专用图表 | `methods/{method}/charts/` |
| 方法专用案例展示 | `methods/{method}/case-studies/` |
| 多方法共用工具函数 | `lib/` |

---

## 7. 代码风格

### 7.1 命名约定

| 类型 | 命名规则 | 示例 |
|------|----------|------|
| 组件文件 | PascalCase | `MDMVisualizer.tsx` |
| 工具函数 | camelCase | `generatePDFPoints()` |
| 常量 | UPPER_SNAKE | `MAX_ITERATIONS` |
| 目录 | lowercase | `case-studies/` |

### 7.2 导出规范

每个目录维护 `index.ts` 统一导出：

```tsx
// methods/mdm/index.ts
export { MDMVisualizer } from './visualizers/MDMVisualizer'
export { SigmaBetaChart } from './charts/SigmaBetaChart'
```

---

## 8. 禁止事项

| 禁止 | 原因 |
|------|------|
| 读取 `_archive/` 目录 | 归档废弃文件，无参考价值 |
| 硬编码数据在 TS/JS 文件 | 数据应放在 content/ 或 public/ |
| 为不同场景创建相似图表 | 应复用组件，用 props 控制 |
| 跳过文档直接写代码 | 必须先检查能否复用 |

---

## 9. 检查清单

### 新建文件前

```
□ 是否读过本文档？
□ 是否搜索过现有组件？
□ 是否确认无法复用？
□ 是否知道样板是谁？
□ 是否知道应该放在哪个目录？
```

### 提交代码前

```
□ 是否有重复代码可以提取？
□ 通用组件是否放到了 shared/？
□ 是否更新了 index.ts 导出？
```
