# Notes: AI 模块 1 深度讨论

## 讨论记录

### Phase 1: 目的与动机

**核心问题**: 为什么需要 AI 来优化 MDM 的偏移量 δ？

**MDM 方法中 δ 的角色**:
- MDM（最小差异法）需要一个偏移量 δ 作为过程参数
- δ 控制梯度偏移判据的阈值
- 不同的 δ 值会导致完全不同的参数估计结果
- 传统做法：人工尝试多个 δ 值，或使用固定经验值

**问题所在**:
- 人工选择 δ 费时费力，依赖经验
- 不同样本的最优 δ 不同，没有通用的固定值
- 用户不知道 δ 该设多少，往往随便选一个

**AI 的介入方式**:
- 不替代 MDM 算法本身
- 替代"选择 δ"这一步
- 本质：学习"样本特征 → 最优 δ"的映射关系

**这属于哪一类 AI 用途？**
- 关系建立（Relationship）：AI 拟合复杂非线性关系
- 不是优化求解（仍走 MDM 框架）
- 不是直接估计（没有端到端输出 β,η,γ）

### Phase 2: 关系定义

**输入**: 一组失效时间样本 [t1, t2, ..., tn]
**输出**: 最优偏移量 δ*

**"最优"的定义**: 需要明确
- 方案 A: MSE(β,η,γ) — 三参数均方误差之和
- 方案 B: MSE(β)+MSE(η) — 只看形状和尺度
- 方案 C: 加权 MSE
- 方案 D: R² 均值
- 方案 E: 自定义

**关键问题**: "最优"是对谁最优？
- 是让 MDM 估计出的 β,η,γ 尽量接近真实值
- 所以需要"真实值"作为参照 → 蒙特卡洛模拟

### Phase 3: 神经网络设计

（待讨论）

### Phase 4: 训练数据生成

（待讨论）

### Phase 5: 训练策略

（待讨论）

### Phase 6: 效果评估

（待讨论）

### Phase 7: 可视化方案

（待讨论）
