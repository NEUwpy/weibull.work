# 03 论文骨架

> 当前修订：2026-08-05
> 职责：规定论文的论证顺序、每节要回答的问题和图表职责。它是正文的骨架，不是实验执行报告。正文不得出现 E6、P4 等内部项目标签。

## 一句话论证

在当前设计域中，经验偏移量 $\delta=0.1$ 已接近最佳统一常数，但逐样本仍有明显选择空间；以排序原始样本预测候选偏移量的损失曲线，可以在不改变 MDM 参数估计过程的情况下进一步降低三参数联合估计误差。

## 论证链

```text
正偏移量改善有限样本稳定性，但取值依赖经验
  -> 先用统一指标比较不同 delta
  -> 统一常数收益很小，增加信息后选择空间明显扩大（oracle 创造潜在空间）
  -> 直接 plug-in：把初步参数估计当作真参数沿用 L3–L5 规则
       —— 被检验且总体失败（全部单步变体劣于 Default，仅 beta=1.5 例外）
  -> 真参数和事后结果在应用中不可得
  -> 用当前样本预测整条候选损失曲线（已检验方案中获得 plug-in 未获得的总体收益）
  -> 由 MDM 在选定 delta 下完成参数估计
  -> 主结果显示精度改善且训练稳定
  -> 用参数留出、传统方法和可靠度寿命补充可信性
  -> 最后补充域匹配的真实案例（尚未完成）
  -> 讨论结果含义、有效原因、边界与实际价值
```

## 第一部分 引言

### 要回答的问题

为什么在已有正偏移量方案的基础上，还需要研究偏移量怎样选择？

### 叙事顺序

1. 三参数 Weibull 分布在寿命和可靠性分析中的作用。
2. 前期 MDM 工作及正偏移量的来源。
3. 经验设定的不足：正偏移量有助于稳定估计，但不同 $\delta$ 也会产生不同估计误差。
4. 研究缺口：尚不清楚是否存在更合适的选择，以及真参数未知时怎样选到。
5. 本文做法：先界定从统一规则到逐样本选择的潜在收益，再用当前样本预测候选损失曲线。
6. 贡献概括：
   - 统一评价不同偏移量选择规则；
   - 量化不同信息下可以获得的精度收益；
   - 提出只用排序原始样本的样本自适应选择方法；
   - 用必要的支撑验证说明结果的稳定性、边界和工程后果。

### 建议开头表述

> 前期研究通过在三参数 Weibull MDM 的位置参数搜索判据中引入正偏移量，改善了有限样本下估计的稳定性，但偏移量仍主要依靠经验设定。不同偏移量也会产生不同的参数估计误差，因此有必要研究是否存在比经验值更合适的选择，以及在真参数未知时如何实现这种选择。

### 不写什么

- 不把问题包装成“用神经网络优化 MDM”；
- 不把 Direct-MLP、真实数据或传统方法比较写成研究缺口；
- 不在引言罗列项目阶段和内部实验编号。

## 第二部分 方法

### 2.1 MDM 与偏移量

#### 目的

用与 MDM 原文一致的符号说明 $\delta$ 在判据中的位置、为什么需要正偏移量，以及它不会改变最终三参数估计任务。

#### 必须包含

- 三参数 Weibull 分布与参数含义；
- MDM 原始判据、搜索变量和估计步骤；
- 正偏移量版本的定义；
- 前作为什么使用 $\delta=0.1$；
- 本文只研究 $\delta$ 的选择，不改写 MDM 的其他部分。

公式和术语以 182-030、182-046 原文为准，正文写作前逐式核对。

### 2.2 如何评价偏移量

#### 目的

把“偏移量好不好”变成可比较的问题，并区分可执行规则与信息参照。

#### 内容

1. 定义单样本三参数联合损失 $\ell_i$ 与汇总 $J_1$。
2. 说明 $J_1$ 由三个参数的标准化平方误差汇总得到，数值越小表示联合估计误差越小。
3. 定义 Default、L1–L6：

| 层级 | 信息 | 论文中的角色 |
|---|---|---|
| Default | 固定 $\delta=0.1$ | 经验基线 |
| L1 | 全局 | 最佳统一常数 |
| L2 | $n$ | 简单可执行规则 |
| L3 | $\beta$ | oracle 参照 |
| L4 | $\beta,n$ | oracle 参照 |
| L5 | $\beta,\gamma/\eta,n$ | 参数组级 oracle 参照 |
| L6 | 每个样本实际损失 | 26 点网格内 hindsight 参照 |

4. 说明 L1–L5 选点和评价分离；L6 不训练，也不是可部署上限。

### 2.3 Monte Carlo 设计

正文用表 1 列出 8 个 $\beta$、固定 $\eta=1000$、5 个 $\gamma/\eta$、4 个 $n$、每组合 300 次和 26 个 $\delta$，并给出 160 个组合、48,000 个样本和 1,248,000 次 MDM 估计的总规模。参数设置决定结论的适用范围，不能只放在附录。

同时说明两种评价协议：

- L1–L5：repeat-id 五折交叉评价；
- MLP：每个 $n$ 按 $\gamma/\eta$ 水平五折留出，测试水平未进入训练，但测试仍覆盖全部 $\beta$。

不将两种协议无说明地合并排名。

### 2.4 利用初估参数选择偏移量（负向验证，方法一段）

正文用一段说明：先以固定 $\delta=0.1$ 的 MDM 或 WMLE 得到初步 $\hat\beta,\hat\eta,\hat\gamma$，再把这些估计当作真参数去查询 L3–L5 对应的条件均值损失曲线并选择 $\delta$。条件集与 L3–L5 相同（仅 $\beta$；$\beta,n$；$\beta,\gamma/\eta,n$），查询方式为就近网格点或对损失曲线连续插值；曲线只用训练折构成。迭代（用新估计重选）在偏移量不变、循环或 10 次更新时停止。本检验只量化“用估计代替真值”损失多少收益，提供负向参照，不与 MLP 排名。

### 2.5 样本自适应选择

#### 目的

说明如何在真参数未知时，仅凭当前样本选择 $\delta$。

#### 输入、网络和输出

对每个样本量训练一个独立 MLP：

$$
X_n=\operatorname{sort}(x_1,\ldots,x_n),
$$

$$
X_n \longrightarrow \operatorname{MLP}_{n}\longrightarrow
\left(\widehat\ell_{\delta_1},\ldots,\widehat\ell_{\delta_{26}}\right),
$$

$$
\hat\delta=\arg\min_{\delta_j}\widehat\ell_{\delta_j},
\qquad
(\hat\beta,\hat\eta,\hat\gamma)=\operatorname{MDM}(X_n;\hat\delta).
$$

- 输入是有量纲排序原始样本，不是统计特征，也不除以样本均值；
- 每位置 StandardScaler 只在训练折拟合；
- 网络结构为 256–128–64，ReLU，Adam；
- 输出整条 26 点风险曲线，而不是单个 hard label；
- MLP 负责选择，MDM 负责估计。

#### 必需图 1：方法结构图

```text
样本 x1...xn
   -> 排序
   -> 训练折标准化
   -> per-n MLP (256-128-64)
   -> 26 个候选 delta 的预测损失
   -> argmin
   -> 选定 delta 下运行 MDM
   -> beta_hat, eta_hat, gamma_hat
```

图中明确区分“训练标签来自 Monte Carlo 真值”和“应用时只输入当前样本”。

## 第三部分 结果
### 3.1 统一偏移量接近最优，但仍有逐样本改善空间

#### 结果顺序

1. 展示 $\delta$ 的整体误差曲线：当前 26 点网格最低点为 $\delta=0.06$（$J_1=0.624518$），$\delta=0.08$ 与其几乎相同（0.624666）；经验值 $\delta=0.1$ 为 0.630409，仅比最低点高约 0.93%，已接近最佳统一常数，但 $\delta=0.12$ 已升至 0.638755，不能再概括为“0.08–0.12 均处于同一平缓区”。
2. 用 L1–L6 表展示信息增加后的 $J_1$：0.6252、0.6230、0.5905、0.5885、0.5813、0.4923。
3. 直接结论：改变统一常数收益有限；按形状参数区分后出现明显改善；逐样本选择存在最大潜在空间。
4. 由结果引出两条可执行路线：先估计参数并据此选择或迭代更新偏移量；用已知真值的 Monte Carlo 样本训练网络，学习样本与候选损失曲线之间的关系。两条路线分别承接 3.2 和 3.3。

#### 表 2：偏移量选择层级

建议列：规则、选择信息、选择/评价方式、pooled $J_1$、相对 Default 改善、论文角色。

不把 L5 与 MLP 放在同一无协议排名表中。

### 3.2 利用初估参数选择偏移量的结果

正文用一段报告：12 种单步初估参数选择变体全部劣于 Default（最优 WMLE 初估/仅 $\beta$/连续插值 pooled $J_1=0.6507$ vs Default 0.6304；配对重复块 bootstrap 95% CI $[0.0186,0.0219]$，全部区间严格为正）；仅在 $\beta=1.5$ 改善（$J_1$ 差 $-0.0104$）；迭代一致变差。因果措辞谨慎：邻近 $\beta$ 网格单元正确率（MDM-0.1 19.8%、WMLE 16.5%）与初估误差和损失关联只作为与“错误路由削弱 plug-in”解释一致的证据，不构成唯一机制的证明。完整方法、变体、CI 与分 $\beta$ 结果放入附录 C。

### 3.3 样本自适应选择获得实质改善

#### 结果顺序

1. 报告 pooled $J_1$ 0.5543，Default 0.6304，L6 0.4923。
2. 报告相对 Default 改善 12.08%，失败率 0%。
3. 报告 $n=7,10,15,20$ 的结果 0.6901、0.5761、0.4855、0.4300。
4. 说明四个样本量下分别取得 Default—L6 差距的 47.2%、57.4%、59.7% 和 61.8%。
5. 用逐样本配对损失差展示个体差异：四个样本量下约 72%–74% 的样本改善，同时保留负值尾部，不把总体收益写成逐样本保证。

#### 表 3：主方法比较

只列同一留出协议和同测试样本上的 Dimensional-RAW、Default、L6；列 pooled、分 $n$、失败率。Normalized-RAW 可放补充材料，正文只在表示选择说明中一句带过。

#### 图 3：分样本量表现

a 面板横轴 $n$、纵轴 $J_1$，三条线为 Dimensional-RAW、Default、L6；阴影表示 Default—L6 的当前候选网格内可实现空间，标注自适应方法取得该空间的比例，不显示 seed。b 面板展示逐样本配对损失差的第 1—99、第 5—95 百分位区间、四分位距、中位数和改善样本比例。图只说明四个已训练样本量下的表现，不使用“未见 $n$ 泛化”措辞。

### 3.4 预测损失曲线支持偏移量选择，但仍有样本级误差

用代表性折外样本的实际/预测损失曲线说明网络如何选择 $\delta$，再用全部测试样本的所选偏移量—hindsight 偏移量对应关系和超额损失分布约束个例解释；正文只解释机制和误差结构，不把单个样本当作效果证据。再按 $n$ 分面展示 $\beta\times\gamma/\eta$ 组合上相对 Default 的改善，说明改善覆盖面和少数退化区域，不只报告总体平均。

#### 图 4：偏移量选择机制

用代表性折外样本的实际/预测损失曲线说明网络如何选择 $\delta$，再用全部测试样本的所选偏移量—hindsight 偏移量对应关系和超额损失分布约束个例解释。正文只解释机制和误差结构，不把单个样本当作效果证据。

#### 图 5：参数空间中的效果分布

按 $n$ 分面展示 $\beta\times\gamma/\eta$ 组合上相对 Default 的改善。正文说明改善覆盖面和少数退化区域；不只报告总体平均，也不逐格复述数值。

### 3.5 支撑验证

正文依次用短段落回答四个问题，细节放补充材料：训练结果是否依赖一次初始化；未见参数水平能否保持收益；与传统方法相比处于什么位置；参数收益能否传递到寿命指标。图 6 集中呈现后三类数值关系；seed 稳定性在正文报告范围，完整结果放附录 B，不再另设支撑验证摘要表。

若真实案例最终执行，作为本节末尾的经验性例证，不单独承担方法成立与否。

#### 图 6：支撑验证汇总

用三面板概括未见 $\beta$、传统方法外部参照和可靠度寿命。正文各用一句报告方向和边界，详细分层图表放补充材料。

## 第四部分 讨论

第四部分承接第三部分，讨论“这些结果说明什么、为什么可能如此、边界在哪里、实际有什么用”，不重复一遍结果表。

### 4.1 不同可用信息下的偏移量选择

本节不再复述 L1—L6，而是把第三部分的结果转化为实际选择规则：

- 若只能采用一个统一偏移量，当前设计内的网格最低点为 $\delta=0.06$，但 $\delta=0.1$ 的 $J_1$ 仅高约 0.9%；前者适合针对当前参数域进行专门设置，后者仍可作为简洁的默认值；
- 样本量 $n$ 在应用时通常已知，但仅按 $n$ 设置固定偏移量相对 $\delta=0.1$ 只改善约 1.2%，相对最佳统一常数的进一步改善不足 0.4%，没有显示出单独维护多套固定规则的明显必要性；
- 真参数未知时，可利用当前排序样本预测 26 点损失曲线并选择偏移量；在训练条件匹配时，样本自适应方法相对 $\delta=0.1$ 将汇总 $J_1$ 降低约 12.1%；
- 对未训练样本量，或者单位和尺度范围明显不匹配的数据，不直接外推 MLP，应保留固定规则或重新训练。

### 4.2 方法为什么可能有效

讨论保持诚实、不过度证明：

1. 不同 $\delta$ 对有限样本误差的影响并不相同，形成可学习的样本级损失曲线；
2. 排序样本同时保留分布形状、离散程度、低端位置和绝对水平，不需要预先指定哪些统计特征最重要；
3. 预测整条损失曲线比直接预测最佳格点保留了候选之间的相对风险信息；
4. 最终仍由 MDM 估计参数，因此方法只改变估计器族中的选择规则。

正偏移量会共同改变三个参数的估计结果，本文通过 $J_1$ 比较不同选择规则下的三参数联合估计误差。

利用初估参数选择偏移量的负结果在讨论中作对比解读：直接 plug-in 总体失败且迭代变差，初步 $\hat\beta$ 正确路由率仅约 17%–20%，与初估噪声导致错误路由的解释一致但不构成唯一机制（连续插值同样失败、误差分层可能与真 $\beta$ 混杂）；这为本文转向直接预测样本级损失曲线提供比较依据，在已检验方案中风险曲线预测获得了 plug-in 未能获得的总体收益。

### 4.3 与传统方法和工程指标的关系

- WMLE 和 LSE 只为当前设计中的三参数联合误差提供外部参照，不参与决定偏移量选择是否成立；
- $J_1$ 与可靠度寿命衡量的问题不同，较低的三参数联合误差不保证每一个 $x_R$ 的误差都更低；
- 当前结果显示参数误差改善向 $x_{0.90}$、$x_{0.95}$ 和 $x_{0.99}$ 的传递有限。若实际任务主要关心某一工程量，应直接用该工程量评价方法。

### 4.4 适用边界

- 参数空间和 $\delta$ 均为离散设计，未见参数水平验证不代表连续空间外推；
- $\eta$ 固定为 1000，有量纲输入导致结论依赖单位和尺度范围；
- 每个 $n$ 独立训练，当前方法只适用于 $n\in\{7,10,15,20\}$；
- L6 是 26 点候选网格内的 hindsight 参照，不是理论最优；
- 当前没有域匹配的真实数据验证，Monte Carlo 结论不能直接推广到所有工程数据。

## 第五部分 结论

结论只保留四件事：

1. $\delta=0.1$ 已接近最佳统一常数；
2. 不同信息下的选择结果表明仍有明显逐样本空间；
3. Dimensional-RAW-MLP 在当前设计中相对 Default 将 pooled $J_1$ 降低约 12.1%；
4. 该收益限于当前离散参数域、已训练样本量和一致的单位尺度，进一步泛化与真实数据仍需验证。

## 正文图表最小集合

| 编号 | 内容 | 作用 | 当前状态 |
|---|---|---|---|
| 图 1 | MDM 偏移量及样本自适应选择流程 | 解释方法 | 已完成（`figures/main/fig1_method_structure.png`） |
| 表 1 | Monte Carlo 参数设计 | 明确参数空间、候选偏移量和重复规模 | 正文内嵌；附录不重复 |
| 表 2 | L1–L6 规则、协议和 $J_1$ | 界定潜在空间 | 已完成（源文件 `figures/tables/table1_l1_l6.md` 沿用正式产物名） |
| 图 2 | 整体 $\delta$–风险曲线与信息层级 | 说明 0.1 接近最佳统一常数，并界定潜在空间 | 已完成（`figures/main/fig2_overall_delta_risk.png`） |
| 表 3 | Dimensional-RAW / Default / L6 主结果 | 给出核心效果 | 已完成（源文件 `figures/tables/table2_main_results.md` 沿用正式产物名） |
| 图 3 | 三方法按 $n$ 的 $J_1$ 与逐样本配对损失差 | 同时展示总体改善、可实现空间和个体差异 | 已完成（`figures/main/fig3_per_n_J1.png`） |
| 图 4 | 风险曲线预测、偏移量对应关系和超额损失 | 解释选择机制及误差结构 | 已完成（`figures/main/fig4_selector_mechanism.png`） |
| 图 5 | $\beta\times\gamma/\eta\times n$ 的改善分布 | 展示收益覆盖面和退化边界 | 已完成（`figures/main/fig5_parameter_landscape.png`） |
| 图 6 | 未见参数、传统方法和可靠度寿命 | 汇总支撑验证 | 已完成（`figures/main/fig6_support_validation.png`） |

旧 G5 图 6–9 和 S1–S8 基于旧特征路线，已经归档，不能作为当前终稿图继续使用。

利用初估参数选择偏移量的负结果不进正文图（正文保持六张主图），其完整图表放入附录 C。

## 附录结构

正文必须独立完成“问题、方法、结果、讨论和结论”的闭环。附录补充会影响复核或复现、但全部放入正文会打断主线的细节。正文中的核心数值、主比较和关键边界不能只在附录出现。

| 附录 | 主题 | 内容 | 与正文的关系 |
|---|---|---|---|
| A | Monte Carlo 设计与评价细节 | 参数网格、样本生成、损失与失败处理、两类交叉评价、复现入口 | 支撑 2.2、2.3 和 2.6，不重复正文公式推导 |
| B | 网络训练、输入表示与稳定性 | 网络配置、折内标准化、三 seed 结果、有量纲与均值归一化候选对照、三参数误差分解 | 支撑 2.5、3.3 和 4.2，保留单位尺度边界并说明 $J_1$ 改善的参数组成 |
| C | 利用初估参数选择偏移量 | PG-beta、PG-beta-n、PG-full，网格与插值，单步与迭代，完整 CI 和分 $\beta$ 结果 | 正文 3.2 给出结论，附录保留全部变体和诊断 |
| D | 未见形状参数水平验证 | 未见 $\beta$ 八折的完整分层表和图 | 支撑 3.5；参数组合退化和样本量边界已在正文完整说明，不再重复 |
| E | 传统方法与可靠度寿命 | WMLE/LSE 参数 Bias、RMSE、分 $n$ 参照，$x_{0.90}$、$x_{0.95}$、$x_{0.99}$ 完整误差 | 支撑 3.5 和 4.3，只作外部参照和交叉例证 |

正文与附录分别采用版本号。当前活动文件为外部工作区的 `Study01论文初稿-v1.0.md` 与 `Study01论文附录-v1.0.md`；v0.3、v0.4 与附录 v0.1 保留不改。图表编号在正文使用图 1 至图 6，在附录按附录字母编号，如图 C1、表 B3、表 C1，避免正文和附录拼接后重号。

正文只在 2.3、2.5、3.2 和 3.5 各保留一次附录入口，分别指向 A、B、C 和 D/E。同一公式、关键数值或结论只在一处完整展开；附录引用正文定义，并补充实施细节或完整分层数据，不重复正文的结果解释和讨论。

## 写作纪律

- 正文使用读者语言，不使用 E1/E6/P4 等内部标签。
- 每个图表前交代动机和数据来源，图表后写直接结果。
- 正文与附录不重复论述；正文承担完整论证，附录只增加方法细节、完整数据和复现信息。
- 不用“条件异质性”“内部风险最优化”等包装简单事实的术语。
- 结果与讨论分工：第三部分报告观察，第四部分解释含义、可能原因和边界。
- 待补结果只留占位，完成复核前不猜测方向。
