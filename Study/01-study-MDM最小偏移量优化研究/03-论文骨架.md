# 03 论文骨架

> 当前修订：2026-08-05
> 职责：规定论文的论证顺序、每节要回答的问题和图表职责。它是正文的骨架，不是实验执行报告。正文不得出现 E6、P4 等内部项目标签。

## 一句话论证

在当前设计域中，经验偏移量 $\delta=0.1$ 已接近最佳统一常数，但逐样本仍存在明显选择空间；以排序原始样本预测候选偏移量的损失曲线，可在不改变 MDM 参数估计过程的情况下恢复其中一部分收益。直接利用初步参数估计做 plug-in 选择不能恢复该收益，这进一步说明样本到风险曲线的预测是必要的可执行主路线。

## 论证链

```text
正偏移量改善有限样本稳定性，但取值依赖经验
  -> 先用统一指标比较不同 delta
  -> 统一常数收益很小，增加信息后选择空间明显扩大（oracle 创造潜在空间）
  -> 直接 plug-in：把初步参数估计当作真参数沿用 L3–L5 规则
       —— 被检验且总体失败（全部单步变体劣于 Default，仅 beta=1.5 例外）
  -> 真参数和事后结果在应用中不可得
  -> 用当前样本预测整条候选损失曲线（可执行主路线）
  -> 由 MDM 在选定 delta 下完成参数估计
  -> 主结果显示精度改善且训练稳定
  -> 用参数留出、传统方法、工程分位点和真实案例补充可信性
  -> 讨论结果含义、有效原因、边界与实际价值
```

## 第一部分 引言

### 要回答的问题

为什么在已有正偏移量方案的基础上，还需要研究偏移量怎样选择？

### 叙事顺序

1. 三参数 Weibull 分布在寿命和可靠性分析中的作用。
2. 前期 MDM 工作及正偏移量的来源。
3. 经验设定的不足：正偏移量有助于稳定估计，但不同 $\delta$ 也会产生不同估计误差。
4. 研究缺口：尚不清楚是否存在更合适的选择，以及真参数未知时怎样选到。
5. 本文做法：先界定从统一规则到逐样本选择的潜在收益，再用当前样本预测候选损失曲线。
6. 贡献概括：
   - 统一评价不同偏移量选择规则；
   - 量化不同信息下可以获得的精度收益；
   - 提出只用排序原始样本的样本自适应选择方法；
   - 用必要的支撑验证说明结果的稳定性、边界和工程后果。

### 建议开头表述

> 前期研究通过在三参数 Weibull MDM 的位置参数搜索判据中引入正偏移量，改善了有限样本下估计的稳定性，但偏移量仍主要依靠经验设定。不同偏移量也会产生不同的参数估计误差，因此有必要研究是否存在比经验值更合适的选择，以及在真参数未知时如何实现这种选择。

### 不写什么

- 不把问题包装成“用神经网络优化 MDM”；
- 不把 Direct-MLP、真实数据或传统方法比较写成研究缺口；
- 不声称已经证明偏差—方差机制；
- 不在引言罗列项目阶段和内部实验编号。

## 第二部分 问题定义与方法

### 2.1 MDM 与偏移量

#### 目的

用与 MDM 原文一致的符号说明 $\delta$ 在判据中的位置、为什么需要正偏移量，以及它不会改变最终三参数估计任务。

#### 必须包含

- 三参数 Weibull 分布与参数含义；
- MDM 原始判据、搜索变量和估计步骤；
- 正偏移量版本的定义；
- 前作为什么使用 $\delta=0.1$；
- 本文只研究 $\delta$ 的选择，不改写 MDM 的其他部分。

公式和术语以 182-030、182-046 原文为准，正文写作前逐式核对。

### 2.2 如何评价偏移量

#### 目的

把“偏移量好不好”变成可比较的问题，并区分可执行规则与信息参照。

#### 内容

1. 定义单样本三参数联合损失 $\ell_i$ 与汇总 $J_1$。
2. 说明 $J_1$ 同时考虑三个参数误差，但不等同于已完成偏差—方差分解。
3. 定义 Default、L1–L6：

| 层级 | 信息 | 论文中的角色 |
|---|---|---|
| Default | 固定 $\delta=0.1$ | 经验基线 |
| L1 | 全局 | 最佳统一常数 |
| L2 | $n$ | 简单可执行规则 |
| L3 | $\beta$ | oracle 参照 |
| L4 | $\beta,n$ | oracle 参照 |
| L5 | $\beta,\gamma/\eta,n$ | 参数组级 oracle 参照 |
| L6 | 每个样本实际损失 | 26 点网格内 hindsight 参照 |

4. 说明 L1–L5 选点和评价分离；L6 不训练，也不是可部署上限。

### 2.3 样本自适应选择

#### 目的

说明如何在真参数未知时，仅凭当前样本选择 $\delta$。

#### 输入、网络和输出

对每个样本量训练一个独立 MLP：

$$
X_n=\operatorname{sort}(x_1,\ldots,x_n),
$$

$$
X_n \longrightarrow \operatorname{MLP}_{n}\longrightarrow
\left(\widehat\ell_{\delta_1},\ldots,\widehat\ell_{\delta_{26}}\right),
$$

$$
\hat\delta=\arg\min_{\delta_j}\widehat\ell_{\delta_j},
\qquad
(\hat\beta,\hat\eta,\hat\gamma)=\operatorname{MDM}(X_n;\hat\delta).
$$

- 输入是有量纲排序原始样本，不是统计特征，也不除以样本均值；
- 每位置 StandardScaler 只在训练折拟合；
- 网络结构为 256–128–64，ReLU，Adam；
- 输出整条 26 点风险曲线，而不是单个 hard label；
- MLP 负责选择，MDM 负责估计。

#### 必需图 1：方法结构图

```text
样本 x1...xn
   -> 排序
   -> 训练折标准化
   -> per-n MLP (256-128-64)
   -> 26 个候选 delta 的预测损失
   -> argmin
   -> 选定 delta 下运行 MDM
   -> beta_hat, eta_hat, gamma_hat
```

图中明确区分“训练标签来自 Monte Carlo 真值”和“应用时只输入当前样本”。

### 2.4 Monte Carlo 设计

用一张表说明：8 个 $\beta$、固定 $\eta=1000$、5 个 $\gamma/\eta$、4 个 $n$、每组合 300 次、26 个 $\delta$，共 160 组合和 48,000 个样本。

同时说明两种评价协议：

- L1–L5：repeat-id 五折交叉评价；
- MLP：每个 $n$ 按 $\gamma/\eta$ 水平五折留出，测试水平未进入训练，但测试仍覆盖全部 $\beta$。

不将两种协议无说明地合并排名。

### 2.5 参数引导选择（负向验证，方法一段）

正文用一段说明：先以固定 $\delta=0.1$ 的 MDM 或 WMLE 得到初步 $\hat\beta,\hat\eta,\hat\gamma$，再把这些估计当作真参数去查询 L3–L5 对应的条件均值损失曲线并选择 $\delta$。条件集与 L3–L5 相同（仅 $\beta$；$\beta,n$；$\beta,\gamma/\eta,n$），查询方式为就近网格点或对损失曲线连续插值；曲线只用训练折构成。迭代（用新估计重选）在偏移量不变、循环或 10 次更新时停止。本检验只量化“用估计代替真值”损失多少收益，提供负向参照，不与 MLP 排名。

## 第三部分 结果

第三部分只回答“得到了什么”，每节交代数据怎么来、比较对象和直接结论。

### 3.1 统一偏移量接近最优，但仍有逐样本改善空间

#### 结果顺序

1. 展示 $\delta$ 的整体误差曲线：当前 26 点网格最低点为 $\delta=0.06$（$J_1=0.624518$），$\delta=0.08$ 与其几乎相同（0.624666）；经验值 $\delta=0.1$ 为 0.630409，仅比最低点高约 0.93%，已接近最佳统一常数，但 $\delta=0.12$ 已升至 0.638755，不能再概括为“0.08–0.12 均处于同一平缓区”。
2. 用 L1–L6 表展示信息增加后的 $J_1$：0.6252、0.6230、0.5905、0.5885、0.5813、0.4923。
3. 直接结论：改变统一常数收益有限；按形状参数区分后出现明显改善；逐样本选择存在最大潜在空间。

#### 表 1：偏移量选择层级

建议列：规则、选择信息、选择/评价方式、pooled $J_1$、相对 Default 改善、论文角色。

不把 L5 与 MLP 放在同一无协议排名表中。

### 3.2 样本自适应选择获得实质改善

#### 结果顺序

1. 报告三 seed pooled $J_1$ 均值 0.5543，Default 0.6304，L6 0.4923。
2. 报告相对 Default 改善 12.08%，失败率 0%。
3. 报告 $n=7,10,15,20$ 的结果 0.6901、0.5761、0.4855、0.4300。
4. 说明结果位于固定经验值与逐样本 hindsight 之间，表明样本信息恢复了部分潜在收益。

#### 表 2：主方法比较

只列同一留出协议和同测试样本上的 Dimensional-RAW、Default、L6；列 pooled、分 $n$、失败率。Normalized-RAW 可放补充材料，正文只在表示选择说明中一句带过。

#### 图 3：分样本量表现

横轴 $n$，纵轴 $J_1$，三条线为 Dimensional-RAW、Default、L6。图只说明四个已训练样本量下的表现，不使用“未见 $n$ 泛化”措辞。

### 3.3 支撑验证

正文用一张紧凑表回答四个问题，细节放补充材料：

| 问题 | 证据 | 正文结论 |
|---|---|---|
| 是否依赖一次初始化 | 三 seed、60 个 fold×seed 模型 | 结果稳定，不依赖单次训练 |
| 未见参数能否使用 | $\gamma/\eta$ 留出已完成；未见 $\beta$（B1）已完成 | pooled $J_1=0.5418$、改善 14.06%、失败率 0%；限当前离散网格 |
| 与传统方法相比如何 | WMLE/LSE（B2）已完成 | 参数层面 Dimensional-RAW 更优，只作外部参照 |
| 参数收益能否传递到寿命指标 | $x_{0.90},x_{0.95},x_{0.99}$（B3）已完成 | 传递有限，$x_{0.95}$ 相对 RMSE 各方法接近，不预写传递方向 |

若真实案例最终执行，作为本节末尾的经验性例证，不单独承担方法成立与否。

#### 图 4：偏移量选择机制

用代表性折外样本的实际/预测损失曲线说明网络如何选择 $\delta$，再用全部测试样本的所选偏移量—hindsight 偏移量对应关系和超额损失分布约束个例解释。正文只解释机制和误差结构，不把单个样本当作效果证据。

#### 图 5：参数空间中的效果分布

按 $n$ 分面展示 $\beta\times\gamma/\eta$ 组合上相对 Default 的改善。正文说明改善覆盖面和少数退化区域；不只报告总体平均，也不逐格复述数值。

#### 图 6：支撑验证汇总

用三面板概括未见 $\beta$、传统方法外部参照和工程分位点。正文各用一句报告方向和边界，详细分层图表放补充材料。

### 3.4 初步参数估计不能直接恢复参数级收益（一个紧凑结果单元）

正文用一段报告：12 种单步参数引导变体全部劣于 Default（最优 WMLE 初估/仅 $\beta$/连续插值 pooled $J_1=0.6507$ vs Default 0.6304；配对重复块 bootstrap 95% CI $[0.0186,0.0219]$，全部区间严格为正）；仅在 $\beta=1.5$ 改善（$J_1$ 差 $-0.0104$）；迭代一致变差。因果措辞谨慎：邻近 $\beta$ 网格单元正确率（MDM-0.1 19.8%、WMLE 16.5%）与初估误差—损失关联只作为与“错误路由削弱 plug-in”解释一致的证据，不构成唯一机制的证明。详细变体、CI 与分 $\beta$ 见补充图 S5、补充表 S5。

## 第四部分 讨论

第四部分承接第三部分，讨论“这些结果说明什么、为什么可能如此、边界在哪里、实际有什么用”，不重复一遍结果表。

### 4.1 不同可用信息下的偏移量选择

本节不再复述 L1—L6，而是把第三部分的结果转化为实际选择规则：

- 若只能采用一个统一偏移量，当前设计内的网格最低点为 $\delta=0.06$，但 $\delta=0.1$ 的 $J_1$ 仅高约 0.9%；前者适合针对当前参数域进行专门设置，后者仍可作为简洁的默认值；
- 样本量 $n$ 在应用时通常已知，但仅按 $n$ 设置固定偏移量相对 $\delta=0.1$ 只改善约 1.2%，相对最佳统一常数的进一步改善不足 0.4%，没有显示出单独维护多套固定规则的明显必要性；
- 真参数未知时，可利用当前排序样本预测 26 点损失曲线并选择偏移量；在训练条件匹配时，样本自适应方法相对 $\delta=0.1$ 将汇总 $J_1$ 降低约 12.1%；
- 对未训练样本量，或者单位和尺度范围明显不匹配的数据，不直接外推 MLP，应保留固定规则或重新训练。

### 4.2 方法为什么可能有效

讨论保持诚实、不过度证明：

1. 不同 $\delta$ 对有限样本误差的影响并不相同，形成可学习的样本级损失曲线；
2. 排序样本同时保留分布形状、离散程度、低端位置和绝对水平，不需要预先指定哪些统计特征最重要；
3. 预测整条损失曲线比直接预测最佳格点保留了候选之间的相对风险信息；
4. 最终仍由 MDM 估计参数，因此方法只改变估计器族中的选择规则。

“偏差—方差权衡”可作为理解正偏移量作用的解释，但除非以后做正式分解，不写成已验证机制。

参数引导负结果在讨论中作对比解读：直接 plug-in 总体失败且迭代变差，初步 $\hat\beta$ 正确路由率仅约 17%–20%，与初估噪声导致错误路由的解释一致但不构成唯一机制（连续插值同样失败、误差分层可能与真 $\beta$ 混杂）；因此样本到风险曲线的预测不依赖参数初估准确性，是当前设计内可执行的主路线。

### 4.3 局限性

- 参数空间和 $\delta$ 均为离散设计；
- $\eta$ 固定为 1000，有量纲输入导致结论依赖单位和尺度范围；
- 每个 $n$ 独立训练，当前方法不支持任意样本量；
- 未见 $\beta$ 验证只在当前 8 点离散网格上进行（B1），不是连续参数空间外推；
- L6 是 hindsight 参照，不代表可达到的理论最优；
- 真实案例证据后置，不能把 Monte Carlo 结论直接推广到所有工程数据。

### 4.4 实际意义

- 本文把偏移量从预先固定的经验设置转化为可以根据当前样本作出的选择；
- 神经网络只负责选择偏移量，最终参数仍由 MDM 给出，因而没有改变原估计流程及其参数解释；
- 论文的价值是说明在何种信息条件下值得进一步选择偏移量，而不是建立通用 AI 参数估计系统。

## 第五部分 结论

结论只保留四件事：

1. $\delta=0.1$ 已接近最佳统一常数；
2. 不同信息下的选择结果表明仍有明显逐样本空间；
3. Dimensional-RAW-MLP 在当前设计中相对 Default 将 pooled $J_1$ 降低约 12.1%；
4. 该收益限于当前离散参数域、已训练样本量和一致的单位尺度，进一步泛化与真实数据仍需验证。

## 正文图表最小集合

| 编号 | 内容 | 作用 | 当前状态 |
|---|---|---|---|
| 图 1 | MDM 偏移量及样本自适应选择流程 | 解释方法 | 已完成（`figures/main/fig1_method_structure.png`） |
| 表 1 | L1–L6 规则、协议和 $J_1$ | 界定潜在空间 | 已完成（`figures/tables/table1_l1_l6.md`） |
| 图 2 | 整体 $\delta$–风险曲线与信息层级 | 说明 0.1 接近最佳统一常数，并界定潜在空间 | 已完成（`figures/main/fig2_overall_delta_risk.png`） |
| 表 2 | Dimensional-RAW / Default / L6 主结果 | 给出核心效果 | 已完成（`figures/tables/table2_main_results.md`） |
| 图 3 | 三方法按 $n$ 的 $J_1$ | 展示样本量分层 | 已完成（`figures/main/fig3_per_n_J1.png`） |
| 图 4 | 风险曲线预测、偏移量对应关系和超额损失 | 解释选择机制及误差结构 | 已完成（`figures/main/fig4_selector_mechanism.png`） |
| 图 5 | $\beta\times\gamma/\eta\times n$ 的改善分布 | 展示收益覆盖面和退化边界 | 已完成（`figures/main/fig5_parameter_landscape.png`） |
| 图 6 | 未见参数、传统方法和工程分位点 | 汇总支撑验证 | 已完成（`figures/main/fig6_support_validation.png`） |
| 表 3 | 支撑验证摘要 | 回答可信性追问 | 已完成（`figures/tables/table3_support_verification.md`） |

旧 G5 图 6–9 和 S1–S8 基于旧特征路线，已经归档，不能作为当前终稿图继续使用。

参数引导负结果不进正文图（正文保持六张主图），作为补充图 S5 与补充表 S5 放入补充材料。

## 写作纪律

- 正文使用读者语言，不使用 E1/E6/P4 等内部标签。
- 每个图表前交代动机和数据来源，图表后写直接结果。
- 不用“条件异质性”“内部风险最优化”等包装简单事实的术语。
- 结果与讨论分工：第三部分报告观察，第四部分解释含义、可能原因和边界。
- 待补结果只留占位，完成复核前不猜测方向。
