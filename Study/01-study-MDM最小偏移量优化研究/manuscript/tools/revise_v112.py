"""Create the focused v1.12/v1.10 revision from the preserved working baseline."""
from pathlib import Path
import re

M=Path(__file__).resolve().parents[1]

def between(s,start,end,new):
    a=s.index(start); b=s.index(end,a)
    return s[:a]+new+'\n\n'+s[b:]

def main():
    s=(M/'Study01论文初稿-v1.11.md').read_text(encoding='utf-8')
    s=s.replace('中文修订稿 v1.11，配套[附录 v1.9](Study01论文附录-v1.9.md)',
                '中文修订稿 v1.12，配套[附录 v1.10](Study01论文附录-v1.10.md)')
    s=s.replace('通过正偏移量改善小样本估计的稳定性，但经验值 0.1 的适用范围及进一步选择的依据尚不明确。',
                '通过正偏移量改善小样本估计的精度与稳定性，但偏移量如何影响联合误差、经验值 0.1 应如何进一步选择，尚需系统研究。',1)
    s=s.replace('经验值 0.1 接近完整设计的最佳统一取值；固定宽度形状参数域',
                '正偏移降低组合内方差的同时改变条件偏差，经验值 0.1 接近完整设计的最佳统一取值；固定宽度形状参数域',1)
    s=s.replace('但 $0.1$ 是根据算例确定的经验取值，是否存在更合适的偏移量以及如何选择，仍值得研究。',
                '该研究指出，随机样本在真参数处未必满足零梯度判据，并以经验偏移量调整搜索位置；$0.1$ 来自 120 个估计案例，更适当的取值与选择方式仍待研究[3]。因此，进一步完善 MDM，需要说明偏移量怎样改变估计误差，并建立超出统一经验常数的选择依据。')
    old='若候选 $\\beta$ 和 $\\gamma$ 与样本所来自的分布相符，$\\eta_i$ 之间的差异应较小。本文以样本标准差 $s_\\eta(\\beta,\\gamma)$ 表示这种差异。对每个候选 $\\gamma$，先求'
    new=r'''若各样本点恰为秩概率 $F_i$ 对应的理论分位点，代入真形状参数与真位置参数后，各 $\eta_i$ 均等于真尺度参数。实际抽样得到的 $t_{(i)}$ 通常偏离这些分位点，因而即使代入真参数，伪尺度参数之间仍存在差异。MDM 利用这种差异随候选参数变化的规律进行估计，但某次样本的差异最小点未必对应真参数[3]。

本文以样本标准差 $s_\eta(\beta,\gamma)$ 表示伪尺度参数之间的差异。对每个候选 $\gamma$，先求'''
    assert old in s;s=s.replace(old,new)
    s=between(s,'因此，本文围绕位置参数搜索判据中的','### 2.2',r'''对于一个给定样本，上述计算随候选 $\gamma$ 形成一组相互对应的三参数估计：$\beta^*(\gamma)$ 由条件最小差异确定，尺度参数由伪尺度参数的平均值确定。调节 $\delta$ 改变的是梯度判据在这组候选解中的选择位置。它先改变位置估计，再通过形状参数的条件优化和尺度参数的计算，使三个估计量共同变化。因此，偏移量虽只出现在位置参数判据中，其效果需要以三参数联合误差评价。

经验正偏移量的作用可以从重复抽样理解。不同样本形成不同的廓线，零梯度判据在这些廓线上的落点可能较为分散；改用正梯度水平后，估计的抽样离散程度与平均位置都会变化[3]。离散程度下降有利于提高精度，但估计中心也可能偏离真值。合适的偏移量因而取决于这两种变化共同形成的误差，而不能仅由结果是否集中判断。第 3.1 节用重复抽样数据分解这两部分影响。

在同一参数条件下，抽样得到的次序统计量与间距仍有差异，候选解路径及其相对真值的位置也随之改变。针对该条件选择一个平均误差较低的偏移量，可以改善重复抽样表现；进一步利用当前样本，则可能区分同一条件内不同候选的相对优劣。这两种选择针对同一个参数估计目标，区别在于所利用的信息。交点对样本曲线变化的局部关系见附录 B.7。''')
    start='汇总时各样本等权，Bias 为标准化误差的均值，SD 包含组合内抽样波动和组合间平均误差差异。稳定性比较则先在每个参数组合内根据 300 次重复抽样计算标准化误差 SD，再汇总 160 个组合的分布。'
    new=r'''汇总时各样本等权，Bias 为标准化误差的均值，SD 包含组合内抽样波动和组合间平均误差差异。本文所讨论的抽样稳定性以固定参数组合内的标准化误差 SD 衡量。

为解释偏移量改变精度的来源，在每个参数组合 $c$ 内分别计算三个标准化误差 $e_{c,k}$ 的均值与方差，再按设计权重 $w_c$ 汇总：

$$
\begin{aligned}
J_1^2(\delta)&=B^2(\delta)+V(\delta),\\
B^2(\delta)&=\sum_cw_c\sum_k\bar e_{c,k}(\delta)^2,\\
V(\delta)&=\sum_cw_c\sum_k v_{c,k}(\delta).
\end{aligned}
$$

其中 $k\in\{\beta,\eta,\gamma\}$，当前设计 $w_c=1/160$；$v_{c,k}$ 为组合内以重复数作分母的经验方差。$B^2$ 衡量各组合估计中心偏离真值的程度，$V$ 衡量组合内的抽样离散程度。二者均先在固定真参数条件下计算，从而保留条件偏差，避免不同组合的正负偏差在混合平均中抵消。具体计算及逐参数 SD 分布见附录 A.6。'''
    assert start in s;s=s.replace(start,new)
    s=between(s,'正偏移量首先收窄了重复抽样下的估计波动。','上述汇总结果仍留下一个问题',r'''正偏移量通过改变条件偏差与抽样方差，影响联合估计精度。由 $\delta=0$ 调至 0.10 时，三参数标准化误差的组合内方差项 $V$ 从 0.7721 降至 0.2062，条件偏差平方项 $B^2$ 则从 0.1269 增至 0.1912。方差项减少 0.5659，超过偏差平方项增加的 0.0643，使联合均方误差从 0.8990 降至 0.3974，$J_1$ 从 0.9481 降至 0.6304（图 2）。这说明，在当前设计中，经验正偏移量的精度收益主要来自抽样离散程度的大幅收窄。逐参数比较也显示，$\beta$、$\eta$ 和 $\gamma$ 的组合内标准化 SD 中位数分别下降 57.0%、39.0% 和 40.9%（附录图 A1）。

方差较小并不意味着联合误差最低。继续增大偏移量至 0.50，方差项进一步降至 0.1176，偏差平方项却增至 0.4628，$J_1$ 回升至 0.7619。26 点扫描中，联合误差最低点位于 $\delta=0.06$，$J_1=0.624518$；$\delta=0.08$ 和 0.10 时分别为 0.624666 和 0.630409。经验值 0.10 比网格最低值高 0.93%，处于当前完整设计的低风险区域。因此，偏移量选择应依据偏差与方差共同决定的联合误差，在原有稳定性改善的基础上寻找更合适的估计落点。

![偏移量对联合误差及其偏差方差分量的影响](figures/main/fig2_offset_risk_decomposition_v112.png)

**图 2  偏移量对联合估计误差的影响。** **a，** 26 个候选偏移量的汇总 $J_1$；**b，** 相应的联合均方误差 $J_1^2$、条件偏差平方项 $B^2$ 与组合内方差项 $V$。各分量先根据每个参数组合的 300 次重复抽样计算，再对 160 个组合等权汇总。''')
    s=between(s,'相同真参数下，不同样本的经验梯度轨迹','### 3.5',r'''相同真参数下，不同抽样结果形成不同的经验梯度曲线，也改变偏移量对应的候选损失。图 7a、b 将 $(\beta,\gamma/\eta,n)=(3,0.5,10)$ 下三个样本的交点与联合损失相对应。对样本 A，将偏移量从 0.10 提高到事后低损失点 0.14，位置估计向较大值移动；对样本 B，将偏移量降到 0.04，位置估计向较小值移动。样本 C 的事后最低点位于 0。三个样本来自同一总体，却沿各自的候选解路径在不同偏移量下取得较低损失。这说明偏移量的选择既受到总体条件影响，也与本次抽样所形成的估计路径有关。

这一样本差异在分组诊断中同样可见。在 20 个预设诊断单元、2,000 个确认样本中，固定 $\delta=0.1$ 时的位置估计 $\hat\gamma_{0.1}/\eta$ 与 L6 偏移量在全部单元内均呈负相关，Spearman 相关系数中位数为 $-0.765$，四分位区间为 $[-0.803,-0.609]$（图 7d）。按该位置估计从低到高分组后，平均超额损失曲线的最低点依次位于 $\delta=0.10$、0.04 和 0.02（图 7c）。因此，固定规则得到的样本估计位置，与候选损失的相对高低存在联系，为利用样本信息选择偏移量提供了依据。完整指标见附录 F.3，损失曲线预测及选点诊断见附录图 B1。

![样本梯度交点与对应候选损失](figures/main/fig7_sample_path_loss_v112.png)

**图 7  样本实现、搜索交点与候选损失。** **a，** 同一参数组合下三个样本的梯度曲线交点区域，竖虚线为真位置 $\gamma/\eta=0.5$；**b，** 同三个样本的 26 点联合损失。空心方块为固定 $\delta=0.1$ 的结果，实心圆为逐样本 L6 结果。**c，** 2,000 个确认样本按单元内固定偏移量位置估计分为低、中、高三组后的平均超额损失，圆点为各组最低点；**d，** 各诊断单元内固定位置估计与 L6 偏移量的 Spearman 相关，横线为各样本量下的中位数。''')
    s=s.replace('统一偏移量的含义取决于它所针对的参数范围、样本量构成和条件权重。',
                '偏移量为 MDM 提供了调整有限样本估计表现的自由度。它通过搜索判据选择三参数候选解，改变估计中心与抽样离散程度；图 2 的分解表明，最小抽样方差与最低联合误差对应的偏移量并不相同。统一值需要在所针对的参数范围、样本量构成和条件权重下，按联合误差确定。',1)
    s=between(s,'这种样本差异可以沿 MDM 的计算过程理解。','归一化排序样本保留了',r'''同一真参数决定的是抽样分布，某个样本的次序统计量和间距则决定当前 MDM 廓线。偏移量是一项估计规则的调节量，其低损失取值取决于样本所形成的候选解路径，而不是总体固有的另一个分布参数。因此，同一真值下出现不同的事后低风险偏移量，与条件平均规则并不矛盾：前者针对本次实现的损失，后者针对该条件下重复抽样的平均损失。图 7 将梯度交点与同样本损失联系起来，并以分组结果说明这种联系具有可用于选择的统计规律。

样本自适应规则的任务，是利用当前可观测信息判断哪些候选偏移量更可能给出较小误差。真参数及本次抽样误差在实际估计时不可知，因而需要在训练设计中学习样本特征与候选损失之间的关系，而不能直接恢复每个样本的事后最佳值。''')
    s=s.replace('1. 统一偏移量取决于评价所采用的参数范围、样本量构成和条件权重。',
                '1. 偏移量通过改变梯度判据的搜索落点，使三个参数的估计联动变化。当前设计中，正偏移降低抽样方差的收益超过偏差增加的代价，从而改善联合估计精度；更合适的统一值则取决于评价所采用的参数范围、样本量构成和条件权重。')
    (M/'Study01论文初稿-v1.12.md').write_text(s,encoding='utf-8')

    a=(M/'Study01论文附录-v1.9.md').read_text(encoding='utf-8')
    a=a.replace('中文修订稿 v1.9','中文修订稿 v1.10').replace('正文 v1.11','正文 v1.12').replace('Study01论文初稿-v1.11.md','Study01论文初稿-v1.12.md')
    addition=r'''### A.6 偏移量风险的条件偏差与方差分解

本分析复用正文 160 个参数组合、每组合 300 次重复的完整 26 点扫描。记 $e_{c,r,k}(\delta)$ 为组合 $c$、重复 $r$、参数 $k$ 的标准化误差，其标准化分母与正文联合损失一致。定义

$$
\bar e_{c,k}(\delta)=\frac{1}{R}\sum_{r=1}^{R}e_{c,r,k}(\delta),\qquad
v_{c,k}(\delta)=\frac{1}{R}\sum_{r=1}^{R}
\left[e_{c,r,k}(\delta)-\bar e_{c,k}(\delta)\right]^2,\qquad R=300.
$$

对各参数的条件偏差平方与组合内方差分别求和，再对组合等权平均，得到正文 $B^2$ 和 $V$，并满足经验恒等式 $J_1^2=B^2+V$。这里采用分母 $R$ 保证分解与当前扫描的经验均方误差完全一致；$B^2$ 是有限重复下的条件偏差平方汇总。图 A1 的 SD 采用分母 $R-1$ 的样本标准差，与原有稳定性比较保持一致。

**表 A4  固定偏移量下联合误差的条件分解。**

| $\delta$ | $B^2$ | $V$ | $J_1^2$ | $J_1$ |
|---:|---:|---:|---:|---:|
| 0.00 | 0.126873 | 0.772114 | 0.898987 | 0.948149 |
| 0.06 | 0.121924 | 0.268099 | 0.390023 | 0.624518 |
| 0.10 | 0.191168 | 0.206248 | 0.397416 | 0.630409 |
| 0.50 | 0.462845 | 0.117624 | 0.580469 | 0.761885 |

由 $\delta=0$ 调至 0.10，三参数方差之和在全部 160 个组合下降，其中 65 个组合的偏差平方之和也下降，126 个组合的联合均方误差下降。因此，汇总结果中的偏差—方差变化不要求每个组合均发生方向相同的交换。

逐参数 SD 的组合间中位数在 $\delta=0$ 时分别为 0.5870、0.4241 和 0.3782，在 $\delta=0.10$ 时分别为 0.2526、0.2589 和 0.2236。$\beta$ 和 $\eta$ 的 160 个组合均表现为波动减小，$\gamma$ 为 154 个组合。

![固定正偏移量的组合内抽样标准差](figures/supplementary/supp_fig_offset_sd_v110.png)

**图 A1  固定正偏移量与无偏移判据的抽样波动。** 每个分布含 160 个参数组合的组合内标准化估计 SD，每组合含 300 次重复。小提琴表示分布，箱体为四分位距，中线为中位数，须线延伸至 1.5 倍四分位距内的最远观测。

'''
    a=a.replace('## 附录 B  网络训练、输入表示与稳定性',addition+'## 附录 B  网络训练、输入表示与稳定性')
    a=a.replace('记参考曲线为 $g_{\\mathrm{ref}}$，曲线差为 $e_X$，',
                '记参考梯度曲线为 $g_{\\mathrm{ref}}(\\gamma)=\\nabla_{X^{\\mathrm{ref}}}(\\gamma)$，曲线差为 $e_X(\\gamma)=\\nabla_X(\\gamma)-g_{\\mathrm{ref}}(\\gamma)$，')
    a=a.replace('当 $|e\'_X|\\ll|g\'_\\mathrm{ref}|$ 且参考斜率非零时，得到正文的一阶交点差。',r'''当 $|e'_X|\ll|g'_{\mathrm{ref}}|$ 且参考斜率非零时，得到一阶交点差

$$
\hat\gamma_{\delta,X}-\hat\gamma_{\delta,\mathrm{ref}}
\approx-\frac{e_X(\hat\gamma_{\delta,\mathrm{ref}})}
{g'_{\mathrm{ref}}(\hat\gamma_{\delta,\mathrm{ref}})}.
$$

''')
    # The precise original spelling differs between versions; resolve by section-local substitution.
    if '得到正文的一阶交点差。' in a:
        a=a.replace('得到正文的一阶交点差。',r'''得到一阶交点差

$$
\hat\gamma_{\delta,X}-\hat\gamma_{\delta,\mathrm{ref}}
\approx-\frac{e_X(\hat\gamma_{\delta,\mathrm{ref}})}
{g'_{\mathrm{ref}}(\hat\gamma_{\delta,\mathrm{ref}})}.
$$

''')
    (M/'Study01论文附录-v1.10.md').write_text(a,encoding='utf-8')

if __name__=='__main__':main()
