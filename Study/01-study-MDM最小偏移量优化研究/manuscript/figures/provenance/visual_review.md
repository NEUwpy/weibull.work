# Study01 投稿图视觉复核

> 复核日期：2026-08-29。当前包含 9 张正文图、7 张稿件引用的附录图和 1 张仅供复核的 seed 稳定性图，每张均导出 PNG、SVG、PDF 和 TIFF。

## 复核结论

- 图 1：全网格、低风险区放大和组合内稳定性三个面板的层次清楚；固定偏移量的作用与后续选择问题分开呈现。
- 图 2：训练与应用路径分开，输入归一化、折内标准化、26 点损失曲线与 MDM 参数估计职责清楚。
- 图 3：面板 a 在 $\beta$–$\gamma/\eta$–$n$ 完整离散设计中标出两个相邻且重叠的固定宽度 $\beta$ 参数域；面板 b、c 分别以三维地形和俯视图展示原始 $J_1$。11 条实际 26 点曲线、离散最低点、1% 近优区间和 $\delta=0.10$ 参照均清楚可辨；插值曲面只辅助观察，且面板 c 的标题、图框和色标已纵向对齐。
- 图 4：五个等大三维面板清楚表示 L1–L5 对同一 $8\times5\times4$ 设计空间的逐层划分；L4、L5 的线和点、三轴刻度及分组数量均可辨识。该图只定义信息条件，不重复表 2 的性能数值。
- 图 5：pooled 风险与分 $n$ 降幅两个面板均使用同一 L1–L6 顺序；L1/L2 的小幅变化和 L3–L5 的较大下降可直接比较，L6 以虚线分隔为事后参照。
- 图 6：主方法相对 Default 的分 $n$ 改善与逐样本损失分布分工明确；阴影只标注 Default–L6 观测差距。
- 图 7：代表曲线、偏移量对应和超额损失三个面板无遮挡，个例与总体证据职责分开。
- 图 8：面板 a 显示同一参数条件下三次真实抽样对应的经验梯度曲线及 L6 交点；面板 b 显示按默认偏移量位置估计分层后的平均超额损失曲线；面板 c 汇总 20 个参数—样本量单元内的位置估计与 L6 偏移量相关。标签、数值和注释无裁切或重叠。
- 图 9：未见 $\beta$、传统方法和可靠度寿命三类支撑验证使用一致配色，不在正文图重复 seed 信息。
- 附录 F1：原正文 E10 条件风险图完整保留，同条件内 L6 选点多样性和五种决策条件的风险职责清楚。
- 附录 F2：原参数空间热图转为支撑性诊断，35 个退化单元保留勾边与数值。
- 附录 F3：四条分 $n$ 数据量曲线可辨识，图注与绘图区分离，明确它只是描述性诊断。

## 自动检查

`scripts/qa_submission_figures.py` 对 17 张图的 68 个导出文件完成以下检查：

- PNG 尺寸和文件体量；
- PDF 头与 SVG 可编辑文本；
- 正文与附录派生 CSV 的数值、行数、分组和单调性；
- E11 的 20 个机制诊断单元、代表梯度曲线和条件超额损失最低点；
- E13 的 11 个固定宽度参数域、区间中心和离散最低点序列；
- 图 4 中 160 个设计单元及 L1–L5 的 1/4/8/32/160 组唯一划分；
- 图 5 中 Default 与 L1–L6 的 pooled 数值、分样本量降幅及 L1/L2 与 L3–L5 的层级关系；
- E10 的 160 个参数条件、五种确认风险及 4×5 数据量诊断；
- 导出文件 SHA256 清单与生成代码/数据源 manifest。

自动结论为 `PASS`。
