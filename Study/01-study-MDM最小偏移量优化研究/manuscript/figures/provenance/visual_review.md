# Study01 投稿图视觉复核

> 复核日期：2026-08-20。当前包含 6 张正文图、7 张稿件引用的附录图和 1 张仅供复核的 seed 稳定性图，每张均导出 PNG、SVG、PDF 和 TIFF。

## 复核结论

- 图 1：训练与应用路径分开，输入归一化、折内标准化、26 点损失曲线与 MDM 参数估计职责清楚。
- 图 2：全网格、低风险区放大和决策参照三个面板的层次清楚；不再把 L1–L6 标为单一可实现阶梯。
- 图 3：主方法相对 Default 的分 $n$ 改善与逐样本损失分布分工明确；阴影只标注 Default–L6 观测差距。
- 图 4：代表曲线、偏移量对应和超额损失三个面板无遮挡，个例与总体证据职责分开。
- 图 5：面板 a 显示同一参数条件下三次真实抽样对应的经验梯度曲线及 L6 交点；面板 b 显示按默认偏移量位置估计分层后的平均超额损失曲线；面板 c 汇总 20 个参数—样本量单元内的位置估计与 L6 偏移量相关。标签、数值和注释无裁切或重叠。
- 图 6：未见 $\beta$、传统方法和可靠度寿命三类支撑验证使用一致配色，不在正文图重复 seed 信息。
- 附录 F1：原正文 E10 条件风险图完整保留，同条件内 L6 选点多样性和五种决策条件的风险职责清楚。
- 附录 F2：原参数空间热图转为支撑性诊断，35 个退化单元保留勾边与数值。
- 附录 F3：四条分 $n$ 数据量曲线可辨识，图注与绘图区分离，明确它只是描述性诊断。

## 自动检查

`scripts/qa_submission_figures.py` 对 14 张图的 56 个导出文件完成以下检查：

- PNG 尺寸和文件体量；
- PDF 头与 SVG 可编辑文本；
- 正文与附录派生 CSV 的数值、行数、分组和单调性；
- E11 的 20 个机制诊断单元、代表梯度曲线和条件超额损失最低点；
- E10 的 160 个参数条件、五种确认风险及 4×5 数据量诊断；
- 导出文件 SHA256 清单与生成代码/数据源 manifest。

自动结论为 `PASS`。
