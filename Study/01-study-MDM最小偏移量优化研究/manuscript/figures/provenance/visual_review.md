# Study01 投稿图视觉复核

> 复核日期：2026-08-20。当前包含 6 张正文图、6 张稿件引用的附录图和 1 张仅供复核的 seed 稳定性图，每张均导出 PNG、SVG、PDF 和 TIFF。

## 复核结论

- 图 1：训练与应用路径分开，输入归一化、折内标准化、26 点损失曲线与 MDM 参数估计职责清楚。
- 图 2：全网格、低风险区放大和决策参照三个面板的层次清楚；不再把 L1–L6 标为单一可实现阶梯。
- 图 3：主方法相对 Default 的分 $n$ 改善与逐样本损失分布分工明确；阴影只标注 Default–L6 观测差距。
- 图 4：代表曲线、偏移量对应和超额损失三个面板无遮挡，个例与总体证据职责分开。
- 图 5：面板 a 同时显示同条件内 L6 选点多样性与三种完全命中率；面板 b 以 $R=J_1^2$ 显示五种决策条件，并在图内明确经验参照不是 Bayes 最优。标签、数值和注释无裁切或重叠。
- 图 6：未见 $\beta$、传统方法和可靠度寿命三类支撑验证使用一致配色，不在正文图重复 seed 信息。
- 附录 F1：原参数空间热图转为支撑性诊断，35 个退化单元保留勾边与数值。
- 附录 F2：四条分 $n$ 数据量曲线可辨识，图注与绘图区分离，明确它只是描述性诊断。

## 自动检查

`scripts/qa_submission_figures.py` 对 13 张图的 52 个导出文件完成以下检查：

- PNG 尺寸和文件体量；
- PDF 头与 SVG 可编辑文本；
- 正文与附录派生 CSV 的数值、行数、分组和单调性；
- E10 的 160 个参数条件、五种确认风险及 4×5 数据量诊断；
- 导出文件 SHA256 清单与生成代码/数据源 manifest。

自动结论为 `PASS`。
