# 图 1 视觉检查

## 静态检查的适配

原技能脚本把组内相对坐标当成画布坐标，把连接端口当成节点中心，并按 LaTeX 源码长度估计公式宽度，产生了大量误报。保留原始报告；本地适配器仅修正这三种解析，重新运行全部几何规则。最小值标记与自己的曲线重叠是有意标注。适配后真实 FAIL 为零。

逐项审阅剩余 16 个 WARN：9 个空框警告对应有子文字的分组；1 个低密度警告因为分组被排除于普通形状计数；6 个线密度警告对应每条候选曲线的 25 条绘线段，均不是数据流箭头。需要在实际导出中确认标签和连接。

## 第 1 轮：draw.io 原生 PNG 导出

九区检查：左上标题可读；上中标签构造与实际曲线一致；右上监督误差汇合明确；左中仅样本支路需要进一步明示；中部两条曲线无文字遮挡；右中反馈线无穿框；左下应用入口及归一化完整；下中模型说明意外断行；右下 MDM 标题被拆成两行。全图无外部图像或公式源码残留。

P1：`fixed_predictor_sub` 的“固定”被拆行；`mdm_title` 的“拟合”被拆行。修复为“分样本量模型”和“MDM／参数拟合”。P1 语义：从含真参数的模拟样本卡片向预测器分支增加“仅样本”，避免误解为真参数输入。P2：不再增加跨面板模型复用箭头，以已训练 MLP 标签表达，避免与反馈和标题交叉。

本记录只列实见问题，不虚构缺陷数量。前期使用原图核对语义，并未复制其布局。

## 第 2 轮：实际尺寸 SVG / PDF 导出

九区逐一检查：训练标题、标签构造、误差汇合、样本分支、两条曲线、反馈、应用入口、选点及旁路、参数输出均完整。第 1 轮的两处节点断行已消除，“仅样本”标签未穿过箭头。

P0（导出问题）：纸面导出样式最初误作用于内部 MathJax SVG，使公式被整体放大。已限定外层尺寸，并以独立 SVG 图像加载导出，复核 7 处公式均恢复正常，文稿图片不受此内部导出错误影响。SVG / PDF 采用原生 draw.io SVG 派生；PNG 直接由 draw.io 导出。未把有错误的预览写入正文。

## 第 3 轮：180 mm 正文宽度验收

对最终导出按 180 mm 宽度检查九区：标题和节点层级可辨；左右边缘文字完整；真参数说明与仅样本分支一致；实际/预测损失区分明确；反馈止于预测模块；下方输入→变换→预测→选点→MDM→输出完整；原始样本旁路无交叉；符号与图注一致；底部图例未裁切。剩余 P0/P1：0。

语义复核：没有以真参数作为应用输入；每样本 26 个候选的实际损失用于标签；训练误差为标准化曲线预测的平方误差；运行时使用对应样本量模型；模型选择偏移量而不直接输出 Weibull 参数；原始样本进入 MDM；曲线注明示意；不把图中曲线当作实测结果。

自评：清晰度 9/10、语义 9/10、层级 8/10、编辑性 10/10、纸面可读性 8/10，共 44/50。此为内部复核，不代替作者的审美反馈。近似项：示意曲线不对应真实样本；所有曲线节点及模型框均可编辑。细小刻度是辅助标记，核心阅读依赖节点标题与路径。
