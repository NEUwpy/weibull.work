# Study01 图表规范化说明

当前中文稿仍为正文 v1.8、附录 v1.7。本轮仅整理图表与阅读格式，RESS 适配暂缓，英文转换留在中文定稿之后。

## 已处理

- 17 张图像资产统一普通文字、刻度和图例的字号下限至 8.6 pt；数学字母及上下标至少 6 pt。所有导出图的原生宽度不超过 190 mm。
- 重新安排方法流程、主结果、机制诊断及附录密集面板的留白、图例和标注，去除压住数据的重复说明。
- 正文保留六图；附录保留八图。原分组图 A1 改为可编辑表 A2，复现清单补编号为表 A3；另保留三张复核图像资产。
- HTML 预览采用原生图宽、9 pt 三线表、数值对齐和可断行路径；公式仍通过实际 KaTeX 渲染检查。
- 四格式图像导出齐全：可编辑 SVG、嵌入字体 PDF、300 dpi PNG、600 dpi TIFF。后续排版宜按原生尺寸插入，不能再大幅缩小图片。

## 验证

图像与数值检查通过：17 张资产、68 个导出文件；26 个派生 CSV 与字号修改前快照逐项完全相同。正文与附录共 455 处公式通过渲染检查，无错误公式、图片断链或公式/表格溢出。所有图像经过联系图检查，密集面板另行逐张查看。未新增模拟实验或训练网络。

复核记录见 `figures/provenance/visual_review.md`、`typography_qa.json` 和 `submission_figure_qa.json`。修改前完整稿件与图像快照位于 `figures/archive/replaced/pre-font-standardization-20260905/`。

## 后续叙述讨论

正文宜围绕三条结论组织证据：统一偏移量依赖评价参数域；样本信息支持更细致的偏移量选择；现有设计下的自适应选择降低汇总联合误差，且改善覆盖四个样本量和三个参数的汇总标准化 RMSE。

不必交代每次尝试与返工。初估参数规则的 12 种变体、网络探索过程和重复的诊断细节可以留在附录或研究记录；只有回答当前论文必要问题时，正文才简要引用。局部退化、评价复用和可靠度寿命的不同排序会影响结论的适用范围，应集中、简洁地说明，避免在结果、讨论和结论中反复出现。

这一轮没有改写论文证据主线；后续可据此逐段删减重复说明，重排最有力的结果，而不按研究发生的先后顺序展开。
