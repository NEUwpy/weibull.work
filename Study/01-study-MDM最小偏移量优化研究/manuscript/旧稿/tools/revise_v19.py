"""Build the evidence-led Chinese revision from the preserved v1.8 snapshot."""
from pathlib import Path
import re

M = Path(__file__).resolve().parents[1]
OLD = M / "figures/archive/replaced/pre-v1.9-20260905"
assert not (M / "Study01论文初稿-v1.9.md").exists(), "Revision already exists; do not overwrite"
assert not (M / "Study01论文附录-v1.8.md").exists(), "Appendix already exists; do not overwrite"
s = (OLD / "Study01论文初稿-v1.8.md").read_text(encoding="utf-8")

def section(start, end, replacement):
    global s
    a, b = s.index(start), s.index(end, s.index(start))
    s = s[:a] + replacement.strip() + "\n\n" + s[b:]

def paragraph(prefix, replacement):
    global s
    a = s.index(prefix)
    b = s.index("\n\n", a)
    s = s[:a] + replacement.strip() + s[b:]

s = s.replace("中文修订稿 v1.8，配套附录 v1.7。正文以固定随机种子 42 的折外模型报告主结果，不作多种子集成，另外两个随机种子用于附录中的初始化敏感性核查。作者、单位、基金和目标期刊格式待定。",
              "中文修订稿 v1.9，配套附录 v1.8。作者、单位和基金待补；目标期刊暂缓确定，英文转换在中文定稿后进行。")
section("## 摘要", "**关键词：**", r"""
## 摘要

三参数 Weibull 分布的最小差异法（MDM）采用正偏移量改善小样本估计，但经验常数难以适应不同参数条件及样本波动。本文研究偏移量的有限样本风险，并提出基于归一化排序样本的自适应选择方法：预测 26 个候选偏移量的损失，再由 MDM 在所选偏移量下估计参数。在 $1.5\leq\beta\leq5.0$、$0.10\leq\gamma/\eta\leq1.00$、$n\in\{7,10,15,20\}$ 的 160 个离散组合中，经验值 0.1 接近完整设计的最佳统一取值，而固定宽度形状参数域由低值向高值平移时，最佳统一偏移量由 0.18 移至 0.04。按位置尺度比水平留出评价，自适应选择将联合误差 $J_1$ 从 0.6304 降至 0.5846，降低 7.27%（已训练选择器和当前设计条件下的 95% 配对重采样区间：6.94%—7.64%）；改善覆盖四个样本量，三个参数的汇总标准化 RMSE 均下降。梯度轨迹与分组损失曲线显示，同一参数条件下的样本波动伴随低风险偏移量的移动。上述结果支持在所评价设计内利用样本信息改进 MDM 偏移量选择；现有折外数据参与过输入表示筛选，仍需独立确认。
""")
paragraph("本文以三参数 Weibull 小样本 MDM", r"""本文围绕两个问题展开：偏移量的适宜取值由哪些条件决定，以及总体参数未知时能否仅依据观测样本作出有效选择。首先用重复抽样风险区分统一取值、参数条件规则和逐样本事后参照，识别偏移量随参数域及样本变化的规律；随后以归一化排序样本预测候选损失曲线，检验这种差异能否转化为参数估计收益；最后通过 MDM 梯度轨迹解释样本波动与低风险偏移量之间的联系。本文的贡献在于偏移量的有限样本风险规律及其样本自适应实现，网络承担候选损失预测任务。""")
paragraph("该式仅解释给定两条样本曲线", r"""该局部关系说明，改变 $\delta$ 会改变交点所处的曲线位置，并通过 MDM 联合求解影响三个参数的误差。它描述两条给定样本曲线的交点差；估计稳定性与风险变化由重复抽样评价。展开条件及边界见附录 B.7。""")
paragraph("其中，$J_{1,m}$", r"""其中，$J_{1,m}$ 为方法 $m$ 在相同评价样本上的汇总指标。正值表示误差降低，负值表示误差增加。下文的“$J_1$ 降幅”均按此定义计算。附录 F 的条件风险诊断另以均方损失 $\mathcal R=J_1^2$ 定义可加的风险差。""")
paragraph("$J_1$ 汇总三个参数", r"""$J_1$ 越小，表示三个标准化参数误差等权汇总后的联合误差越小。该权重定义本文的参数估计目标。训练时使用单样本损失 $\ell_i(\delta)$ 构造 26 点曲线，评价时使用 $J_1$ 汇总，并分别报告三个参数的标准化 Bias、SD 和 RMSE。稳定性比较则先在每个参数组合内根据 300 次重复抽样计算标准化误差 SD，再汇总 160 个组合的分布。""")
old_domain = s[s.index("为检验统一低风险偏移量"):s.index("\n\n",s.index("为检验统一低风险偏移量"))]
paragraph("为检验统一低风险偏移量", r"""为检验统一偏移量对参数域的敏感性，将 $\beta$ 水平加密至步长 0.25，构造宽度为 1.00、包含 5 个连续水平的 11 个滑动区间，区间中心由 2.00 移至 4.50。其余参数、样本量和权重保持不变，每个参数组合使用 100 次重复。各域分别计算 26 点汇总风险曲线、离散最低点及最低 $J_1$ 以上 1% 内的候选范围，并以 5,000 次重复块 bootstrap 描述最低点稳定性；样本构造见附录 A.1。""")
paragraph("以上结果说明，正偏移量", r"""固定经验值因此提供了低风险基准；进一步选择的依据在于参数域和样本之间的风险差异。""")
paragraph("固定宽度 $\\beta$ 域平移后", r"""固定宽度形状参数域的比较显示，统一偏移量随参数域有序移动（图 3）。区间由 $[1.50,2.50]$ 移至 $[4.00,5.00]$ 时，离散最低点由 $\delta=0.18$ 降至 0.04，1% 近优范围由 0.16—0.26 移至 0.04。$\delta=0.10$ 在 $[2.25,3.25]$ 和 $[2.50,3.50]$ 内达到最低风险，在最低与最高两个区间则分别比最低 $J_1$ 高 5.06% 和 9.16%。重复块 bootstrap 的最低点区间支持这一移动方向。统一偏移量因而是针对给定参数域的风险折中。""")
paragraph("以固定 $\\delta=0.10$ 为基准", r"""重新选择统一偏移量或仅按样本量查表的收益较小：L1 和 L2 的 $J_1$ 分别为 0.6252 和 0.6230，相对 Default 降低 0.83% 和 1.17%（表 2）。""")
paragraph("图 4 将表 2", r"""分样本量结果呈现相同趋势：L1/L2 的降幅均不足 2%，L3—L5 在四个样本量下均有更大改善（图 4）。因此，参数条件比单独的样本量分类提供了更充分的选择依据。""")
paragraph("L3–L5 说明参数条件", r"""参数条件和样本实现中的风险差异为自适应选择提供了依据。下一节检验归一化排序样本能否在无需真参数输入的情况下，将这种差异转化为估计收益。""")
s = s.replace("### 3.3 样本自适应偏移量选择", "### 3.3 样本自适应选择的参数估计收益")
# Retain conditional interval interpretation beside its primary estimate.
paragraph("在按每个 $n$ 的", r"""在位置尺度比水平留出评价的同一 48,000 个样本上，样本自适应选择将汇总 $J_1$ 从固定偏移量的 0.630409 降至 0.584553，相对降低 7.27%；配对重复块 bootstrap 95% 区间为 6.94%—7.64%（表 3）。该区间条件于已训练选择器和当前设计，不包含表示筛选与重新训练的不确定性。L6 的 $J_1$ 为 0.492297，三种规则均无估计失败。""")
paragraph("各样本等权汇总。这里的 Bias", r"""注：各样本等权。$\beta$、$\eta$ 误差分别除以各自真值，$\gamma$ 误差除以真尺度参数 $\eta$。Bias 和 SD 为混合设计下标准化误差的均值和标准差；SD 同时包含组合内抽样波动与组合间平均误差差异，口径不同于图 2 的组合内 SD。P5–P95 为标准化带符号误差的第 5—95 百分位区间。""")
paragraph("四个已训练样本量下", r"""改善覆盖四个已训练样本量。$n=7,10,15,20$ 时，$J_1$ 相对固定偏移量分别降低 8.12%、7.62%、6.32% 和 5.98%（图 5a）；各方法的误差均随样本量增大而下降。""")
paragraph("汇总 $J_1$ 之外", r"""逐样本配对结果也呈现整体改善。令 $\Delta\ell_i=\ell_{i,\mathrm{Default}}-\ell_{i,\mathrm{Adaptive}}$，正值表示自适应误差更低。四个样本量下，正值样本比例分别为 62.5%、66.0%、62.2% 和 58.5%，各组中位数均为正（图 5b）。""")
hetero = s[s.index("参数组合层面的改善存在差异。"):s.index("\n\n",s.index("参数组合层面的改善存在差异。"))]
s = s.replace(hetero + "\n\n", "")
pos = s.index("### 3.4")
s = s[:pos] + r"""在参数组合层面，160 个单元中 125 个改善、35 个退化，最大退化为 17.50%；退化较多出现在位置尺度比网格边界，并在较大样本量下更常见。对设计单元均值进行配对重采样时，汇总降幅的 95% 设计构成敏感性范围为 6.04%—8.46%。完整分布见附录 F 图 F2。

""" + s[pos:]
s = s.replace("### 3.4 样本实现与选择机制", "### 3.4 样本波动与低风险偏移量的移动")
paragraph("预测曲线的代表例", r"""相同真参数下，不同样本的经验梯度轨迹及搜索交点仍有差异（图 6a、b）。这一现象与第 3.2 节的逐样本偏移量变化相对应。损失曲线预测及选点对应的补充诊断见附录图 B1。""")
paragraph("除参数组合间差异外", r"""在 20 个预设诊断单元、2,000 个确认样本中，固定 $\delta=0.1$ 时的位置估计 $\hat\gamma_{0.1}/\eta$ 与 L6 偏移量在全部单元内均呈负相关，Spearman 相关系数中位数为 $-0.765$，四分位区间为 $[-0.803,-0.609]$（图 6d）。按该位置估计从低到高分组后，平均超额损失曲线的最低点依次位于 $\delta=0.10$、0.04 和 0.02（图 6c）。轨迹、相关与分组曲线共同表明，低风险偏移量随样本实现而变化。完整指标及边界解比例见附录 F.3。""")
paragraph("在同域拟合且使用独立重复确认", "")
s = s.replace("### 3.5 支撑验证", "### 3.5 留出验证与传统方法参照")
section("### 4.1", "### 4.2", r"""
### 4.1 偏移量选择的实际含义

偏移量的选择应与预期评价范围一致。若需要一个统一常数，可在事先明确的参数域、样本量构成及权重下计算汇总风险曲线；当前宽混合设计中的 $\delta=0.1$ 接近最低值，为简单应用提供了经验基准。仅按样本量设置多套固定规则的附加收益较小，主要选择空间来自参数条件和样本实现的差异。

样本自适应方法通过观测样本预测候选损失，使总体参数未知时仍可针对当前样本选择偏移量。其收益落实到三个参数的汇总标准化 RMSE，同时保留了 MDM 的参数求解过程。均值归一化还使输入在正尺度变换下保持不变，便于在相同参数比和样本量条件下处理不同量纲的样本；这一性质由第 2.4 节的等变关系及附录 B.3 的数值检查支持。
""")
section("### 4.2", "### 4.3", r"""
### 4.2 从样本结构到候选损失

归一化排序样本保留了相对位置、间距与离散结构。这些量参与伪尺度参数、廓线及其梯度的构造，从而影响位置搜索交点和三个参数的联合估计。图 6 的轨迹与条件损失提供了这条数值联系的关联性证据；它解释了样本适应的依据，尚未分解网络具体利用哪些信息及各机制的收益贡献。

以当前训练设计的参数混合分布及其权重定义条件期望，仅使用可观测输入 $Z$ 的理想规则为

$$
\delta_Z^*(Z)=\arg\min_{\delta\in\mathcal D}
\operatorname{E}\!\left[\ell(\delta,X,\theta)\mid Z\right].
$$

损失曲线预测据此为每个候选保留损失大小和相邻候选之间的差异，最终评价应落在所选候选的实际估计损失上。L6 则额外知道真参数及当前样本全部候选的已实现损失，所以与 L6 的差距同时涉及可学习的选择误差和事后信息差，不能全部解释为网络误差。同域可观测输入参照进一步区分这两类问题，见附录 F；初估参数查表的完整对照见附录 C。
""")
section("### 4.3", "### 4.4", r"""
### 4.3 参数估计收益与可靠度寿命目标

本文以三个标准化参数误差的等权联合损失选择偏移量，传统方法参照也据此评价。可靠度寿命 $x_R$ 则是参数的组合函数，其误差权重随可靠度和真参数变化，并包含参数误差之间的协方差项（附录 E.3）。因此，参数联合误差降低与某一寿命点精度提高是不同的评价要求。

第 3.5 节的结果支持将当前方法用于所评价设计中的联合参数估计改进；若任务主要关注特定可靠度寿命，应直接依据该寿命点的误差选择方法。以寿命误差重新定义候选损失，需要相应训练与评价，属于不同的目标设定。
""")
section("### 4.4", "## 5 结论", r"""
### 4.4 适用范围与后续确认

现有证据限于独立同分布、完整观测、非负位置参数的离散设计，并只对 $n=7,10,15,20$ 分别训练模型。参数单元的效果分布和形状参数留出结果说明，汇总改善不保证每个条件均改善；连续参数域、其他样本量及模型偏离仍需验证。L6 的参照范围为预设的 26 点候选网格，针对右端损失仍下降样本的扩展核查见附录 A.5。

输入表示筛选复用了现有折外评价数据。后续应先冻结表示、训练与选点规则，再以新的独立重复样本确认整体收益和条件差异。真实数据验证也应与研究范围匹配，并根据实际数据处理测量误差、删失和混合失效机制。
""")
section("## 5 结论", "## 数据与代码可用性", r"""
## 5 结论

三参数 Weibull 小样本 MDM 的适宜偏移量同时受参数域和样本实现影响。统一值反映给定评价范围下的风险折中，样本内的相对结构则为进一步选择提供依据。

以归一化排序样本预测候选损失曲线，再由 MDM 完成参数求解，在当前离散设计的折外评价中将汇总 $J_1$ 从 0.6304 降至 0.5846，降低约 7.3%；这一收益覆盖四个已训练样本量，并体现为三个参数的汇总标准化 RMSE 下降。本文由此建立了偏移量风险规律、样本信息和参数估计收益之间的证据联系，为冻结规则后的独立确认提供了基础。
""")
s = re.sub(r"\n{3,}", "\n\n", s)
(M / "Study01论文初稿-v1.9.md").write_text(s, encoding="utf-8")

a = (OLD / "Study01论文附录-v1.7.md").read_text(encoding="utf-8")
a = a.replace("中文修订稿 v1.7", "中文修订稿 v1.8").replace("Study01论文初稿-v1.8.md", "Study01论文初稿-v1.9.md")
pos = a.index("### A.2")
a = a[:pos] + "形状参数域敏感性分析的补充设计如下。" + old_domain + "\n\n" + a[pos:]
a = a.replace("**a，** 同一参数单元内 L6 事后偏移量的多样性及三种规则与 L6 的完全一致率分别为 L5 12.6%、论文 MLP 23.0% 和灵活 Z 参照 21.8%；", "**a，** 同一参数单元内 L6 事后偏移量的多样性；三种规则与 L6 的完全一致率另见 F.3；")
(M / "Study01论文附录-v1.8.md").write_text(a, encoding="utf-8")
print("Created main v1.9 and appendix v1.8")
