"""Create the Chinese argument-restoration revision without regenerating figures."""
from pathlib import Path
import hashlib
import json
import re

root = Path(__file__).resolve().parents[1]
source = root / 'Study01论文初稿-v1.9.md'
target = root / 'Study01论文初稿-v1.10.md'
assert not target.exists(), target
original = source.read_text(encoding='utf-8')
text = original

def paragraph(prefix, replacement):
    global text
    hits = [p for p in text.split('\n\n') if p.startswith(prefix)]
    assert len(hits) == 1, (prefix, len(hits))
    text = text.replace(hits[0], replacement.strip(), 1)

def section(start, end, replacement):
    global text
    a, b = text.index(start), text.index(end)
    text = text[:a] + replacement.strip() + '\n\n' + text[b:]

paragraph('> 中文修订稿', '> 中文修订稿 v1.10，配套[附录 v1.8](Study01论文附录-v1.8.md)。作者、单位和基金待补；目标期刊暂缓确定，英文转换在中文定稿后进行。')
paragraph('三参数 Weibull 分布的最小差异法（MDM）采用', r'''
三参数 Weibull 分布的最小差异法（MDM）通过正偏移量改善小样本估计的稳定性，但经验值 0.1 的适用范围及进一步选择的依据尚不明确。本文研究偏移量的有限样本风险，并以归一化排序样本预测 26 个候选偏移量的损失，再由 MDM 在所选偏移量下估计参数。在形状参数 $1.5\leq\beta\leq5.0$、位置尺度比 $0.10\leq\gamma/\eta\leq1.00$ 和样本量 $n\in\{7,10,15,20\}$ 构成的 160 个离散组合中，经验值 0.1 接近完整设计的最佳统一取值；固定宽度形状参数域由低值向高值平移时，最佳统一偏移量则由 0.18 移至 0.04。相同参数条件下，样本间距与位置结构的变化伴随 MDM 梯度轨迹、搜索交点和低风险偏移量的移动，表明条件平均规则仍不能描述全部样本差异。按位置尺度比水平留出评价，样本自适应选择将联合误差 $J_1$ 从 0.6304 降至 0.5846，降低 7.27%；改善覆盖四个样本量，三个参数的汇总标准化 RMSE 均下降。以已训练选择器和当前设计为条件的 95% 配对重采样区间为 6.94%—7.64%；评价样本参与过输入表示筛选，尚需独立确认。结果支持在所评价设计内保留 MDM 的参数求解过程，利用样本信息进一步降低固定偏移量下的联合估计误差。
''')
paragraph('本文围绕两个问题展开', r'''
对于 MDM，关键在于区分两种选择依据：参数条件改变时，适合该条件的平均偏移量可能改变；即使参数条件相同，有限样本的具体实现也可能改变候选偏移量的损失。前者决定统一常数的适用范围，后者关系到能否根据当前样本进一步改善估计。本文围绕这两个问题比较固定规则、参数条件规则与逐样本事后参照，并提出基于归一化排序样本的候选损失曲线预测方法。结果表明，经验值 0.1 在当前宽混合设计中接近最佳统一取值，但统一最低点随参数域移动，同一条件内的低风险偏移量也随样本变化。利用这些样本信息选择偏移量，在所评价离散设计中进一步降低了三个参数的汇总标准化误差；MDM 梯度轨迹的诊断则为理解这种样本差异提供了数值依据。
''')
paragraph('该局部关系说明', r'''
该局部关系把固定 $\delta$ 下两组样本的交点差，与梯度曲线之差及交点附近的斜率联系起来。改变 $\delta$ 会改变搜索交点及其所在的曲线位置，进而通过 $\hat\beta=\beta^*(\hat\gamma)$ 和尺度参数计算影响三个参数的误差。因此，偏移量虽然出现在位置参数搜索判据中，其选择需要依据联合估计误差评价。上述展开只描述两条给定样本曲线的局部交点差；估计稳定性与风险变化仍由重复抽样评价，展开条件及边界见附录 B.7。
''')
paragraph('整条曲线标签同时给出', r'''
采用整条损失曲线作为监督信号，是为了同时保留候选偏移量的优劣顺序和选错时的损失代价。若只用最低损失格点作为类别标签，两个损失几乎相同的候选与两个损失相差很大的候选都会被区分为不同类别；曲线标签则保留了这种代价差别，使训练目标与最终的联合误差评价相联系。训练时利用 Monte Carlo 样本的真参数计算各候选损失；应用时只需当前观测样本，网络确定 $\delta$ 后由 MDM 完成 Weibull 参数估计。完整流程见图 1。
''')
# An extra Obsidian embed duplicates the standard Markdown figure immediately below it.
text, count = re.subn(r'^!\[\[Study/01-study-MDM最小偏移量优化研究/manuscript/figures/main/fig1_offset_baseline\.png\]\]\n', '', text, flags=re.M)
assert count == 1, count
paragraph('固定经验值因此提供了低风险基准', r'''
上述汇总结果仍留下一个问题：经验值接近最低点，是偏移量本身具有普遍适用性，还是不同参数条件混合后的结果？固定宽度参数域的比较用于区分这两种解释。
''')
paragraph('参数条件和样本实现中的风险差异为自适应选择提供了依据', r'''
条件规则和事后参照揭示了偏移量选择随参数和样本而变化，但它们使用了实际估计时未知的真参数或损失。可执行方法需要从观测样本中提取与这些差异有关的信息。下节评价归一化排序样本的损失曲线预测能否在这一信息约束下改善参数估计。
''')
section('### 4.1 ', '### 4.4 ', r'''
### 4.1 偏移量选择的实际含义

统一偏移量的含义取决于它所针对的参数范围、样本量构成和条件权重。在当前宽混合设计中，经验值 $\delta=0.1$ 的 $J_1$ 仅比描述性最低点高 0.93%，说明单纯把默认值换成另一个统一常数，能够获得的改善有限。但固定宽度形状参数域平移后，统一最低点由 0.18 移至 0.04，表明这一低风险表现依赖于参数域。对于范围较窄且事先明确的应用域，可依据该域的汇总风险曲线确定统一值；当前结果并不支持脱离评价范围推荐唯一的最优常数。

已知样本量提供的额外选择依据也较有限。在重复编号交叉评价中，统一规则 L1 与按样本量规则 L2 相对 Default 的降幅分别为 0.83% 和 1.17%，按 $n$ 维护多套固定偏移量未带来较大收益。相比之下，参数条件规则呈现更明显的风险差异，同一参数条件内的事后低风险偏移量又随样本变化。这说明进一步选择需要考虑当前样本所反映的分布特征，而仅增加固定规则的数量并不足够。

样本自适应方法在位置尺度比水平留出评价中，将 $J_1$ 相对同一固定基准降低 7.27%，且三个参数的汇总标准化 RMSE 均下降。这一结果说明，在总体参数未知的情况下，观测样本仍可提供有用的偏移量选择信息。L1—L2 与 MLP 的划分方式不同，前述降幅分别说明各自评价任务中的收益，不用于计算跨协议的净增益。实际采用样本自适应规则还需要相应样本量的训练模型，其适用范围受训练设计约束。

### 4.2 样本差异如何影响偏移量选择

参数条件与样本实现分别对应平均风险和单次抽样损失两个层面。L3—L5 表明，不同参数条件下的平均损失曲线存在差异；同一参数条件内，L5 与 L6 的选点却仅有 12.6% 完全一致，说明知道参数条件仍不足以确定每个样本的低风险偏移量。因此，条件平均规则能够描述参数域中的差异，却不能代替对当前样本的判断。

这种样本差异可以沿 MDM 的计算过程理解。抽样得到的次序统计量和间距不同，各样本点构造的伪尺度参数随之改变，形成不同的廓线 $S_X(\gamma)$ 及梯度 $\nabla_X(\gamma)$。对于具有内部交点的样本，即使采用同一判据 $\nabla_X(\hat\gamma)=\delta$，梯度曲线的变化仍会移动搜索交点，改变位置估计 $\hat\gamma$；随后，$\hat\beta=\beta^*(\hat\gamma)$ 及伪尺度参数的平均值也随之变化，最终改变候选偏移量对应的三参数联合损失。20 个诊断单元中，固定偏移量位置估计与事后低风险偏移量均呈反向关系，分组损失曲线的最低点也随之移动（图 6）。这些结果为上述计算联系提供了关联性证据，但尚未确定网络具体利用了哪些样本特征，或分解各环节对收益的贡献。

归一化排序样本保留了相对位置、间距和离散结构，使网络能够利用这些信息预测候选损失，而无需先确定总体参数。另一种自然的实现是先估计参数，再查询条件平均规则；不过，以估计值替代真值会引入条件匹配误差，而且即使准确匹配参数条件，条件平均规则仍会遗漏样本内的损失差异。附录 C 中已检验的初估参数规则未降低整体 $J_1$，说明所检验的查表规则未能将真参数参照的收益转化为实际估计收益。直接预测损失曲线则把观测样本与选择目标联系起来，省去了先估参数再匹配条件规则的中间步骤。

这种联系对应的是给定可观测样本后的期望损失。以当前训练设计的参数混合分布及其权重定义条件期望，仅使用可观测输入 $Z$ 的理想规则为

$$
\delta_Z^*(Z)=\arg\min_{\delta\in\mathcal D}
\operatorname{E}\!\left[\ell(\delta,X,\theta)\mid Z\right].
$$

损失曲线回归旨在近似这些候选的条件期望损失，再以预测最低点作出选择。最终需要评价的是所选候选带来的实际估计误差：当损失曲线底部较平坦时，选点未与 L6 完全重合也可能只付出很小代价，因而精确格点一致率不能代替损失评价。L6 还使用真参数及当前样本所有候选的已实现损失，其事后最小值所利用的信息多于可执行规则；MLP 与 L6 的差距同时包含预测和选择误差，以及条件决策与事后选择的信息差，不能全部解释为网络尚未学好。

附录 F 进一步比较了在全部设计单元上拟合的同结构 MLP 与较灵活的可观测输入规则。后者在独立确认样本上取得更低风险，说明在该评价设置下，仅使用样本信息仍有进一步改善的空间。这一同域比较与正文的未见位置尺度比水平评价承担不同任务；经验参照不等于精确的条件最优风险，与 L6 的剩余差距也不能完全归于不可获得的信息。

### 4.3 参数估计收益与可靠度寿命目标

在当前参数设计和联合误差准则下，样本自适应 MDM 的 $J_1$ 低于 WMLE 和 LSE，说明其收益在传统参数估计方法的比较中仍有意义。但方法排序依赖评价目标：本文对三个标准化参数误差的平方等权求和，可靠度寿命 $x_R$ 则是三个参数的组合函数，其局部误差取决于各参数的敏感度以及估计误差之间的协方差（附录 E.3）。即使三个参数的边际均方误差分别下降，也不能据此确定某个寿命点的误差会以相同比例下降。

寿命点比较体现了这一差别。样本自适应选择相对固定偏移量的寿命误差变化较小，而 WMLE 在三个已评价寿命点上的相对 RMSE 均更低。因此，本文所建立的优势应解释为联合参数估计精度的改善；若应用主要关心某一可靠度寿命，应直接按该寿命点的误差选择方法。候选损失也可以围绕寿命目标定义，但相应收益需要通过新的训练与评价检验。
''')
section('## 5 结论', '## 数据与代码可用性', r'''
## 5 结论

三参数 Weibull 小样本 MDM 的统一偏移量取决于评价所采用的参数范围、样本量构成和条件权重。经验值 $\delta=0.1$ 在当前宽混合设计中接近最佳统一取值；固定宽度形状参数域由低值向高值平移时，统一最低点由 0.18 移至 0.04。相同参数条件下，抽样差异还会改变 MDM 梯度轨迹、搜索交点及候选损失，表明按参数条件确定平均规则后，仍有必要考察当前样本的信息。

以归一化排序样本预测候选损失曲线，在保留 MDM 参数求解过程的同时，实现了无需真参数输入的偏移量选择。当前离散设计的折外评价中，汇总 $J_1$ 从 0.6304 降至 0.5846，降低 7.27%；这一改善覆盖四个已训练样本量，三个参数的汇总标准化 RMSE 也均下降。该收益限于所评价的参数设计与样本量，独立于输入表示筛选的确认尚待完成。现有结果表明，固定正偏移量提供低风险基准后，利用观测样本选择偏移量仍能进一步改善 MDM 的联合参数估计。
''')

# Preserve all quantitative tables, display equations, captions, images and citations.
for pattern in (r'^\|.*$', r'^\*\*(?:图|表) .*$', r'^!\[[^\[].*$', r'^\[\d+\].*$'):
    assert re.findall(pattern, original, re.M) == re.findall(pattern, text, re.M), pattern
assert re.findall(r'^\$\$\n(.*?)\n\$\$', original, re.M | re.S) == re.findall(r'^\$\$\n(.*?)\n\$\$', text, re.M | re.S)
assets = {}
for subdir in ('main', 'supplementary', 'tables', 'data'):
    for p in (root / 'figures' / subdir).rglob('*'):
        if p.is_file():
            assets[p.relative_to(root).as_posix()] = hashlib.sha256(p.read_bytes()).hexdigest()
baseline = {
    'source_sha256': hashlib.sha256(source.read_bytes()).hexdigest(),
    'appendix_sha256': hashlib.sha256((root / 'Study01论文附录-v1.8.md').read_bytes()).hexdigest(),
    'assets_sha256': assets,
}
(root / 'revision-v1.10-baseline.json').write_text(json.dumps(baseline, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
target.write_text(text, encoding='utf-8', newline='\n')
print(json.dumps({'created': target.name, 'protected_assets': len(assets), 'tables_equations_captions_references': 'unchanged'}, ensure_ascii=False))
