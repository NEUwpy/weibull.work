# 160 组合新设计 MDM 数据（复用，逐样本估计 1,248,000 行）不纳入 git。
# 分片 chunks/ 与合并后 mc_scan_raw.csv gitignore；可追溯哈希记录于
# data_sha256sums.txt（本目录，纳入 git）。
mc_scan_raw.csv
chunks/

# 后台运行日志（可变文件，不纳入 git 也不进 SHA256 清单）
*.log
*.log.err
generation_detached*
