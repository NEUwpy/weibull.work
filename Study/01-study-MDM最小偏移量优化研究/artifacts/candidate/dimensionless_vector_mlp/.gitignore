# Dimensionless Vector-MLP candidate — large local artifacts are NOT tracked in git.
# Per-sample outputs (15 models x 45k samples), model pickle files, and the local
# manifest are kept locally and fingerprinted in the tracked SHA256SUMS + manifest.json.
local_outputs/
