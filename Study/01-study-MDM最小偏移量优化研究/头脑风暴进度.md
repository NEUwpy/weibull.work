# 头脑风暴进度

> 创建日期：2026-07-03
> 来源：微信对话（brainstorming session），衔接 CLI session 20260703_121742_3e5f43
> 作用：记录已澄清结论 + 待讨论清单。这是"最直接反映我的想法"的文档，优先级高于其他文档（其他文档由 AI 脑补，可能有偏差）。

---

## 已澄清的结论（共识，不再讨论）

### C1. 论文核心叙事

MDM 方法被提出 → 作者用 δ=0.1（随手取的小值）→ 已证明不错 → 但作者自己说"可能有更优值，留待后续工作" → **本文就是做这个后续工作**

不绕弯子讨论"MDM 适不适用"，直奔偏移量优化。

### C2. 六级分级框架（替代旧文档的 Default/L0-L5）

| 层级 | 偏移量决策粒度 | 实现方式 | 说明 |
|------|--------------|---------|------|
| L1 全局 | 一个常数（替代 0.1） | 直接取 | 全局最优常数是多少？是否比 0.1 好？ |
| L2 按 n | 每个 n 一个值 | 查表 | 精度提升多少？ |
| L3 按 β | 每个 β 一个值 | **NN** | β 不可知，只能用 NN 学习 |
| L4 按 β+n | 每个 β,n 组合一个值 | **NN** | 同上 |
| L5 按 β,γ/η,n | 每个 β,γ/η,n 组合一个值 | **NN** | 同上 |
| L6 逐样本 | 每个样本一个值 | **NN** | hindsight oracle，上限 |

**关键约束：L3 起必须用 NN**（因为真 β 不可知，只能从样本特征学习映射）

### C3. 评价口径

- Research 01 探究了半天多视角评价 → 结论是证明已有共识，不需要搬进论文大费周章
- 论文选一个指标就行
- 工程视角（RMSE_xR）暂时不讨论

### C4. 适用范围刻画

- 不作为论文主问题
- 实验中必然会涉及（方法间比较、多参数网格）
- 属于"做的时候会知道，但不主要体现"

### C5. NN 的定位

- 不是"为了用 NN 而用 NN"
- 是因为 L3 起 β 不可知，只能从样本特征学习映射
- NN 建立的是"样本 → 最优 δ"的映射
- 用上 NN 可以让论文更好发（但不为此牺牲逻辑正确性）

### C6. 论文主问题（修正后）

旧三问：
1. ~~MDM 在不同条件下的估计表现如何？~~ → 删，不作为主问题
2. δ 显著影响精度吗？固定 0.1 够好吗？→ 保留
3. 逐级提升精度能好多少？怎么用？→ 保留并强化

新主问题：
- **Q1：δ 是否显著影响 MDM 精度？固定 0.1 是不是最优全局常数？**
- **Q2：从 L1 到 L6 逐级提升精度，每级能好多少？在哪个层级精度提升出现边际递减？**
- **Q3：L3 起 β 不可知，用 NN 学习"样本→δ"映射能否逼近 oracle 层级的精度？**

### C7. 目标期刊

- MDM 原文期刊：IJSSD（International Journal of Structural Stability and Dynamics）
  - IF 3.8，JCR Q1，中科院 3 区
- 或东北大学学报（自然科学版）
- 读者群：做结构可靠性的人，知道 MDM 是什么

### C8. 论文意义的底层逻辑

- 其他方法（MLE 等）被提出后有大量后续改进工作
- MDM 被提出后没有后续完善 → 这是一个空白
- 哪怕不写论文，也需要知道这些事情（δ 的规律、适用范围）
- δ=0.1 是随手取的 → "随便取个 0.2 如果没有比它好的就成了悖论" → 必然存在更好的值
- 但要平衡计算成本和精度提升 → 可能到某个层级就不需要再细分了

---

## 待讨论清单（尚未固定）

### D1. 评价指标具体选哪个？ ✅ 已定（2026-07-03）
- **选 J1（相对误差型）**：J₁ = √(mean[(Δβ/β)² + (Δη/η)² + (Δγ/η)²])
- γ 归一化：除尺度参数真值 η（不除 γ 自身）
- 参数权重：等权（w_β = w_η = w_γ = 1）
- 辅助指标：每参数 Bias + SD（诊断用）、失败率（gate 用）
- 文献先例：182-050 / 182-097 的 Joint RMSE 列；J1 为相对误差变体
- 详见：`调研-评价指标/01-调研报告.md`

### D2. NN 具体输入特征 ✅ 已定（2026-07-03）
- **特征集 A3（8 个）**：n, x_(1), x_(n), Med, x̄, s, CV, g₁（偏度）
- **消融实验 5 组**（A0-A4，附录）：
  - A0: {n, x̄} | A1: {n, x̄, s, g₁} | A2: Yang 复现 {n, x_(1), x_(n), Med, x̄, Mode, CV} | A3: 推荐集 | A4: 全集
- **预处理**：有量纲特征（x_(1), x_(n), Med, x̄, s）做 z-score；无量纲（CV, g₁, n）不动；标准化参数从训练集计算
- 文献依据：184-009（Yang Xiaoyu, PEM 2025）7 特征方案 + Pearson 相关性筛选；Abbasi 5 特征方案
- 详见：`调研-NN输入特征/01-调研报告.md`

### D3. δ 搜索范围和粒度
- 旧文档提案：0.00 到 0.50，步长 0.02
- 你今天没讨论这个，需要确认

### D4. 实验层级与 E1-E5 的映射
- 旧文档的 E1-E5 框架是"MDM基线 → δ扫描 → 显式规则 → NN → 边界"
- 你的六级框架是"L1-L6 逐级评估 + NN"
- 需要重新设计实验编号和结构

### D5. 对比方法
- 旧文档提到 MLE/LSE 对照
- 你说"方法间比较会涉及"，但没明确需要哪些方法
- MLE 作为对照是否足够？需要 LSE 吗？

### D6. repeats 和计算预算
- 旧文档提 500/1000
- 你今天没讨论

### D7. 论文章节结构
- 旧骨架有10章（含独立的"显式规则"和"基线表现"章）
- 新框架可能更紧凑（删显式规则、降基线为背景）
- 需要在骨架文档里重写

---

## 本文档与其他文档的关系

- **本文档优先级最高**：记录用户的原始想法，是最直接反映意图的
- 其他文档（00-05）由 AI 根据讨论整理，可能有偏差
- 当其他文档与本文档冲突时，以本文档为准
- 后续每轮头脑风暴后更新本文档，把 D 区的条目移到 C 区
