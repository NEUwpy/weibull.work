# 04 待复核清单

> 创建日期：2026-07-02  
> 当前修订：2026-07-29
> 修订口径：既有 E1–E4、R1–R3/P6–P8 复核结论继续有效；投稿正文因新增方法比较、工程分位点和域匹配真实案例而暂缓冻结。旧勾选项表示既有技术包已完成，不代表 2026-07-27 扩展验证已经完成。
> P0–P1 记录：`study01xu@be8d7b81` 已获 Codex `APPROVE`。P2 v1 已判定失效并隔离。P2 v2 正式生成与评价已于 2026-07-29 完成（授权提交 `eee90efa`、封存提交 `c15f4c13`），Vector-MLP 在 P2-NI/P2-PI 15/15 模型全部胜出，0 失败、P0_INTEGRITY=PASS，封存于 `READY_FOR_INDEPENDENT_REVIEW`，等待 Codex 独立复核给出 APPROVE/REVISE/BLOCK。

## 2026-07-27 扩展验证总检查

### H1. 协议与证据保护

- [x] 在 `00-研究问题与边界.md` 明确新增研究问题和硬边界。
- [x] 在 `02-实验协议.md` 冻结新增验证的目标、比较公平性、泛化分轴、真实数据准入和停止条件。
- [x] 在 `07-剩余实验目标与规划.md` 记录 coworker 执行阶段，不新增平行规划文档。
- [x] 用户冻结工程主分位点 `x_0.95`、补充 `x_0.90/x_0.50`，以及传统主方法 `MLE/LSE/WMLE`。
- [ ] 新增代码证明不会覆盖 E1–E4、R1–R3/P6–P8 正式产物。
- [ ] 新增 formal 产物区分 generation commit、artifact seal commit 和 approval commit。

### H2. 内部可信性

- [x] 从 E4a/E4d 正式产物重算 15 模型分布并复现关键值。
- [ ] 同时报告中心、跨模型波动和按 `n` 分层结果。
- [ ] 不把 retained-subset comparison 写成单特征因果消融。
- [ ] Study1.5 RAW/F13 只有在样本键、划分和指标合同可比时进入补充材料。

### H3. 分轴泛化

- [x] 为现有 E4b/E4c 每个组合机械标注 `parameter_state × n_state` 正交状态。
- [x] 不再把混合 `off-grid` 总结果称为“域内非格点”。
- [x] 每个已有轨道都与自己的 Default/L1 基线比较。
- [x] 15 个模型先各自汇总，再报告模型间分布。
- [x] 现有组合不足时，只补经批准的最小正交格点。
- [x] 对未训练样本量 `n=15` 完成正式验证（P2-NI v2 已正式运行，Vector-MLP median J1=0.453 vs Default=0.550 vs L1=0.548）。
- [x] P2 v2 生成、基线评价、Vector-MLP 生产路径适配及 fail-closed smoke 完成；旧 v1 不得作为证据。
- [x] Default/L1/Vector 按 `fold × seed` 共用训练损失 P99；评价产物原子写入并与生成文件统一 SHA256 封存。
- [ ] 对 P2 v2 正式产物完成独立复核；获批（APPROVE）前不写入正文结论，也不进入 P3。

### H4. 估计路线比较

- [ ] Direct-MLP 与 Vector-MLP 使用相同 13 特征、训练样本、full-combo folds、seeds 和测试样本。
- [ ] Direct-MLP 输出变换保证 `beta>0`、`eta>0`、`gamma>=0`。
- [ ] Direct-MLP 训练目标与 `J1` 的相对参数尺度一致。
- [ ] MDM-Default、MDM-Vector-MLP、Direct-MLP 和冻结传统方法使用统一失败合同。
- [ ] 主结果使用相同全样本，不使用各方法不同的 valid-only 子集排名。
- [ ] 分开报告抽样稳定性和训练稳定性。
- [ ] 报告中位数/IQR/P95/P99、失败率和按 `n`/`beta` 分层，不只给 pooled `J1`。

### H5. 工程寿命分位点

- [ ] 工程指标直接从正式逐样本三参数估计派生，不重跑估计器。
- [ ] 主分位点及补充分位点使用公共、受测实现。
- [ ] 报告相对 Bias、RMSE、MAE和 P95 绝对相对误差。
- [ ] 检查参数排名和工程分位点排名是否一致。
- [ ] 不根据工程结果回头调模型，不训练直接分位点网络。

### H6. 域匹配真实案例

- [ ] 数据来源、许可、版本和 SHA256 可核对。
- [ ] 数据集至少 60 个完整、无删失、单一主要失效模式寿命。
- [ ] 先冻结 Weibull 适配度、bootstrap 参数域重叠和特征覆盖阈值。
- [ ] 域匹配判断不读取方法相对表现。
- [ ] 数据集和阈值经用户/Codex 冻结后才运行正式比较。
- [ ] 所有方法共享相同小样本/留出拆分。
- [ ] NIST P8 结果保留，并使用同一域诊断重新说明其角色。
- [ ] 一个真实案例不写成普遍部署证明。

### H7. 投稿回写

- [ ] 新增结论进入现有 claims-to-data 审计链。
- [ ] v0.2 保留为扩展验证前稿，正式结果完成后另存后续版本。
- [ ] 新主图/表只在其承担独立逻辑任务时加入。
- [ ] 摘要、讨论和结论在新增实验独立批准后再冻结。

## A. 文献与术语

- [x] 核对 `182-030` 中 MDM / MDE 的英文术语、公式编号和算法步骤。（Ch1-Ch2 初稿已引）
- [x] 核对 `182-030` 是否明确讨论小样本优势，以及比较对象是什么。
- [x] 核对 `182-046` 中 `delta = 0.1` 的提出方式、作用范围和实验依据。
- [x] 摘录 `182-046` 中"更适当偏移值需要进一步研究"的原文位置。
- [ ] 核对同团队及其他 NN 直接估参论文，确认输入、输出、损失、参数空间和比较协议；这是 Direct-MLP 公平对照及 Discussion 定稿的前置条件。

## B. 正式代码

- [x] 建立 `Study/01-study-MDM最小偏移量优化研究/code/`。
- [x] 正式代码不直接读取旧 `docs/research/04.../实验数据` 作为结果。
- [x] 正式代码调用当前 `python/methods/mdm.py`。
- [x] 正式代码记录 `git_commit`、seed、参数网格和输出路径。（manifest 含 method_versions、git_commit、seed_namespace 等全部必填字段）
- [x] 正式代码包含层级聚合单元测试或 sanity check。（三轮审计验证 J₁ 聚合正确性）
- [x] 正式代码检查样本自适应模型输入无真值泄漏。（E3b contract tests 已验证 vector input 不含真参数、配置 ID、seed、`repeat_id` 或 candidate `delta`）

## C. 正式实验产物

- [x] Formal E1 输出 `manifest.json`、`results.csv`、`summary.json`、δ-risk 曲线、L1/L2 阶梯。
- [x] Formal E2 输出 `manifest.json`、`results.csv`、`summary.json`、L3-L6 阶梯表。
- [x] Formal E3 输出多 seed 样本自适应 δ 选择主结果。（E3a/E3b existing-grid 已封存；E3b `seed_stability.csv` 含 42/2026/3407）
- [x] Formal E4a 输出主网格正式特征组消融；E4b/E4c 输出 boundary/off-grid reference-only 结果。
- [x] Formal E4d 输出 NN selector 的 boundary/off-grid 外推诊断（P3b APPROVE, `25cf7e2`） |
- [x] 所有表格都能由正式 `summary.json` 或 `results.csv` 重新生成。（E1/E2/E3/E4a/E4b/E4c/E4d/R2/P6-P8 全部完成）|

> ~~旧 E1（MDM 基线 manifest）~~ → 降为背景，不单独输出
> ~~旧 E3（显式规则 valid-only/fallback）~~ → 已删除
> ~~旧 E5 → 调整为 E4~~

## D. 指标与统计

- [x] 主指标已确定（J₁ = √(mean[(Δβ/β)² + (Δη/η)² + (Δγ/η)²])，D1 决策）。
- [x] `J₁ = sqrt(mean_i loss_i)`，没有直接平均已开方 `J₁`。（三轮审计第一轮已修正此问题）
- [x] `Bias/SD` 作为辅助诊断指标同时报告。
- [x] 样本自适应主结果有多 seed 幅度量化。（E3b pooled J1=0.544009–0.547003，最大差值 0.002994，为均值的 0.55%；相对 L2 降幅 13.52%–14.00%）
- [x] 失败率、有效率和计算成本有基本记录。（manifest 含 repeats、gate 失败率）

- [ ] 工程主分位点及补充分位点经用户冻结，并与 `python/studies/common/metrics.py` 定义一致。
- [ ] 参数 `J1` 与工程寿命分位点指标分开报告，不互相替代。

## E. 图表

- [x] 图表来自正式 artifacts，不来自旧 `docs/research/04/图像`。（Fig 1-9 + S1-S8 全部由 generate_g5_figures.py 从正式产物生成，12张图3种格式均已完成；数据与视觉逻辑可复现，不承诺跨环境字节一致）
- [x] Ch6 将完整 delta distribution diagnostic plot 降为补充图；主文用紧凑表分列 exact $P(\hat\delta=0)$、exact $P(\hat\delta=0.50)$ 和 extreme/near-boundary rate，三 seed 表保持稳定性职责。
- [x] Ch6 Figure 8 改为可复现的 vector-output MLP 方法流程图；原 pooled J1 柱状图降为补充图，Table 5 保持定量结果权威。
- [x] 正式特征组消融归 Ch7/E4a；Ch6 删除原 Table 9，只保留前向说明，不因现有 pilot 不足而机械降为补充材料。
- [x] Ch4 Figure 3/4 分别承担 L1 全局抵消与 L2 条件化后仍有抵消的独立职责；Figure 4 保留主文但不重复泛化的 $\beta$ 主效应口径。
- [x] Ch5 Figure 5 Panel A 只展示绝对 $J_1$，Panel B 只展示逐级相对 $J_1$ 降幅；其口径与 Table 4 “改善 vs 上一级”一致。
- [x] Ch5 将 L2→L3 写为主要信息增益，将 L5→L6 单独标记为 hindsight gap，不把逐样本事后选择包装为下一级部署信息。
- [x] Ch5 Figure 7 保留主文，只用于解释 hindsight gap 的逐样本来源及不可部署性；不重复 Figure 6 的 $\beta$/$n$ 选点跨度比较，不将 L6 分布写成部署查表。
- [x] Ch6 Table 6 只报 pooled/分 $n$ 的跨 seed $J_1$ 稳定性；边界选择率跨 seed 波动移至 Supplementary Table S1，不在主表混合两个职责。
- [x] Ch6 Table 7 保留主文，只拆解 primary seed 42 的精确上下端点与 near-boundary 构成；不承担跨 seed 稳健性判断，也不构成部署推荐。
- [x] Ch6 Table 8 删除对小分母敏感的 mean relative regret，主文只保留 mean regret 与 near-1%/2%/5% hit rate；完整诊断保留在正式 artifact。
- [x] Ch6 Table 8 只将 near-hit 差异写为 pooled $J_1$ 之外的补充观测，不据此排除少数极端样本驱动；更强判断需逐样本损失差分布证据。
- [x] Ch6 移除 Tabular-L6 的方法清单、Table 5、Table 8 和正文比较；代码与 sealed artifact 仅作 provenance 保留。
- [x] Ch6 Table 5 删除全为 0 的 failure-rate 列；正文一句说明当前 selected-point failure rate 均为 0，更宽范围的失败处理稳健性留给 Ch7。
- [x] Ch6 Table 5 保留 Default、L1–L6 与 Vector-MLP-L4/L5/L6 全部 10 行；完整展示比较景观，但正文主张仍聚焦 L2、L5、L6 三个锚点。
- [x] Ch6 对 Vector-MLP-L4/L5/L6 只报告 0.606229/0.596829/0.547003 与 1.55%/8.35% 描述性降幅；因仅 L6 有三-seed检查，不写成稳定监督粒度消融。
- [x] Ch6 Table 5 按 pooled $J_1$ 从低到高排列以突出效果；同时用方法名、`性质`列和正文防止把可部署、oracle、hindsight 误作等价排行。
- [x] Ch6 Table 5 `性质`列明确 Vector-MLP 的部署输入与 L4/L5/L6 离线监督标签，并将 L3–L5/L6 分别标成组级 oracle/逐样本 hindsight 参照。
- [x] Ch6 分 $n$ 正文改报相对 L2 的 11.05%/14.69%/17.32% 降幅，绝对值留在 Table 5；不把三个点的递增描述外推为单调规律。
- [x] Ch6 三-seed稳定性改报 pooled $J_1$ 范围、最大差值、相对均值比例与相对 L2 效应区间，逐-seed精确值只留在 Table 6。
- [x] Ch6 开篇和骨架删除“实质信号”“明显优于”等抽象判断，直接报告相对 L2 的 13.52% pooled $J_1$ 降幅及跨 $n$/三-seed一致性。
- [x] Ch4 将“$n$ 强烈影响 $J_1$”改为 $n=20$ 相对 $n=7$ 低 33.96%，同时报告最优 $\delta$ 仅由 0.10 移至 0.08。
- [x] Ch4 的 $\gamma/\eta$ 差异只作为次级经验模式报告，删除未经独立证据支持的“搜索敏感性”机制猜测。
- [x] Ch4 用各 $\beta$ 下 0/9、1/9、3/9、9/9、9/9 的改善组合数和精确范围量化方向反转，不再写“稳定改善约2%–4%”。
- [x] Ch4 删除“工程上可忽略”“几乎没有收益”“收益极小”等未定义阈值判断，改报 L1 相对 Default、L2 相对 L1 的 pooled $J_1$ 降幅 0.048%/0.059%，并与按 $\beta$ 分组的 0.77%–8.11% 变化比较。
- [x] Ch5 用 300 个正式 seed 样本轻量审计 $\beta$–profile 几何；局部梯度斜率与 $\beta$ 的跨 $n$ 趋势方向一致（$\rho=-0.463/-0.495/-0.529$），但正文明确不据此声称尾部形态因果机制已识别。
- [x] Ch5 将 L2→L3→L4→L5 的逐级降幅写为 7.51%→0.51%→1.88%；删除“迅速衰减”“边际递减区”，明确后两级均低于 L2→L3但彼此不单调。
- [x] Ch5 删除 L3 最优 $\delta^*$ 与 $\beta$“强负相关”的未量化表述，改报 5 个正式设计点 0.36/0.20/0.12/0.04/0.04 与 Spearman $\rho=-0.975$；明确末两点并列且不作总体推断。
- [x] Ch5 Figure 6 删除“$\beta$ 主效应、$n$ 微弱调节”的术语判断，改报跨 $\beta$ 与跨 $n$ 的平均 $\delta^*$ 跨度 0.37/0.08、比值 4.67，以及 L3→L4 pooled $J_1$ 降幅 0.51%。
- [x] Ch5 L5 删除“$\gamma/\eta$ 效应同样微弱”和单个组合示例，改报 15 个 $(\beta,n)$ 单元内跨度均值 0.092、中位数 0.08、范围 0–0.22、零跨度 1/15，以及 L4→L5 pooled $J_1$ 降幅 1.88%。
- [x] Ch5 删除“13.42% hindsight gap 中有相当部分来自逐样本差异和偶然性”的未分解归因；端点质量与组内分散只用于说明该 gap 不能直接解释为可部署信息收益。
- [x] Ch5 cross-fit 段删除“留出 $J_1$ 只轻微上调”，改报 L2/L4/L5 上调 0.030%/0.085%/0.132%、最大 0.132%，以及 L2→L5 的精确留出 $J_1$ 顺序。
- [x] Ch5 将“最大逐级增益”限定在 L2→L5 组级信息层级：L2→L3 为 7.51%；L5→L6 的 13.42% 作为 hindsight gap 单列，不参与组级信息增益排序。
- [x] Ch5 §2 标题和机制提问删除“主要驱动/主要分层变量”，改为7.51%逐级降幅及 $\beta$ 分组对应的选点/profile 几何变化。
- [x] Ch6 将“部署时可获得的输入”与“模型或选择信号已经可部署”分开；E3b 只支撑 Vector-MLP 在当前正式离散网格 combo-holdout 上取得改进，模型可部署性仍待 Ch7 和真实数据验证。
- [x] Ch6 将 near-5% hit rate 从 18.78% 提高到 40.90% 限定为选点改善的逐样本诊断证据；不据此识别 MLP 学到的内部表示，也不排除未经测试的固定规则。
- [x] Ch4–Ch6 删除“无法系统性超越”“几乎不能系统改进”；统一报告 L1 相对 Default、L2 相对 L1 的 pooled $J_1$ 降幅 0.048%/0.059%，并与按 $\beta$ 分组的 0.77%–8.11% 变化比较。
- [ ] 每张主图只服务一个论文主张。
- [ ] 表格列名标注指标方向，例如 `J₁ ↓`；Bias/SD 只作诊断，不作排名指标。
- [ ] Figure caption 写清参数网格、repeats、baseline 和指标。
- [ ] SVG/PDF/PNG 或绘图脚本可追溯。

## F. 正文主张

- [x] 摘要中的每个数字都来自正式实验，并由 claims-to-data 审计覆盖。
- [x] Introduction 不写未经文献核查的"首次"。
- [x] δ=0.1 写为经验值基线，不是"错误"，也不是"唯一最优"。
- [x] L3-L5 明确标注为 oracle 参照；L6 明确标注为扫描网格内 hindsight benchmark。
- [x] 样本自适应 δ 选择定位为"真参数不可见时的可观测近似方案"；NN 只是候选实现，不等同于已完成部署。
- [x] "稳定优于"只在多 seed 支持后使用。（E3b 三 seed 均优于 L2；正文仍限定为 existing-grid）
- [x] E4a 60-run 正式特征组比较已经整合进正文，并限定为预定义 retained-subset comparison。
- [x] 当前论文未启动不同学习器比较，也不提前宣称 MLP 优于其他学习器；如后续启动，必须另行冻结公平合同。
- [x] 实践建议以精度、训练需求和外部验证结果为依据，并报告按 n 分层表现；未量化的计算成本不写成定量结论。
- [ ] Direct-MLP 比较完成前，不宣称保留 MDM 骨架优于直接参数回归。
- [ ] 工程分位点比较完成前，不宣称较低参数 `J1` 等同于更高工程准确性。
- [ ] 分轴泛化完成前，不使用混合 `off-grid` 总数作连续空间或域内插值结论。
- [ ] 域匹配真实案例完成前，不将 Vector-MLP 称为已获得真实场景验证。

## G. 投稿前总 STOP 条件

如果以下任一项未解决，暂停投稿正文定稿：

1. 正式实验还没有 manifest。
2. 正式代码仍依赖旧结果 CSV。
3. 样本自适应模型输入存在真值泄漏。
4. 层级聚合合同错误。
5. 主结果只有单 seed。（E3b 已解除；后续 E4 若加入模型仍需检查）
6. 正式实验与论文主张冲突但正文没有改。
7. 原文 `delta=0.1` 语境没有核对。
8. Direct-MLP 与 Vector-MLP 的输入、划分、训练预算或失败合同不一致。
9. 泛化轨道同时改变多个轴却被写成单轴结论。
10. 工程分位点实现或可靠度记号没有冻结。
11. 真实数据集在查看方法结果后才决定纳入。
