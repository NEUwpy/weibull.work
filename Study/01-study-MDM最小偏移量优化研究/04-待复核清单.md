# 04 待复核清单

> 当前修订：2026-08-06
> 职责：只列从当前状态到论文正文写作所需的剩余事项。历史执行清单已归档，不在此重复。

## A. 论文基础（已完成）

- [x] 主问题收敛为 MDM 偏移量是否有更合适的选择以及怎样实现选择。
- [x] 最终方法确定为 Dimensional-RAW-MLP；旧特征路线和 Normalized-RAW 不再承担主结论。
- [x] 当前 160 组合设计、L1–L6 潜在空间、三 seed 主结果和分 $n$ 结果完成并封存。
- [x] Default、L6 与 Dimensional-RAW 的同测试样本比较完成。
- [x] 论文主张、实验协议、证据索引和五部分骨架同步。
- [x] 旧稿、旧审计、历史记录和待移出材料统一归入 `archive/`。
- [x] Direct-MLP、输入表示机制和其他估计路线归入项目根 `../../Research/`。
- [x] 正式产物保留原路径，未因整理而移动或重封存。

## B. 正文前必须补齐的支撑证据

### B1. 未见 $\beta$ 复核

- [x] 冻结按 $\beta$ 水平留出的最小设计（8 折 leave-one-beta-out）。
- [x] 复用现有 160 组合风险数据重训 per-n 网络，不重跑 MDM。
- [x] 与 Default、L6 在相同测试样本上比较。
- [x] 报告每个留出 $\beta$、pooled、按 $n$、三 seed 和失败率。
- [x] 明确结论只适用于当前离散 $\beta$ 网格。

产物：`artifacts/formal/E6_dimensional_raw/unseen_beta/`；pooled $J_1=0.5418$（逐样本损失汇总）、改善 14.06%、失败率 0%。

### B2. WMLE/LSE 外部参照

- [x] 使用当前 160 组合和相同样本运行 WMLE、LSE。
- [x] 报告 pooled $J_1$、三参数 RMSE、失败率和按 $n$ 结果。
- [x] 正文准备一张精简表；详细分层结果放附录 E。
- [x] 若某方法实现不稳定或成本不合理，如实记录并跳过，不为此重建平台。

产物：`artifacts/formal/E6_dimensional_raw/traditional_ref/`；WMLE 0.7288（1/48000 未收敛已计入）、LSE 0.8725。

### B3. 工程寿命分位点

- [x] 从 E6 选择结果和共享候选估计派生 $x_{0.90},x_{0.95},x_{0.99}$。
- [x] 比较 Dimensional-RAW、Default、L6；已完成的 WMLE/LSE 可一并列入。
- [x] 报告相对 Bias、RMSE、MAE、P95 和失败率。
- [x] 核对参数 $J_1$ 改善是否传递到工程指标，不预设一致。

产物：`artifacts/formal/E6_dimensional_raw/quantiles/`；$x_{0.95}$ 相对 RMSE 各方法接近（0.199–0.214），传递有限。

## C. 图表与附录准备

- [x] 按当前方法绘制“排序原始样本—MLP—损失曲线—选 $\delta$—MDM”结构图。
- [x] 从当前 160 组合数据重绘整体 $\delta$–风险曲线。
- [x] 生成 L1–L6 规则/协议/结果表。
- [x] 生成 Dimensional-RAW、Default、L6 主结果表和分 $n$ 图；图 3 同时给出 Default—L6 可实现空间和逐样本配对损失分布，不重复展示 seed。
- [x] 将 seed 分布、未见参数、传统方法和分位点详细结果组织到独立版本附录。
- [x] 复制形成正文与附录 v1.0；正文保持完整论证，附录 A 至 E 组织复现细节、网络稳定性、三参数误差分解、利用初估参数选择偏移量的结果、参数留出和工程指标；v0.3、v0.4 与附录 v0.1 保留不动。
- [x] 每个图表记录来源文件和生成脚本；不复用旧 G5 特征路线图充当当前结果。

产物：`artifacts/formal/E6_dimensional_raw/paper/`（`README.md` 为索引，`fig*.png`/`table*.md`/`supp_*`；生成脚本 `code/generate_paper_figures.py`）。

## D. 写作前核对

- [ ] 使用 `paper-logic-skeleton` 审查 `03-论文骨架.md` 的中心主张、论证依赖、章节职责、图表必要性和正文/附录分工；该项不代替证据与语言审查。
- [ ] 逐式核对 182-030 与 182-046 中的 MDM 符号、判据和偏移量表述。
- [x] 将 `01-证据索引.md` 中所有“待补”项更新为实际结果或明确删除相应主张。
- [ ] 核对摘要中的 12.1%、21.9% 和支撑验证结论均来自当前 E6 证据。
- [ ] 确认正文不混用旧特征路线、P2/P4、旧分位点或 NIST 数字。
- [x] 确认外部 `Study01论文初稿-v1.0.md` 与 `Study01论文附录-v1.0.md` 为当前活动稿；v0.3、v0.4 与附录 v0.1 作为历史版本保留。

## E. 后置项

- [ ] 论文主体接近完成后，再决定是否补充域匹配真实案例。
- [ ] 投稿期刊、作者顺序、基金、贡献声明和参考文献格式在成稿阶段确定。
- [ ] 论文稳定后，再决定是否将 `archive/` 整体移出 Study01。

## F. 利用初估参数选择偏移量（Phase B 全量已完成，负结论）

- [x] 冻结 plug-in 设计：`PG-beta` / `PG-beta-n` / `PG-full`（对照 L3/L4/L5），`MDM-0.1` 与 `WMLE` 初始估计，nearest-grid 与 interpolated 两种映射，迭代停靠规则（fixed point / cycle / 10 次更新；初始无效回退 $\delta=0.1$）。
- [x] 最小实现：`code/run_pg_selector.py` + `tests/test_pg_selector.py`（24 项测试通过；复用 160 组合缓存，不重跑 MDM；WMLE 按 B2 冻结 worker 重算，worker/生产实现与扫描数据源哈希均已绑定）。
- [x] Codex 复核后的修正：全量模式自描述（通用文件名 + 模式元数据）、run-start 溯源先于输出捕获、`prov_err_vs_delta` 按 estimator/family/mapping 与 one-step/terminal 分层、新增最近 $\beta$ 网格单元正确率诊断、确定性配对 repeat-block bootstrap（PG vs Default）。
- [x] Phase B 全量 48,000 样本（repeats 0–299，576,000 逐样本行）→ `artifacts/formal/pg_selector/`（最终包；oracle 与密封 `crossfit_layers` 完全一致）。
- [x] 最终结论：全部 12 个单步 PG 变体 $J_1$ 均高于 Default（最佳 WMLE/`PG-beta`/interpolated 0.650696 vs Default 0.630409）；配对 bootstrap 全部 CI 严格为正（95.3%–99.0% 的 repeat 块更差）；oracle-gap 回收率 −50.9% 至 −130.1%；迭代一致劣于单步；WMLE $\hat\beta$ 仅 16.5% 落入正确 $\beta$ 网格单元（证据与该机制解释一致，非唯一机制的证明）。
- [x] 分 $\beta$ 表：`summary_by_beta.csv`（`summary.json`/`manifest.json` 引用并封存）。最佳单步规则仅在 $\beta=1.5$ 优于 Default（0.53727 vs 0.54770，差 −0.01043），$\beta=2.0$–5.0 均更差；pooled 负结论如实表述此例外。
- [x] Mentor 定调：作为简洁负向支撑实验保留在 Study01（非新主方法，非 Research 旁路）；详细 12 变体、迭代、bootstrap 和分 $\beta$ 结果进附录 C，正文只用最佳 plug-in 结果与负结论。
- [x] $\gamma/\eta$ 估计方式（$\hat\gamma/\hat\eta$ vs $\hat\gamma/\eta_{\mathrm{true}}$）按冻结决定不增设对照。

## 不再列为本文任务

- 未见 $n$ 泛化：当前 per-n specialist 没有未训练 $n$ 的模型，这是方法边界，不是缺一张表。
- Direct-MLP 完整比较：独立 Research。
- 跨单位/跨尺度方法：独立扩展。
- NIST 6061-T6 案例：保留封存，不进入本文。
- 旧特征消融与旧 G5–G7 稿件：历史材料。
