# 04 待复核清单

> 创建日期：2026-07-02  
> 修订口径：复核重点从“旧 E03/E04 能否入正文”改为“正式实验是否已经独立完成并可投稿”。

## A. 文献与术语

- [ ] 核对 `182-030` 中 MDM / MDE 的英文术语、公式编号和算法步骤。
- [ ] 核对 `182-030` 是否明确讨论小样本优势，以及比较对象是什么。
- [ ] 核对 `182-046` 中 `delta = 0.1` 的提出方式、作用范围和实验依据。
- [ ] 摘录 `182-046` 中“更适当偏移值需要进一步研究”的原文位置。
- [ ] 核对同团队 NN 直接估参论文，确认它与本文路线的差异。

## B. 正式代码

- [ ] 建立 `Study/01-study-MDM最小偏移量优化研究/code/`。
- [ ] 正式代码不直接读取旧 `docs/research/04.../实验数据` 作为结果。
- [ ] 正式代码调用当前 `python/methods/mdm.py`。
- [ ] 正式代码记录 `git_commit`、seed、参数网格和输出路径。
- [ ] 正式代码包含层级聚合单元测试或 sanity check。
- [ ] 正式代码检查 NN 输入无真值泄漏。

## C. 正式实验产物

- [ ] Formal E1 输出 `manifest.json`、`results.csv`、`summary.json`。
- [ ] Formal E2 输出 `manifest.json`、`results.csv`、`summary.json`、`layer_ladder.csv`。
- [ ] Formal E3 输出全样本、valid-only、fallback 后结果。
- [ ] Formal E4 输出多 seed NN 主结果。
- [ ] Formal E5 输出泛化、工程视角和失败处理结果。
- [ ] 所有表格都能由正式 `summary.json` 或 `results.csv` 重新生成。

## D. 指标与统计

- [ ] `J_param = sqrt(mean_i loss_i)`，没有直接平均已开方 `J_param`。
- [ ] `Bias/SD/RMSE` 同时报告，避免只用单一分数。
- [ ] 工程视角按指定 `x_R` 报告 `Bias_xR/SD_xR/RMSE_xR`。
- [ ] NN 主结果有多 seed 均值和波动。
- [ ] 失败率、有效率和 fallback 后结果同时报告。
- [ ] 计算成本或运行时间有基本记录。

## E. 图表

- [ ] 图表来自正式 artifacts，不来自旧 `docs/research/04/图像`。
- [ ] 每张主图只服务一个论文主张。
- [ ] 表格列名标注指标方向，例如 `J_param ↓`、`RMSE_x95 ↓`。
- [ ] Figure caption 写清参数网格、repeats、baseline 和指标。
- [ ] SVG/PDF/PNG 或绘图脚本可追溯。

## F. 正文主张

- [ ] 摘要中的每个数字都来自正式实验。
- [ ] Introduction 不写未经文献核查的“首次”。
- [ ] `delta=0.1` 写为强基线，而不是错误基线。
- [ ] L2-L5 明确标注为 oracle 或训练标签来源。
- [ ] “稳定优于”只在多 seed 和 blocked split 支持后使用。
- [ ] `x0.95` 若收益小，明确写为边界。

## G. 投稿前总 STOP 条件

如果以下任一项未解决，暂停投稿正文定稿：

1. 正式实验还没有 manifest。
2. 正式代码仍依赖旧结果 CSV。
3. NN 输入存在真值泄漏。
4. 层级聚合合同错误。
5. 主结果只有单 seed。
6. 正式实验与论文主张冲突但正文没有改。
7. 原文 `delta=0.1` 语境没有核对。

