# 04 待复核清单

> 创建日期：2026-07-02  
> 修订口径：复核重点从“旧 E03/E04 能否入正文”改为“正式实验是否已经独立完成并可投稿”。

## A. 文献与术语

- [x] 核对 `182-030` 中 MDM / MDE 的英文术语、公式编号和算法步骤。（Ch1-Ch2 初稿已引）
- [x] 核对 `182-030` 是否明确讨论小样本优势，以及比较对象是什么。
- [x] 核对 `182-046` 中 `delta = 0.1` 的提出方式、作用范围和实验依据。
- [x] 摘录 `182-046` 中"更适当偏移值需要进一步研究"的原文位置。
- [ ] 核对同团队 NN 直接估参论文，确认它与本文路线的差异。（E3 启动前需确认）

## B. 正式代码

- [x] 建立 `Study/01-study-MDM最小偏移量优化研究/code/`。
- [x] 正式代码不直接读取旧 `docs/research/04.../实验数据` 作为结果。
- [x] 正式代码调用当前 `python/methods/mdm.py`。
- [x] 正式代码记录 `git_commit`、seed、参数网格和输出路径。（manifest 含 method_versions、git_commit、seed_namespace 等全部必填字段）
- [x] 正式代码包含层级聚合单元测试或 sanity check。（三轮审计验证 J₁ 聚合正确性）
- [ ] 正式代码检查样本自适应模型输入无真值泄漏。（E3 实现时需确认）

## C. 正式实验产物

- [x] Formal E1 输出 `manifest.json`、`results.csv`、`summary.json`、δ-risk 曲线、L1/L2 阶梯。
- [x] Formal E2 输出 `manifest.json`、`results.csv`、`summary.json`、L3-L6 阶梯表。
- [ ] Formal E3 输出多 seed 样本自适应 δ 选择主结果。
- [ ] Formal E4 输出泛化、稳健性结果。
- [ ] 所有表格都能由正式 `summary.json` 或 `results.csv` 重新生成。（E1/E2 ✓，E3/E4 待完成后验证）

> ~~旧 E1（MDM 基线 manifest）~~ → 降为背景，不单独输出
> ~~旧 E3（显式规则 valid-only/fallback）~~ → 已删除
> ~~旧 E5 → 调整为 E4~~

## D. 指标与统计

- [x] 主指标已确定（J₁ = √(mean[(Δβ/β)² + (Δη/η)² + (Δγ/η)²])，D1 决策）。
- [x] `J₁ = sqrt(mean_i loss_i)`，没有直接平均已开方 `J₁`。（三轮审计第一轮已修正此问题）
- [x] `Bias/SD` 作为辅助诊断指标同时报告。
- [ ] 样本自适应主结果有多 seed 均值和波动。（E3 待实现）
- [x] 失败率、有效率和计算成本有基本记录。（manifest 含 repeats、gate 失败率）

> ~~旧文档的 x0.95 工程视角指标~~ → 暂不讨论。

## E. 图表

- [ ] 图表来自正式 artifacts，不来自旧 `docs/research/04/图像`。
- [ ] 每张主图只服务一个论文主张。
- [ ] 表格列名标注指标方向，例如 `J₁ ↓`；Bias/SD 只作诊断，不作排名指标。
- [ ] Figure caption 写清参数网格、repeats、baseline 和指标。
- [ ] SVG/PDF/PNG 或绘图脚本可追溯。

## F. 正文主张

- [ ] 摘要中的每个数字都来自正式实验。
- [ ] Introduction 不写未经文献核查的"首次"。
- [ ] δ=0.1 写为经验值基线，不是"错误"，也不是"最优"。
- [ ] L3-L5 明确标注为 oracle 参照；L6 明确标注为扫描网格内 hindsight benchmark。
- [ ] 样本自适应 δ 选择定位为"真参数不可见时的可部署近似方案"；NN 只是候选实现，不是炫技也不是论文目的。
- [ ] "稳定优于"只在多 seed 支持后使用。
- [ ] 实践推荐层级有明确依据（精度 vs 计算成本），并报告按 n 分层表现。

## G. 投稿前总 STOP 条件

如果以下任一项未解决，暂停投稿正文定稿：

1. 正式实验还没有 manifest。
2. 正式代码仍依赖旧结果 CSV。
3. 样本自适应模型输入存在真值泄漏。
4. 层级聚合合同错误。
5. 主结果只有单 seed。
6. 正式实验与论文主张冲突但正文没有改。
7. 原文 `delta=0.1` 语境没有核对。
