# 04 待复核清单（论文完成清单）

> 创建日期：2026-07-02
> 当前修订：2026-08-04
> 职责：简洁的论文完成清单。旧的逐项技术复核记录（E1–E4、R1–R3/P6–P8、P0–P4 的执行检查）已由封存审计与 `待移出Study01/` 承载，本清单只跟踪论文完成所需的剩余事项。
> 基线：E1/E2 潜在空间证据与 Dimensional-RAW 主设计域结果已完成；E6 参数泛化/传统方法同条件比较/工程寿命分位点待按 E6 重新派生；旧特征路线与归一化候选为历史/候选证据。

## A. 主线收敛（已完成）

- [x] 论文核心问题收敛：只研究 MDM 框架内偏移量的最优性定义、信息层级和样本自适应选择。
- [x] 核心实验确定为 E1/E2 潜在空间证据 + 新设计域样本自适应方法（Dimensional-RAW-MLP：排序原始样本 → 分 n MLP → 26 点损失曲线 → 偏移量选择 → MDM）；L1–L6 在新 160 组合设计上统一以既有正式 cross-fit 定义重算。
- [x] 样本自适应最终方法迁移完成：正式输入为排序的原始样本 $X_n=\operatorname{sort}(x)$（不除以均值），每个样本量独立网络，不使用 padding/mask/跨 n 联合网络；逐位置标准化仅由训练折拟合。
- [x] 评价口径收口：留出划分按 `n` 的 `gamma/eta` 水平进行（未检验未见 `beta`）；L1–L6（repeat-id cross-fit）与样本自适应方法（按 `n` 的 `gamma/eta` 水平留出）分开呈现，并列展示注明评价协议。
- [x] Normalized-RAW（上一轮 x/mean(x) 归一化路线）标记为已完成但未采用的候选对照，分支与证据保留。
- [x] 支撑验证与独立 Research 分层：泛化/seed 稳定性与输入表示尺度性质/传统方法参照/工程分位点/真实案例为支撑验证；Direct-MLP 完整比较为独立 Research。
- [x] 传统方法口径：WMLE/LSE 为外部参照；MLE 封存不消费。
- [x] 工程分位点口径：`x_0.95` 为主，`x_0.90/x_0.99` 补充，删除 `x_0.50`。
- [x] 真实案例后置，不阻塞写作；NIST 不进入本文。
- [x] 活动文档中不再出现过期主张（Direct-MLP 必须入主线、MLE 为主方法、x_0.50、NIST 必入正文、真实案例阻塞写作、泛化/消融/方法比较各成平行主线）。

## B. 论文写作（进行中）

- [ ] 外部论文稿 v0.3 按五部分骨架补齐正文（唯一活动论文稿）。
- [ ] 摘要、讨论和结论在所有支撑验证独立批准后再冻结。
- [ ] 正文只引用正式产物数字；pilot 数字不出现在摘要、结论或主结果表。
- [ ] 每张主图只服务一个论文主张；Figure caption 写清参数网格、repeats、baseline 和指标。
- [ ] 表格列名标注指标方向（如 `J₁ ↓`）；Bias/SD 只作诊断，不作排名指标。
- [ ] 所有 claim 进入 claims-to-data 审计链（`manuscript/audit/`）。

## C. 支撑验证（部分完成，部分待按 E6 派生）

- [ ] 按 E6 派生参数插值、样本量插值/外推与边界外推结果；旧 P2-NI/P2-PI/E4d 只作历史线索。
- [x] E6 seed 稳定性与输入表示尺度性质已完成（60 个 fold×seed 分布 + 有量纲输入单位依赖性）；正文简述，细节进补充材料。
- [ ] 按 E6 派生 WMLE/LSE 同条件外部参照；MLE 不进入结果表。
- [ ] 按 E6 逐样本估计派生工程寿命分位点 `x_0.90/x_0.95/x_0.99`，报告相对 Bias/RMSE/MAE/P95；旧 P4 派生只作历史线索。

## D. 后置项（不阻塞写作）

- [ ] 域匹配真实案例（论文主体接近完成后决定是否执行；NIST 不进入本文）。
- [ ] 大规模连续空间重训与无限参数扩展：不启动。

## E. 投稿前检查

- [ ] `git diff --check` 通过。
- [ ] 所有被修改 Markdown 的相对链接可解析。
- [ ] 正式数据、SHA256SUMS、manifest 和 P4 大文件未变化。
- [ ] 外部论文 v0.3 未变化。
