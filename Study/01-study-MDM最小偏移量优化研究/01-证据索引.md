# 01 证据索引

> 当前修订：2026-08-05
> 职责：把论文可写主张映射到当前正式产物。内部提交号、运行日志和旧路线结果只用于复核，不进入正文或图注。

## 状态说明

| 状态 | 含义 |
|---|---|
| 可写 | 当前正式产物足以支持限定后的主张 |
| 待补 | 设计已明确，但当前 E6 证据尚未生成或派生 |
| 历史 | 产物保留，但不能支持当前 Dimensional-RAW 方法 |
| Research | 可独立研究，不属于本文证据链 |

## 论文核心主张

| ID | 可写主张 | 直接证据 | 状态与边界 |
|---|---|---|---|
| C1 | 当前整体误差曲线在 $\delta=0.06$ 处最低，$\delta=0.08$ 与其几乎相同；$\delta=0.1$ 的 $J_1$ 仅高约 0.93%，重新选择统一常数的收益有限 | `artifacts/formal/E5_normalized_raw/shared_data/chunks/`、`artifacts/formal/E6_dimensional_raw/specialist/crossfit_layers.csv` | 可写；整体曲线为描述性汇总，L1 为选择/评价分离结果；均限于当前设计与 $J_1$ |
| C2 | 增加选择偏移量时可用的信息，可以明显降低三参数联合估计误差 | 同上 | 可写；L3–L5 是真参数参照，L6 是 hindsight |
| C3 | 逐样本候选选择仍有约 21.9% 的潜在空间 | `summary.json`、`crossfit_layers.csv` | 可写；L6 仅是 26 点网格内事后参照 |
| C4 | 排序原始样本可以用于预测损失曲线并选择 $\delta$ | `raw_specialist_results.csv`、`summary.json`、`manifest.json` | 可写；每个 $n$ 独立模型 |
| C5 | Dimensional-RAW-MLP 相对 Default 将 pooled $J_1$ 改善 12.08% | `summary.json`、`model_comparison.csv` | 可写；同测试样本、当前单位和尺度范围 |
| C6 | 主结果不依赖单次随机初始化 | `seed_stability.csv`、`summary.json` | 可写；三 seed 0.5545/0.5523/0.5560 |
| C7 | 随 $n$ 增大，四个已训练样本量下的 $J_1$ 均降低 | `summary.json`、`model_comparison.csv` | 可写为分层观察；不是未见 $n$ 泛化 |
| C8 | 当前留出评价覆盖训练未包含的 $\gamma/\eta$ 水平 | `split_report.csv`、`manifest.json` | 可写；每折仍覆盖全部 8 个 $\beta$，不支持未见 $\beta$ |

## 核心数值表

### 统一偏移量的整体误差曲线

来源：`artifacts/formal/E5_normalized_raw/shared_data/chunks/` 中当前 160 组合的共享损失数据。对全部样本按候选 $\delta$ 汇总后：

| $\delta$ | pooled $J_1$ |
|---:|---:|
| 0.06 | 0.624518 |
| 0.08 | 0.624666 |
| 0.10 | 0.630409 |
| 0.12 | 0.638755 |

$\delta=0.06$ 是当前 26 点网格上的描述性最低点；$\delta=0.1$ 比该最低点高 0.9345%。由于该曲线使用同一批数据比较候选值，论文同时以 L1 的 repeat-id cross-fit 结果 0.625198 作为选择/评价分离后的验证。

### L1–L6 选择空间

来源：`artifacts/formal/E6_dimensional_raw/specialist/crossfit_layers.csv`。

| 规则 | $J_1$ |
|---|---:|
| Default | 0.630409 |
| L1 | 0.625198 |
| L2 | 0.623035 |
| L3 | 0.590526 |
| L4 | 0.588514 |
| L5 | 0.581294 |
| L6 | 0.492297 |

L1–L5 使用 repeat-id 五折交叉评价；L6 逐样本读取候选网格中的实际最小损失。不要把 L6 描述为模型，也不要将 L5 与 Dimensional-RAW 在没有评价协议说明时直接排名。

### Dimensional-RAW 主结果

来源：`artifacts/formal/E6_dimensional_raw/specialist/summary.json`。

| 指标 | 数值 |
|---|---:|
| 三 seed pooled $J_1$ 均值 | 0.554275 |
| 三 seed 标准差 | 0.001534 |
| 相对 Default 改善 | 12.0769% |
| $n=7$ | 0.690140 |
| $n=10$ | 0.576094 |
| $n=15$ | 0.485550 |
| $n=20$ | 0.430034 |
| 失败率 | 0% |

Default 与 L6 的同测试样本结果分别为 0.630409 和 0.492297。Normalized-RAW 为同设计候选对照 0.5850，仅用于说明表示选择过程，不作为论文贡献或主结果。

## 当前正式设计与方法证据

| 内容 | 文件 |
|---|---|
| 参数网格、样本量、候选 $\delta$、网络配置 | `artifacts/formal/E6_dimensional_raw/specialist/manifest.json`、`code/dim_raw_config.py` |
| 共享风险曲线数据清单与哈希 | `artifacts/formal/E5_normalized_raw/shared_data/manifest.json`、`data_sha256sums.txt` |
| 留出划分 | `artifacts/formal/E6_dimensional_raw/specialist/split_report.csv` |
| 逐模型/逐样本选择结果 | `raw_specialist_results.csv`、`model_comparison.csv` |
| 多 seed 稳定性 | `seed_stability.csv` |
| 排序原始输入与训练折标准化检查 | `representation_check.json` |
| 核心汇总 | `summary.json` |
| 正式产物哈希 | `SHA256SUMS` |

## 支撑验证

| 问题 | 当前证据 | 状态 | 写作边界 |
|---|---|---|---|
| 结果是否依赖单次训练 | 60 个 fold×seed 模型；三 seed 汇总 | 可写 | 正文简述，分布进补充材料 |
| 未见 $\gamma/\eta$ 水平 | 五折逐水平留出 | 可写 | 是当前离散设计内留出，不是连续外推 |
| 未见 $\beta$ | 尚无正式 E6 留出结果 | 待补 | 可复用风险数据重新训练，不需重跑 MDM |
| 未见 $n$ | 当前方法没有对应模型 | 不适用 | 明确为方法边界；若研究需改变架构或另训网络 |
| WMLE/LSE 同条件参照 | 旧 P4 仅属特征路线 | 待补 | 只作外部坐标，不决定本文主结论 |
| $x_{0.90}/x_{0.95}/x_{0.99}$ | 旧分位点证据仅属特征路线 | 待补 | 由 E6 选择结果与共享候选估计派生 |
| 真实数据 | NIST 结果与本文域不匹配 | 后置 | 不进入当前稿；不得删除其封存证据 |

## 当前正式代码

| 文件 | 职责 |
|---|---|
| `code/dim_raw_config.py` | 当前 160 组合设计和训练配置 |
| `code/run_E6a_data_inventory.py` | 核对共享损失数据和哈希 |
| `code/run_E6b_dimensional_raw_specialist.py` | 训练、选择、评价和 L1–L6 汇总的正式入口 |
| `code/run_E6b_smoke.py` | 当前方法的最小端到端 smoke |
| `code/analyze_E1_E2_crossfit.py` | L1–L5 repeat-id cross-fit 共用实现 |
| `code/finalize_e6_manifest.py` | E6 provenance 与封存清单收口 |
| `tests/test_dim_raw_contract.py` | 当前方法的核心合同测试 |

其余代码保留用于历史复现或独立 Research，分类见 `code/README.md`。旧 G5 图表脚本基于旧特征路线，不能直接生成当前论文终稿图。

## 文献证据

| 用途 | 来源 |
|---|---|
| MDM 方法、符号和算法 | `src/content/182-030-pdf原文.md`、`src/content/182-030-pdf翻译.md` |
| 正偏移量与经验值 $\delta=0.1$ 的原始语境 | `src/content/182-046-pdf原文.md` |

正文写作前应再次逐式核对两篇原文，尤其是 MDM 判据、偏移量定义和前作对稳定性的表述。

## 历史与 Research 证据

| 材料 | 位置 | 当前用途 |
|---|---|---|
| 旧 13 特征 Vector-MLP | `artifacts/formal/E3b_vector_mlp/`、`E3_sample_adaptive/` | 历史；不支持当前方法 |
| 旧特征路线泛化 P2 | `artifacts/formal/extended_validation/p2_generalization_v2/` | 历史；不支持 E6 泛化 |
| 旧六方法 P4 | `artifacts/formal/p4_formal_compare/` | Research/历史；不支持 E6 同条件比较 |
| 旧路线工程分位点 | `artifacts/formal/quantile_derivation/` | 历史；不支持 E6 分位点主张 |
| Normalized-RAW | `artifacts/formal/E5_normalized_raw/` | 候选对照与 E6 共享数据来源 |
| Direct-MLP | P3/P4 代码与产物，索引见 `Research/README.md` | 独立 Research |
| NIST 6061-T6 | `artifacts/formal/real_data/nist-6061-t6-fatigue/` | 封存负结果，不进入本文 |
| 旧 G7 稿件与审计 | `archive/legacy-manuscript/` | 历史复核，不是活动稿 |

## 正文消费规则

1. 只写入本索引标记为“可写”的主张。
2. 待补结果完成并独立复核前，只保留占位，不预写方向性结论。
3. 历史路线的数字不能与 E6 数字拼成同一因果链。
4. 内部提交号、dirty 状态、授权记录和运行事故不进入论文正文。
5. 任何图表都应能回指本索引中的正式文件和明确评价协议。
