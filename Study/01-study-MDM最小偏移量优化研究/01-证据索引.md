# 01 证据索引

> 创建日期：2026-07-02
> 当前修订：2026-08-04
> 职责：主张 → 实验 → 正式数据位置的详细映射。旧 `docs/research/03/04` 结果只作为 pilot evidence；正式论文证据由本 Study 目录下的正式实验产物承担。所有封存产物保持只读。

## 状态标记

| 状态 | 含义 |
|------|------|
| 文献证据 | 可用于背景、方法来源、术语核对 |
| pilot 证据 | 旧探索结果，只能用于形成假设和设计正式实验 |
| 正式证据可用 | 已由 Study 正式实验生成并通过复核 |
| 封存不消费 | 已封存，但按当前论文口径不再作为论文证据 |
| 不进入正文 | 可留作历史或内部参考，但不作为论文证据 |

## 文献证据

| 证据用途 | 来源 | 状态 | 后续动作 |
|----------|------|------|----------|
| MDM 第一篇方法来源 | `src/content/182-030-pdf原文.md`、`src/content/182-030-pdf翻译.md`；PDF/图片在 `public/182-030-图片/` | 文献证据 | 核对 MDM/MDE 术语、公式、算法步骤和小样本表述 |
| MDM 第二篇偏移量来源 | `src/content/182-046-pdf原文.md`；PDF/图片在 `public/182-046-图片/` | 文献证据 | 核对 `delta=0.1` 的原文语境、实验依据和“需要进一步研究”的表述 |

## Pilot 证据

这些材料只能说明“值得重新做正式实验”，不能直接进入投稿正文。

| pilot 假设 | pilot 来源 | 状态 | 正式实验对应 |
|------------|------------|------|--------------|
| MDM 在小样本参数估计中可能有优势 | `docs/research/04.../E01-6_*` | pilot 证据 | E1 |
| `delta=0.1` 可能是强全局基线 | `docs/research/04.../E03_ladder_table_v2.csv` | pilot 证据 | E1 |
| 最优 `delta` 可能主要受 `beta` 驱动，`n` 次之，`gamma/eta` 较弱 | `docs/research/04.../E03_*` | pilot 证据 | E2 |
| 逐样本 hindsight 可能存在额外空间 | `docs/research/04.../E03_L5_per_sample_v2.csv` | pilot 证据 | E2 |
| risk curve 标签可能优于 hard label | `docs/research/04.../E04_main_results.json` | pilot 证据 | E3b 参考，不作独立消融主线 |
| 跨 `gamma/eta` 可能是泛化边界 | `docs/research/04.../E04_generalization.json` | pilot 证据 | 泛化支撑验证 |

## 核心实验证据（论文核心结果）

| 主张 | 实验 | 主要结果 | 正式数据位置 | 边界 |
|------|------|----------|--------------|------|
| 经验值 `delta=0.1` 位于平缓低风险区，已接近全局最优 | E1 | L1 最优 δ=0.08；L1 相对 Default、L2 相对 L1 的 pooled J1 降幅约 0.048%/0.059% | `artifacts/formal/E1_baseline/` | 限于主设计域；不声称任意参数空间最优 |
| 全局固定规则收益有限，条件异质性存在 | E1 + E1/E2-CF | 按参数组合使用不同偏移量可降低联合估计误差约 9.8% | `artifacts/formal/E1_E2_crossfit/` | cross-fit 下结构结论稳定 |
| L3–L6 信息层级存在实质精度梯度 | E2 | L2→L3 为最大单步信息增益（7.51%）；L6 hindsight pooled J1=0.4945；逐样本事后选择潜在降幅约 21.9% | `artifacts/formal/E2_oracle_layers/` | L3–L5 是 oracle 参照，L6 是固定网格内 hindsight 参照 |
| 主要增益来自 β 分层，n 仍需保留为报告维度 | E2 + β-profile 审计 | profile 曲线几何随 β 系统性变化 | `artifacts/formal/E2_beta_profile_audit/` | 关联限于现有 5 个 β 值，不作因果机制证明 |
| 真参数未知时，可观测样本信息能恢复部分潜在收益 | E3b | Vector-MLP pooled J1=0.547（三 seed 0.547/0.546/0.544），介于 L5=0.571 与 L6=0.495 之间（仅为 J1 数值位置；L5 按真参数组合选择，Vector-MLP 逐样本预测风险曲线，决策粒度不同，不能解释为网络拥有比真参数 oracle 更多的信息）；相对 L2 降幅约 13.5% | `artifacts/formal/E3b_vector_mlp/`、`artifacts/formal/E3_sample_adaptive/` | 限于正式离散网格组合留出评价；不外推连续空间泛化 |

> E3b 封存说明（内部 provenance，仅供审计核对）：封存提交 `bedd65a`；产物目录 `artifacts/formal/E3b_vector_mlp/`；代码入口 `code/run_E3b_vector_mlp.py`；合同测试 `python/tests/test_study01_e3b_contract.py`（11 passed）。生成时 manifest 记录 base HEAD `04e99c5` 且 workspace dirty（因 E3b 产物在提交前创建）。**以上封存提交、base HEAD 与 dirty workspace 均为内部 provenance，不得写入论文正文或图注。**

## 支撑验证证据

| 验证 | 主张 | 实验 | 主要结果 | 正式数据位置 | 状态 |
|------|------|------|----------|--------------|------|
| 泛化 | 未训练样本量 `n=15` 上收益保留 | P2-NI | Vector-MLP 15/15 模型胜出；median J1 0.453 vs Default 0.550 vs L1 0.548 | `artifacts/formal/extended_validation/p2_generalization_v2/` | 正式证据可用（Codex APPROVE `53932687`） |
| 泛化 | 参数中点插值上收益保留 | P2-PI | Vector-MLP 15/15 模型胜出；median J1 0.545 vs Default 0.625 vs L1 0.625 | 同上 | 正式证据可用（同上） |
| 泛化 | 边界/off-grid 外推行为 | E4d | boundary/off-grid selector 外推诊断；相对同轨道 Default/L1 需按模型和泛化轴重分析 | `artifacts/formal/E4_robustness/`（manifest_e4d.json） | 正式证据可用（P3b APPROVE） |
| 内部可信性 | 特征组是否稳定贡献选择信号 | E4a | full 0.5456 / scale-quantile 0.5506 / shape 0.5816 / n-only 0.6378（描述性均值） | `artifacts/formal/E4_robustness/`（E4a） | 正式证据可用（60-run，不称单特征因果消融） |
| 内部可信性 | 结果是否依赖某次随机训练 | E3b/E4 多 seed | 三 seed pooled J1 0.547003/0.546133/0.544009；15 模型模型级分布 | `artifacts/formal/E3b_vector_mlp/`、`artifacts/formal/E4_robustness/` | 正式证据可用 |
| 传统参照 | WMLE 作为主要外部参照 | P4 | WMLE 在四轨道上作为传统方法参照（同一样本、同划分） | `artifacts/formal/p4_formal_compare/` | 正式证据可用（seal `f00b561d`） |
| 传统参照 | LSE 作为另一类外部参照 | P4 | LSE 同上 | `artifacts/formal/p4_formal_compare/` | 正式证据可用（seal `f00b561d`） |
| 工程分位点 | 参数收益是否传递到 `x_0.95`/`x_0.90`/`x_0.99` | 工程分位点派生 | Vector-MLP 相对 MDM-Default 的 `x_0.95` 相对 RMSE：main_holdout −3.60%、param_interp −0.80%、extrap_diag −1.97%、n_interp +0.20%——主设计域保留部分收益但明显小于参数联合误差改善，样本量插值轨道未保留；参数排名不能直接代表分位点排名 | `artifacts/formal/quantile_derivation/` | 正式证据可用（Codex APPROVE `b9bb815a`） |
| 真实数据 | 域匹配真实案例 | 待定 | 后置到论文主体接近完成 | — | 待定（NIST 不进入本文） |

> 分位点结果的消费边界：本文只消费 **MDM-Vector-MLP 相对 MDM-Default 的收益传递**（核心）与 **WMLE/LSE 外部参照**；**Direct-MLP 的分位点结果仅在 `Research/` 登记，不用于本文主张**；"参数排名与分位点排名不一致"是交叉验证结果，不作本文新贡献。

## 传统方法参照与 MLE 处理

- **WMLE**：保留为主要传统方法外部参照；**LSE**：保留为另一类传统方法外部参照。
- **MLE**：已封存（P4 四轨道数据在 `artifacts/formal/p4_formal_compare/`），**不再审查、不再补跑、不进入论文结果表**。封存结果不得删除或篡改。
- **Direct-MLP 与 MDM 的完整比较**：属于独立 Research，不进入本文主结果链；材料与指向见 `Research/README.md`。

## 独立 Research 材料索引

| Research 类别 | 材料 | 位置 |
|---------------|------|------|
| Direct-MLP 与 MDM 路线比较 | Direct-MLP 公平比较实现与 smoke | `code/run_p3_direct_mlp.py`、`code/run_p3_fair_compare.py`、`code/run_p3_smoke.py`、`tests/test_p3_direct_mlp.py` |
| Direct-MLP 与 MDM 路线比较 | P4 正式六方法比较封存产物 | `artifacts/formal/p4_formal_compare/`、`code/run_p4_formal_compare.py`、`tests/test_p4_formal_compare.py` |
| 神经网络输入表示与样本量 | 输入特征与样本量专题调研 | `Research/神经网络输入表示与样本量/` |
| RAW 与 Tabular 候选模型 | E3b RAW 候选模型（原位登记） | `artifacts/candidate/E3b_RAW_specialist/`、`code/run_E3b_RAW_specialist.py`、`code/plot_tabular_l6_architecture.py` |
| 评价指标与其他估计路线 | 评价指标调研与方法横向对比 | `Research/评价指标与其他估计路线/` |

## 不进入正文的证据

以下内容只作内部参考：

1. 没有 manifest 的旧 CSV 数字。
2. 只在对话或提示词中出现的结论。
3. 未说明 seed、split、参数网格的训练结果。
4. valid-only 但没有 fallback 全样本性能的结果。
5. 与当前 `python/methods/mdm.py` 语义不一致的旧结果。
6. 已封存的 MLE 比较结果（按当前论文口径不再消费）。
7. Direct-MLP 与 MDM 的完整比较（属于独立 Research）。

## 当前正式代码与图表脚本

正式代码放在 `code/`。已存在入口：

| 模块 | 责任 |
|------|------|
| `config.py` | 正式参数网格、delta grid、seed、输出路径 |
| `metrics.py` | J1、每参数 Bias/SD、失败率等指标 helpers |
| `utils.py` | 路径、时间戳、git commit、JSON/CSV 写出等公共工具 |
| `generate_mc_data.py` | 生成/合并 E1/E2 共用 MC 扫描数据，并生成 MLE anchor |
| `analyze_E1.py` | Default/L1/L2 汇总、δ-risk 曲线数据、E1 summary |
| `analyze_E2.py` | L3-L6 oracle/hindsight 层级汇总、E2 summary |
| `analyze_beta_profile_audit.py` | 重建 300 个正式 seed 样本，提取 β–profile 几何诊断 |
| `analyze_E1_E2_crossfit.py` | 基于现有 MC 缓存的 L1-L5 五折选点/评价分离审计 |
| `run_E3b_vector_mlp.py` | Vector-MLP 风险曲线选择器正式入口 |
| `run_E4_formal_validation.py` | E4 特征消融/边界/off-grid 正式验证 |
| `run_p2_*.py`、`p2_config.py` | P2 泛化补点正式生成/评价 |
| `run_p3_*.py`、`p3_config.py` | Direct-MLP 实现与 smoke（Research 入口） |
| `run_p4_formal_compare.py`、`p4_config.py` | P4 正式六方法比较（Research 入口） |
| `generate_g5_figures.py` | 主图表生成 |
| `plot_fig*.py`、`plot_ch6_workflow.py` | 各图表脚本 |

正式代码必须调用当前生产实现或共享流水线：`python/methods/mdm.py`、`python/studies/common/sample.py`、`python/studies/common/metrics.py`。

## 缺口清单（论文完成视角）

1. 原文公式和术语逐句核对（进行中）。
2. ~~工程寿命分位点 `x_0.90`/`x_0.95`/`x_0.99` 由 P4 逐样本估计派生~~ ✓ 已完成（`artifacts/formal/quantile_derivation/`）。
3. 域匹配真实案例（后置，不阻塞写作）。
4. 外部论文稿 v0.3 按新骨架补齐正文（唯一活动论文稿）。
