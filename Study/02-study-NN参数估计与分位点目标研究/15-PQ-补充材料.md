# Study02 P–Q 补充材料：核心合同与可信性证据

> 与 `14-PQ-论文初稿.md` 配套。后加范围扩展的结果与证据仍在仓库中，但不属于本稿附件。

## S1. 核心研究合同

| 项目 | 冻结定义 |
|---|---|
| 参数空间 | β={1.5,2,…,5}；η=1000；γ/η={.10,.25,.50,.75,1}；n={7,10,15,20} |
| 样本 | 每参数—n 组合 300 个独立 repeats；`repeat_id mod 5` 分折 |
| 路线 | `P_equal` / `Q_param`，相同三输出网络与合法解码 |
| 唯一干预 | 训练损失和与其对齐的 validation checkpoint 选择 |
| 模型单元 | 10 seed×4 n×5 fold=200 个 P/Q 配对单元 |
| 主评价 | held-out \(x_{0.95}\) pooled rRMSE |

`P_equal` 为三个归一化参数平方误差的等系数和；它是任务无关参照，不代表三个参数的实际
目标敏感度相同。`Q_param` 直接优化 \(x_{0.95}\) 相对平方误差，其三输出只作内部参数化。

## S2. 训练与配对

- MLP：`n-256-128-64-3`，ReLU，float64；合法解码保证
  \(\hat\beta>0\)、\(\hat\eta>0\)、\(0<\hat\gamma<\min(X)\)。
- Adam，lr=1e-3，weight decay=1e-4，batch=256，max epochs=300，patience=20。
- seeds：42、2026、3407、17、73、314、2718、4099、8128、12011。
- P/Q 在每个模型单元内共享训练/验证/测试行、scaler、初始化、batch、网络与预算。
- 完整网格为 400 fits（200 对）；0 未完成、0 NaN、0 非有限、0 支持域违规。

## S3. 主推断

\[
I=(\mathrm{rRMSE}_P-\mathrm{rRMSE}_Q)/\mathrm{rRMSE}_P.
\]

以 `(n,fold,seed)` 模型级 MSE 为配对单元，按 n 分层重采样 fold，同时全局重采样 seed，
路线保持配对；B=200,000。逐 n 结果为描述性分层，不进行 60-cell 显著性计数。

## S4. 主结果

| 对比 | P rRMSE | Q rRMSE | Q 相对改善 | 95% CI |
|---|---:|---:|---:|---:|
| pooled 网格 P–Q | 0.166059 | 0.161075 | 3.001% | [2.195%,3.793%] |

网格逐 n：n=7/10/15/20 的 Q 相对改善分别为 3.05%、3.66%、2.70%、1.92%，对应区间
均排除 0。10/10 seed pooled 方向有利于 Q；184/200 配对单元有利于 Q。线性 MSE 差
`Q−P` 为 −0.001630，95% CI [−0.002098,−0.001174]。

## S5. 敏感度与误差补偿

Q 损失在解码参数空间满足

\[
\frac{\partial L_Q}{\partial\hat\theta_j}
=\frac{2(\hat x_R-x_R)}{x_R^2}
\frac{\partial\hat x_R}{\partial\hat\theta_j},
\]

所以目标敏感度直接进入参数方向的梯度权重。逐行精确恒等式

\[
\frac{\hat x-x}{x}=C_\beta+C_\eta+C_\gamma
\]

在先期 3-seed 保存预测上成立至约 1e-7；精确抵消指数为 P 0.352、Q 0.915。该结果描述
拟合后的误差组织，不单独声称训练动力学全因果链已被识别。

## S6. 可信性与证据

- 10 seeds 用于确认方向与训练随机性；不是所有子分析的默认重复次数。
- 主结果：`artifacts/pq_s5b_revision/analysis/summary_s5b.json` 与 `cell_mse.csv`。
- 先期主证据与机制：`artifacts/pq_iid_main/`，SHA 清单 252 条。
- 10-seed 增量证据：`artifacts/pq_s5b_revision/`，总 SHA 清单 1769 条，已在干净 archive
  验证 0 missing / 0 mismatch。
- 运行与验证：`python -m study02pq.s5b_revision --phase verify`。

## S7. 范围扩展归档

连续参数支持、联合中点、gamma-holdout OOD、Q_direct、多可靠度水平和容量实验均原样保留在
相应 artifact 与历史报告中。它们属于后加的探索性范围扩展，不用于本稿核心主张，也不作为
本稿“尚欠实验”。如后续研究这些问题，必须重新定义独立 RQ、estimand 和协议。

## S8. 限制

本文为未删失仿真研究，固定 \(\eta=1000\)，仅覆盖冻结网格、\(n=7\)–20、一个主网络与
\(R=0.95\)。fold 训练集有重叠，10 seeds 仍是有限算法重复。结论不涉及 Q 内部参数准确性、
完整分布恢复、其他参数域、真实工程数据或分布迁移。

---

*状态：核心论文合同已收口，等待独立审查。*
