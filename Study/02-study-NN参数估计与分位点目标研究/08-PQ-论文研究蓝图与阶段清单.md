# Study02 论文研究蓝图与阶段清单

> 冻结日期：2026-08-06
> 用途：长任务的唯一总入口；只记录科学主线、必要实验和阶段状态，不建立额外调度框架。

## 1. 论文要回答什么

拟题：**面向目标可靠度分位点的三参数 Weibull 神经估计：任务对齐损失的精度收益、作用机制与泛化边界**。

核心问题不是笼统判断 Q 是否优于 P，而是：

1. 当工程目标明确为估计 \(x_p\) 时，直接优化 \(x_p\) 的 Q 路线相对参数精度 P 路线能提高多少？
2. 改善来自什么机制：参数误差如何沿分位点敏感方向投影、抵消或累积？
3. 改善在哪些样本量、参数区域、目标分位点和网络容量下稳定，边界在哪里？

论文的预注册式方向假设是 Q 在同分布目标任务上更优，但正式结论必须由效应量、不确定性、机制证据与边界实验共同支持，不能写成“由定义必然更优”。

## 2. 不变的共同科学合同

主设计与 Study01 保持同一参数空间和样本身份体系，以便后续横向分析：

- \(\beta\in\{1.5,2,2.5,3,3.5,4,4.5,5\}\)；\(\eta=1000\)；\(\gamma/\eta\in\{0.10,0.25,0.50,0.75,1.00\}\)；
- \(n\in\{7,10,15,20\}\)，每个参数—样本量组合 300 次重复，训练种子 42/2026/3407；
- 使用 Study01 相同生成器、namespace、sample key、输入表示和基线网络 `256-128-64`；
- P/Q 只允许损失函数不同；数据、初始化、batch 顺序、scaler、早停与评价逐 fit 配对；
- 三参数输出必须结构性满足 Weibull 支持域，P 使用直接参数相对误差，Q 使用目标分位点相对平方误差；
- 主问题采用每个精确参数组合内部按 repeat 分层的独立 train/validation/test；整层 gamma 留出只作为 OOD 补充。

## 3. 最小但完整的证据闭环

### M1 主效应：Q 好多少

- 完成 5 fold × 3 seed × 4 n × 2 route = 120 个配对 fit；
- 主指标为目标分位点 rRMSE 与逐模型配对 MSE 差；
- 主区间按 fold × seed 设计层级交叉重采样，逐样本区间只作条件性补充；
- 报告 pooled、按 n、seed、fold 和参数区域的效应量及不确定性。

### M2 机制：为什么好

优先复用 M1 保存的逐样本预测，不为机制另建大型训练流水线：

- 分解 \(x_p=\gamma+\eta[-\log(p)]^{1/\beta}\) 对 \(\beta,\eta,\gamma\) 的局部敏感度；
- 比较 P/Q 参数误差向量在目标分位点梯度方向上的投影；
- 检验 Q 是否通过参数误差抵消落在近似等分位点曲面，而 P 的小参数误差仍可能沿高敏感方向累积；
- 将机制量与每个参数区域、n 下的 Q 改善关联，区分证据支持、描述性解释和假说。

### M3 可信性与边界

只做能直接回答论文质疑的有限扩展：

- 目标分位点：\(x_{0.90},x_{0.95},x_{0.99}\)，分别训练目标特异 Q，并做交叉目标评价；
- 网络容量：在固定少量 fold 上比较小型、基线、大型网络，判断结论是否依赖单一容量；
- 参数空间：主分析保持 Study01 冻结网格；增加网格内插值测试，整层 gamma 留出作为 OOD 边界；
- 重复性：所有关键结论保留 3 seed，明确 3 seed 对训练不确定性的限制。

真实数据、删失/截断、污染、跨尺度迁移不是本轮成稿的前置条件；只有在上述证据仍不足以回答审稿质疑时才单独授权。

## 4. 阶段与完成标准

| 阶段 | 状态 | 完成标准 |
|---|---|---|
| S0 问题与协议冻结 | **已获 Codex + mentor 批准（2026-08-07，tip `ca82ffb3`）** | RQ、Study01 对齐、estimand、拆分、证据合同通过 Codex + mentor 审查；候选：`09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json`。R1 修复：x_p 定义、estimand/估计一致性、对齐范围、严格协议校验、成本依据。S1 为独立排队任务 |
| S1 主效应 | **已获 Codex + mentor APPROVE**（2026-08-07，tip `928497b`；结果 tip `5a90f1f7`，证据封存 `4d5fd3b3`） | 120 fits 完整、可校验、可复算；主效应与分层结果完成 |
| S2 机制 | **已获 Codex + mentor APPROVE**（2026-08-07，tip `584569f8`；`10-PQ-作用机制分析.md`） | 精确分解—目标敏感度链条有定量证据，能解释主要异质性；详见 §6 S2 摘要 |
| S3 可信性与边界 | **已获 Codex APPROVE（2026-08-07，S3 R2 报告经 mailbox 提交，Codex message 20 APPROVE）**（实现 `e8cb21a`/E3 `4cf482f7`；`12-PQ-可信性与边界结果.md`） | 多分位点、网络容量、插值/OOD 的必要实验完成 |
| S4 综合报告与图表 | **完成，待 Codex S4 REVIEW**（`13-PQ-综合科学报告.md` + `figures/pq-paper/fig{1,2,3,4}`，脚本 `code/study02pq/paper_figures.py`） | 形成“问题—方法—结果—机制—边界—建议”科学报告及论文级图表；状态 `READY FOR CODEX S4 REVIEW` |
| S5 论文初稿与审查 | 待开始 | IMRaD 初稿、补充材料、证据索引、完整性检查和模拟审稿修订完成 |

## 5. 协作与停止规则

- Codex：冻结计划、审核科学合同、复算关键结果、解释机制、决定是否进入下一阶段；
- Claude Code：在同一 `C:\weibull` 可见窗口执行代码、实验、证据整理和初稿落地；
- mentor 只在 S0 冻结、S1 主结果、S2 机制结论、S5 成稿前四个关键门检查；
- 每阶段只维护本表和对应科学产物，不建立 scheduler、authority、ledger 或通用实验平台；
- 遇到科学合同改变、Study01 身份不一致、无法恢复的证据缺失或需要显著扩展范围时暂停并交 Codex；
- 历史 r4/v3 和 v2 原样保留，分别标作 OOD 补充和 `P_loggap` 敏感性，不覆盖、不冒充新主证据。

## 6. 当前已知起点

- S0 同分布主协议冻结候选：`09-PQ-同分布主协议冻结.md`（RQ、estimand、拆分、统计层级、
  证据 schema、M1–M3 矩阵与成本；配置 `configs/pq-iid-protocol-v1.json`）。
- 同分布 fold-1 pilot 共 24 fits：Q pooled rRMSE `0.15903`，P `0.16596`，Q 相对改善约 `4.18%`；12/12 个 n×seed 单元方向一致。
- 该 pilot 只用于验证新主合同可运行并支持正式全量实验，不能代替 M1 的 120-fit 主证据。
- target-finetune pilot 显示同分布验证改善不自动转化为未见 gamma 外推改善，支持将 OOD 作为明确边界而非主问题。
- **S1 M1 主效应证据已封存并获 Codex + mentor APPROVE**（2026-08-07，tip `928497b`）：
  iid-v1 120 fits（0 失败/0 支撑违规）；pooled P 0.1663 / Q 0.1611，rel_change (Q−P)/P
  **−3.1%**，hat_Δ **−0.00171**（95% CI **[−0.00225, −0.00119]**，不含 0，Q 更优）；
  按 n/seed/fold 方向全部 Q 更优、按 n 的 CI 全部不含 0；按区域描述性方向存在翻转
  （低 β、低 γ/η 为 Q 更优；高 β、高 γ/η 为 P 更优）。
- **S2 机制分析 R1 修订完成**（2026-08-07，复用 S1 证据，未重训，未动 120 NPZ/120 fit
  metadata）：`10-PQ-作用机制分析.md` + `code/study02pq/mechanism.py` +
  `analysis/mechanism_{summary,by_region,cell_pairs}` + 16 项机制测试（全套 **62 passed**）。
  核心发现：
  - **精确无余项分解**（恒等式，逐行成立至 ~1.8e-7）：`actual = C_β+C_η+C_γ`；精确抵消
    `cancel_exact` pooled P **0.352** / Q **0.915**（Codex 独立校验一致）——Q 通过大而
    相互补偿的参数变化维持 x_0.95（直接证据，不依赖一阶近似）。一阶投影仅为局部线性
    诊断：P r2_origin=0.989；Q rms_proj 6.37 / rms_rem 6.36，corr(proj,rem)=−0.9995
    （代数近乎强制，不作为原因），r2_origin=−1556（origin-anchored，非方差 R²）。
  - **区域翻转与目标敏感度 `‖s‖`**（有界机制）：`‖s‖` 随 β、γ/η 单调下降（2.165→1.222；
    2.593→0.825），与区域 Q−P 差负相关（β −0.952 / γ/η −0.872；n 恒定故相关未定义）；
    `delta_rms_actual` 与封存 S1 符号 **17/17 复现**（一致性检验）；n 从不翻转。不再主张
    未量化/未定义的「曲率大小致翻转」。
  - S1 证明保全：`splits_manifest.json` 与顶层运行证明恢复为 S1 封存值；S2 分析证明另置
    嵌套 `s2_analysis_provenance`（baseline_tip `928497b`、no_training=true）。
  **已获 Codex + mentor APPROVE（tip `584569f8`）**，S2 finding 全关闭；获准仅进入冻结的 S3。
  非 blocking 论文期告诫（须在 S4/S5 陈述）：`‖s‖` 依赖 P loss 继承的归一化参数坐标；
  对称精确分解是精确但非唯一的分配方式。
- **S3 可信性与边界（M3）实验与分析完成；R2 修订（S3-001）完成并已获 Codex APPROVE**（2026-08-07，
  Codex message 20 APPROVE；E1/E2 实现 `e8cb21a`，E3 R2 重跑 `4cf482f7`，**336 新 fits**；
  冻结协议 `11-PQ-可信性与边界协议.md` + `configs/pq-s3-boundary-v1.json`；结果
  `12-PQ-可信性与边界结果.md`）。**非阻塞告诫 S3-002**：不把中点 Q 反转归因为网格过拟合/
  笼统网格特异，已在 S4/S5 统一收口：只写“所测联合中点上优势未保持”。
  - **E1 目标可靠度水平**：目标特异 Q 全水平优于 P，95% CI 均不含 0 —— x0.90 −2.49%
    （P 0.13834 / Q90 0.13490，Δ [−0.00121, −0.00069]）、x0.95 −3.14%（与 S1 封存复算 1e-6
    一致）、x0.99 −6.50%（P 0.22239 / Q99 0.20792，Δ [−0.00749, −0.00502]）；收益随目标更
    极端而增大。**Q 窄带对齐**：交叉目标离目标退化 +16%~+300%（Q90 在 0.99 +142.7%、Q99 在
    0.90 +148.2%）；P 跨水平最稳健。
  - **E2 网络容量**（fold {1,3} 描述性）：small 64-32 −1.57%、baseline −3.25%、large 512-256-128
    −2.70% —— P–Q 优势**不绑定**单一基线容量。
  - **E3 域内插值（R2 修订后）**：120/120 重训证据与 S1 字节一致（PASS）。**R2 S3-001**：
    初版将未标准化中点样本送入标准化训练网络 → 伪 130%/40% rRMSE 与 P 37.7% γ 边界；
    修复 `eval_interp`（应用调用方 (n,fold) scaler，commit `4cf482f7`）并**仅重跑 E3**后：
    中点 pooled rRMSE **P 0.14349 / Q 0.14617**（与网格点 ~0.16 同量级，**0/504,000 行边界**）
    ——**域内插值推广成立**；但 Q 网格点优势不在中点（P 优 **+1.86%**，CI [+0.000539,
    +0.000985] 不含 0），仅最低 β=1.75、最低 γ/η=0.175 中点 Q 仍优。**所测联合中点上 Q 优势
    未保持**；Q 的目标对齐是窄带的（E1 交叉目标矩阵一致）。不归因为网格过拟合/笼统网格特异
    （S3-002）。
  - **E4 OOD**（复用封存 r4/v3，不重训）：gamma 层级 OOD **Q 高 P 8.77%（(Q−P)/P，P 更优）**，
    Q 优势反转（历史口径 (Q−P)/Q = 8.07% 为“P 相对 Q 的误差降低”，分母不同）；
    iid 网格点/域内中点/gamma-holdout OOD 分属不同数据合同，禁止池化。
  - **机制一致性（S2 × S3）**：精确分解在每目标 p 成立（残差 ~1e-15）；`cancel_exact`
    Q 0.93/0.91/0.72 vs P 0.36/0.35/0.33；‖s‖ 区域负相关（β −0.94~−0.97）与 S2 一致。
  - **验证**：336 fits 0 失败/0 支撑违规；逐根 `manifest.json` `run_code_sha == git_full_sha`
    （target/capacity `e8cb21a`，interp `4cf482f7`）绑定 `analysis_sha256`；
    SHA256SUMS 807/807 全验；全套测试 133 passed（1 pre-existing 无关失败）。
- **S4 综合科学报告与论文级图表完成**（2026-08-07，基线 `4ae78950`；待 Codex S4 REVIEW）：
  `13-PQ-综合科学报告.md` 按“科学问题—设计—主结果—机制—可信性—边界—结论与建议—限制”组织，
  区分 confirmatory（S1 x0.95）/ mechanism（S2）/ robustness-boundary（S3+E4），统一核心叙述
  （Q 在 x0.95 上低约 3.1%、crossed CI 不含 0；机制 = 参数误差沿目标函数抵消而非单参数更准；
  边界 = 所测联合中点上优势未保持、gamma-holdout OOD 反转）。图表
  `figures/pq-paper/fig{1,2,3,4}.{png,pdf}`（200 dpi PNG + 矢量 PDF，Okabe-Ito 色盲友好）由
  `code/study02pq/paper_figures.py` 只读 sealed JSON/CSV 生成，禁止手抄数值。报告含 claim→evidence
  表逐项指向 sealed 分析文件。状态 `READY FOR CODEX S4 REVIEW`（不自行 APPROVE，不进入 S5）。
