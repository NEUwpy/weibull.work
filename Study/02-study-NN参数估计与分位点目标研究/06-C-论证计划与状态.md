# Study/02 C 阶段：科学论证计划与状态（R1 修正版）

> 创建日期：2026-08-01；**2026-08-02 R1 修正后重写**。
> 职责：把 A/B 已有实验结果提升为可用于论文加工的科学论证——明确 C 要回答的科学问题、核心主张与边界、主张—证据—机制—替代解释—缺口的矩阵，以及当前状态。本文件是 C 阶段的论证控制文档，不是论文正文。
> **当前状态：R1 修正完成。** 原 C2–C4（绑定 `8cc93147`）基于 B4/B5 的无效 P 预处理（未施加 A-E1 输入 scaler），其"D 相对 P 更优 / 目标对齐收益"结论**已被取代**。当前机制分析见 `07-C-机制分析与补充证据.md`（修正版）、论文前论证见 `08-C-论文前科学论证报告.md`（修正版）、权威证据见 `05-B-增量实验报告.md` 与 `BINC-20260802-02`。Git history 保存原版。

## 一、C 要回答的科学问题（修正后）

| 问题 | C 的答案（当前最小充分表达） |
|---|---|
| **Why** | 可靠性工程最终关心寿命分位点。若只需 \(x_{0.95}\)，保留完整 Weibull 参数化是否付精度代价？ |
| **What** | 正确评估下（P 施加其训练输入 scaler），**D 相对 P 在 core 无精度优势**：pooled I=−0.120（P 更好），P 在 n=5–9/12 显著更优，无任何 n D 更优。原 I=0.3926 是 raw-P 伪影。 |
| **How** | **未识别**。原"训练目标对齐收益"（P 0.6943 vs Dctrl 0.3296）基于无效 P 比较，不可保留；需用修正输入重新形成受控证据才可重新评价。 |
| **替代解释** | D 的优势只在训练支持外（stress low/loc、NIST）部分出现；core 内无优势。MLE 存活者偏差、可用率行级偏差等边界保留。 |
| **边界** | 有效：core 内 P 不劣于 D。不确定：域外 D 的部分优势、NIST 无已知真值、stress 无 OOD 保证。 |
| **取舍** | D 牺牲 P 的完整 β/η/γ 输出；但修正后 P 不为此付 core 精度代价（P core RMSE 0.2709 ≤ D 0.3034）。 |

## 二、核心主张矩阵（修正后）

| 主张 | 修正后表述 | 证据 | 状态 |
|---|---|---|---|
| C-A | core 上 P 不劣于 D（pooled I=−0.120；n=5–9/12 P 显著更优、无 D 优势） | `BINC-02` analysis_summary + b4_correction（n5 P 0.403 = Controller 0.402965） | 支持（修正后） |
| C-B | 原"目标对齐收益"**未识别**（旧比较无效；未用修正输入重做受控 P-vs-Dctrl） | — | 待重新评价（未识别） |
| C-C | P 保留完整合法 β/η/γ 且不付 core x0.95 精度代价 | P core 0.2709 vs D 0.3034 | 支持（修正后） |
| C-D | D 仅在训练支持外（stress low/loc、NIST）有部分条件优势 | `B5_correction` | 支持（域外，条件性） |

## 三、工作包与结果（修正后）

- **C2（机制）**：n 异质性、参数→x0.95 传播、β 边界。修正后：P 的 n=5"灾难"是伪影（γ/η 反事实 0.457/0.148 vs 原 2.445/1.719）；低 β P 强优、高 β 持平；D 各 n 轻微负偏。
- **C3（替代解释）**：core D 可用率 100%（修正后 P 亦 100%）；MLE shared-valid 选择较易子集（保留）；"参数区方向混合"不再适用（修正后 P 各区域均不劣于 D）。
- **C4（信息取舍）**：P 完整参数是功能价值且不付 core 代价；域外边界保留；传统方法 MDM/LRE 仍是更强基线。
- **conformal/NIST（R7）**：P core cov90/cov95 近标称（0.895–0.911/0.945–0.957，有限样本 order statistic）、可用率 100%；NIST D pinball 略优但两者 anti-conservative。

## 四、当前状态与下一步

- C 阶段 R1 修正完成；机制分析、论文前论证已按修正证据重写。
- 遗留缺口（非阻塞）："目标对齐收益"未用修正输入重做受控比较（未识别）；中间 n 的 P 优势未独立重训交叉验证；域外 D 优势机制未消融。
- 等待 reviewer 对 B-增量（`05-B-增量实验报告.md`）的 APPROVE。
