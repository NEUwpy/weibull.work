# Study/02 P–Q：结果报告

> 创建日期：2026-08-05
> 状态：**seed 42 临时方向报告（PROVISIONAL）**。seed 2026/3407 运行后将用三 seed
> 配对结果替换临时数字。协议冻结于 seed 42 结果之前，见 `01-PQ-冻结协议.md`。

## 0. 结论（临时，PROVISIONAL）

> ⚠️ 以下数字来自 **seed 42 单独**（40 fits，48,000 held-out 样本）；三个 seed 全部完成
> 前不得作为最终结论引用。

**seed 42（临时）**：在完全相同的数据/网络/初始化/划分/训练设置下，**Q（目标分位点损失）
在 held-out \(x_{0.95}\) 相对 RMSE 上略优于 P（参数精度损失）**：

- pooled：P rRMSE = 0.2223，Q rRMSE = 0.2142；配对差值（Q−P，平方误差空间）=
  −0.00354，95% CI [−0.00422, −0.00284]（不包含 0）；相对改善 ≈ −3.6%。
- 四个 n 上 Q 均更优（n=7 −1.5%，n=10 −2.9%，n=15 −7.8%，n=20 −4.0%），CI 均不含 0。
- 20 个 cell 中 Q 在 11 个 cell 更优、P 在 9 个 cell 更优（cell 级方向混合）。
- 失败模型 0/40；非有限输出 0；非法参数 0。

**方向一致性待定**：需 seed 2026/3407 确认是否与 seed 42 方向一致（若不一致将如实报告）。

## 1. 正式规模与运行

| 项 | 值 |
|---|---|
| 预期 fits | 120（4 n × 5 fold × 2 route × 3 seed） |
| seed 42 fits | 40 / 40（全部 converged；0 NaN；0 非有限；0 非法） |
| seed 42 运行时 | 441 s（best_epoch 均值 103，范围 32–191） |
| 环境 | Python 3.11.9；torch 2.12.0+cpu；numpy 2.4.6；pandas 3.0.3 |
| 代码 | `code/study02pq/`（commit `f07b2928`）；协议 SHA `62b1c807…`；配置 SHA `49036850…` |

## 2. 配对验证（seed 42）

`artifacts/pq/pairing_report.csv`：20 个 `(n, fold, seed=42)` 配对全部匹配
（初始参数张量、batch 顺序、网络结构、scaler、train/val/test 行 SHA 一致），唯一差异为
loss route。✓

## 3. seed 42 结果（临时）

### 3.1 pooled 与按 n

| group | n_test | P rRMSE | Q rRMSE | 配对差值 (Q−P) | 95% CI | 相对改善 |
|---|---|---|---|---|---|---|
| pooled (seed 42) | 48000 | 0.22227 | 0.21417 | −0.00354 | [−0.00422, −0.00284] | −3.6% |
| n=7 | 12000 | 0.26279 | 0.25884 | −0.00206 | [−0.00344, −0.00075] | −1.5% |
| n=10 | 12000 | 0.22835 | 0.22173 | −0.00298 | [−0.00506, −0.00118] | −2.9% |
| n=15 | 12000 | 0.20832 | 0.19205 | −0.00651 | [−0.00792, −0.00516] | −7.8% |
| n=20 | 12000 | 0.18171 | 0.17444 | −0.00259 | [−0.00330, −0.00191] | −4.0% |

### 3.2 按 fold（seed 42 pooled across n）

| group | n_test | P rRMSE | Q rRMSE | 配对差值 | 95% CI | 相对改善 |
|---|---|---|---|---|---|---|
| fold_1 | 9600 | 0.25410 | 0.25409 | −0.00001 | [−0.00154, 0.00149] | 0.0% |
| fold_2 | 9600 | 0.23920 | 0.21865 | −0.00941 | [−0.01203, −0.00711] | −8.6% |
| fold_3 | 9600 | 0.22336 | 0.21009 | −0.00575 | [−0.00741, −0.00419] | −5.9% |
| fold_4 | 9600 | 0.20984 | 0.20813 | −0.00072 | [−0.00166, 0.00015] | −0.8% |
| fold_5 | 9600 | 0.17696 | 0.17180 | −0.00180 | [−0.00260, −0.00103] | −2.9% |

### 3.3 逐 cell（n × fold，seed 42）方向

| 方向 | cell 数 | 说明 |
|---|---|---|
| Q 更优（差值 CI 不含 0 且 < 0） | 11 | n7 f1/f5、n10 f1/f2、n15 f1/f2、n20 f1/f3/f4/f5 等 |
| P 更优（差值 CI 不含 0 且 > 0） | 9 | 其余 cell |
| 方向混合 | — | cell 级混合，pooled 一致指向 Q |

（逐 cell 数字见 `artifacts/pq/analysis/paired_summary_by_group.csv` 的 `cell` 行与
`direction_by_cell.csv`。）

## 4. 诊断（非成功标准）

- 参数误差仅用于诊断：Q 以合法但可能退化的 β̂ 达到分位点目标（协议 §2 不主张 Q 参数准确）。
- seed 42 中：0 个非法参数、0 个非有限预测、0 个 NaN 训练。
- 支撑集违规（γ̂ ≥ min X）计数见 `per_fit_metrics.csv`（诊断用）。

## 5. 证据路径与 SHA

| 材料 | 路径 | 说明 |
|---|---|---|
| 逐 fit 指标 | `artifacts/pq/per_fit_metrics.csv` | 40 fit 行（seed 42） |
| 配对报告 | `artifacts/pq/pairing_report.csv` | all_match=True |
| 配对汇总 | `artifacts/pq/paired_summary.csv` | pooled + 每 cell |
| 分组汇总 | `artifacts/pq/analysis/paired_summary_by_group.csv` | cell/seed/n/fold/pooled |
| 方向统计 | `artifacts/pq/analysis/direction_by_cell.csv`、`direction_by_seed.csv` | |
| 失败计数 | `artifacts/pq/analysis/failure_counts.json` | 0 failures |
| 逐 fit checkpoint | `artifacts/pq/checkpoints/<fit_id>.json` | tracked |
| 逐 fit 预测 | `artifacts/pq/predictions/<fit_id>.csv` | gitignore，SHA 入 SHA256SUMS |
| 哈希清单 | `artifacts/pq/SHA256SUMS` | |
| 总 manifest | `artifacts/pq/manifest.json` | git_head、配置 SHA、data_integrity |
| 运行日志 | `artifacts/pq/run_seed42.log` | |

## 6. 三 seed 最终结果（待替换本节临时数字）

（seed 2026 + 3407 运行完成后，用三 seed 配对结果替换 §3 全部临时数字，并在此报告方向
一致性、三 seed 合并结果与最终结论。）
