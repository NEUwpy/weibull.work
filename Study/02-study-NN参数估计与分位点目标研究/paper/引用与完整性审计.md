# Study02 核心论文引用与完整性审计

> 审计对象：`论文初稿.md`、`补充材料.md`，并与 `../docs/研究说明.md` 的当前口径交叉核对。
> 状态：科学主线已收口，独立方法定向复审 APPROVE；不表示期刊同行评议。

## 1. 核心主张—证据映射

| 主张 | 证据 | 边界 |
|---|---|---|
| Q 相对 P 改善 3.001%，95% CI [2.195%,3.793%] | `artifacts/pq_s5b_revision/analysis/summary_s5b.json` | 唯一确认性效应 |
| 10/10 seed 方向一致；184/200 单元有利于 Q | 同上 + `cell_mse.csv` | 说明非单 seed 偶然 |
| P/Q 除损失与相应 checkpoint 目标外逐单元配对 | pairing/fit metadata/manifest | 不扩大为不同表示比较 |
| 同一 u 坐标中 Q 梯度含目标敏感度；网格 \(\|s\|\) 变化 5.74 倍 | Weibull 公式 + `code/study02pq/mechanism.py` + `pq_paper_core/analysis/mechanism_sensitivity_grid.csv` | 精确损失几何 + 解析真值点地图 |
| Q 的误差分量抵消更强（0.915 vs 0.351；200/200） | `pq_paper_core/analysis/mechanism_paper_core.json` + `mechanism_paper_cells.csv` | 结果空间证据，非完整动力学因果 |
| 冻结网格内低敏感度区域存在 P 描述性反超 | `pq_paper_core/analysis/mechanism_paper_regions.csv` | 必须披露的异质性，未做多重校正 |
| 0 非有限、0 支持域违规 | S5B verify + manifests | 合法性与完整性 |

## 2. 术语和科学边界

- `P_equal` = 三个归一化平方参数误差项使用相同系数；不表示实际目标敏感度相同。
- `Q_param` = 相同三参数输出、解码与骨干，仅训练/验证目标改变；内部参数不作准确性主张。
- “敏感度几何”必须在与 P 相同的无量纲 u 坐标中表述；网络更新还乘解码器/共享网络 Jacobian
  并受优化器影响，不写成已证的完整动力学因果。
- “误差补偿”限于保存预测的精确结果空间分解；不写成已完整识别的优化动力学。
- 10 seeds 是可信性验证，不是每项研究必须重复十次的方法规范。
- 不声称 Q 普遍更优，不搜索 P 的“正确”权重，不把扩展参数支持或 OOD 设为当前论文缺口。

## 3. 引用审计

正文参考文献共 16 条。其作用分为：Weibull 基础 [1,2,3,4]；既有参数估计方法
[5,6,7,12,13,14]；目标/任务对齐的一般思想 [8,9,10,11,15]；Weibull 参数与插件分位点准确性
不必同序的直接动机 [16]。本文不把确定性
\(x_R\) 相对平方损失称为标准条件分位数损失，也不借上述文献主张本文方法的一致性或优先性。

最终门运行 `python paper/tools/audit_zero_orphan.py`，要求 0 orphan / 0 unused。

## 4. 图与证据来源

| 图 | 当前论文用途 | 数据源 |
|---|---|---|
| Fig1 | 10-seed 冻结网格主效应 | S5B `summary_s5b.json` |
| Fig2 | x0.95 无量纲敏感度、区域关联与精确补偿 | `pq_paper_core/analysis/` 四个派生文件 |

Fig3（多目标/容量）与 Fig4（连续/中点/OOD）文件继续保留，但退出当前论文正文，不得据此改写
当前科学问题。Fig2 生成脚本已改为只消费 x0.95 论文核心派生物；sealed 训练证据未修改。

## 5. 已完成证据门

- 机制聚焦测试：19 passed；默认协议全套：81 passed。
- S5B verify：grid 280/280、continuous 600/600、`all_valid=true`；扩展部分仅作证据保存。
- S5B `SHA256SUMS`：1769/1769，0 missing、0 mismatch；干净 `git archive` 同样通过。
- 主稿必须保持 16/16 引用、0 orphan / 0 unused。
- `pq_paper_core/analysis/manifest.json` 绑定生成代码、S5B 协议、P–Q 环境锁、两组输入 manifest 和四个派生输出；
  全量审计为 200 对键 0 mismatch、每路 480,000 行 0 非有限、0 支持域违规。
- 当前修订只从已有 10-seed 证据重算机制派生物与 Fig2；不训练、不修改 sealed 证据。

---

*状态：独立方法定向复审通过；未来任何扩展仍须服从 `../docs/研究说明.md` 的研究边界。*
