# Study/02 可信性与边界（S3）：完整精度预测 CSV（gitignore；SHA 不入 SHA256SUMS）。
# 压缩逐样本证据 evidence/*.npz 与 fit_metadata/*.json 均 tracked。
# run_all_seeds.log 纳入证据（tracked，覆盖根 .gitignore 的 *.log）。
predictions/
!run_all_seeds.log
