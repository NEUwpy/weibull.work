# Study/02 P–Q：执行状态（r4 direct-P primary）

> 创建日期：2026-08-05；修订：r4（Codex 合同决策 000003）
> 职责：实验进度、运行 ID、下一步。协议见 `01-PQ-冻结协议.md`（v3/r4）。

## 当前状态

| 字段 | 值 |
|------|----|
| 阶段 | **r4 primary 120 fits 完成；R4 REVISE 修复中；待 Codex 再审核** |
| 协议 | `01-PQ-冻结协议.md` v3 + `configs/pq-protocol-v3.json`（FROZEN） |
| 环境锁 | `configs/pq-environment-v2.json`（Python 3.11.9 / torch 2.12.0+cpu / numpy 2.4.6 / pandas 3.0.3） |
| 分支 | `codex/study02-pq-controlled-20260805-r1`（baseline `e37fdf70`） |
| r4 合同 | **direct-P**（approved 形式）+ **Codex 授权 domain-explicit 解码器** `γ̂=min(X)(δ+(1-2δ)·sigmoid(o₃))` |
| r4 preflight | ✅ 60 P-fits（3 seed × 20 cell）0 塌缩 / 0 支撑违规 / 0 非有限 / 0 边缘质量；Q 抽查 6 cell 正常；22 tests passed |
| 完成 fits | r4 primary 120 fits 运行中 |
| 产物 | `artifacts/pq_v3/`（r4 primary）；`artifacts/pq_v2/`（v2/P_loggap sensitivity，键 schema 已修复）；`artifacts/pq/`（v1 preliminary） |

## 执行顺序（r4）

1. [x] 核查 Codex 任务（直接-P 修复 + 证据 schema 修复）
2. [x] 证据 schema 修复：键精确 dtype + 从 120 预测 CSV 无重训重封 v2 evidence（120/120，与 master 逐行一致）；新增 key-identity 测试
3. [x] 恢复 direct-P 损失（approved 形式）；发现 exp 解码器塌缩（preflight 6/8）→ **发 blocker**
4. [x] Codex 合同决策（mailbox 000003）：授权 domain-explicit 解码器；**冻结协议 v3**
5. [x] r4 preflight：新解码器 + direct-P，3 seed × 20 cell P 全过（0 塌缩/0 违规）
6. [x] r4 primary 120 fits（1522 s，0 失败/0 支撑违规）
7. [x] 分析（交叉 fold×seed 推断）+ 验证 + 文档 + 推送 + report（tip 03587ab1）
8. [x] Codex R4 REVISE（主数值复现，6 项有界修复）→ 修复中（effect 分母显式、边界诊断、
   manifest/generator v3、状态/逻辑文档、repair 显式 v2、因果表述修正）
9. [ ] 修复后重封 + 推送 + 待 Codex 再审核

## r4 合同要点（详见 `01-PQ-冻结协议.md` §1.1/1.2）

- 合法化：`γ̂ = min(X)·(δ + (1-2δ)·sigmoid(o₃))`，δ=1e-7，γ_lower=0；结构性 0<γ̂<min(X)；
  依赖冻结正位置域（γ∈[100,1000]），不声称负位置成立。
- P 损失：`((β̂-β)/β)² + ((η̂-η)/η)² + ((γ̂-γ)/η)²`（approved direct 形式）。
- 不加 log-gap、辅助 γ 损失、直通梯度、route 特定 init/解码器。
- v2/P_loggap 如实标 sensitivity（不删、不覆盖、不作为 r4 primary）。

## 中断恢复

1. 读本文件 → `01-PQ-冻结协议.md` → `git status`/`git log`。
2. 检查 `artifacts/pq_v3/` 的 fit_metadata 进度（幂等续接）。
3. 从第一个未完成阶段继续。
