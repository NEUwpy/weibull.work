# Study/02 P–Q：执行状态（v2）

> 创建日期：2026-08-05；修订：v2（Codex R1 REVISE）
> 职责：实验进度、运行 ID、下一步。协议见 `01-PQ-冻结协议.md`（v2）。

## 当前状态

| 字段 | 值 |
|------|----|
| 阶段 | **v2 全量重跑（120 fits）进行中** |
| 协议 | `01-PQ-冻结协议.md` v2 + `configs/pq-protocol-v2.json`（FROZEN） |
| 环境锁 | `configs/pq-environment-v2.json`（Python 3.11.9 / torch 2.12.0+cpu / numpy 2.4.6 / pandas 3.0.3） |
| 分支 | `codex/study02-pq-controlled-20260805-r1`（baseline `d0278d98`） |
| 预期/完成 fits | 120 / 进行中（v2 全量，不复用 v1） |
| 产物 | `artifacts/pq_v2/`（evidence npz tracked、fit_metadata、predictions gitignore） |
| v1 状态 | `artifacts/pq/` 保留，preliminary/superseded |

## 执行顺序

1. [x] Study01 权威输入核验（Dimensional-RAW、样本契约、折定义、manifest/SHA）
2. [x] 分支建立与目录重组（归档 D 路线、前置实验、主线 P–Q）
3. [x] 协议 v1 冻结 → v1 全量 120 fits → provisional → 三 seed 报告（preliminary）
4. [x] **Codex R1 REVISE** → v2 修订（合法化 / 推断 / 证据 / 环境）
5. [x] v2 实现与测试 — 18 tests passed（含支撑合法性 production test、主推断 bootstrap test）
6. [ ] v2 全量 120 fits（进行中）
7. [ ] v2 三 seed 分析（主推断 (n,fold)×seed 聚类）
8. [ ] v2 文档收口 + 推送 + READY FOR CODEX R2 REVIEW

## v2 关键修订（详见 `01-PQ-冻结协议.md` §11）

- 合法化：γ̂ = min(X) − η(exp(o₃)+δ)，结构性严格 γ̂<min(X)（支撑违规 0）；P 损失 γ 项 = log-gap。
- 推断：主推断 (n,fold)×seed 聚类；逐样本 bootstrap 为次要；删除 60-cell 显著数量。
- 证据：压缩 npz evidence tracked；fit metadata（无模型 state）；SHA256SUMS 只列 tracked。
- 环境：专用 env 锁文件（pip SSL 拦截无法装 pinned env，Codex 允许新环境+锁）。

## 中断恢复

1. 读本文件 → `01-PQ-冻结协议.md` → `git status`/`git log`。
2. 检查 `artifacts/pq_v2/` 的 fit_metadata 进度（幂等续接）。
3. 从第一个未完成阶段继续。
