# Study02 P–Q：执行状态

> 当前更新：2026-08-10。当前权威任务是论文科学主线收口，不新增训练。
> **长任务恢复锚点**：baseline `4adbb329`；当前阶段为“机制证据与 Fig2 收口”。若上下文压缩，
> 先读本节与 `08-PQ-论文研究蓝图与阶段清单.md` §8，再继续；不得从历史 S5B 扩展恢复任务。

## 当前状态

| 项目 | 状态 |
|---|---|
| 核心科学合同 | 冻结网格同分布 `P_equal` vs `Q_param`，只改变训练/验证损失 |
| 核心效应 | 完成：10 seeds、200 对/400 fits，Q 改善 3.001% [2.195%,3.793%] |
| 机制 | 完成：同一 u 坐标的敏感度几何 + 10-seed 结果空间误差补偿 |
| 可信性 | 完成：10/10 seed 方向一致、crossed bootstrap、合法性和证据完整性 |
| 论文 | 已整合机制、estimand、区间性质和区域异质性；独立方法定向复审 APPROVE |
| 新训练 | **未授权，也不需要** |
| Git | 当前分支 `codex/study02-paper-20260806`；未合并 main |

## 已完成任务

- [x] 识别 gamma-holdout 不能作为同分布主问题证据；
- [x] 冻结与 Study01 参数网格一致的 repeat-stratified 主协议；
- [x] 完成 3-seed 发现阶段和逐样本机制分析；
- [x] 完成 10-seed 确认阶段与 paired fold×seed bootstrap；
- [x] 完成逐样本证据、fit metadata、manifest、SHA 和干净 archive 验证；
- [x] 纠正 S5B 把扩展合同升级为论文主问题的范围漂移；
- [x] 把连续/OOD/Q_direct 等现有材料保留为探索性归档；
- [x] 重写项目入口、科学报告、论文、补充材料和审计边界。

## 当前成稿收口任务（按顺序）

- [x] **C1 机制量化**：在 P 归一化坐标下整理 `s_beta/s_eta/s_gamma` 的网格变化，证明 Q 的
  目标敏感度权重随参数点变化；只解析计算，不训练。
- [x] **C2 新 Fig2**：A=逐参数敏感度，B=敏感度与 Q 收益的探索性关联，C=x0.95 精确误差
  补偿；移除旧多目标 panel。
- [x] **C3 正文整合**：同步 `10`、`13`、`14`、`15`、`16`，写清 3% 的绝对/相对含义、
  `P_equal` 的参照地位与机制证据层级。
- [x] **C4 聚焦文献与完整性**：只核验与 Weibull 参数估计、目标对齐和评分—目标关系直接相关
  的引用及主张，不做无边界综述。
- [x] **C5 独立审稿与一轮修订**：实际 diff 经方法定向复审 APPROVE，无 remaining blocking。
- [x] **C6 最终门**：机制专测 19 passed，默认协议全套 81 passed，引用 16/16、0 orphan/
  0 unused，Fig2 与派生物可幂等重生，`git diff --check` 通过；随本变更提交推送。

**停止条件**：C1–C6 闭合且无影响核心结论的真实缺陷即停止。连续/OOD/Q_direct/新网络、
新参数空间、P 权重搜索、更多 seed 和真实数据均不属于本任务。

## 实验层级规则

- 发现：最少预定 seed 足以判断现象和机制方向；
- 确认：增加 seed 只为排除偶然性和估计区间；现有 10 seeds 已足够承担此职责；
- 可信性：核心研究清楚后集中验证，不要求每项分析机械重复十次；
- 扩展：必须先证明会改变哪条当前核心结论，否则进入未来工作。

## 中断恢复

1. 读 `00-PQ-研究问题与边界.md`；
2. 读 `08-PQ-论文研究蓝图与阶段清单.md`；
3. 查看本文件和 `git status`；
4. 若无新授权，不训练、不重算、不修改 sealed artifacts。

## 历史状态说明

旧 v1、v2/P_loggap、r4 gamma-holdout、S3 边界和 S5B 连续扩展均原样保留。它们可用于追溯，
但不再覆盖当前科学合同。详细历史见 `06-PQ-缺口与审查记录.md`、`17` 和 `18`。
