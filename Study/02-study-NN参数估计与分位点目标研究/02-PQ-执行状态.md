# Study/02 P–Q：执行状态

> 创建日期：2026-08-05
> 职责：实验进度、运行 ID、下一步。协议见 `01-PQ-冻结协议.md`。

## 当前状态

| 字段 | 值 |
|------|----|
| 阶段 | smoke 通过 → **seed 42 正式运行（进行中）** |
| 协议 | `01-PQ-冻结协议.md` + `configs/pq-protocol-v1.json`（FROZEN 2026-08-05） |
| 分支 | `codex/study02-pq-controlled-20260805-r1`（baseline `d0278d98`） |
| 实现 commit | `f07b2928`（code/study02pq） |
| 协议 SHA256 | 协议 `62b1c807...`；配置 `49036850...`（configs/pq-protocol-v1.sha256） |
| 预期 fits | 120（4 n × 5 fold × 2 route × 3 seed） |
| 完成 fits | 0（seed 42 进行中） |
| 运行 ID | 见 `artifacts/pq/manifest.json` |

## 执行顺序（冻结）

1. [x] Study01 权威输入核验（Dimensional-RAW 配置、样本契约、折定义、manifest/SHA）
2. [x] 分支建立与目录重组（归档 D 路线、前置实验、主线 P–Q）
3. [x] 协议与配置冻结
4. [x] smoke（微型非证据数据，production-path）— SMOKE PASS
5. [x] 实现与测试 — 15 tests passed（含配对、Q 梯度经 Weibull、与 Study01 sealed split 对照）
6. [ ] 正式 seed 42（40 fits）→ provisional 方向报告
7. [ ] seed 2026 + 3407（80 fits）
8. [ ] 三 seed 最终分析与报告

## 收敛探针（非正式 seed 999，仅实现验证，不作证据）

- n=7 f0：P rRMSE 0.397（best_ep 118），Q rRMSE 0.407（best_ep 30；β̂ 退化属预期内部表示）
- n=10 f4：P 0.193，Q 0.217；n=20 f2：P 0.156，Q 0.186
- 无 NaN；Q 以合法但退化的 β̂ 达到 x0.95 目标（协议 §2 不主张 Q 参数准确）

## 中断恢复

1. 读本文件 → `01-PQ-冻结协议.md` → `git status`/`git log`。
2. 检查 `artifacts/pq/` 的 per-fit 进度（checkpoint 幂等续接）。
3. 从第一个未完成阶段继续。

## 中断恢复

1. 读本文件 → `01-PQ-冻结协议.md` → `git status`/`git log`。
2. 检查 `artifacts/pq/` 的 per-fit 进度（checkpoint 幂等续接）。
3. 从第一个未完成阶段继续。
