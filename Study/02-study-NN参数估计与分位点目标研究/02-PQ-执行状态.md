# Study02 P–Q：执行状态

> 更新：2026-08-07（**S1 已获 APPROVE；S2 机制分析已获 Codex + mentor APPROVE；S3 可信性与边界实验与分析完成，报告已提交 mailbox，待 Codex S3 审阅**）
> 长任务总入口：`08-PQ-论文研究蓝图与阶段清单.md`。

## 当前状态

| 字段 | 值 |
|------|----|
| 阶段 | **S3：可信性与边界（M3）实验与分析完成，报告待 Codex S3 审阅**（S1 tip `928497b` + S2 tip `584569f8` 已获 APPROVE；S3 实现 `e8cb21a`、336 fits、`12-PQ-可信性与边界结果.md`，经 mailbox 提交，不自行 APPROVE、未开始 S4） |
| 协议 | 同分布主协议 **S0 已获 Codex + mentor 批准**（2026-08-07，tip `ca82ffb3`）：`09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json`（`PQ_PROTOCOL=iid-v1`）；v3/r4 保留为 gamma-holdout OOD 补充 |
| R1 REVISE（S0） | S0-001 \(x_p\) 定义（可靠度寿命点）、S0-002 estimand/有限估计一致性、S0-003 Study01 对齐范围、S0-004 严格 `PQ_PROTOCOL` 校验；成本依据修正（r4 optimizer-train 8160）。已修复并提交 |
| **R1 REVISE（S2，2026-08-07）** | **S2-001** proj/rem 改为局部线性诊断、新增精确无余项分解（`actual=C_β+C_η+C_γ`）与精确抵消 `cancel_exact`、均方比改名 `r2_origin`（origin-anchored）；**S2-002** 新增目标敏感度范数 `‖s‖` 区域分析、区域反转改述为有界敏感度机制、移除「曲率致翻转」未支持主张；**S2-003** `splits_manifest.json` 与顶层运行证明恢复为 S1 封存值、S2 分析证明另置于嵌套 `s2_analysis_provenance`；**S2-004** 参数误差分位点入库 `u_quantiles`；**S2-005** `u_perp` 改述为「真值点局部切空间近似」。已修复并提交 |
| **S2 APPROVE（2026-08-07）** | **Codex + mentor APPROVE**（tip `584569f8`）：全部 S2 finding 关闭；独立复核确认精确恒等式（逐行 ~1.8e-7）、`cancel_exact` P 0.351835 / Q 0.914911、`‖s‖` 区域相关（β −0.952 / γ/η −0.872 / n 恒定）、`splits_manifest.json` 与 S1 `928497b9` 字节一致、SHA256SUMS 252/252。**非 blocking 论文期告诫**：`‖s‖` 依赖 P loss 继承的归一化参数坐标、对称精确分配是精确但非唯一的分配方式——S4/S5 必须显式陈述。**获准仅进入冻结的 S3 可信性与边界任务** |
| **S3 可信性与边界（2026-08-07）** | **实验与分析完成，报告经 mailbox 提交待 Codex S3 审阅**（`11-PQ-可信性与边界协议.md` + `configs/pq-s3-boundary-v1.json`，实现 `e8cb21a`）：**336 新 fits**（E1 120 + E2 96 + E3 120）全 converged、0 失败/0 支撑违规；逐根 `manifest.json`（`run_code_sha == e8cb21a`、`analysis_sha256` 绑定）+ `SHA256SUMS`（**807/807 全验**）；E3 120/120 重训证据与 S1 字节一致；**x0.95 复算与 S1 封存 1e-6 一致**。结果见 `12-PQ-可信性与边界结果.md`：E1 目标特异 Q 全水平优（x0.90 −2.49% / x0.95 −3.14% / x0.99 −6.50%，CI 均不含 0）；Q 窄带对齐（离目标 +16%~+300%）；E2 容量不绑定基线（small −1.57% / baseline −3.25% / large −2.70%）；E3 域内中点不推广（P 130.0 / Q 40.3，Q −69.0%），P 路线 37.7% 插值行 γ̂≈min(X) 边界；机制与 S2 一致（`cancel_exact` Q 0.93/0.91/0.72 vs P 0.36/0.35/0.33，‖s‖ 区域负相关）。全量测试 **132 passed**（1 pre-existing 无关 `study02b_inc` 失败）。**未自行 APPROVE，未开始 S4** |
| 环境锁 | `configs/pq-environment-v2.json`（Python 3.11.9 / torch 2.12.0+cpu / numpy 2.4.6 / pandas 3.0.3） |
| 分支 | `codex/study02-paper-20260806`，基于最新 `origin/main` 并保留完整 P–Q 历史 |
| r4 合同 | **direct-P**（approved 形式）+ **Codex 授权 domain-explicit 解码器** `γ̂=min(X)(δ+(1-2δ)·sigmoid(o₃))` |
| r4 preflight | ✅ 60 P-fits（3 seed × 20 cell）0 塌缩 / 0 支撑违规 / 0 非有限 / 0 边缘质量；Q 抽查 6 cell 正常；26 tests passed |
| S1 主效应（iid-v1 M1） | **120 fits 已运行并封存证据**（结果 tip `5a90f1f7`，封存 `4d5fd3b3`）：0 失败/0 支撑违规/0 非有限/0 非法；pooled P 0.1663 / Q 0.1611，hat_Δ −0.00171（95% CI [−0.00225, −0.00119] 不含 0），rel_change (Q−P)/P −3.1%；按 n/seed/fold 全 Q 更优（按 n 的 CI 全不含 0）；按区域低 β/低 γ/η 为 Q 更优、高 β/高 γ/η 为 P 更优（描述性）。独立复算 VERIFY PASS（hat_Δ 与 CI 逐位一致）；SHA256SUMS 249 项全验 |
| 已完成新证据 | 同分布 fold-1 pilot 24 fits；target-finetune OOD 边界 pilot；S0 冻结候选（R1 修复版，**40 tests passed**，S0 APPROVED）；**S1 iid-v1 M1 120 fits**（**46 tests passed**）；**S3 可信性与边界 336 fits**（E1 120 + E2 96 + E3 120，**9 项 s3 测试，全套 132 passed**） |
| 下一步 | **等待 Codex S3 审阅**（S3 报告已经 mailbox 提交，以 `READY FOR CODEX S3 REVIEW` 结尾）；收到 revise 则修订并重新报告，收到 approve 则仅由 Codex 结束本阶段；不自行 APPROVE |
| 历史产物 | `pq_v3/` 为 gamma-holdout OOD；`pq_v2/` 为 P_loggap sensitivity；`pq/` 为 preliminary |

## 论文任务进度

1. [x] 识别旧 r4 回答的是未见 gamma 外推，而非同分布目标准确性。
2. [x] 实现 repeat-stratified split 并完成 24-fit 最小 pilot；26 tests passed。
3. [x] 运行 target-finetune pilot，确认 OOD 不能由同分布改善直接外推。
4. [x] 建立论文级研究蓝图，限定主效应、机制、可信性与边界四类证据。
5. [x] 提交同分布主协议 **S0 冻结候选**（`09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json` + iid 测试）；33 tests passed。
5a. [x] **R1 REVISE 响应**（Codex + mentor 2026-08-07）：S0-001 \(x_p\) 定义、S0-002 estimand 一致性、S0-003 对齐范围、S0-004 严格协议校验 + 成本依据修正；**40 tests passed**。
6. [x] **Codex + mentor 批准 S0**（2026-08-07，tip `ca82ffb3`）；S1 为独立排队任务。
7. [x] **S1 主效应（M1）**：iid-v1 120 fits 完成、证据封存（结果 tip `5a90f1f7`）、主效应与分层结果产出、独立复算通过；**Codex + mentor APPROVE**（tip `928497b`）。
8. [x] **S2 机制分析（M2）**：`10-PQ-作用机制分析.md` + `mechanism.py` + 16 项机制测试（全套 **62 passed**）；机制输出 `analysis/mechanism_{summary,by_region,cell_pairs}`；区域翻转 17/17 复现 S1（一致性检验）；精确分解恒等式逐行成立（max 残差 ~1.8e-7）；精确抵消 `cancel_exact` P 0.352 / Q 0.915；目标敏感度 `‖s‖` 区域相关（β −0.95 / γ/η −0.87）。**R1 修订完成 + Codex + mentor APPROVE（tip `584569f8`）**。
9. [x] **S3 可信性与边界（M3）**：任务已排队领取，执行中（冻结范围：多分位点、网络容量、插值/OOD；见 `09` §8 M3 矩阵）。

## 不变边界

- 主参数空间、样本身份和基线网络与 Study01 对齐。
- P 为直接参数误差，Q 为目标分位点误差；除损失外逐 fit 配对。
- 主问题用精确参数组合内部的独立重复样本；gamma-holdout 只作 OOD。
- 不建立大型通用实验框架；不在本轮擅自扩展到真实数据、删失、污染或跨尺度。

## 中断恢复

1. 读 `08-PQ-论文研究蓝图与阶段清单.md` 和本文件。
2. 查看 Git 状态及最近一次阶段报告。
3. 从表中第一个未完成阶段继续；已完成正式产物只读复用。
