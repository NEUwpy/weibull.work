# Study/02 P–Q：证据索引

> 创建日期：2026-08-05
> 职责：主张 → 实验 → 证据位置与 SHA 的映射。正式结果运行完成后补充结果小节。

## 1. 权威输入（Study01 对齐，baseline `d0278d98`）

| 输入 | 路径 | blob SHA | 文件 SHA256 |
|---|---|---|---|
| Dimensional-RAW 配置 | `Study/01-.../code/dim_raw_config.py` | `929ee91a…` | `33baacb6…` |
| 样本生成契约 | `python/studies/common/sample.py` | `5466e4c6…` | `3e57ddcf…` |
| 折定义（代码） | `Study/01-.../run_E6b_dimensional_raw_specialist.py` | `56ee0f04…` | `9ec5b97f…` |
| 折定义（封存） | `Study/01-.../specialist/split_report.csv` | `d4193e8a…` | `e1a542b2…` |
| 共享数据 manifest | `.../E5_normalized_raw/shared_data/manifest.json` | `1606b47e…` | `8b574d70…` |
| 共享数据 SHA 清单 | `.../shared_data/data_sha256sums.txt` | `b2674da3…` | `000c82a8…` |
| Study01 正式结果 manifest | `.../E6_dimensional_raw/specialist/manifest.json` | `c2141305…` | `f68c1225…` |

（完整 SHA 见 `01-PQ-冻结协议.md` §0。）

## 2. 冻结协议与配置

| 材料 | 位置 | SHA256 |
|---|---|---|
| 研究问题与边界 | `00-PQ-研究问题与边界.md` | `2c811463…` |
| 冻结协议 | `01-PQ-冻结协议.md` | `62b1c807…` |
| 执行状态 | `02-PQ-执行状态.md` | 见 git |
| 机器可读配置 | `configs/pq-protocol-v1.json` | `49036850…`（`configs/pq-protocol-v1.sha256`） |

## 3. 实现与测试（commit `f07b2928`）

| 材料 | 位置 |
|---|---|
| 实现 | `code/study02pq/`（config/data/losses/model/training/evaluate/run） |
| 测试 | `code/study02pq/test_pq.py`（15 passed） |
| smoke | `code/study02pq/smoke.py`（SMOKE PASS） |

## 4. 数据与划分证据

- 样本重建契约：`generate_sample(..., seed="study01_nrmc_v1")`（确定性）。
- 划分：`artifacts/pq/splits_manifest.json`（每 `(n, fold)` 的 train/val/test 行 SHA、
  行数：train+val 9600、test 2400）。
- 数据规模自检：`artifacts/pq/manifest.json` → `data_integrity`（48,000 样本、160 组合）。

## 5. 正式结果（120 fits，完成）

- 逐 fit 指标：`artifacts/pq/per_fit_metrics.csv`（120 行；0 失败/0 NaN/0 非有限/0 非法）
- P/Q 配对报告：`artifacts/pq/pairing_report.csv`（60 对，全部 all_match=True）
- 配对汇总：`artifacts/pq/paired_summary.csv`（cell + pooled）
- 分组汇总：`artifacts/pq/analysis/paired_summary_by_group.csv`
- 方向统计：`artifacts/pq/analysis/direction_by_cell.csv`、`direction_by_seed.csv`
- 失败计数：`artifacts/pq/analysis/failure_counts.json`
- 逐 fit checkpoint：`artifacts/pq/checkpoints/<fit_id>.json`（tracked）
- 逐 fit 预测：`artifacts/pq/predictions/<fit_id>.csv`（gitignore，SHA 记入 SHA256SUMS）
- 哈希清单：`artifacts/pq/SHA256SUMS`
- 总 manifest：`artifacts/pq/manifest.json`（git_head、配置 SHA、data_integrity）
- 划分 manifest：`artifacts/pq/splits_manifest.json`

关键结果（见 `04-PQ-结果报告.md`）：pooled Q 略优于 P（−2.4%，CI 不含 0）；集中于
n=10/15；n=20 反转 P 更优；cell 级 seed 间方向不一致。

## 6. 运行记录

- seed 42 正式运行：日志 `artifacts/pq/run_seed42.log`（40 fits，441 s）。
- seed 2026 + 3407：日志 `artifacts/pq/run_seeds2026_3407.log`（80 fits，872 s）。
- 三 seed 聚合：`artifacts/pq/manifest.json`（seeds=[42,2026,3407]，总运行时 1313 s）。
