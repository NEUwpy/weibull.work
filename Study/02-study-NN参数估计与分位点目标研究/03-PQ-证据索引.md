# Study/02 P–Q：证据索引（r4 primary；同分布主协议 S0 冻结候选）

> 创建日期：2026-08-05；修订：2026-08-07（S0 R1 REVISE 修复：x_p 定义、estimand/估计一致性、对齐范围、严格协议校验）
> 职责：主张 → 实验 → 证据位置与 SHA 的映射。v1（preliminary/superseded）保留于
> `artifacts/pq/`；v2/P_loggap（sensitivity）保留于 `artifacts/pq_v2/`（键 schema 已修复）。
> **主问题定位**：r4/v3 为 gamma-holdout **OOD 补充**；同分布主协议冻结候选见
> `09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json`（`PQ_PROTOCOL=iid-v1`）。

## 0iid. 同分布主协议（S0 冻结候选，2026-08-06；R1 修复 + **S0 APPROVED** 2026-08-07）

| 材料 | 位置 | 说明 |
|---|---|---|
| 冻结协议 | `09-PQ-同分布主协议冻结.md` | RQ、x_p 定义、estimand（Δ* + 有限 MC/CV 估计）、repeat-stratified 5-fold、P/Q 等价控制、统计层级、证据 schema、M1–M3 矩阵与成本、mentor 门 |
| 机器可读配置 | `configs/pq-iid-protocol-v1.json` | 同分布主协议设计/推断/证据冻结（含 x_p_definition、alignment_scope）；`PQ_PROTOCOL=iid-v1` 加载 |
| 实现选择 | `code/study02pq/config.py` | 默认仍 v3（r4）；`PQ_PROTOCOL=iid-v1` 切到 iid 配置与 `artifacts/pq_iid_main/`；**未知值 ValueError**（R1 S0-004） |
| 测试 | `code/study02pq/test_pq_iid.py` | 11 项：配置选择/隔离、**未知协议值拒绝**、规模自洽、x_p 定义、完整 300-repeat 五折平衡、iid 配对与支持域；全套 40 passed |
| pilot 证据 | `artifacts/pq_iid_pilot/` | fold-1 24 fits 描述性 pilot（`evidence.npz`、`per_unit.csv`、`summary.json`、`REPORT.md`） |
| pilot 结论 | pooled P 0.1660 / Q 0.1590，Q 相对改善 ≈ 4.2%，12/12 n×seed 单元 Q 更优 | 只支持继续验证，不作正式效应量 |

## 1. 权威输入（Study01 对齐，baseline `e37fdf70`）

| 输入 | 路径 | blob SHA | 文件 SHA256 |
|---|---|---|---|
| Dimensional-RAW 配置 | `Study/01-…/code/dim_raw_config.py` | `929ee91a…` | `33baacb6…` |
| 样本生成契约 | `python/studies/common/sample.py` | `5466e4c6…` | `3e57ddcf…` |
| 折定义（代码） | `…/run_E6b_dimensional_raw_specialist.py` | `56ee0f04…` | `9ec5b97f…` |
| 折定义（封存） | `…/specialist/split_report.csv` | `d4193e8a…` | `e1a542b2…` |
| 共享数据 manifest | `…/E5_normalized_raw/shared_data/manifest.json` | `1606b47e…` | `8b574d70…` |
| 共享数据 SHA 清单 | `…/shared_data/data_sha256sums.txt` | `b2674da3…` | `000c82a8…` |
| Study01 正式结果 manifest | `…/E6_dimensional_raw/specialist/manifest.json` | `c2141305…` | `f68c1225…` |

## 2. 冻结协议与配置（r4）

| 材料 | 位置 | 说明 |
|---|---|---|
| 研究问题与边界 | `00-PQ-研究问题与边界.md` | 科学问题不变 |
| 冻结协议 v3/r4 | `01-PQ-冻结协议.md` | direct-P + Codex 授权解码器 |
| 机器可读配置 v3 | `configs/pq-protocol-v3.json` | r4 primary |
| 环境锁 | `configs/pq-environment-v2.json` | Python 3.11.9 / torch 2.12.0+cpu 等 |

## 3. 实现与测试（r4）

| 材料 | 位置 |
|---|---|
| 实现 | `code/study02pq/`（r4：domain-explicit 解码器、direct-P、精确键 schema） |
| 测试 | `code/study02pq/test_pq.py`（22 passed：支撑合法性、frozen-grid representability、exact direct-P formula、主推断、Q 梯度、与 sealed split 对照） |
| smoke | `code/study02pq/smoke.py`（SMOKE PASS） |

## 4. 数据与划分证据（r4）

- 样本重建：`generate_sample(…, seed="study01_nrmc_v1")`；数据规模自检
  `manifest.json → data_integrity`（48,000 样本、160 组合）。
- 划分：`artifacts/pq_v3/splits_manifest.json`（每 `(n,fold)` 行数 train+val 9600 / test 2400，
  含 test 样本字节 SHA）。

## 5. 正式结果（r4 primary，120 fits）

- 逐 fit 指标：`artifacts/pq_v3/per_fit_metrics.csv`（120 行；0 失败/0 支撑违规/0 非有限/0 非法）
- P/Q 配对报告：`artifacts/pq_v3/pairing_report.csv`（60 对 all_match）
- 压缩逐样本证据：`artifacts/pq_v3/evidence/<fit_id>.npz`（tracked，精确键 dtype）
- fit metadata：`artifacts/pq_v3/fit_metadata/<fit_id>.json`（tracked，无模型 state）
- 完整精度预测：`artifacts/pq_v3/predictions/<fit_id>.csv`（gitignore）
- 分析：`artifacts/pq_v3/analysis/`（summary_v3.json、by_n_seed_descriptive.csv、
  failure_counts.json、**boundary_diagnostic.json**（R4-05 解码器边界诊断，tracked））
- 哈希清单：`artifacts/pq_v3/SHA256SUMS`（只列 tracked；文本 LF 规范；git archive 249/249 通过）
- 总 manifest：`artifacts/pq_v3/manifest.json`
- 运行日志：`artifacts/pq_v3/run_all_seeds.log`

关键结果（见 `04-PQ-结果报告.md` r4）：pooled P 优于 Q 约 **8.1%**（主推断 CI
[+0.00223, +0.01268] 不含 0），方向在 n/seed/fold 分层全部一致；n=7 显著、n=10/15/20 方向
一致；支撑违规 0。

## 6. 运行记录与版本关系

- r4 primary：`artifacts/pq_v3/`（120 fits，1522 s，0 失败）。
- v2/P_loggap（sensitivity）：`artifacts/pq_v2/`（120 fits；Q 优于 P_loggap 10.4%；键 schema
  已修复，不进入 r4 主结论）。
- v1（preliminary/superseded）：`artifacts/pq/`（120 fits；有支撑违规，不进入论文证据）。
