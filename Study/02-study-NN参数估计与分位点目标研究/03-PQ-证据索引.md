# Study/02 P–Q：证据索引（v2）

> 创建日期：2026-08-05；修订：v2（Codex R1 REVISE）
> 职责：主张 → 实验 → 证据位置与 SHA 的映射。v1（preliminary/superseded）产物保留于
> `artifacts/pq/`。

## 1. 权威输入（Study01 对齐，baseline `d0278d98`）

| 输入 | 路径 | blob SHA | 文件 SHA256 |
|---|---|---|---|
| Dimensional-RAW 配置 | `Study/01-…/code/dim_raw_config.py` | `929ee91a…` | `33baacb6…` |
| 样本生成契约 | `python/studies/common/sample.py` | `5466e4c6…` | `3e57ddcf…` |
| 折定义（代码） | `…/run_E6b_dimensional_raw_specialist.py` | `56ee0f04…` | `9ec5b97f…` |
| 折定义（封存） | `…/specialist/split_report.csv` | `d4193e8a…` | `e1a542b2…` |
| 共享数据 manifest | `…/E5_normalized_raw/shared_data/manifest.json` | `1606b47e…` | `8b574d70…` |
| 共享数据 SHA 清单 | `…/shared_data/data_sha256sums.txt` | `b2674da3…` | `000c82a8…` |
| Study01 正式结果 manifest | `…/E6_dimensional_raw/specialist/manifest.json` | `c2141305…` | `f68c1225…` |

## 2. 冻结协议与配置（v2）

| 材料 | 位置 | SHA256 |
|---|---|---|
| 研究问题与边界 | `00-PQ-研究问题与边界.md` | 见 git |
| 冻结协议 v2 | `01-PQ-冻结协议.md` | 见 git |
| 机器可读配置 v2 | `configs/pq-protocol-v2.json` | `27d57535…`（`pq-protocol-v2.sha256`） |
| 环境锁 v2 | `configs/pq-environment-v2.json` | 见 git |

## 3. 实现与测试（v2）

| 材料 | 位置 |
|---|---|
| 实现 | `code/study02pq/`（v2：location-scale 合法化、主推断、压缩证据） |
| 测试 | `code/study02pq/test_pq.py`（18 passed：支撑合法性、主推断、Q 梯度、与 sealed split 对照） |
| smoke | `code/study02pq/smoke.py`（SMOKE PASS） |

## 4. 数据与划分证据（v2）

- 样本重建：`generate_sample(…, seed="study01_nrmc_v1")`；数据规模自检
  `manifest.json → data_integrity`（48,000 样本、160 组合）。
- 划分：`artifacts/pq_v2/splits_manifest.json`（每 `(n,fold)` 行数 train+val 9600 / test 2400，
  含 test 样本字节 SHA）。

## 5. 正式结果（v2，120 fits）

- 逐 fit 指标：`artifacts/pq_v2/per_fit_metrics.csv`（120 行；0 失败/0 支撑违规/0 非有限/0 非法）
- P/Q 配对报告：`artifacts/pq_v2/pairing_report.csv`（60 对 all_match）
- 压缩逐样本证据：`artifacts/pq_v2/evidence/<fit_id>.npz`（tracked）
- fit metadata：`artifacts/pq_v2/fit_metadata/<fit_id>.json`（tracked，无模型 state）
- 完整精度预测：`artifacts/pq_v2/predictions/<fit_id>.csv`（gitignore）
- 分析：`artifacts/pq_v2/analysis/`（summary_v2.json、by_n_seed_descriptive.csv、failure_counts.json）
- 哈希清单：`artifacts/pq_v2/SHA256SUMS`（只列 tracked）
- 总 manifest：`artifacts/pq_v2/manifest.json`
- 运行日志：`artifacts/pq_v2/run_all_seeds.log`

关键结果（见 `04-PQ-结果报告.md` v2）：pooled Q 优于 P 约 **−10.4%**（主推断 CI 不含 0），
方向在 n/seed/fold 分层全部一致；n=15/20 显著、n=7/10 方向一致；支撑违规 0。

## 6. 运行记录

- v2 全量：日志 `artifacts/pq_v2/run_all_seeds.log`（120 fits，1278 s，0 失败）。
- v1（preliminary/superseded）：`artifacts/pq/`（120 fits，1313 s；有 6,348 支撑违规，
  不进入论文证据）。
