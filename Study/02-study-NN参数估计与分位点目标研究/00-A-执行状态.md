# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | formal_training_validation_implementation（Claude 执行者棒：D7 selection evidence 闭环 + 非 ranking 规则独立复核已实现并验证——点证据不可变链、trace 绑规则诊断 SHA、pre-unseal 从点证据重算规则（不信 selected）、相对 RMSE 比率、失败 fit 真实计入；待 Codex R4）|
| 当前所有者 | Claude 执行者棒（分支 `claude/study02-a-20260715`，角色=执行者；Codex 是唯一规划者与审批者）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | `a89efb8`（claude 分支：R3 攻击套件 + failure-rate 符号修正）。本棒 R3 还含 `fa4c015`（相对 RMSE bootstrap）、`507f9b8`（点证据链 + 规则诊断）、`c4c6365`（v3 trace + pre-unseal 独立重算）。详见 `coworker/reports/2026-07-18-study02-g3-selection-evidence-r3-claude.md` |
| 下一动作 | Codex R4 审批；剩余：staged A-E1 执行、D8（占位符解析/deferred-spec/前驱链）、完整临时 smoke、A-E1 formal 分阶段启动。A-E1 formal **未授权** |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
