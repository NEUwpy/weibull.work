# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | 所有 pre-formal 组件已 Codex APPROVE：staged A-E1 source-of-truth（`10d6fcf`）、point_evidence 迁出（`4d5c9cd`+`e9f455a`+`28b1d18`）、anchor 性能（`d480c13`）、alias-chain 目录校验（`cd4d04a`）、accreditation authority preflight（`cd4d04a`）。合并至 main 通过 `6c955b6e`（原分支 tip `8e9abd33`，来自 `claude/study02-a-20260715`）。**364 非 slow 测试通过**（含 CLI dispatch 测试）、`compileall` OK、`verify_frozen_hashes` OK、**349-smoke PASSED 2h13min 保持**。operational wiring：`formal-execute --module A-E1` 已接至 `run_a_e1_staged()`（含连续 failure 守卫）；A-E3/A-E2 继续使用 `run_module()`。**真实 formal A-E1 未启动、test 全程 sealed**。awaiting Codex review of wiring |
| 当前所有者 | Codex Controller（仓库 main @ `c50ad0c1`；工作树 `codex/study02-a-preflight-20260721` @ `7fff842c`）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | 2026-07-21 wiring fix（commit `7fff842c`：`formal-execute --module A-E1` 分派至 `run_a_e1_staged()`；`run_a_e1_staged()` 增加 8 次连续 scientific failure 守卫；CLI dispatch 测试；`formal_scheduler.py` 未改）。前序：REVISE 两缺口闭合（`cd4d04a`）、point_evidence 迁出（3 commits）、staged source-of-truth（`10d6fcf`）、controller-anchor perf（`d480c13`） |
| 下一动作 | Codex 复审 operational wiring（`7fff842c`）：(1) `formal-execute --module A-E1` → `run_a_e1_staged()` 分派正确。(2) A-E3/A-E2 → `run_module()` 保持。(3) 连续 failure 守卫与 `run_module()` 一致（`_MAX_CONSECUTIVE_FAILURES = 8`）。(4) `run-id/artifact-root/cache-root/owner-id/max-fits` 原样传递。(5) 364 非 slow 测试通过。(6) 冻结 matrix/plan/schema/selection rule/scheduler/scientific metrics/test 状态未改。(7) 状态文档已同步。**A-E1 formal 仍未经授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
