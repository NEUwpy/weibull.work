# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | 所有 pre-formal 组件已 Codex APPROVE。首次真实 formal A-E1 启动（`A-E1-formal-20260721-154250`）后因两个确定性缺口 blocked：(1) Windows cp936 下 `_assert_scoped_code_clean` git subprocess 解码不确定；(2) `_validate_dataset_semantics` 的 2e-5 容差对 float32 target/anchor 独立量化过严（G3-fit-0039 row 45258 差异 0.00122）。旧 run 永久 blocked/aborted（39 succeeded / 310 pending / 0 failed / test_access_count=0），39 个 checkpoint 不迁移。修复已提交（`d6411a46`）：显式 UTF-8 + 容差 1e-4 + production 100k 回归 + attack 测试。**370 非 slow 测试通过**、`compileall` OK、`verify_frozen_hashes` OK、`git diff --check` clean。awaiting Codex review of fix |
| 当前所有者 | Codex Controller（分支 `codex/study02-a-preflight-20260721`）|
| 活动实验 run ID | none（旧 run `A-E1-formal-20260721-154250` 已永久 blocked/aborted；新 formal 尚未授权；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | 2026-07-22 numerical/encoding fix（commit `d6411a46`：`_assert_scoped_code_clean` 显式 UTF-8；`_validate_dataset_semantics` 容差 2e-5→1e-4；production 100k 回归 + attack 测试。前序：R3 REVISE `6aa6f7bb`） |
| 下一动作 | Codex 复审 numerical/encoding fix（`d6411a46`）。旧 run `A-E1-formal-20260721-154250`（39 succeeded / 310 pending / 0 failed / test_access_count=0）永久保留为 blocked/aborted 证据；39 个 checkpoint 不迁移至新 authority。修复批准后需建立新 run-id 重新启动 A-E1 formal。**A-E1 formal 仍未经授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
