# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | staged_source_of_truth_fixed_awaiting_review：A-E1 staged 执行按权威 matrix 正确触发——`fit_id→matrix row` 唯一映射 + plan↔matrix 校验（精确 fit_id 对应 + per-row `matrix_row_sha256` 绑定，缺失/重复/额外/错配 fail-closed）、`_a_e1_fit_stage` 从 matrix 行读 `fit_kind`（不写回 plan）、staged 状态从 receipt 恢复（重启重验 trace/receipt/ledger 复用 top4/winner，不重训/不覆盖/不注入）。**349-fit full-chain smoke 跑通：349/349 succeeded、无 placeholder 进入 runner、4 个 staged receipt 在正确边界（stage1 先于 stage2）、final selection/receipt/ledger、F2/V + final_aliases、完整 8-record staged ledger、`test_access_count=0`（state + 全部 699 events 直读确认）**。controller-anchor 校验已修复（消除嵌套 prefix replay 及重复 I/O；N=32/64/96 实测 0.95/1.60/2.24s，近线性，残余 ~O(N)/rebuild）。**真实 formal A-E1 未启动、test 全程 sealed**。待 Codex 统一 preflight 审核 |
| 当前所有者 | Codex Controller（分支 `claude/study02-a-20260715`；relay 模式：Claude 连续实现 → Codex 统一审核）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | staged source-of-truth 修复（commit `10d6fcf`：matrix authority + plan↔matrix 校验 + matrix 读 fit_kind + receipt 恢复 + 幂等 final；25 个新单测；350 非 slow 测试在 clean tip 通过）。前序 anchor 性能修复 `d480c13`、staged/accreditation `0beda20`。本棒推送后 tip 为 docs 同步 commit（见 git log） |
| 下一动作 | Codex 统一 preflight 审核：(1) **staged source-of-truth（`10d6fcf`, FIXED awaiting review）**——已实测 349-fit full chain 按权威 matrix 正确触发。(2) **point_evidence vs scheduler output-dir 校验 — CRITICAL OPEN BLOCKER（新暴露，交 Codex）**：`build_module_selection` 把 `outputs/{fit_id}/point_evidence.json` 共址写入 144 个 selection-candidate fit（search_stage1/2），而 scheduler 的 `_validate_success_files` 要求该目录只含 3 个冻结 `expected_outputs`，导致 selection 后任何 `_rebuild_authority`/`status_run` 在这些 fit 上 raise（smoke 末尾 status_run 即此；formal 路径 `resolve_a_e1_staged_selection` 在 `score_fit=None` 下也会命中）。**与 source-of-truth 正交、pre-existing**（原 `run_a_e1_staged` 同序，只是被 source-of-truth bug 提前阻断未达）。修复跨切面：要么把 point_evidence 移出 fit output 目录（波及 `run_study02a.py`/pre-unseal bundle/provenance 断言），要么让 scheduler 放行该共址 selection 产物（scheduler 输出合同变更）。(3) accreditation 合同、v1→v3。**A-E1 formal 未授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
