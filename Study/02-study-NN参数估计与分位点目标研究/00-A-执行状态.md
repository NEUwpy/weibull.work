# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | staged_implementation_awaiting_unified_review：A-E1 staged 执行状态机已实现（per-route stage1/stage2 receipt，解除 build_module_selection 死锁）+ pre-unseal accreditation CLI（build/authorize/resolve-deferred）+ @slow 生产 smoke；**controller-anchor 校验已修复为单次 replay（O(N²)→O(N)，N=96 实测 90.3s→2.24s）**；**真实 formal A-E1 未启动、test 全程 sealed**。待 Codex 统一 preflight 审核 |
| 当前所有者 | Codex Controller（分支 `claude/study02-a-20260715`；relay 模式：Claude 连续实现 → Codex 统一审核）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | anchor 性能修复 tip `d480c13`（controller-anchor 单次 replay；10 个等价/fail-closed 测试；N=32/64/96 基准 40× 提速）。前序 staged/accreditation tip `0beda20`。本地 HEAD=`d480c13`，待网络恢复后推送 |
| 下一动作 | Codex 统一 preflight 审核：(1) **scheduler anchor 性能修复（d480c13, awaiting review）**；(2) **staged source-of-truth mismatch — CRITICAL OPEN BLOCKER**：`_a_e1_fit_stage` 从 plan.jsonl 读不存在的 `fit_kind`（设计上在冻结 matrix），全量运行中所有 fit 被判 `concrete`，**staged selection（stage1→top4→stage2→winner）从未触发**；349-record smoke 因此 **FAIL（非 partial pass）**——placeholder rows 被误当 concrete 执行，349 非有效 fit 数、~2h 非有效 staged 耗时外推依据；`test_access_count=0`（test 全程 sealed）。修复方向：按 `fit_id` 对齐冻结 matrix 权威行读阶段类型（缺失/重复/不一致 fail-closed），**不把 `fit_kind` 复制回 plan**。(3) accreditation 合同、v1→v3。**A-E1 formal 未授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
