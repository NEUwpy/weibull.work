# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与 duplex mailbox 续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A` 与长任务计划。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | 前置研究 A **精简复现路线 P0–P6 已完成并终验通过**：19/19 问有证据覆盖（17 answered + A4/A8 partial） |
| 已批准基线证据 | **A-E1 r5**（`A-E1-formal-r5-20260727-222417`，tip `d2a056f`，349 fits，V 路线，staged_resolution_ledger 8/8，APPROVE/frozen）与 **A-E3 r2**（`A-E3-formal-r2-20260730-111949`，tip `996434b2`，266 fits，`huber + m12 + joint + fixed`，staged_resolution_ledger 10/10，经 Claude 独立审核 + Codex APPROVE）。旧 formal 引擎只读冻结，不再扩建 scheduler/lease/authority/unseal/consume/hash-chain/capsule/攻击防护；历史 blocked/aborted run（A-E1 r1–r4、A-E3 r1）仅作只读历史，不恢复、不拼接、不冒充证据 |
| 当前所有者 | Codex Controller（Claude coworker 自 14:12 后无响应；Codex 按持久 R3 审查单接管 P2 执行，mailbox 任务保留供恢复） |
| 活动实验 run ID | E1–E4 均 terminal；无活动进程；formal test 全程 sealed |
| 正式成果 | `06-A-前置研究报告.md`；逐题证据附录为 `05-A-证据索引.md` |
| 最近提交 | P6 综合候选 tip `a53161d0`：已在独立 detached clean worktree 通过 E1–E4 全部 22 项预检，9/9 tracked 工件哈希一致，worktree 洁净 |
| 下一动作 | 前置研究无剩余动作；后续是否进入 Study/02 分位点主研究由用户另行决定 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。长任务期间的阶段协调以 duplex mailbox 为准。

## 单写者与续接规则

1. 协调经 duplex mailbox（任务 `study02-lean-prestudy-completion-20260731`）；开始写文件或启动实验前，先读本文件、长任务计划、mailbox `STATUS.md`/`TRANSCRIPT.md` 和 `git status`/`git log`。
2. 同一时刻只能有一个 executor 写入。Codex 是 reviewer，负责阶段审查与最终 APPROVE；executor 不得自我批准或越阶段。
3. 窗口中断后按「长任务计划 → 本文件 → mailbox `STATUS.md`/`TRANSCRIPT.md` → `git status`/`git log`」恢复，从第一个未完成阶段继续。
4. 每个阶段形成 coherent commit 后，原子更新本文件的当前阶段、最近提交与下一动作；提交 SHA 记录于此。
5. 不允许两个执行者在同一工作树编辑相同文件；并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证（轻量文档/配置/hash 检查 + 相关测试）
  -> coherent commit（精确暂存本阶段文件）
  -> mailbox 阶段报告（exact tip、改动文件、命令与结果、剩余 dirty paths、阻塞、下一阶段建议）
  -> Codex 审查至 APPROVE / REVISE
  -> 更新本文件与长任务计划清单
```

合并 main、发布、部署、推送远端或更新 `08-更新日志.md` 不在精简路线的阶段闸门内；如需，由用户/Codex 在 P6 收口后单独处理。
