# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | A-E1 formal r5 已 APPROVE/frozen 为 A-E3 predecessor；A-E3 formal r1 **永久 blocked/aborted**（evidence schema crash），修复分支 `codex/study02-a-e3-evidence-schema-fix-20260730`（tip `723e17ce`，**awaiting Codex review**） |
| 阶段状态 | A-E1 formal r5（`A-E1-formal-r5-20260727-222417`，tip `d2a056f`）349 fits 全 terminal、staged_resolution_ledger 8/8 完整、baseline winner=V，已 frozen 作为 A-E3 predecessor。A-E3 formal r1（`A-E3-formal-r1-20260729-214640`）在 G3-fit-0483（第一个 independent output_form fit，position 134）crash：`execute_claimed_fit` 向 evidence.json 写入 `output_form` extra field，scheduler `_EVIDENCE_FIELDS` frozen exact-schema 拒绝（`ValueError: fit evidence output must match its exact canonical schema`）。134 succeeded / 131 pending / 1 dead claimed / 0 failed；authority `e11cb2a2…`（无 drift）；tac=0（test sealed）。**r1 永久 blocked/aborted**：不恢复、不迁移 134 checkpoint、不拼接。修复分支 `codex/study02-a-e3-evidence-schema-fix-20260730`（from `cc2a9708`，tip `723e17ce`）：删除 `execute_claimed_fit` 的 `evidence["output_form"]` 写入；删除无消费者的 `_PreparedFit.output_form_evidence` 字段；修正 `output_form_contract.py` 失真说明（metadata 不写入 evidence、非 authoritative audit record、模型从 authority-bound plan/matrix route + 冻结 contract + input_dim 确定性重建）；contract/factory/capacity/SHA 不变。回归测试 2/2 通过（slow + fast）。**状态：awaiting Codex review**；**A-E3 formal r2 尚未授权**。 |
| 当前所有者 | none |
| 活动实验 run ID | none（r1 永久 `blocked/aborted`，现场 run dir 保留；r5 A-E1 已完成并 frozen 为 predecessor，不得恢复、删除、复用或拼接） |
| 最近批准阶段 | A-E1 formal r5 完成（staged ledger 8/8，V 获胜）；A-E3 orchestration R7 code APPROVE（formal r1 已 blocked/aborted） |
| 最近代码 checkpoint | A-E3 orchestration R7（tip `9ef028ce`：output-form contract v2 + IndependentContainer + select_independent_capacity；n_strategy fixed/shared 10-record staged ledger；versioned cross-commit authority schema v1/v2 + content-addressed historical verifier + legacy capsule；G3/pre-unseal/A-E2 integration）。**formal r1 在此 tip 下 crash**（evidence schema）。修复在 `codex/study02-a-e3-evidence-schema-fix-20260730`（tip `723e17ce`，**awaiting Codex review**）。 |
| 下一动作 | Codex review 修复分支 `codex/study02-a-e3-evidence-schema-fix-20260730`（tip `723e17ce`）→ 用户决定是否 commit + push（新 SHA）→ 在用户显式授权 A-E3 formal r2 后，从零执行（以 r5 A-E1 为 predecessor）。**A-E3 formal r2 目前尚未授权**。不恢复 r1、不迁移 134 checkpoint；不进入 A-E2、approval、test unseal、test consume、9d 或 G4。 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
