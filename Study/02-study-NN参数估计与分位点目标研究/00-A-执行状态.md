# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | point_evidence_relocated_awaiting_review + REVISE 两缺口已闭合：Codex 复审 APPROVE 了 point_evidence 迁出/冻结 matrix 派生/n 恢复/failed-fit/349-smoke PASSED；REVISE 要求的两 fail-closed 缺口已补——(1) `_validate_selection_point_evidence_dir` 复用 scheduler `_reject_alias`（目录+父级 `run_dir/selection/point_evidence`、`run_dir/selection`、`run_dir` 拒 symlink/junction/reparse/hardlink）+ `_contained`（解析后位于 run_dir 内）+ 逐条 `os.scandir`/`is_file(follow_symlinks=False)`/`_reject_alias(entry, require_file=True)`；(2) `accredit_build` 首步执行**完整 `_rebuild_authority`** 权威 preflight，以其 replay-verified manifest/plan/fit_states 为唯一事实源（不再直读 `manifest.json`/`plan.jsonl`/receipt JSON），逐 selection fit 三源一致（scheduler fit_state == terminal receipt state == failed iff point-evidence.failed），preflight/一致性失败前**不写**任何 fit_status/ceiling/leakage/pre_unseal_bundle。`formal_scheduler.py` 输出合同未改；冻结 matrix/plan/schema/selection rule/科学指标/staged 执行未改。**362 非 slow 测试通过**（含 symlink/junction 目录 alias 拒绝 + 篡改 receipt/plan/event 的 preflight attack 测试，篡改时无诊断产物；focused post-selection authority 测试绿；**349-smoke PASSED 2h13min 保持**）。**真实 formal A-E1 未启动、test 全程 sealed**。待 Codex 复审 REVISE |
| 当前所有者 | Codex Controller（分支 `claude/study02-a-20260715`；relay 模式：Claude 连续实现 → Codex 统一审核）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | REVISE 两缺口闭合（commit `cd4d04a`：`_validate_selection_point_evidence_dir` 复用 `_reject_alias`+`_contained` 目录 alias-chain；`accredit_build` 首步 `_rebuild_authority` 权威 preflight + 三源一致 + 失败无诊断产物；alias/junction 目录拒绝测试 + receipt/plan/event 篡改 attack 测试；`formal_scheduler.py` 未改）。前序 point_evidence 迁出 `4d5c9cd`+`e9f455a`+`28b1d18`（3 commits）、staged source-of-truth `10d6fcf`、controller-anchor perf `d480c13` |
| 下一动作 | Codex 复审 REVISE（`cd4d04a`）：(1) **目录 alias-chain 缺口 — 已闭合**（`_reject_alias` 目录+父级 + `_contained` run_dir 内）+ symlink/junction 拒绝测试。(2) **accreditation authority preflight 缺口 — 已闭合**（`_rebuild_authority` 首步 + replay-verified manifest/plan/fit_states + 三源一致 + 失败无诊断产物；receipt/plan/event 篡改 attack 测试，篡改时无诊断生成）。(3) point_evidence 迁出（`4d5c9cd`）保持 APPROVED。(4) accreditation 合同、v1→v3。**362 非 slow 测试通过；349-smoke PASSED 保持；A-E1 formal 未授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
