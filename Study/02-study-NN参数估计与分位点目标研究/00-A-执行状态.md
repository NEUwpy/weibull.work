# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | point_evidence_relocated_awaiting_review：CRITICAL point_evidence-vs-scheduler blocker 已按 Codex 方案 (a) 修复——`build_module_selection` 把每个 fit 的 point evidence 写到 `run_dir/selection/point_evidence/{fit_id}.json`（selection 自有目录），**不再共址 `outputs/{fit_id}/`**；scheduler 权威 fit 输出目录保持严格等于冻结 `expected_outputs`，`formal_scheduler.py` 输出目录校验**未改动**（Codex BLOCK 任何 allowed-extra/snapshot 排除/放宽）。candidate 由冻结 matrix 确定，但 point evidence 属 **selection 后生命周期**，不能作为训练成功前必须存在的输出，故迁出而非放行。`accredit_build` 从冻结 matrix `build_decision_specs` 派生预期 selection fit 集（不扫 outputs 目录）、读迁出 point evidence、从 `n_mode`/`fixed_n` 恢复 n（plan 无 `n`）、失败 selection fit 不静默 skip（按 scheduler terminal receipt `failure_code` + point-evidence failure record + evidence.json 缺失三源一致生成失败 fit_status，否则 fail-closed）。**selection 后真实 `_rebuild_authority`/`status_run` 通过、`test_access_count=0`**（focused slow test 17s 实测绿；356 非 slow 测试通过；349-smoke 后台跑真实 final check）。staged source-of-truth + controller-anchor perf 修复保持。**真实 formal A-E1 未启动、test 全程 sealed**。待 Codex 复审 relocation |
| 当前所有者 | Codex Controller（分支 `claude/study02-a-20260715`；relay 模式：Claude 连续实现 → Codex 统一审核）|
| 活动实验 run ID | none（未启动 formal run；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | point_evidence 迁出修复（commit `4d5c9cd`：option a，`selection/point_evidence/{fit_id}.json`；`accredit_build` 权威派生 + n 修复 + 失败 fit；新测试含 focused post-selection authority-rebuild + 349-smoke 恢复**真实 status_run/_rebuild_authority final check**，不再直读 `scheduler_state.json` workaround；`formal_scheduler.py` 输出校验未改）。前序 staged source-of-truth `10d6fcf`、controller-anchor perf `d480c13` |
| 下一动作 | Codex 复审 point_evidence 迁出（`4d5c9cd`）：(1) **point_evidence vs scheduler output-dir blocker — RESOLVED（option a 迁出，awaiting re-review）**——selection 后真实 `status_run`/`_rebuild_authority` 通过（focused slow test 实测 17s 绿；356 非 slow 测试通过；349-fit smoke 后台验证中）。(2) staged source-of-truth（`10d6fcf`）保持 FIXED。(3) accreditation 合同、v1→v3。**A-E1 formal 未授权**；不进入 A-E3/A-E2、accredit-authorize、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
