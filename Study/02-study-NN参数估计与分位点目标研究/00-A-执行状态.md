# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | A-E1 formal r5 已完成并 frozen 为 A-E3 predecessor；A-E3 orchestration R3 修复完成（output-form 合同 + n_strategy + versioned authority），尚未 formal 授权 |
| 阶段状态 | A-E1 formal r5（`A-E1-formal-r5-20260727-222417`，tip `d2a056f`）349 fits 全 terminal、staged_resolution_ledger 8/8 完整、baseline winner=V，已 frozen 作为 A-E3 predecessor。A-E3 orchestration R3（A=`cdb689f`/B=`47100dd`/C=`1667fd1`）修复 r1 三 blocker：output-form 合同（joint=Sequential vs independent=IndependentContainer，capacity-selected，SHA-bound）、n_strategy 决策（10-record staged ledger，record 9 fixed/shared winner）、versioned cross-commit authority（v1 r5 可重放 / v2 per-module authority triple）。sealed smoke（G.16）端到端验证 joint + independent + fixed + shared 全经过真实 model factory + checkpoint-forward + selection。non-slow 603/0 全过。**A-E3 formal 尚未授权**：需 Codex R3 代码复审通过后授权。test 始终 sealed、`test_access_count=0`，未发生 approval、unseal 或 test 动作，19 个问题仍无 formal 结论。 |
| 当前所有者 | none |
| 活动实验 run ID | none（r1/r2/r3 永久 `blocked/aborted`；r5 A-E1 已完成并 frozen 为 predecessor，不得恢复、删除、复用或拼接） |
| 最近批准阶段 | A-E1 formal r5 完成（staged ledger 8/8，V 获胜）；A-E3 orchestration R3 修复完成但 formal 未授权 |
| 最近代码 checkpoint | A-E3 orchestration R3（A：output_form_contract + IndependentContainer + select_independent_capacity；B：n_strategy 10-record staged ledger + fixed/shared cohort；C：versioned predecessor v1/v2 + content-addressed historical verifier + G3 per-module authority） |
| 下一动作 | Codex R3 代码复审通过后，申请 A-E3 formal training/validation 显式授权（以 r5 A-E1 为 predecessor）。授权前重新检查 HEAD/clean tree、lease/process、冻结配置哈希和仓外运行盘容量。不得进入 approval、test unseal、test consume、9d 或 G4。 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
