# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 / R12 准备收口完成；A-E1 formal r3 已停止，等待稳定分支上的全新 r4 授权 |
| 阶段状态 | R9-R12 的 staged resolution、三模块 sealed accreditation、checkpoint provenance、选择 trace/diagnostics 重建、统一 sealed state 与 fail-closed 入口已完成代码收口。代码 checkpoint 为 `1bdd9906e87b53cd0cd1ad81bcfbb8ed8197a5a1`。r3 在授权 commit `39d630507128706286b103717eb2717ccdeb4666` 上完成 3 个 fit 后，因无关 Study01 merge 将 HEAD 推进到 `db593d12` 而触发 authority drift；run 已永久 `blocked/aborted`，3 个 fit 不得复用。test 始终 sealed、`test_access_count=0`，未发生 approval、unseal 或 test 动作，19 个问题仍无 formal 结论。 |
| 当前所有者 | none |
| 活动实验 run ID | none（r1 `A-E1-formal-20260721-154250`、r2 `A-E1-formal-r2-20260722-233648`、r3 `A-E1-formal-r3-20260726` 均永久 `blocked/aborted`；不得恢复、删除、复用或拼接） |
| 最近批准阶段 | G3 pilot v7；R12 准备收口使代码达到“可申请全新 A-E1 formal training/validation 授权”的状态，但不等于 formal 授权、实验完成或 test 启封 |
| 最近代码 checkpoint | `1bdd9906e87b53cd0cd1ad81bcfbb8ed8197a5a1`（R12 sealed accreditation gaps closeout） |
| 下一动作 | 先在最终 clean、pushed `main` 上创建同目录稳定 formal 分支并固定完整 commit；再依据 `coworker/plans/2026-07-26-study02-a-e1-formal-launch-contract.md` 申请全新 r4 A-E1 formal training/validation 显式授权。授权前重新检查 HEAD/clean tree、lease/process、冻结配置哈希和仓外运行盘容量。不得进入 A-E3/A-E2、approval、test unseal、test consume、9d 或 G4。 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
