# Study/02 前置研究 A：执行状态

> 创建日期：2026-07-12
> 职责：提供可被人工与定时续接任务共同读取的原子阶段状态；研究定义见 `01-A`/`02-A`，执行计划见 `03-A`。

## 当前状态

| 字段 | 值 |
|------|----|
| 当前阶段 | G3 |
| 阶段状态 | 所有 pre-formal 组件已 Codex APPROVE。两次真实 formal A-E1 均 blocked/aborted：(1) r1（39 succeeded）因 encoding+tolerance；(2) r2（141 succeeded）因 Dataset/Batch 错配 + 非有限评分溢出。三个修复已提交：encoding/tolerance `d6411a46`、Dataset/Batch `12c16b5`、R6 非有限评分 `c7333b69`。r2 完整性审计通过（283 events chain valid、141x3 outputs SHA verified）；141 个 checkpoint 只读评分全部 finite（42833 条降级为 penalty=10）。**G3 test 控制面 R8 已修正**：`live_claim` 字段、controller-anchor 验证、`_validate_selection_evidence` 正确签名、plan distribution、scheduler terminal receipt SHA、fail-closed 诊断工件、落盘+SHA 绑定、三 run 一致性。`formal-consume-test` CLI 保持 fail-closed。26 测试通过。`compileall` OK、`verify_frozen_hashes` OK、`git diff --check` clean。awaiting Codex R8 review |
| 当前所有者 | Codex Controller（分支 `codex/study02-a-preflight-20260721`）|
| 活动实验 run ID | none（旧 run r1 `A-E1-formal-20260721-154250` 和 r2 `A-E1-formal-r2-20260722-233648` 均永久 blocked/aborted；新 formal 尚未授权；test 继续 sealed）|
| 最近批准阶段 | G3 pilot v7（oracle APPROVE，仅允许 formal training/validation；test 继续 sealed）|
| 最近推送提交 | 2026-07-22 R6 non-finite evaluation fix（commit `c7333b69`：非有限 error/L_param 降级为 penalty=10；`_require_finite_evaluation` fail-closed gate；10 个 R6 回归测试。前序：Dataset/Batch `12c16b5`、R4 `3beb9f11`、encoding/tolerance `d6411a46`） |
| 下一动作 | Codex R8 复审 G3 控制面修正（live_claim + controller + _validate_selection_evidence + distribution + receipt SHA + 落盘绑定 + 一致性）。报告：`coworker/reports/2026-07-25-study02-g3-test-control-r8-opencode.md`。**A-E1 formal 仍未经授权**；不进入 A-E3/A-E2、test unseal、9d、G4 |

当前 heartbeat automation id 为 `study02-a`，其续接提示使用 Git 规范路径 `Study/02-study-NN参数估计与分位点目标研究/...`，不得写成仅在大小写不敏感文件系统可用的小写路径。

## 单写者与续接规则

1. 开始写文件或启动实验前，先检查本文件、`.slim/deepwork/study02-a.lease.json` 和活动进程。
2. 同一时刻只能有一个写入者。已有有效租约或活动实验时，heartbeat 只能读取、监测和报告，不得重复启动或并发修改。
3. 租约必须记录 owner、stage、run ID、PID（如有）、started_at、heartbeat_at 和 planned outputs；续接前同时核对 PID/产物更新时间，不能只按墙钟判定过期。
4. 阶段切换和提交推送后，原子更新本文件；记录批准审查、提交 SHA 和远端 SHA。
5. 不允许两个执行者在同一工作树编辑相同文件。并行工作只能使用独立 worktree 或明确不相交的文件范围。

## 阶段关闭顺序

```text
验证
  -> 阶段报告
  -> oracle 审查
  -> 修订至 APPROVE
  -> 更新 08-更新日志.md
  -> 精确暂存本阶段文件
  -> 提交
  -> 推送
  -> 记录本地与远端 SHA
```
