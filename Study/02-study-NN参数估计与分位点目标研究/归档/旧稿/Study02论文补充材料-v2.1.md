# Study02 P–Q–QCP 补充材料：实验细节与机制分析

> 与《Study02论文初稿-v2.1》配套，2026-09-04 同步。正文集中于目标对齐、参数补偿及 QCP 修复；本文件保留训练细节、辅助分析与历史预算结果。

## S1. 实验设计

| 项目 | 冻结定义 |
|---|---|
| 参数空间 | β={1.5,2,…,5}；η=1000；γ/η={.10,.25,.50,.75,1}；n={7,10,15,20} |
| 样本 | 每参数—n 组合 300 个独立 repeats；`repeat_id mod 5` 分折 |
| 路线 | `P_equal` / `Q_param` / `QCP`，相同三输出网络与合法解码 |
| 科学干预 | P/Q 改变主损失；QCP 保持 Q 为主目标并增加 P 可行域 |
| 模型单元 | 10 seed×4 n×5 fold=200 个三路线配对单元 |
| 训练目标与主评价 | 以 held-out \(x_{0.95}\) pooled RMSRE 判断目标对齐 |
| 跨寿命点评价 | 对同一组三参数预测派生 \(x_{0.90}\)、\(x_{0.95}\)、\(x_{0.99}\)，判断单点目标的曲线代价与 QCP 恢复 |
| 探索性消融 | 3 seed×2 fold×4 n=24 个 P/M95/Q 匹配单元；不与主单元混池、不作 CI |

`P_equal` 为三个归一化参数平方误差的等系数和；归一化消除量纲差异，但等系数仍是人为度量约定，不代表三个参数的实际目标敏感度相同。`Q_param` 直接优化 \(x_{0.95}\) 相对平方误差，其三输出只作内部参数化。Q 通过链式法则诱导随参数点和当前预测变化的动态参数度量，并保留参数交叉项。QCP 求解 \(\min L_Q\) subject to \(L_P\le\tau_{\mathrm{cell}}\)：Q 定义最终什么重要，P 只定义内部参数允许偏离多远。

## S2. 训练与配对

- MLP：`n-256-128-64-3`，ReLU，float64；合法解码保证
  \(\hat\beta>0\)、\(\hat\eta>0\)、\(0<\hat\gamma<\min(X)\)。
- Adam，lr=1e-3，weight decay=1e-4，batch=256；当前三路线横向分析统一 max epochs=600、patience=60。
- 优化器设置共同冻结，不做路线专属调参；损失数值尺度、QCP 约束与各路线 checkpoint 规则属于被比较的运行合同。
- seeds：42、2026、3407、17、73、314、2718、4099、8128、12011。
- P/Q/QCP 在每个模型单元内共享训练/验证/测试行、scaler、初始化、首 epoch batch、网络与最大预算。
- 当前分析每条路线 200 fits、480,000 条 held-out 预测；0 未完成、0 NaN、0 非有限、0 支持域违规。

48,000 组独立寿命样本来自 160 个固定真值单元各 300 次抽样；每组样本在五折中恰好作为一次测试数据，并被 10 个训练种子对应的模型分别预测。因此，每路线的 480,000 条预测包含共享测试样本的算法重复。每个 \((n,\mathrm{fold},\mathrm{seed})\) 分别训练网络，200 个模型单元不等于 160 个固定真值单元。

### S2.1 实验先后顺序与阈值来源

| 阶段 | 设置与用途 | 测试信息的使用 |
|---|---|---|
| 早期 P/Q 比较 | 300 epochs / patience 20；匹配 P 模型的最佳验证参数损失作为后续约束参考 | 完成封存评价后用于路线开发与机制分析 |
| QCP 约束与资源选择 | 8 个验证单元、48 个候选 fits；另 8 个资源 fits；确定 \(c=1.5,\rho=0.1\) 与 600/60 | 两个选择阶段测试指标访问均为 0 |
| QCP 正式评价 | 200 个 fits，\(\tau_j=1.5L_{P,\mathrm{ref},j}\) | 固定设置后评价；参考量仍来自早期 P，而非当前 P600 |
| 当前共同预算比较 | P/Q 各 200 个 600/60 fits，复用 QCP 的 200 个 600/60 fits | 已知早期测试结果后的预算敏感性分析 |
| 当前机制与分布分析 | 读取已有预测，派生跨寿命点、区域及误差形态 | 事后分析，不产生新的训练或首次封存确认 |

QCP checkpoint 的可行性指验证集平均参数损失满足阈值，不是每条预测的误差保证。正文所报 87.2 s 为 QCP 单 fit 中位训练耗时，不包含早期 P 参考训练及约束筛选成本。

## S3. 主推断

\[
I=(\mathrm{rRMSE}_P-\mathrm{rRMSE}_Q)/\mathrm{rRMSE}_P.
\]

估计对象是冻结网格内各 n 和设计单元等权时，对新 held-out repeat 与训练随机性而言，
两条冻结算法的 rRMSE 及其相对差。有限估计先对 200 个 `(n,fold,seed)` 配对单元的
MSE 等权平均，再开方计算 rRMSE 与 I。按 n 分层重采样 fold，同时全局重采样 seed，
路线保持配对；B=200,000。因 fold 训练集重叠且只有 10 seeds，95% CI 是设计级经验
bootstrap 近似区间。逐 n 区间未做多重校正，仅作描述性分层。当前 QCP−Q 与 QCP−P 对比沿用相同配对聚合和 crossed bootstrap 规则。

## S4. 当前三路线结果

下表 RMSRE 指 $x_{0.95}$ 的均方根相对误差，即逐条相对误差平方后的平均值再开根号；数值越小越好，不是准确率。代码历史字段 `rRMSE` 与 RMSRE 指同一公式。

| 路线 | pooled RMSRE | 平均参数损失 | 参数补偿指数 |
|---|---:|---:|---:|
| P | 16.432% | 0.05385 | 0.33226 |
| Q | 16.092% | 71.71416 | 0.91455 |
| QCP | **15.841%** | 0.05522 | 0.34532 |

| 对比 | rRMSE 相对改善 | 95% CI | 有利单元 | 有利 seed |
|---|---:|---:|---:|---:|
| Q vs P | 2.069% | [1.280%,2.811%] | 172/200 | 10/10 |
| QCP vs Q | 1.562% | [1.241%,1.898%] | 196/200 | 10/10 |
| QCP vs P | 3.599% | [3.027%,4.180%] | 200/200 | 10/10 |

QCP 的 200/200 个所选 checkpoint 均满足验证 P 约束。该同预算汇总从冻结证据派生，因测试已经打开，证据等级为 post-test sensitivity，而不是新的首次封存确认。

![图S1：共同预算结果与训练 epoch](figures/supplementary/supp_fig1_common_budget_results.png)

*图S1　A 为目标寿命点的 pooled RMSRE，B 为配对相对效应及经验 bootstrap 区间，C 为参数补偿指数，D 为最佳 epoch 分布。旧 300-epoch 上限仅用于对照训练阶段，不是当前三路线预算。*

### S4.1 跨寿命点三路线总比较

跨寿命点分析不新增训练，而是把 200 个配对模型单元保存的三参数预测代入同一 Weibull 寿命公式。正改善表示表头左侧路线的 pooled RMSRE 更低。

| 寿命点 | P RMSRE | Q RMSRE | QCP RMSRE | Q 相对 P | QCP 相对 Q | QCP 相对 P | QCP-vs-P 有利真值单元 |
|---|---:|---:|---:|---:|---:|---:|---:|
| $x_{0.90}$ | 13.657% | 18.630% | **13.336%** | −36.411% | **28.417%** | **2.353%** | 74/160 |
| $x_{0.95}$ | 16.432% | 16.092% | **15.841%** | **2.069%** | **1.562%** | **3.599%** | 76/160 |
| $x_{0.99}$ | 21.960% | 35.820% | **20.647%** | −63.114% | **42.360%** | **5.981%** | 77/160 |

Q 只在直接训练的 $x_{0.95}$ 上优于 P，在 $x_{0.90}$ 与 $x_{0.99}$ 上明显退化；QCP 相对 Q 的三个配对区间和相对 P 的三个配对区间均排除 0。后者是 pooled 风险优势，并非逐真值单元支配；三个离散寿命点也不能替代完整分布验证。证据来自 `artifacts/qcp_cross_quantile_recovery/`，属于冻结预测上的 post-test mechanism analysis。

| 寿命点 | Q 相对 P 改善的 95% CI | QCP 相对 Q 改善的 95% CI | QCP 相对 P 改善的 95% CI |
|---|---:|---:|---:|
| $x_{0.90}$ | −38.508%–−34.321% | 27.501%–29.282% | 1.809%–2.904% |
| $x_{0.95}$ | 1.280%–2.811% | 1.241%–1.898% | 3.027%–4.180% |
| $x_{0.99}$ | −65.268%–−61.158% | 41.876%–42.855% | 5.277%–6.673% |

正文表2的区域效应中位数直接由跨寿命点分析的 truth_cell_effects.csv 计算：每行取 \(1-\mathrm{RMSRE}_{QCP}/\mathrm{RMSRE}_P\)，再按寿命点取 160 行的中位数。该描述性重汇总没有改变原始证据，也未新增显著性检验。因为各单元行数相同，总体 MSE 同样对真值单元等权；总体效应与中位数、方向计数的差异来自误差幅度与统计量定义，而不是某些单元拥有更多样本。

### S4.2 样本量与 P 等效样本量

对封存的 200 个模型单元按 n 聚合，三条路线 rRMSE 均随单次估计的寿命观测数增加而下降。P 的四点误差曲线为

\[
E_P(n)=0.568n^{-0.515},\qquad R^2=0.9983,
\]

指数的设计级经验 bootstrap 95% CI 为 [0.479,0.550]。定义

\[
n_{\mathrm{eff},m}(n)=n\left[E_P(n)/E_m(n)\right]^{1/b}.
\]

| n | P rRMSE | Q rRMSE | QCP rRMSE | Q 等效新增 n（95% CI） | QCP 等效新增 n（95% CI） |
|---:|---:|---:|---:|---:|---:|
| 7 | 20.74% | 20.30% | 19.92% | 0.30 [0.18,0.42] | 0.57 [0.47,0.66] |
| 10 | 17.37% | 16.91% | 16.69% | 0.54 [0.07,0.98] | 0.81 [0.46,1.18] |
| 15 | 14.28% | 14.04% | 13.84% | 0.51 [0.03,0.99] | 0.94 [0.57,1.34] |
| 20 | 12.00% | 11.86% | 11.69% | 0.46 [0.25,0.63] | 1.04 [0.87,1.23] |

这里的 n 是每个待估样本中的观测数，不是训练仿真集规模。等效样本量是条件于当前 P 误差曲线的事后解释性换算，不是实际新增观测或确认性外推；n=20 的结果包含轻度曲线外推。

![图S2：样本量规律与 P 等效观测增益](figures/supplementary/supp_fig2_sample_size_equivalence.png)

*图S2　A 为四种样本量的 RMSRE 与经验 bootstrap 95% CI；B 为条件于 P 四点误差曲线的等效新增观测数。它描述方法收益的量级，不是实际采样替代率。*

### S4.3 偏差—方差分解

冻结网格上的真实 $x_{0.95}$ 为 238.05–1552.09，相差约 6.5 倍。为避免不同真实寿命的数值变化混入标准差，对每个固定 `(n,beta,gamma/eta)` 真值单元分别计算相对误差偏差与方差，再对 160 个单元等权汇总。

在单元 \(c\) 内令 \(b_c=\operatorname{mean}(e_c)\)、\(v_c=\operatorname{mean}[(e_c-b_c)^2]\)，则偏差 RMS 分量为 \(\sqrt{\operatorname{mean}_c b_c^2}\)，单元内标准差分量为 \(\sqrt{\operatorname{mean}_c v_c}\)。后者同时包含抽样重复与训练随机性带来的波动。

| 路线 | 有方向相对偏差 | 单元偏差 RMS 分量 | 单元内标准差分量 | RMSRE |
|---|---:|---:|---:|---:|
| P | −1.513% | 7.091% | 14.824% | 16.432% |
| Q | −2.713% | 6.994% | 14.493% | 16.092% |
| QCP | −2.482% | 7.001% | 14.210% | 15.841% |

逐路线均满足 `RMSRE² = 单元偏差RMS² + 单元内标准差²`，数值残差不超过 $3.5\times10^{-18}$。有方向偏差允许不同区域抵消；单元偏差 RMS 不允许抵消。当前改善主要来自单元内标准差分量下降，不能写成系统偏差已消除。该分解为 post-test descriptive analysis。

## S5. 敏感度与误差补偿

### S5.1 参数贡献的精确对称分解

对 \(R=0.95\)，令 \(a=-\ln R\)、\(t_0=a^{1/\beta}\)、\(t_1=a^{1/\hat\beta}\)，定义

\[
c_\gamma=\frac{\hat\gamma-\gamma}{x_R},\qquad
c_\eta=\frac{(\hat\eta-\eta)(t_0+t_1)}{2x_R},\qquad
c_\beta=\frac{(\eta+\hat\eta)(t_1-t_0)}{2x_R}.
\]

逐行精确满足 \(e=c_\beta+c_\eta+c_\gamma\)。正文的 \(C\) 为
\(1-|e|/(|c_\beta|+|c_\eta|+|c_\gamma|)\) 的逐行均值；三项均为零时取零。该对称分解与分析代码一致，以对称形式处理形状与尺度的乘积项。补偿指数是该分解下的描述性指标，不是参数可识别性的统计检验。

以 P 为参照，QCP 消除 Q 超额补偿的比例为
\((C_Q-C_{QCP})/(C_Q-C_P)=97.8\%\)。它描述补偿程度的回落，不等于误差降低 97.8%。

### S5.2 局部几何与有限误差

令 \(u=((\hat\beta-\beta)/\beta,(\hat\eta-\eta)/\eta,(\hat\gamma-\gamma)/\eta)\)、
\(e=(\hat x_R-x_R)/x_R\)。在与 P 相同的坐标中，

\[
L_P=\|u\|^2,\quad \nabla_uL_P=2u,\quad H_P=2I,
\qquad
L_Q=e(u)^2,\quad \nabla_uL_Q=2e(u)g(u),
\]

其中 \(g(u)=\nabla_u e(u)\)。真值处记 \(s_0=g(0)\)，则
\(H_Q(0)=2s_0s_0^T\)。冻结 40 点上，\(\|s_0\|=0.766\text{--}4.393\)（5.74 倍），敏感度
方向最大相差 20.3°。所以 `P_equal` 是固定欧氏几何，Q 则在当前预测位置传播目标误差与目标敏感度。

真值点矩阵代理为

\[
L_{M95}=(s_0^Tu)^2.
\]

它逐真值点变化，但对当前预测位置静态，且因 \(s_0s_0^T\) 秩一而有二维零空间。若
\(v\ne0\) 且 \(s_0^Tv=0\)，则 \(L_{M95}(\lambda v)=0\)；有限扰动下真实目标仍可因高阶项
变化。路径平均敏感度给出精确恒等式

\[
\bar s(u)=\int_0^1g(tu)dt,\qquad e(u)=\bar s(u)^Tu,\qquad
L_Q=u^T[\bar s(u)\bar s(u)^T]u.
\]

只有保留 \(\bar s(u)\) 的导数时，这一动态矩阵写法才与 Q 的前向和梯度恒等；`detach` 后是另一
surrogate。

### S5.3 静态敏感度代理消融

探索性 M95 消融沿用早期 300/20 P/Q 预算，使用 3 seeds × 2 folds × 4 n 的 24 个匹配单元：P/M95/Q rRMSE 分别为
0.1676/0.2005/0.1622；M95 相对 P 变差 19.61%，24/24 单元均未优于 P 或 Q。该消融只说明
真值点静态秩一代理在当前冻结合同中不足以替代 Q；不报告 CI 或显著性，不将零空间或非线性曲率
分别写成已识别因果，也不推广为所有目标感知参数度量无效。

### S5.4 真值点局部代理为什么失效

对 24 个 P/M95/Q 匹配单元，定义 \(\ell=s_0^Tu\)、\(r=e-\ell\)，则逐行精确有

\[
e^2=\ell^2+2\ell r+r^2.
\]

M95−P 的设计单元等权均值分解为：

| 分量 | M95−P |
|---|---:|
| \(E[\ell^2]\) | −0.0097557 |
| \(E[2\ell r]\) | +0.0041514 |
| \(E[r^2]\) | +0.0177040 |
| \(E[e^2]\) | +0.0120997 |

最大逐行恒等误差为 \(9.1\times10^{-13}\)。该表说明静态局部项的改善确实发生，但交叉项和
非线性余项足以反转真实目标误差。这是代数恒等式，不把这些分量解释为独立的 SGD
因果贡献。

该分解显示，M95 的局部目标变好，但局部近似遗漏的有限误差项更大，使真实 \(x_{0.95}\) 误差反而增加。它是正文动态敏感度表达的辅助检验，不改变 P/Q/QCP 的主比较。

![图S3：更新信号与静态代理检验](figures/supplementary/supp_fig3_target_sensitivity_mechanism.png)

*图S3　a 对比 P 与 Q 的更新信号，b 为 24 个匹配单元的 P/M95/Q RMSRE，c 展示局部近似项与遗漏项。b、c 为探索性机制结果。图源中的 B5 表示 \(x_{0.95}\)。*

## S6. 路线开发阶段的探索性工程误差审计

本节沿用原 `300/20` P/Q 证据，用于解释误差再分配，不与 S4 的当前 `600/60` 主数值混用。在同一 200 个配对模型单元、每路线 480,000 条 held-out 预测上，令
\(e=(\hat x_{0.95}-x_{0.95})/x_{0.95}\)。附加审计得到：

| 指标 | P | Q | Q 相对 P |
|---|---:|---:|---:|
| MAE | 0.11048 | 0.11225 | 变差 1.61% |
| 各设计单元内绝对误差上 10% 尾部均值的等权平均（cell-wise CVaR90） | 0.37042 | 0.35336 | 改善 4.61% |
| 高估 >10% | 14.47% | 13.21% | −1.26 pp |
| 高估 >20% | 6.90% | 6.01% | −0.89 pp |
| 低估 >10% | 25.40% | 28.70% | +3.30 pp |
| 低估 >20% | 7.22% | 9.10% | +1.88 pp |

高估侧 MSE 贡献下降 20.9%，低估侧贡献增加 19.6%，净 MSE 下降 5.91%。这些是事后探索性指标，
未做多重性校正；10%/20% 是敏感性阈值。仅当 \(x_{0.95}\) 用作保证寿命阈值时，正误差才可解释为
潜在非保守高估。复算材料位于 `artifacts/pq_engineering_audit/`。

![图S4：历史预算下的有符号误差与尾部分析](figures/supplementary/supp_fig4_error_distribution.png)

*图S4　使用早期 300/20 P/Q 证据，a–c 为总体误差分布，d 为配对模型单元的高估与低估 MSE 贡献。阈值为描述性敏感性阈值，本图不用于报告当前共同预算的收益。*

## S7. 可信性与证据

- 10 seeds 用于确认方向与训练随机性；不是所有子分析的默认重复次数。
- 原 P/Q 首次封存结果：`artifacts/pq_s5b_revision/analysis/summary_s5b.json` 与 `cell_mse.csv`。
- 当前三路线汇总：`artifacts/qcp_main_analysis/analysis/summary.json`、`model_cells.csv`、`resource_cells.csv`、`manifest.json` 与 `SHA256SUMS`。
- 跨寿命点与真值区域：artifacts/qcp_cross_quantile_recovery/analysis/ 下的 summary、three_route_comparison、truth_cell_effects；对应正文表1–2和图2。
- 区域分布及多指标：artifacts/qcp_resolution_distribution/analysis/ 下的 summary、truth_cell_metrics、representative_predictions；对应正文图3与表3。
- 样本量与偏差分解：artifacts/qcp_sample_size_analysis/、artifacts/qcp_bias_variance/；分别对应 S4.2、S4.3。

- QCP 冻结证据：`artifacts/qcp_constrained_pilot/`、`qcp_constrained_resource/` 与 `qcp_constrained_confirm/`。
- 当前论文机制派生物：`artifacts/pq_paper_core/analysis/` 与 `artifacts/pq_mechanism_closure/`，包含敏感度网格、区域、200 个配对单元、
  `manifest.json` 和 `SHA256SUMS`；全部只读派生，未训练。
- 探索性静态矩阵消融：`artifacts/pq_target_matrix_pilot/`，24 个 P/M95/Q 匹配单元；不与
  200 单元主机制混池推断。
- 先期 3-seed 主证据与机制底稿：`artifacts/pq_iid_main/`，SHA 清单 252 条。
- 10-seed 主结果新增种子的溯源输入：`artifacts/pq_s5b_revision/grid_extra/`；
  `pq_s5b_revision/continuous/` 及 Q_direct 明确不属于当前论文证据。S5B 总 SHA 清单 1769 条曾在
  干净 archive 验证 0 missing / 0 mismatch，但论文只消费上述白名单子集。
- 运行与验证：`python -m study02pq.s5b_revision --phase verify`。

## S8. 范围扩展归档

连续参数支持、联合中点、gamma-holdout OOD、Q_direct、多可靠度水平和容量实验均原样保留在
相应 artifact 与历史报告中。它们属于后加的探索性范围扩展，不用于本稿核心主张，也不作为
本稿“尚欠实验”。如后续研究这些问题，必须重新定义独立 RQ、estimand 和协议。

## S9. 限制

本文为固定尺度、未删失仿真研究，使用给定参数网格、四种小样本量与共同网络结构。折间训练集有重叠，10 个种子仍为有限算法重复。QCP 的监督训练与约束选择使用仿真真参数；训练好的网络仅需寿命样本即可预测。实际应用仍需验证单位尺度和数据分布的匹配；只有在无标签实际数据上重新训练时，才需要另行构造训练依据。三个寿命点的改善描述了当前评价点的表现，而非整个可靠度范围的精度保证。

---

*状态：已同步 P/Q/QCP 当前主线，等待用户审阅；未投稿。*
