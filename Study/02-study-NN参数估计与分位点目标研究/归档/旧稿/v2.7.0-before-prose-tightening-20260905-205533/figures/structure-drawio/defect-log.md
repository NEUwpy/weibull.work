# 图1导出检查

## 第一轮

证据：review/cycle1-zh.png、cycle1-en.png。九区检查覆盖标题/文本、箭头、框体、间距、颜色、字体、总体布局、图像资产、样式一致性。

发现：英文推理说明过长（P1，静态检查也预警）；标题字号层级不足（P1，静态检查预警）；路线标题未突出（P1）；Q目标可靠度未直接标出（P1）；英文上方输入与解码框换行较多（P2）。箭头、框体、颜色及无外部资产检查未发现阻断项。未为满足数量配额虚构缺陷。

## 第二轮

证据：review/cycle2-zh.png、cycle2-en.png。九区复查：推理文字已缩短，面板标题36，路线名称加粗，Q目标0.95已标出；两项静态预警清零。英文输入/解码仍为三行，予以缩短，使共同流程两行对齐。其余区域无P0/P1。

## 第三轮与最终核验

证据：study02-framework-zh-paper-width.png、study02-framework-en-paper-width.png；最终PDF同源导出。

九区逐项复核：文字完整，三条数据箭头方向及端点正确；模块无交叠，等距卡片无拥挤；P/Q/QCP配色一致；标题/正文层级清楚；上下两层含义分离；无图片资产；中文英文结构一致。第二轮的两处多余换行均消除。静态视觉检查0 FAIL/0 WARN，XML检查无重复ID、外部图片或缺失端点。

反向语义审查：图不是模型串联、不是完整计算图；样本最小值的解码依赖在图注说明；QCP阈值来自早期参考P而非当前P测试结果；验证可行性不表示逐样本保证；不画QCP优于QP的结论；不将补充对照误作独立新数据确认；不暗示固定网络推理依赖真参数。无剩余P0/P1。

自评：可读性9/10（底部索引比主节点密集）；箭头准确性9/10（有意省略解码的样本最小值旁路，图注明确）；配色9/10（共享模块均灰蓝，依赖标签区分）；布局9/10（说明行仍较宽）；风格9/10（信息卡片占比较高，适合研究设计而非模块细节）。总分45/50。剩余限制：用于180 mm通栏；单栏需重新排版，不能直接缩小。


## 用户指出的问题与本次重画

**用户发现、上一轮自查遗漏：** 原图以分类卡片代替了神经网络结构和监督原理，未达到交付目的。上一轮45/50的自评只覆盖排版，遗漏内容充分性，不应作为本次完成依据。旧图及其构建文件保存在previous-frame-only/。

### 神经网络版第1轮

证据：review/neural-cycle1-zh.png、neural-cycle1-en.png。覆盖九区（文本、箭头、框体、间距、颜色、字体、布局、图形资产、风格）。实际缺陷：原始输出标签压线、样本最小值旁路穿越注释、英文标题偏长、三参数标签HTML空格折叠、下方网络符号只有两个输出点、部分数据箭头端点未绑定。已移动路径/标签、缩短文字、加入不换行空格、改为三个输出点并绑定数据节点。静态检查初始大量端点相交为缺少source/target导致，已补齐神经网络权重连接的真实端点。

### 第2轮

证据：review/neural-cycle2-zh.png、neural-cycle2-en.png。九区复查：节点网络可识别、回传路径不穿过其他模块；发现中文Ψ表达因额外参数展开而挤出框体（P1），Q路径中心未对齐（P2），QCP空心检查点缺明确图例（P1）。修复为紧凑惩罚标签，正文图注解释μ、ρ，统一路径中心并加“○选中”。

### 第3轮及修复复核

PDF直接渲染检查：review/neural-pdf-final-zh.png、neural-pdf-final-en.png。中文框内溢出已消除；英文“Constraint penalty”又引入换行拥挤，缩短为“Penalty Ψ”并重新导出，不能把中文通过当成英文通过。最终两种语言都再次核验。

### 最终反向审查

- 神经元列、全连接、ReLU、层宽、原始输出、三参数解码都已出现。
- P/Q输出表示相同，区别落实到三个独立误差项与一个非线性寿命点误差，而非仅方法名称。
- QCP保留蓝色目标支路，增加绿色参数惩罚及独立阈值输入；Ψ的乘子更新细节明确留给正文，不暗示固定加权QP。
- 三个虚线回路均回到各自网络θ，不串联三个方案。
- 原始样本最小值来自排序样本第一项，未使用标准化值解码。
- 验证图明确为示意；QCP圈定的是可行点中的最低LQ，不是所有点中最低者。
- 最终中文、英文SVG/PNG/PDF无观察到的裁切或重要文本重叠；次要注释需要通栏阅读。

### 静态预警复核

两份XML零FAIL。保留的预警按原因逐项归类：神经元/检查点被启发式当作装饰块，实际均有以上语义；全连接线被判密集，但相邻层全连接正是要求表达的结构；跨三个不同损失图的行列间距不宜强求统一；三参数HTML空格触发高度估算，最终导出只占一行；方法色及选中/不可行点的深浅用于同一语义。坐标轴使用显式端点，非模型连线；输入箭头指向输入层括号，使用显式坐标。未修改检查器或隐藏FAIL。

自评：网络与方法可辨识度9/10；箭头语义9/10；文字可读性8/10（底部说明较小）；布局9/10；配色一致性9/10，总44/50。剩余限制：网络用代表节点，Ψ详细表达和μ逐轮更新见正文；单栏缩放需重排。此次以方法是否被画出来作为首要检查，不再只评价卡片排版。
