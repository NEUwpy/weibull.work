# 结果有效但不再服务当前研究问题

> 分类说明：下列证据在各自协议下有效（未失效），但它们回答的是旧 P–D 问题或旧设计域，
> 与当前 P–Q 问题无关，因此不进入当前主线与论文正文。

## 有效证据清单

| 材料 | 有效性 | 为何不再服务当前问题 |
|---|---|---|
| D 路线（直接标量分位点）B1–B3 训练与 B6 复现 | 在 D 协议内有效 | Q 路线已取代 D：Q 仍输出三参数 Weibull，不是标量回归；不得把旧 D 网络与 P 比较 |
| B-增量密集 n 响应（n=5..30） | 有效（R1 修正版） | P–Q 对齐 Study01 设计 n={7,10,15,20}，不是密集 n 设计 |
| B-增量 β/ρ 参数边界证据 | 有效 | P–Q 对齐 Study01 参数域（β={1.5..5}, γ={100..1000}, η=1000） |
| 传统方法（MDM/LRE/MLE）参照 | 有效 | P–Q 主分析是 P/Q 配对受控比较，不新增传统方法参照；Study01 横向对齐另行说明 |
| C2–C4 机制分析（n 异质性、参数→x0.95 传播、目标对齐归因） | 在各自口径下有效 | 机制叙事绑定旧 P–D 框架 |
| B 阶段 NIST 6061-T6 真实数据 | 有效 | P–Q 冻结协议不包含真实数据后置项 |
| conformal 校准方法 | 方法本身有效 | P–Q 主分析以配对重采样 CI 为主，不含 conformal 区间 |

## 消费规则

- 这些证据在需要解释历史时可以从本索引定位读取。
- 不得把 D 路线数字与 P–Q 主实验混排、混比或作为 P–Q 的对照。
- 旧 B 阶段结论若被引用，需同时引用 `05-B-增量实验报告.md`（修正版）作为该问题权威口径。
