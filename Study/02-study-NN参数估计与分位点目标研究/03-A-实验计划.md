# Study/02 前置研究 A：实验计划

> 创建日期：2026-07-12
> 当前阶段：前置研究 A **精简复现路线** P2a（E1 preflight：A5/A6/A13 脚本与配置就绪，待 Codex 审查；审查通过前不得启动训练）。P0 已完成（tip `1a859258`），P1 已完成（tip `133b8805`）。A-E1 r5 与 A-E3 r2 已 APPROVE 并保留为基线；旧 formal 引擎只读冻结（formal test 永封），独立确认由 `confirmation` 留出集提供。A4/A8 残留为 partial，留待后续最小补检。
> 职责：管理执行顺序、依赖、阶段闸门和进度；实验定义以 `02-A-实验协议.md` 为准，长任务总合同见 `coworker/plans/2026-07-31-study02-lean-prestudy-completion.md`。

## 一、总体路径

精简路线用最小、可复现、足够严谨的实现回答 19 问，保留 A-E1 r5 / A-E3 r2 既有证据，停止扩建 formal 安全/授权流水线：

```text
P0 计划与文档重构（本阶段）
  -> P1 E0 既有证据整理（复用 A-E1/A-E3）
  -> P2 E1 训练设计敏感性
  -> P3 E2 比较与泛化
  -> P4 E3 等变性与稳健性
  -> P5 E4 工程可信度
  -> P6 收口（19/19 绑定证据与边界，Codex 终审）
  -> 进入 Study/02 分位点主研究
```

## 二、阶段与闸门

| 阶段 | 目标 | 回答问题 | 完成闸门 | 状态 |
|------|------|----------|----------|------|
| P0 | 计划与权威文档重构：根 README、Study/02 README、`00-A`–`03-A` 同步精简路线 | — | 文档一致、无 stale 当前态、coworker 测试通过、Codex APPROVE | ✅ 完成（tip `1a859258`，Codex 已接受） |
| P1 | E0 既有证据整理：复用 A-E1 r5 / A-E3 r2 | A1、A4、A7、A8、A17、A18 | 6 问绑定可复现证据与适用边界 | ✅ 完成（tip `133b8805`，Codex 已接受） |
| P2 | E1 训练设计敏感性 | A5、A6、A13 | 3 问有训练量/参数分布/范围先验证据 | P2a preflight 已提交（脚本/配置就绪，21 拟合，待 Codex 审查） |
| P3 | E2 比较与泛化 | A2、A3、A9、A10、A19 | 5 问有方法比较、未见组合、稳定性、合法性、样本量趋势证据 | 待开始 |
| P4 | E3 等变性与稳健性 | A14、A15、A16 | 3 问有尺度/平移/异常值证据 | 待开始 |
| P5 | E4 工程可信度 | A11、A12 | 2 问有真实数据可用性与单次估计可信度证据 | 待开始 |
| P6 | 收口 | 全部 19 问 | 19/19 绑定可复现证据、适用边界、失败/不支持结果；干净 checkout 复跑必要入口；Codex 终审 APPROVE | 待开始 |

已完成的历史基础（保留为只读事实）：

- G0 问题/边界/执行基础设施冻结（`01ba990`）。
- G1 既有 NN Weibull 参数估计研究核对与可复现基线（`ef1d8c0`）。
- G2 formal 科学协议冻结（`ebef84a`）。
- **A-E1 r5**（`A-E1-formal-r5-20260727-222417`，tip `d2a056f`）：349 fits 全 terminal、staged_resolution_ledger 8/8、baseline winner=V，APPROVE/frozen。
- **A-E3 r2**（`A-E3-formal-r2-20260730-111949`，tip `996434b2`）：266 fits 全 terminal、staged_resolution_ledger 10/10、`huber + m12 + joint + fixed`，经 Claude 独立审核 + Codex APPROVE。
- 历史 blocked/aborted run（A-E1 r1–r4、A-E3 r1）仅作只读历史，不恢复、不拼接、不冒充证据。

## 三、当前动作：P0 计划与权威文档重构

P0 仅做文档：根 `README.md` 当前快照、Study/02 `README.md`、`00-A`/`01-A`/`02-A`/`03-A` 同步为精简路线；明确旧 formal 引擎只读冻结、最小复现契约、E0–E4 与 P0–P6、mailbox 恢复入口。移除/改写 stale 的“r2 尚未授权”“mandatory per-module formal/unseal”措辞，保留仍有效的历史事实。**P0 经 Codex 审查前不得启动任何实验。**

## 四、停止条件与边界

允许：修改权威文档、Study/02 代码、必要测试与精简实验产物索引；在 `C:\weibull-runs\study02\lean` 保存较大产物；P0 经 Codex 审查后自主实现并运行 P1–P6 所需最小实验；每阶段一个 coherent commit 并 mailbox 报告。

不允许：修改/删除/重跑 A-E1 r5、A-E3 r1/r2 formal 产物；提前开展主研究或把 validation 冒充 held-out test；新增/扩展 formal scheduler、lease、authority、unseal/consume、hash-chain、capsule 或攻击面测试；为“以后可能复用”建新框架；合并 main、发布、部署、上传数据。

请求用户决策（硬停止）：研究问题/主指标/参数范围/数据角色需实质改变；需访问范围外私有/外部数据；预计新增训练显著超过 120 fits 或单阶段超 24 小时且无法靠复用/缩减解决；结果暴露改变论文主线的重大矛盾；不可恢复的数据或 Git provenance 问题。普通代码错误、实验失败、无显著差异或局部不支持结论不是停止理由，应如实记录并继续完成问题矩阵。

## 五、进度维护规则

- 本文件只维护当前阶段、下一步和闸门状态；实验定义变更写 `02-A`，研究问题变更写 `01-A`。
- 协调经 duplex mailbox（任务 `study02-lean-prestudy-completion-20260731`）：每阶段 coherent commit → mailbox 报告（exact tip、改动文件、命令与结果、剩余 dirty、下一阶段建议）→ Codex 审查至 APPROVE/REVISE。
- executor 不得自我批准或越阶段；Codex APPROVE 某阶段后方可开始下一阶段，终审 APPROVE 仅在 P6 与 19/19 完成后发出。
- 窗口中断后按「长任务计划 → `00-A-执行状态.md` → mailbox `STATUS.md`/`TRANSCRIPT.md` → `git status`/`git log`」恢复，从第一个未勾选阶段继续。
- 阶段报告默认存 mailbox archive；只在阶段成果需长期引用时写 tracked report。
- 被替代的计划版本移入 `history/`，不得覆盖决策来源；提交前必须保留并审计工作树中既有用户改动，不得用重置/覆盖/无关格式化清除。
