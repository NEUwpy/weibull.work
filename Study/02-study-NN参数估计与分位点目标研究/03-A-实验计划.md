# Study/02 前置研究 A：实验计划

> 创建日期：2026-07-12
> 当前阶段：A-E1 formal r5 已完成并 frozen 为 A-E3 predecessor；A-E3 orchestration r1 代码就绪，尚未 formal 授权；test 仍 sealed，19 个问题尚无 formal 结论
> 职责：管理执行顺序、依赖、阶段闸门和进度；实验定义以 `02-A-实验协议.md` 为准。

## 一、总体路径

```text
G0 问题冻结
  -> G1 文献与复现基线
  -> G2 协议决策冻结
  -> G3 P0 基线实验
  -> G4 P1 比较与边界实验
  -> G5 P2 可信性与工程实验
  -> G6 前置研究 A 收口
  -> 进入 Study/02 分位点主研究
```

## 二、阶段与闸门

| 阶段 | 目标 | 主要产出 | 完成闸门 | 状态 |
|------|------|----------|----------|------|
| G0 | 冻结前置研究问题、边界与持续执行基础设施 | `00-A-执行状态.md`、`01-A-研究问题.md`、总执行合同 | 问题池、目标、loop、单写者、审查和 Git 阶段规则获 APPROVE并完成首次推送 | 已完成（`01ba990`） |
| G1 | 核对既有 NN Weibull 参数估计研究并建立可复现基线 | 文献证据表、复现差异记录、基线代码计划 | 能说明复现对象、已知配置、缺失信息和不可复现点 | 已完成（`ef1d8c0`） |
| G2 | 冻结 formal 实验协议 | `02-A-实验协议.md` 修订版 | 参数空间、样本量、方法池、指标、切分、重复和产物结构均明确 | 已完成（`ebef84a`） |
| G3 | 回答 P0 基线问题 | A-E1、A-E2、A-E3 及 A10/A9 相关结果 | 输入、训练数据、损失、结构、输出形式和跨样本量方案有证据支持 | 进行中（A-E1 formal r5 已完成：349 fits 全 terminal、staged ledger 8/8、baseline winner=V，frozen 为 predecessor。A-E3 orchestration r1 代码就绪：CLI `formal-execute` / `formal-staged` 已接通 `run_a_e3_staged` / `resolve_a_e3_staged_selection`，predecessor staged-ledger 绑定、6 staged receipts、checkpoint-forward `build_module_selection(A-E3)`、9-record staged ledger、A-E2 predecessor 校验在 sealed smoke 端到端跑通。**A-E3 formal 尚未授权**；test sealed。准备闭合和已完成 run 均不等于 G3 问题完成，19 个问题仍等待 formal evidence） |
| G4 | 回答 P1 比较与边界问题 | A-E4、A-E5 部分结果 | 方法优势、泛化、隐含先验、尺度/平移和样本量趋势有边界结论 | 待开始 |
| G5 | 回答 P2 可信性与工程问题 | A-E5 异常值、A-E6 结果 | 真实数据、单次可信度和异常值敏感性有可审计结论 | 待开始 |
| G6 | 收口前置研究 A | 问题—证据矩阵、参数路线基线、限制清单 | 19 个问题全部有答案并通过复核 | 待开始 |

## 三、当前执行顺序

### 已完成：文献与复现审计

- 确定主要复现论文及版本。
- 提取数据生成、输入特征、输出、网络、损失、切分和评价设置。
- 区分论文明确给出的配置与需要合理补全的缺失信息。
- 建立“既有论文主张—证据—本研究疑问”对应表。

### 已完成：逐项冻结协议决策

建议按以下依赖顺序讨论：

1. 参数化、参数空间和样本量。
2. 评价指标与传统方法池。
3. 数据生成、数据切分和重复策略。
4. 样本值与样本特征输入形式。
5. 参数目标、输出约束和 NN 候选结构。
6. pilot 规模、formal 规模和停止条件。
7. 真实数据、可信度和异常值验证。
8. formal 预注册包、配置哈希、training/validation/calibration/test 四角色、模块级一次测试集启封程序和 GitHub 数据体量策略。

### 当前动作：A-E3 orchestration r1 代码就绪，等待 A-E3 formal 授权

A-E1 formal r5 已完成（349 fits 全 terminal、staged_resolution_ledger 8/8、baseline winner=V），frozen 为 A-E3 predecessor。A-E3 orchestration r1（C1–C5）代码收口：predecessor staged-ledger 绑定（C1）、A-E3 stage classifier + scoring resolver（C2）、stage builders + recover/ensure（C3）、`run_a_e3_staged` 驱动 + `resolve_a_e3_staged_selection` 9-record ledger（C4）、CLI wiring + sealed smoke + G.15（C5）。`formal-execute --module A-E3` 从 A-E1 predecessor run 构建 `PredecessorTrace`（含 staged_ledger SHA）并调用 `run_a_e3_staged`；`formal-staged --module A-E3` 调用 `resolve_a_e3_staged_selection`。sealed smoke（G.16）与 G.15 已端到端验证：9 staged records 从 `_ZERO_HASH` 链起、6 A-E3 decisions、`_validate_predecessor("A-E2", trace)` 通过（含 staged_ledger 绑定）。**A-E3 formal 尚未授权**：下一步是申请 A-E3 formal training/validation 显式授权（以 r5 A-E1 为 predecessor），获批后才能创建真实 A-E3 run。不得进入 A-E2、approval、test unseal 或 test consume。

## 四、当前停止条件

在 A-E3 formal training/validation 显式授权前：

- 不启动或恢复任何 A-E3/A-E2 formal run，不创建 run/lease，不执行候选启动命令。
- 不复用或拼接 r1/r2/r3，不进入 A-E2 formal。
- 不生成 approval，不 authorize/unseal，不生成、读取或消费 formal test。
- 不把 orchestration 代码就绪、pilot 或 sealed smoke 写成 19 个问题的研究答案，也不提前宣布某种输入、网络或传统方法更优。

## 五、进度维护规则

- 本文件只维护当前阶段、下一步和闸门状态。
- 完成一个阶段时，记录完成日期和对应证据路径。
- 实验定义变更写入 `02-A-实验协议.md`，研究问题变更写入 `01-A-研究问题.md`。
- 被替代的计划版本移入 `history/`，不得覆盖决策来源。
- 当前维护口径为直接在本地 `main` 上形成边界清楚的 checkpoint，再按审查结论推送 `origin/main`；不得把旧 `codex/` 分支或 worktree 当作当前续接点。
- 阶段提交前必须保留并审计工作树中既有用户改动；不得用重置、覆盖或无关格式化清除它们。
- Study/02 研究 A 的总执行合同见 `coworker/plans/2026-07-12-study02-a-full-execution.md`。
- 每个 G 阶段严格按“验证 → 报告 → oracle 审查 → 修订至 APPROVE → 更新日志 → 提交 → 推送 → 记录远端 SHA”关闭。
- heartbeat 或人工续接必须先读 `00-A-执行状态.md` 并检查 `.slim/deepwork/study02-a.lease.json`；存在有效写入者或活动实验时只监测，不重复执行。
