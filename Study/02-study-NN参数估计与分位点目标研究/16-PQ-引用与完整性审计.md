# Study02 S5B 引用与完整性审计

> 审计对象：`14-PQ-论文初稿.md`、`15-PQ-补充材料.md` 与 S5B 新证据。
> 状态：修订完成，等待独立复审；不得预写 APPROVE。

## 1. 核心主张—证据映射

| 主张 | 证据 | 边界 |
|---|---|---|
| 网格 Q_param 相对 P_equal 改善 3.00% [2.19%,3.79%] | `artifacts/pq_s5b_revision/analysis/summary_s5b.json` | 唯一确认性主张 |
| 10 seed、200 对、184/200 Q 更好 | 同上 + `cell_mse.csv` | 旧 3-seed 为 57/60 |
| 连续 P–Q 0.33% [−0.14%,0.81%] | 同上 | 探索性，不确认差异 |
| 连续 Q-direct 相对 P 0.74% [0.33%,1.18%] | 同上 | 结构和损失共同变化 |
| 中点 P 优 1.86%；gamma OOD P 优 8.77% | `pq_s3_interp`、`pq_v3` 封存分析 | 不与主合同汇总 |
| Q 呈现更强误差抵消 | `pq_iid_main/analysis/mechanism_summary.json` | 结果空间恒等结构，非训练因果 |
| Study01 MDM rRMSE 0.214235 | Study01 `E6_dimensional_raw/quantiles/summary.csv`，SHA `b5f7dbf...` | 描述性，非公平因果对照 |

## 2. 已修正的事实与术语

- “120 paired fits”改为“60 paired comparisons / 120 total fits”；完整 S5B 网格为 200 对/400 fits。
- 旧证据方向计数改为 57/60；完整网格为 184/200。
- SHA 数量分根报告：`pq_iid_main` 252；target/capacity/interp 247/196/364（合计 807）；`pq_v3` 249。
- “作用机制”改为“结果空间误差补偿结构”；训练动力学原因明确未检验。
- `Q_param` 三输出改称潜在参数化，不作物理参数准确性主张。
- 可靠度寿命统一写作 $x_R$、$R(x_R)=R$；避免与 CDF 分位点混淆。
- 论文定位改为受控仿真/方法学研究；真实、删失和工程验证列为未来工作。

## 3. 引用审计

正文参考文献共 15 条。新增文献的功能为：

| 编号 | 支持内容 | 核验标识 |
|---:|---|---|
| 12 | 三参数 Weibull 神经网络参数估计先例 | DOI `10.1504/EJIE.2008.018438` |
| 13 | 三参数 Weibull 拟合方法综述与评价 | DOI `10.1109/TDEI.2009.4784578` |
| 14 | 三参数 Weibull 一致估计方法 | DOI `10.1016/j.csda.2012.09.005` |
| 15 | 点预测目标与评分函数的一般关系 | DOI `10.1198/jasa.2011.r10138` |

对 [15] 的表述已限定为一般类比：本文的 $x_R$ 相对平方损失不是标准分位数评分规则，不借此主张一致性。对 [8] 也只称思想类比。最终门必须运行 `16-audit-zero-orphan.py`，要求 0 orphan / 0 unused。

## 4. 图、代码与证据来源

| 图 | 数据源 | 禁止性解释 |
|---|---|---|
| Fig1 | S5B 10-seed summary | 不再显示旧 3-seed 主数值 |
| Fig2 | 旧封存 mechanism summary/region | 不称训练因果机制 |
| Fig3 | S3 target/capacity | 容量仅描述性 |
| Fig4 | S5B continuous + midpoint + OOD | 四合同不池化 |

生成脚本 `code/study02pq/paper_figures.py` 不手抄主数值。S5B 训练运行 SHA 为 `8c047b2fb8b1c85886a6f341a26e815039ec9a1c`，机器协议 SHA 为 `170e702bae1e83abe56c71157e91eefd1da0ff67c0a1873745d98543751cb0e2`。

## 5. 最终完整性门（2026-08-10 实测）

- [x] `git diff --check` 通过。
- [x] 默认协议 `pytest code/study02pq -q`：**78 passed**。
- [x] `PQ_PROTOCOL=iid-v1 python -m study02pq.s5b_revision --phase verify`：grid 280/280、continuous 600/600、`all_valid=true`。
- [x] 200,000 次分析用冻结 seed 重新运行；关键数值与正文一致。
- [x] S5B `SHA256SUMS` **1769/1769**：0 missing、0 hash mismatch；清单与 manifest 已加入 Git。
- [x] `16-audit-zero-orphan.py`：15/15 引用，0 orphan / 0 unused。
- [x] 四张 PNG 与四张 PDF 存在；更新后的 Fig1、Fig2、Fig4 已视觉检查，标签可读。
- [x] 当前稿无自评 `APPROVE`；“误差补偿结构”与未证训练原因分开；无“普遍更优”主张。

补充说明：若把 `PQ_PROTOCOL=iid-v1` 环境变量错误地保留到全套回归测试中，3 个专门断言 v3 默认配置的测试会按设计失败；上面 78 passed 是清除该变量后的默认协议结果，S5B verify 则在 iid-v1 下单独运行。

---

提交 `fd9d0fcf` 已用 `git archive` 生成独立 ZIP，并在归档内按 LF 规范逐条校验：**1769/1769，0 missing，0 hash mismatch**。这同时确认清单所列证据均存在于提交而非仅存在于本机工作树。

*状态：`S5B REVISION COMPLETE — AWAITING INDEPENDENT RE-REVIEW`；工作树与干净 archive 完整性门均通过。*
