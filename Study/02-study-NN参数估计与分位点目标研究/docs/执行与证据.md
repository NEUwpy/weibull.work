# Study02 执行与证据

> 用途：任务中断或交接后，通过这一份文档恢复当前状态。不从 `history/` 中的旧阶段计划恢复任务。

## 当前状态

| 项目 | 状态 |
|---|---|
| 科学问题 | 冻结网格同分布 `P_equal` vs `Q_param` |
| 主实验 | 完成：10 seeds、200 对/400 fits |
| 主效应 | Q 改善 3.001%，95% 设计级经验 bootstrap 区间 [2.195%,3.793%] |
| 机制 | 完成：同一 u 坐标的敏感度几何 + 10-seed 精确补偿 |
| 论文 | `paper/论文初稿.md`，已纳入目标敏感度机制与工程误差再分配；独立方法/一致性复审通过 |
| 新训练 | 数据量 pilot 32 fits 已完成；目标矩阵 pilot 24/24 fits 已完成，未进入论文 |
| 论文封存 | tag `study02-paper-core-v1` → `0834bce0` |
| 当前增量分支 | `codex/study02-target-matrix-20260810` |

## 机制闭环任务（当前）

当前分支：`codex/study02-mechanism-proof-20260810`。用户已授权把 `P_equal` 固定等系数几何、
`Q_param` 当前预测点动态目标敏感度、真值点 M95 局部代理及其失败消融完整论证进论文。
冻结合同见 [`../protocols/11-PQ-机制闭环分析合同.md`](../protocols/11-PQ-机制闭环分析合同.md)。

进度清单：

- [x] 数学、实验与论文缺口独立审计；结论是不新增训练；
- [ ] 生成 P/Q 200 单元与 P/M/Q 24 单元的精确终态几何分解；
- [ ] 补齐网格梯度/Hessian、零空间反例和动态割线恒等等生产测试；
- [ ] 形成机器证据、机制图和独立报告；
- [ ] 修订论文、补充材料、研究说明与证据白名单；
- [ ] 独立方法复审、引用与 SHA 完整性门、提交推送。

唯一停止边界：不训练新 fit，不扩 B1/B10、多目标、ridge、权重搜索、新网络或 OOD。

## 独立机制增量（2026-08-10）

现有论文证据以 tag `study02-paper-core-v1`（`0834bce0`）封存。新分支只研究完整解析敏感度
矩阵参数损失 `P_matrix` 能否逼近直接目标损失 Q，详见
[`../protocols/10-目标敏感度矩阵研究合同.md`](../protocols/10-目标敏感度矩阵研究合同.md)。
该增量使用独立产物目录和 3-seed × 2-fold 的 24-fit 最小筛选；
结果在用户判断前不进入论文正文，也不改变本文件下列既有关键数值。

| 合同 | 状态 | 机器证据 | 独立报告 | 论文准入 |
|---|---|---|---|---|
| 当前论文核心 | FROZEN | `pq_s5b_revision`、`pq_paper_core`、`pq_engineering_audit` | — | ADMITTED |
| 数据量 pilot | COMPLETE | `pq_data_scale_pilot` | `数据量学习曲线.md` | EXCLUDED / awaiting user decision |
| 目标敏感度矩阵 | COMPLETE / NOT PAPER EVIDENCE | `pq_target_matrix_pilot` | `independent/pq-target-matrix-pilot-v1-结果报告.md` | EXCLUDED / awaiting user decision |

目标矩阵 pilot 的最小结果：P rRMSE=0.167623、M95=0.200492、Q=0.162182；M95 相对 P 变差
19.61%，24/24 单元均未优于 P 或 Q。M95 虽将真值点局部矩阵损失降低35%，但等权参数误差约为
P 的19.1倍、真实 B5 MSE 高43%，与“秩一局部代理留下零空间且不能跟随非线性目标曲面”的解释相容；
本 pilot 未分别识别两者的因果贡献。
完整边界见独立报告；该结果不自动修改论文。

## 当前增量筛选（2026-08-10）

训练数据量学习曲线筛选已完成。合同与结果见 `docs/数据量学习曲线.md`：基线180复用，新增360/720两档，seed 42、fold 1/3、4个n、P/Q配对，共32个新fits。Q rRMSE为0.162276→0.160630→0.159722；趋势单调但边际递减。该筛选不改变当前论文主结论；如需正式补充，只确认360档并等待用户授权。

## 关键数值

- P pooled rRMSE：0.166059。
- Q pooled rRMSE：0.161075。
- Q 绝对降低：0.004983；相对改善：3.001%。
- 10/10 seed pooled 方向有利于 Q。
- 184/200 个模型单元的目标误差有利于 Q。
- 精确抵消指数 P=0.351，Q=0.915；200/200 个配对单元的 Q 抵消指数更高。
- 冻结网格 \(\|s\|\) 范围 0.7657–4.3933，敏感度方向最大相差 20.3°。
- 工程审计：高估 >10% 为 14.47%→13.21%，高估 >20% 为 6.90%→6.01%。
- 代价：MAE 11.048%→11.225%，低估 >10% 为 25.40%→28.70%。
- cell-wise CVaR90 改善 4.61%；高估侧 MSE 贡献下降 20.9%。

注意：184/200 说的是 Q 目标误差更低的模型单元；200/200 说的是 Q 的精确抵消指数更高。两者不是同一个指标。

## 证据入口

| 证据 | 路径 |
|---|---|
| 10-seed 主汇总 | `artifacts/pq_s5b_revision/analysis/summary_s5b.json` |
| 模型单元 MSE | `artifacts/pq_s5b_revision/analysis/cell_mse.csv` |
| 论文机制汇总 | `artifacts/pq_paper_core/analysis/mechanism_paper_core.json` |
| 40 点敏感度 | `artifacts/pq_paper_core/analysis/mechanism_sensitivity_grid.csv` |
| 13 个参数区域 | `artifacts/pq_paper_core/analysis/mechanism_paper_regions.csv` |
| 200 个补偿配对单元 | `artifacts/pq_paper_core/analysis/mechanism_paper_cells.csv` |
| 机制派生证明 | `artifacts/pq_paper_core/analysis/manifest.json` + `SHA256SUMS` |
| 工程误差审计 | `artifacts/pq_engineering_audit/summary.json`、`cell_metrics.csv`、`manifest.json` |
| 工程误差分布图 | `figures/pq-paper/fig3_error_distribution.png` + `.pdf` |
| 环境锁 | `configs/pq-environment-v2.json` |
| 当前主协议 | `protocols/09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json` |
| 数据量筛选 | `artifacts/pq_data_scale_pilot/analysis/summary.json` + `docs/数据量学习曲线.md` |

## 当前文档分工

- `README.md`：唯一入口和分类导航。
- `docs/研究说明.md`：当前问题、结论、梯度与机制。
- 本文：进度、证据、验证和交接。
- `paper/`：论文正文、补充材料和引用审计。
- `protocols/`：人可读冻结协议。
- `docs/reference/`：长篇技术细节。
- `history/`：被取代的过程文档，不作当前事实源。

## 验证命令

在 Study02 根目录：

```powershell
$env:PQ_PROTOCOL='iid-v1'
python -m pytest code/study02pq/test_pq_mechanism.py -q
Remove-Item Env:PQ_PROTOCOL
python -m pytest code/study02pq -q
python paper/tools/audit_zero_orphan.py
git diff --check
```

最近通过记录：机制聚焦 19 passed，默认协议全套 81 passed，引用 16/16、0 orphan、0 unused。

## 任务边界

当前任务不从历史中恢复连续参数、中点、gamma-holdout OOD、Q_direct、多目标、新网络、P 权重搜索或更多 seed。
只有当新问题可能实质改变当前核心结论时，才另立研究合同。
