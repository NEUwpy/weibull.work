# v2.5.0 修订与剩余验证

> 历史阶段记录：下文的“未启动”“等待确认”描述v2.5.0时点。用户于2026-09-05授权落实审查修改，新增对照按协议25执行；当前状态见[v2.7.0修订与验证](v2.7.0-投稿修订与验证.md)。旧阶段原文保留用于追溯。

本轮按用户确认的递进组织论文：为了任务对齐采用Q → 目标点总体风险小幅下降、其他寿命点明显退化 → 分析参数补偿 → 提出QCP约束并检验修复。不是把P、Q、QCP拍平成三个平行算法。

## 已完成

- 正文和附录升级为v2.5.0；v2.4.1正文/附录/当时全部图件原样收纳到 `../归档/旧稿/v2.4.1/`。
- 摘要/引言/讨论/结论区分总体平方风险与典型单元；保留全部不利计数与中位数，97.8%下移为约束发挥作用的诊断。
- 删除摘要“只改变训练损失”的不准确说法：现有比较包含训练与对应验证选点规则。增加条件网络与局部秩一之间的逻辑衔接，并给出实际参数解码公式。
- 从600份配对预测（每路线48万条）复算参数分布、位置边界、单侧高估比例；现有三路线总体RMSRE以参数重建后与旧摘要数值一致（rtol=1e-12）。存储参数为float32，派生计算升为float64；不把保存的float32 rel_err当成原始float64计算结果。
- QCP对P目标点的前5单元贡献净ΔMSE的102.6%；四分组分析为描述性，不作带数学耦合的相关因果推断。160个单元共享网络，不做独立二项符号检验。
- Q形状/尺度中位数20.39/67.00；距位置上界0.1%以内仅0.014%，不支持“普遍贴边”的猜测。
- 当前高估>20%：P 6.85%、Q 6.02%、QCP 6.07%；如实保留QCP不优于Q的这一项，不声称全指标支配。
- 前置成本：200次P参考训练0.821h、48次约束筛选0.331h、8次预算检查0.108h；QCP全额归属训练成本约6.84h，复用已有参考时约6.02h。为训练计时之和，不是墙钟历时。
- 图2中文化并移出参数图例；图3改为配对风险+累计净贡献，删除与表3重复的全域指标面板；图1无平滑路线连接的既有修复保留。
- 正文百分比一般保留两位小数，小概率边界保留必要精度；负数区间使用“至”。附录保留较高精度便于复算。
- 参考文献[3] DOI经Wiley原页核验补齐：[Weibull Models](https://onlinelibrary.wiley.com/doi/book/10.1002/047147326X)。[4]原版DOI未核实，不以新版DOI冒充。

## 尚未完成的决定性验证

1. **共同选点标准的P/Q消融**：P训练按验证LQ选点，对照Q训练按LQ选点；若进一步分离QCP作用，还需Q训练按同样LP可行性筛选。实查当前600份元数据全部 `history_recorded=false`，两个证据根目录没有逐轮 `.pt/.pth/.ckpt`；不能靠重读现有日志完成，需要新增训练。不得据此回填旧结果或覆盖已有预测。
2. **三寿命点联合损失对照**：用于判断额外分位点监督能否以更简单方式修复Q；需预先固定权重、预算和选点准则，再训练。现有结果只证明QCP有修复作用，未证明它优于此替代方案。

这两项需要新增训练。本轮未启动；等待用户对新增实验范围的确认。若执行，复用已有数据、模型、拆分，独立输出，不建设新调度或审批框架；pilot结论与完整多seed结论分开报告。

## 后置或不采纳

- 变动eta、参数点留出属于外部有效性扩展；当前仍限定固定尺度网格内新样本，不扩成一般估计器结论。
- 经典估计仅可作外部刻度，不把Study01重新并入本论文；调权P的大范围搜索不开展，保留M95仅针对已测代理的结论。
- 次级指标目前保留描述性点估计，不给显著性结论；完整配对区间可在投稿要求明确后补充。
- 历史处理器型号未被可核验记录绑定；正文如实说明CPU/float64/单线程，硬件型号仍需原执行环境记录补证。
- 参考文献最终编号、图形尺寸和附录统一语言按选定期刊再处理，不伪称已达到某刊投稿格式。

## 复现与核查

运行 `python manuscript/tools/review_diagnostics_v250.py`，只读既有预测并输出至 `artifacts/manuscript_review_v250/` 和当前投稿图2–3。summary记录读取的预测与成本元数据SHA，正式训练代码、协议、模型与旧预测均不修改。图件输出PNG/PDF/SVG，已目视核对面板、坐标与图注。

本轮不提交/推送Git，不修改Study01既存编辑。当前稿待用户阅读；新增训练尚未完成，不能把“改稿完成”写成“所有证据缺口已闭合”。
