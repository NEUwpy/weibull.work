# Study 02：面向寿命分位点目标的 NN Weibull 参数估计研究

> 创建日期：2026-07-11
> 当前状态：前置研究 A 已完成；主研究 B 的 B1–B6 全部完成并 APPROVE（最终分支 `codex/study02-quantile-formal-20260731`；B6 实现/证据 tip `2e0e842d`，最终报告/文档 tip `b5c5404`）。正式实验结论见 `05-B-正式实验报告.md`，证据哈希与路径见 `04-B-证据索引.md`。
> **B2 增量实验（2026-08-02）已完成**（分支 `codex/study02-b-incremental-20h-20260802`）：密集 n 响应（n=5..30 共 19 个 n）与 β/ρ 参数边界证据（350 新 fit、core 24,320 + grid 295,920 数据集、normalized ~19.2h）；5 个既有 n 逐点复现 B4，新增发现 n=6/8/9/12 P-better 带与 β 主轴（低 β P 优、高 β D 优）。报告见 `05-B2-增量实验报告.md`，**待 reviewer 审查**。
> C 阶段已完成并 APPROVED（分支 `codex/study02-c-scientific-argument-20260801`）：把 A/B 已有实验结果提升为科学论证；C0/C1–C5 全部通过，C5 经对抗性复核与可复现性稳定（code commit `8cc93147`）。范围冻结与证据缺口审计见 `06-C-论证计划与状态.md`，C2–C4 机制分析与补充证据见 `07-C-机制分析与补充证据.md`（含 2026-08-02 C-增量细化），论文前论证底稿见 `08-C-论文前科学论证报告.md`（C5 最终版，APPROVED，含增量 Claims 细化）。
> 前置研究成果：`06-A-前置研究报告.md`。

## 目录定位

本目录承载 Study/02 的研究设计、前置研究、正式实验、证据和论文材料。当前唯一论文主线暂定为：

> 比较两条分别优化的估计路线：一条先用 NN 估计三参数 Weibull 分布，再间接计算目标寿命分位点；另一条用 NN 直接估计目标寿命分位点。如果工程上只需要 \(x_{0.95}\)，保留完整 Weibull 参数化究竟付出了多少分位点精度代价？

这里的研究目的不是为了使用神经网络，而是从可靠性工程最终关心的寿命分位点出发，检验训练目标与工程目标对齐后能否产生可量化、可泛化且边界清楚的收益。

本文统一采用可靠度分位点记号：

\[
x_R=F^{-1}(1-R)=\gamma+\eta[-\ln(R)]^{1/\beta}.
\]

因此，\(x_{0.95}\) 表示可靠度仍为 95% 时的寿命，等价于失效分布的下侧 5% 分位点，不是通常统计记号中的第 95 百分位。

## 当前研究结构

### 前置研究：NN 直接参数估计基础

该部分是进入主研究前的积累和方案冻结依据，不预设为独立论文。它需要澄清既有 NN Weibull 参数估计研究中尚未充分回答的问题，例如：

- 为什么使用样本统计特征训练；直接输入样本值会怎样？
- NN 与传统参数估计方法如何公平比较？
- 模型在参数范围、样本量和未见组合上的泛化能力如何？
- 哪些输入特征、网络结构和训练设置真正有贡献？

前置研究的范围由既有 NN Weibull 参数估计论文留下、且会阻碍主研究继续推进的具体疑问决定。它需要对这些疑问形成证据化回答，并由此为主研究提供可复用的输入表示、基线模型、训练协议和比较口径。它形成研究积累，但是否具备独立发表价值留待证据形成后判断。

### 主研究：面向寿命分位点的目标转换

主研究比较参数精度目标与寿命分位点目标，并至少回答：

- 目标分位点的样本外预测精度能改善多少？
- 改善是否跨参数区域、样本量和数据条件成立？
- 参数估计精度及非目标分位点是否付出代价？
- 直接预测参数后计算分位点，与直接预测分位点相比如何？
- 主研究自身需要哪些消融、泛化和稳健性验证？

前置研究不能替代主研究自身的消融与泛化实验；它只负责减少基础方案的不确定性。

当前只聚焦 \(x_{0.95}\)（B5 寿命）作为训练目标和主要论文结论，不同时训练 \(x_{0.90}\) 或 \(x_{0.99}\)。后续可将非目标可靠度点作为跨分位点泛化检查，例如评价按 \(x_{0.95}\) 训练的模型在 \(x_{0.99}\) 上偏差多大；这不属于当前核心训练任务。

当前核心比较是两条完整估计路线：

1. **参数路线**：NN 输出 \((\beta,\eta,\gamma)\)，以参数估计准确性为训练目标，再由预测参数计算 \(x_{0.95}\)。
2. **工程目标路线**：NN 直接输出 \(x_{0.95}\)，以该分位点的估计准确性为训练目标。

核心问题是：如果工程上只需要 \(x_{0.95}\)，保留完整 Weibull 参数化究竟付出了多少分位点精度代价？

两条路线采用双层比较，但优先级如下：主分析允许参数路线和直接分位点路线在合理且对等的资源预算下分别优化，比较各自可达到的实际最佳 \(x_{0.95}\) 表现；相同数据、相同输入和相同模型骨干的严格受控比较放在后续验证中，用于辅助解释差距来源。

## 当前文件

| 文件 | 作用 |
|------|------|
| `00-A-执行状态.md` | 当前阶段、单写者租约规则、最近批准与推送 SHA；供人工和 loop 续接 |
| `01-A-研究问题.md` | 前置研究 A 的权威问题清单、范围、优先级和完成标准 |
| `02-A-实验协议.md` | 前置研究 A 的证据规则、实验模块和待冻结的正式配置；不记录执行进度 |
| `03-A-实验计划.md` | 前置研究 A 的执行顺序、阶段闸门、当前状态和下一步 |
| `04-A-文献与复现审计.md` | G1 文献证据、复现对象、已确认配置、缺失信息和重建边界 |
| `05-A-证据索引.md` | 19 个问题的逐题答案、证据来源、适用边界与复现索引 |
| `06-A-前置研究报告.md` | 前置研究 A 的正式成果报告、结论与对主研究的落实建议 |
| `06-C-论证计划与状态.md` | C 阶段科学论证计划与状态：C 要回答的问题、主张/证据/机制/替代/缺口矩阵、C2–C5 工作包与决策门、中断恢复清单、C2 结果索引 |
| `07-C-机制分析与补充证据.md` | C2–C4 机制分析与补充证据：n 异质性、参数→x0.95 传播、目标对齐归因、替代解释排除（availability×error、MLE 存活者偏差）、条件选择表；每问题 问题→分析→结果→证明了什么→没证明什么→替代解释 |
| `08-C-论文前科学论证报告.md` | 论文前科学论证底稿（C5 最终版，APPROVED）：研究问题与一句话答案、7 条 Claim（Claim→Evidence→Reasoning→Alternative→Boundary）、可主张/不可主张清单、是否需补实验、对抗性审查结果表 |
| `01-B-研究问题.md` | 主研究 B 的核心问题、B1–B7、确认性判定和结论边界 |
| `02-B-实验协议.md` | P/D/T 路线、数据角色、实验规模、指标、统计分析、产物与停止条件 |
| `03-B-执行计划与状态.md` | 主研究 B 的 B0–B6 进度、协作边界、中断恢复和完成定义 |
| `04-B-证据索引.md` | 主研究 B 的逐题证据索引、运行哈希、fit 计数与 B1–B7 映射 |
| `05-B-正式实验报告.md` | 主研究 B 的正式研究结论与论文主张边界 |
| `05-B2-增量实验报告.md` | B2 增量实验（2026-08-02）：密集 n 响应 + β/ρ 参数边界证据、归一化工作量、复现入口 |
| `configs/A-g2-protocol-v1.json` | G2 已冻结的机器可读实验配置 |
| `configs/A-g2-search-v1.json` | 特征公式、损失、架构、优化器与两阶段搜索规则 |
| `configs/A-g2-configs.sha256` | G2 两个冻结 JSON 配置的 SHA-256 |
| `requirements.txt` | Study/02 正式可复现依赖（torch CPU + numpy/scipy/pandas，含 `--extra-index-url`），新环境安装入口 |
| `history/README.md` | 说明历史材料的归档边界 |
| `history/2026-07-11-2026-07-12-grill-me-主题确定记录.md` | 主题确立与 A 问题池形成过程；仅用于追溯决策来源 |

## 当前阅读顺序

1. 本 README：了解 Study/02 总体定位和当前边界。
2. `05-B-正式实验报告.md`：正式研究结论与论文主张边界。
3. `06-C-论证计划与状态.md`：C 阶段科学论证计划、主张/缺口矩阵与工作包决策门。
4. `04-B-证据索引.md`：逐题证据、运行哈希与 B1–B7 映射。
5. `01-B-研究问题.md`：必须回答的问题和允许形成的结论。
6. `02-B-实验协议.md`：路线、数据角色、指标、统计判定和边界。
7. `06-A-前置研究报告.md`：参数路线的前置依据；需要逐题核查 A 时再读 `05-A-证据索引.md`。
8. 只有需要追溯 A 的执行或决策来源时，才读取 `00-A`–`04-A` 与 `history/`。

## 当前边界

- 已确定采用可靠度分位点 \(x_R=F^{-1}(1-R)\)，当前只训练和主张 \(x_{0.95}\)；\(x_{0.90}\) 和 \(x_{0.99}\) 留作后续跨分位点泛化检查。
- 前置研究 A 已完成并关闭；A4/A8 的 partial 是证据边界，不再通过扩大消融改变标签。
- A-E1 r5 与 A-E3 r2 的 formal 证据已 APPROVE 并保留为基线；旧 formal 引擎（formal scheduler、authority、unseal/consume、hash-chain、capsule、攻击防护、lease）**只读冻结，不再扩建**。历史 blocked/aborted run（A-E1 r1–r4、A-E3 r1）仅作只读历史，不得恢复、拼接或冒充证据。
- A formal test namespaces 保持 sealed；B 使用新的 training/validation/calibration/test namespace，数据角色见 `02-B-实验协议.md`，不启封或消费 A test。
- 主研究 B 只训练并主张 \(x_{0.95}\)，不扩展到删失、截断或多个目标分位点联合训练。
- B 以最小可复现代码完成研究，不复活或扩建 A 的 formal scheduler、authority、unseal/consume、hash-chain、capsule、攻击防护或 lease。
- 主研究 B 已完成：core pooled I=0.3926 [0.3444,0.4347]（D 相对 P，supported and material），但优势分层且非全域（n=7/10 不显著、core 上 MDM/LRE 更优、压力层可用率下降）。D 不产生参数结论；NIST 无已知真 x0.95；stress 覆盖无 OOD 保证。详细边界见 `05-B-正式实验报告.md`。

## 环境与复现入口

Study/02 正式代码（`code/study02a/*.py`）依赖 PyTorch。为避免依赖某一台机器的隐含环境，可复现依赖声明在本目录的 `requirements.txt`：

```
pip install -r "Study/02-study-NN参数估计与分位点目标研究/requirements.txt"
```

它通过 `--extra-index-url` 拉取 CPU 版 torch，并 pin 了验证过的 numpy/scipy/pandas 版本（2026-07-17）。

冻结工件的字节哈希（`configs/A-g2-protocol-v1.json`、`configs/A-g2-search-v1.json`、`configs/A-g3-pilot-amendment-v4.json`、`artifacts/pilot/G3-matrix/experiment_matrix.csv`）按 LF 内容批准。仓库根的 `.gitattributes` 对这些文本类型强制 `eol=lf`，因此即使在 Windows 默认 `core.autocrlf=true` 上做全新 checkout，工作树也是 LF，`verify_frozen_hashes` / `FROZEN_MATRIX_SHA256` 无需人工改 Git 配置或手工重写文件即可通过。

## 研究原则

本 Study 继承 `study/研究原则.md`：问题驱动、可信性优先、仿真数据证明统计性质、真实数据证明工程价值，并明确方法的适用边界。
