# Study02：面向明确可靠度寿命目标的损失对齐研究

> 当前状态：核心实验与机制论证完成，Mentor 叙述必要性终审已通过，等待用户审阅；未投稿、未合并 main。
> 历史文档没有删除，只是移出活动区。
> 24-fit M95 静态局部矩阵实验已作为探索性机制消融纳入论文；它不改变确认性主效应，
> 不报告 CI 或显著性。详细结果见当前结论及独立报告。

## 先读哪些

| 需求 | 文档 | 性质 |
|---|---|---|
| 弄清研究问题、结论和“梯度” | [`docs/研究说明.md`](docs/研究说明.md) | **当前权威说明** |
| 看真正的论文正文 | [`paper/论文初稿.md`](paper/论文初稿.md) | **论文工作稿** |
| 查进度、数值、证据路径和验证命令 | [`docs/执行与证据.md`](docs/执行与证据.md) | **项目接手入口** |
| 查论文的补充方法与表格 | [`paper/补充材料.md`](paper/补充材料.md) | 论文附件 |
| 复核统计合同 | [`protocols/09-PQ-同分布主协议冻结.md`](protocols/09-PQ-同分布主协议冻结.md) | 技术协议 |
| 深入复核机制公式 | [`docs/reference/10-PQ-作用机制分析.md`](docs/reference/10-PQ-作用机制分析.md) | 技术报告 |
| 看机制论证的问题—方法—结果—结论 | [`docs/independent/pq-mechanism-closure-v1-结果报告.md`](docs/independent/pq-mechanism-closure-v1-结果报告.md) | 独立结果报告 |
| 查目标敏感度矩阵增量 | [`protocols/10-目标敏感度矩阵研究合同.md`](protocols/10-目标敏感度矩阵研究合同.md) | 独立研究合同 |

平时只读前三项。`history/`、`已归档/`、`前置实验/` 只用于追溯，不定义当前结论。

## 当前结论

在 Study01 冻结参数网格、相同三输出网络和相同训练配置下，只改变训练与验证损失：

- `P_equal`：优化三个归一化参数误差的等系数和；
- `Q_param`：直接优化目标可靠度寿命点 \(x_{R=0.95}\) 的相对误差。

10-seed 主实验共 200 个 P/Q 配对模型单元（400 fits）：

- P rRMSE = 0.166059；
- Q rRMSE = 0.161075；
- 绝对降低 0.004983，相对改善 3.001%；
- 95% 设计级经验 bootstrap 区间 [2.195%, 3.793%]；
- 10/10 seed 的 pooled 方向均有利于 Q，184/200 个模型单元有利于 Q。

这支持的是：**对当前冻结的 `P_equal` 参照，直接训练明确目标可以让该目标的均方误差平均降低，
并通过当前 B5 敏感度更新避免人工固定一组全域参数权重。**

机制链只保留理解结论所必需的三步：`P_equal` 在归一化参数坐标中使用固定参数误差规则；
`Q_param` 在每个当前预测位置，按 B5 误差以及 B5 对三个参数的实际敏感度更新网络；真值点静态
矩阵 M95 虽复现 Q 的局部二次项，却不能跟随有限参数误差下目标关系的变化。24 单元探索性消融中，
M95 rRMSE=0.2005，差于 P 的0.1676和Q的0.1622，且24/24单元均未优于二者。精确分解进一步
确认：M95 的局部近似确实变好，但它遗漏的额外目标误差更大，最终使真实 B5 误差变差。这排除了
“Q 只是一套真值点静态权重”的简单解释，但不声称已经识别了完整训练动力学。

3% 不是全面精度改善。探索性工程审计显示：Q 的 MAE 略差，但寿命高估 >10% 从 14.47% 降至
13.21%，高估 >20% 从 6.90% 降至 6.01%，cell-wise CVaR90 改善 4.61%；代价是
寿命低估 >10% 从 25.40% 增至 28.70%。当 \(x_{0.95}\) 被用作保证寿命阈值时，这提示 Q 的
潜在应用价值是**减少高估与极端尾部，同时接受更多低估**。Q 本身没有显式优化非对称
风险；它不表示 Q 在每个参数区域都赢，也不表示 Q 的三个内部参数估计更准。

## “Q 自动传梯度”是什么

简单说，梯度就是：**要让损失下降，每个参数应往哪个方向改、改动信号有多强。**

Q 的计算链是：

```text
样本 → 神经网络 → β̂,η̂,γ̂ → x̂R → Q 损失
```

反向传播时，链式求导会自动计算“\(β,η,γ\) 分别变一点，\(x_R\) 会变多少”。对 \(x_R\) 影响大的方向
得到更强的目标信号，影响小的方向得到更弱的信号；三个参数对同一目标的共同作用也会一起进入
更新。这不是人工先定一组参数权重，也不是网络额外学了三个“神秘权重”，而是 \(x_R\) 公式和
链式求导在当前预测位置自然产生的更新信号。

更完整、带例子的说明见 [`docs/研究说明.md`](docs/研究说明.md)。

## 目录职责

| 目录 | 职责 |
|---|---|
| `docs/` | 当前说明、执行与证据入口 |
| `paper/` | 论文初稿、补充材料、引用审计 |
| `protocols/` | 冻结实验协议；机器配置仍在 `configs/` |
| `docs/reference/` | 需要复核公式时才读的技术长报告 |
| `code/study02pq/` | P/Q 实现、分析和测试 |
| `artifacts/pq_s5b_revision/` | 10-seed 主效应证据 |
| `artifacts/pq_paper_core/` | 当前论文机制派生证据 |
| `artifacts/pq_mechanism_closure/` | 机制数学验证与局部近似误差分解（manifest 与 SHA256SUMS 门已通过） |
| `artifacts/pq_target_matrix_pilot/` | 探索性 M95 静态局部代理消融 |
| `figures/pq-paper/` | 论文图 |
| `history/` | P/Q 旧协议、阶段报告和审稿过程 |
| `前置实验/` | Study02-A 前置研究，已关闭 |
| `已归档/` | 不再服务当前 P/Q 问题的旧 D 路线 |
