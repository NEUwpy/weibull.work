# Study/02 P–Q：缺口与审查记录

> 创建日期：2026-08-05；修订：2026-08-07（S0 R1 REVISE 修复 + **S0 APPROVE** + **S1 M1 证据封存**）
> 职责：已知缺口、已知限制、协议偏离、审查记录。审查后由 reviewer 追加。

## 0s0. S0 同分布主协议冻结候选（2026-08-06 提交；2026-08-07 R1 修复；**2026-08-07 S0 APPROVE**）

- **S0 已获 Codex + mentor 批准**（2026-08-07，tip `ca82ffb3`）：R1 四项 blocking 全关闭，
  final integrity（local=remote、worktree clean、40 passed、环境锁匹配）通过；S1 为独立排队
  任务，未收到 S1 任务消息不启动训练。

- 同分布主协议冻结候选：`09-PQ-同分布主协议冻结.md` + `configs/pq-iid-protocol-v1.json`
  （`PQ_PROTOCOL=iid-v1`）。冻结内容：RQ、estimand（模型级配对差值 Δ + 描述性 rel_change）、
  repeat-stratified 5-fold（180/60/60）、P/Q 等价控制、统计层级（fold×seed 交叉，
  B=200,000）、证据 schema（`artifacts/pq_iid_main/`）、M1–M3 矩阵与成本。
- **新增测试**：`code/study02pq/test_pq_iid.py`（11 项：配置选择/隔离、**未知协议值拒绝**、
  规模自洽、x_p 定义、完整 300-repeat 五折平衡、iid 配对与支持域），全套 **40 passed**。
- **R1 REVISE（2026-08-07）已响应**：S0-001 \(x_p\)=可靠度寿命点定义（09/配置/README）；
  S0-002 estimand 改为超总体式目标 \(\Delta^*\)、5-fold×3-seed 明确为有限 MC/CV 估计、
  bootstrap 明确为设计级经验不确定性近似（含覆盖限制）、修正加权表述（Δ 与 pooled MSE 差
  加权相同、rel_change 差别在函数形式）；S0-003 列出与 Study01 完全相同/有意不同清单；
  S0-004 `PQ_PROTOCOL` 只接受 v3/iid-v1、未知值 `ValueError`；成本依据修正（r4
  optimizer-train = 8160，非 9600）。
- **不启动**：未获 Codex + mentor 接受前不启动正式 120-fit 主实验。

**S0 已知缺口 / S1 前置项**：

- [x] 冻结候选文档、配置、测试已就位（含 R1 修复）。
- [x] run.py 已接入 iid 的 `split_strategy`/`split_repeat_fold` 与 `run_code_sha`
  （实现 commit `34f93b17`，S1 执行前置，S0 冻结未变更 run.py）。
- [x] 3-seed 对训练随机性的限制（协议内显式报告，`summary_iid.json` coverage_limitations；
  不擅自加 seed）。
- [ ] M3 精确范围（可靠度水平、容量、插值/OOD）在 S2→S3 门复查确认。

## 0s1. S1 主效应（iid-v1 M1）状态（2026-08-07 证据封存，待 Codex + mentor 审查）

- **完成**：iid 运行路径接线（run.py `split_strategy`/splits manifest 分派/`write_aggregates`
  协议分派 + RUN_CODE_SHA 绑定）与 analyze.py iid 分派；生产 smoke **SMOKE PASS (iid-v1)**；
  **46 tests passed**（v3 默认回归安全，refactor 后 v3 输出与 r4 sealed 逐字一致）。
- **正式运行**：120 fits（5 fold × 4 n × 3 seed × 2 route），**0 失败/0 支撑违规/0 非有限/
  0 非法/0 NaN**，总运行 1224 s；配对 60 对 all_match；证据键精确 dtype；splits manifest
  每 (n,fold) 恰 180/60/60（7200/2400/2400 样本）。
- **主结果**：pooled P 0.1663 / Q 0.1611，hat_Δ **−0.00171**（95% CI **[−0.00225, −0.00119]**
  不含 0），rel_change (Q−P)/P **−3.1%**。按 n/seed/fold 全部 Q 更优（按 n 的 CI 全部不含 0）。
- **区域异质性（描述性，需 M2 解释）**：低 β（1.5–3.0）与低 γ/η（0.1–0.25）为 Q 更优
  （β=1.5 −0.01464、γ/η=0.1 −0.01394）；高 β（3.5–5.0）与高 γ/η（0.5–1.0）为 **P 更优**
  （β=5.0 +0.00147、γ/η=0.5 +0.00546）。
- **复算与完整性与归档**：verify_iid.py 独立重算 **hat_Δ 与 CI 逐位一致**（rRMSE 差 float32
  累加伪影 ~1e-8 rel）；SHA256SUMS **249 项全验**；`git archive HEAD` 干净副本可复现。
- **协议偏离**：无实验偏离；M2/M3 未开始；未自行 APPROVE；未合并 main。
- **审查记录**：见 §4 待追加 S1 审查行。

## 0r4. r4 状态（direct-P primary，待 Codex 审核）

- 科学问题不变：direct-P vs Q 对 x0.95 的相对表现。
- **r4 合同**：P = approved direct 形式；共享 domain-explicit 解码器
  `γ̂=min(X)(δ+(1-2δ)sigmoid(o₃))`（Codex mailbox 000003 授权；结构性 0<γ̂<min(X)，
  依赖冻结正位置域）。
- **r4 preflight**：60 P-fits（3 seed × 20 cell）0 塌缩/0 支撑违规/0 非有限/0 边缘质量；
  Q 抽查正常；22 tests passed（含 frozen-grid representability、exact direct-P formula）。
- **r4 primary 120 fits**：0 失败/0 支撑违规/0 非有限/0 非法；配对 60 对 all_match。
- **r4 结果**：pooled P 0.2008 vs Q 0.2184，P 优约 8.1%（主推断 CI [+0.00223, +0.01268]
  不含 0），方向在 n/seed/fold 分层全部一致。
- 证据 schema 修复：键精确 dtype（beta/goe float64、n/rid int32），v2 evidence 无重训重封
  （120/120，与 master 逐行一致）。
- v2/P_loggap 标 sensitivity（`artifacts/pq_v2/`，不进入 r4 主结论）；v1 preliminary。

## 0. Codex R1 REVISE 响应状态

| 审核项 | 状态 |
|---|---|
| 1. 三参数合法化（γ̂<min(X)） | ✅ v2 location-scale decode + 支撑合法性 production test |
| 2. 统计推断（设计单元×seed 聚类） | ✅ 主推断 (n,fold)×seed 聚类；逐样本 bootstrap 降为次要；删 60-cell 显著数量 |
| 3. 证据可用性（压缩证据、fit metadata、manifest 绑定、SHA 无 missing） | ✅ evidence npz tracked / fit_metadata / manifest 完整绑定 / SHA256SUMS 只列 tracked |
| 4. 正式环境冻结 | ✅ `configs/pq-environment-v2.json`（专用 P-Q env 锁） |
| 5. 全量重跑（合法化改变合同） | ✅ v2 120 fits 完成（0 失败 / 0 支撑违规；不复用 v1；v1 保留 preliminary/superseded） |
| 6. 文档同步（Study02 + 根 README） | ✅ 根 README 纠 D 残留；Study02 文档 v2 |
| v1 状态 | `artifacts/pq/` 保留，preliminary/superseded，未删除/未覆盖 |

## 0.5 v2 结果要点（Codex R2 后）

pooled Q 优于 P 约 **−10.4%**（主推断 fold×seed 交叉 CI [−0.02026, −0.00466] 不含 0；
R2 修正后纳入 seed 重采样，与 Codex 独立 200k 验证一致）；方向在 n/seed/fold 分层全部
一致（n=15/20 显著、n=7/10 方向一致）；支撑违规 0。详见 `04-PQ-结果报告.md`。v1 结果
（−2.4%，n=20 反转）因 P 支撑违规塌缩污染，不再使用。

## 0.6 Codex R2 REVISE 响应状态（窄范围，未重训/未改模型/未碰 v1/未合并 main）

| 审核项 | 状态 |
|---|---|
| 主 bootstrap 未纳入 seed 不确定性 | ✅ 主推断改为 fold×seed 交叉重采样（按 n 分层抽 fold + 全局有放回抽 seed）；CI [−0.02026, −0.00466]（结论不变） |
| SHA 文本按 CRLF 工作树字节，干净副本不可移植 | ✅ SHA256SUMS 文本按 LF 规范化（匹配 git 存储字节）；`git archive HEAD` 干净副本 248/248 通过 |
| 三个分析产物未入 SHA/manifest | ✅ 已加入 SHA256SUMS 与 manifest（analysis_sha256） |
| run_all_seeds.log 被忽略，干净副本缺失 | ✅ 已显式纳入证据（`!run_all_seeds.log` 覆盖根 `*.log`，tracked，入 SHA 与 manifest） |
| manifest 未绑定分析产物 SHA | ✅ manifest 新增 analysis_sha256 与 run_all_seeds_log_sha256 |

## 1. 已知限制（设计内）

- 结论限于冻结设计域：β={1.5..5}、γ={100..1000}、η=1000、n={7,10,15,20}、三 seed。
- 纯仿真、无删失/截断；不涉及真实工程数据。
- 有量纲输入不具备单位不变性，不主张跨单位/跨尺度泛化。
- Q 以合法但可能退化的内部参数表示达到目标分位点（协议 §2 明确不主张参数准确）。
- 每个 `(n, fold)` 的 validation 行是 `(n, fold)` 的确定性函数（不随训练 seed 变化）；
  这与 sklearn 内部随机 val 切分不同，属协议规定，报告需说明。

## 2. 已知缺口（验证项状态）

- [x] 三 seed 配对结果（120 fits 完成；0 失败）。
- [x] tracked 压缩逐样本证据及正式分析产物的 SHA256 清单完整生成；gitignored 完整精度 predictions 不进入正式清单。
- [x] 120 fits 全部明确终态。
- [x] local==remote branch tip（推送时确认）。
- [x] 协议冻结先于 seed 42 结果（`01-PQ-冻结协议.md` FROZEN 2026-08-05）。
- [ ] Study01 同样本横向辅助比较（可选，未生成；见 §3）。

## 3. 协议偏离 / 跳过项

- **未生成 Study01 同样本辅助比较表**（任务 §七 允许“可额外生成”，且不得阻塞主实验）：
  Study01 E6 封存结果是 MDM 偏移量选择的 J1 指标，无直接匹配的 x0.95 结果；要派生需重建
  Study01 的 MDM 损失数据（分片 gitignore、本机不在盘），等价于重跑其数据生成，超出本任务
  边界。已跳过并记录。
- 未训练 Study01 final-dev 模型（P–Q 只做折评价；协议 §5.1 明示）。
- 共享数据分片（160 MDM chunks）本机不在盘，但原始样本按确定性契约重建并与 Study01
  正式流程同源（`run_E6b.build_raw_sample_map`）；分片 SHA 记录于 `data_sha256sums.txt`。
- 运行 `--seed 2026 --seed 3407` 时聚合文件只含当次 seed（实现已按设计在末尾以
  `--aggregate` 全 seed 重建；无数据丢失，120 fits 齐全）。

## 4. 审查记录

| 日期 | 审查者 | 结论 | 备注 |
|---|---|---|---|
| 2026-08-05 | Codex | R1 REVISE | 要求修复三参数合法化、统计推断、证据可用性与环境冻结，并全量重跑 v2。 |
| 2026-08-05 | Codex | R2 REVISE | 要求 fold×seed 交叉重采样及可移植 SHA/分析产物绑定；窄修订，不重训。 |
| 2026-08-05 | Codex | R3 APPROVE | 针对 v2/P_loggap（19 tests、200k 复算、git archive 248/248）。**已被 r4 direct-P 合同复核取代/撤回**，不得视为对 r4 的批准。 |
| 2026-08-05 | Codex | r4 REVISE | 主数值复现（P 0.20076/Q 0.21838，CI [0.00223,0.01268]）；6 项有界修复：manifest/generator v3、状态/逻辑文档、效应量分母、repair 显式 v2、解码器边界诊断、因果表述。不重训。 |
| 2026-08-07 | Codex + mentor | **S0 R1 REVISE** | 四项 blocking（S0-001 x_p 定义、S0-002 estimand/估计一致性、S0-003 对齐范围、S0-004 严格协议校验）+ 成本依据修正。只改 S0 文档/config/tests；修复已提交（40 tests passed），待复审。 |
| 2026-08-07 | Codex + mentor | **S0 APPROVE** | R1 四项全关闭（tip `ca82ffb3`）；final integrity：local=remote、worktree clean、40 passed、环境锁匹配、未启动 S1。S1 为独立排队任务。 |
