# 可信性验证系统规范

> 本文档是 [02-规则.md](02-规则.md) 第 3.2 节的细化规范。

## 概述

可信性验证（Verification，原案例展示）是预计算的验证可视化，用于：
1. **验证方法正确性**——复现论文结果
2. **自由探索**——放置其他验证内容

**特点**：预计算，页面形式灵活可定制。

## 目录结构

```
public/case-studies/{method}/verification-{paperId}/
├── config.md           # 配置文件（必需）
├── curves.json         # 曲线数据
├── data.csv            # 样本数据
├── results.csv         # 估计结果
└── summary.json        # 统计摘要
```

**命名规范**：`verification-{paperId}`，例如 `verification-182-046`

## 配置文件格式 (config.md)

```yaml
---
id: "verification-182-046"
name: "复现182-046: 基于统计最小差异原理的威布尔分布参数估计方法"
description: "复现论文图5：30个样本的∇(γ)-γ曲线"
paper:
  id: "182-046"                    # 文献ID，用于链接到图书馆
  title: "基于统计最小差异原理的威布尔分布参数估计方法"
  authors: "谢里阳, 朱文慧, 吴宁祥, 杨小玉"
  journal: "东北大学学报（自然科学版）"
  year: 2025
  figure: "图5"                    # 复现的图表编号
verification:
  trueParams:                      # 真实参数
    beta: 2.0
    eta: 1000
    gamma: 1000
  sampleSize: 7                    # 样本量
  offset: 0.1                      # 偏移量（MDM专用）
  nSamples: 30                     # 样本数
  paperImage: "/182-046-图片/xxx.jpg"  # 论文原图
  curvesData: "/case-studies/mdm/verification-182-046/curves.json"
  samplesData: "/case-studies/mdm/verification-182-046/data.csv"
  resultsData: "/case-studies/mdm/verification-182-046/results.csv"
  summaryData: "/case-studies/mdm/verification-182-046/summary.json"
---

# 复现论文 182-046 图5

## 图5 说明
...（Markdown 正文）
```

## 添加新验证（3步）

### 1. 创建目录和配置

```bash
mkdir public/case-studies/mdm/verification-182-xxx
```

创建 `config.md`（参考上述格式）

### 2. 创建数据文件

| 文件 | 格式 | 用途 |
|------|------|------|
| `curves.json` | JSON | 梯度曲线数据 |
| `data.csv` | CSV | 样本数据（sample_id, t₁, t₂, ...） |
| `results.csv` | CSV | 估计结果（sample_id, est_beta, est_eta, est_gamma, bias_*） |
| `summary.json` | JSON | 统计摘要 |

### 3. 验证

API 会自动发现新验证项，无需注册。

## 组件架构

```
src/components/methods/
├── CaseStudyViewer.tsx           # 通用入口组件
└── shared/verification/
    ├── types.ts                  # 类型定义
    ├── VerificationItem.tsx      # 验证项组件（左右对比布局）
    └── index.ts
```

### CaseStudyViewer

通用入口组件，自动：
- 从 API 加载验证列表
- 提供下拉选择器
- 根据选中项加载对应数据

### VerificationItem

单个验证项组件，布局结构：

```
┌─────────────────────────────────────────────────────┐
│ 参数条件（样本/真实参数/仿真设置）                    │
├──────────────────────┬──────────────────────────────┤
│  左栏：论文结果        │  右栏：系统复现               │
│  (图片/表格/文本)      │  (图表/表格)                 │
└──────────────────────┴──────────────────────────────┘
```

Props：
- `title`: 标题
- `params`: 参数条件数组
- `samplesData`: 可折叠的样本数据表格
- `paperContent`: 左栏内容（image/table/text）
- `systemContent`: 右栏内容（React 节点）

## API 路由

`src/app/api/case-studies/{methodId}/route.ts`

返回该方法的验证列表：

```json
{
  "cases": [
    {
      "id": "verification-182-046",
      "name": "复现182-046: ...",
      "paper": { ... },
      "verification": { ... }
    }
  ]
}
```

## 类型定义

核心类型见 `src/components/methods/shared/verification/types.ts`：

| 类型 | 用途 |
|------|------|
| `VerificationConfig` | 验证配置（从 config.md 解析） |
| `SampleData` | 样本数据 |
| `EstimateResult` | 估计结果 |
| `SampleCurve` | 梯度曲线数据 |
| `SummaryData` | 统计摘要 |

## 相关文件

- 前端组件：`src/components/methods/`
- 共享组件：`src/components/methods/shared/verification/`
- API 路由：`src/app/api/case-studies/{methodId}/route.ts`
- 数据文件：`public/case-studies/{method}/verification-{paperId}/`
