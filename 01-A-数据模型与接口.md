# 数据模型与接口

> 本文档是 [01-结构.md](01-结构.md) 的深化文档，定义核心数据模型与 API 接口。

---

## 1. 核心数据模型

### 1.1 前端类型 (TypeScript)

**WeibullResult** — 参数估计结果 (`src/lib/weibull.ts`):

```typescript
{
  beta: number | null       // 形状参数
  eta: number | null        // 尺度参数
  gamma: number             // 位置参数
  rSquared: number | null   // 拟合优度
  points: PlotPoint[]       // 概率图坐标
  converged?: boolean | string
}
```

**DataPoint** — 数据输入:

```typescript
{ id: number, value: number, status: 'F' | 'S' }  // F=失效, S=截尾
```

**DataSource** — 多曲线模式:

```typescript
{
  id: string, name: string, color: string,
  data: DataPoint[],
  result?: WeibullResult,
  sourceType: 'case' | 'group-subcase' | 'custom'
}
```

**CardData** — 计算器卡片 (`src/app/page.tsx`):

```typescript
{
  id: string, type: 'blank' | 'method' | 'chart' | 'data',
  data?: DataPoint[], result?: WeibullResult,
  methodId?: string, color: string,
  fitMode?: 'fit' | 'manual',   // fit=样本拟合, manual=手动参数
  is3P?: boolean,               // 2参数/3参数模式
  dataSources?: DataSource[]     // 多案例批量模式
}
```

### 1.2 后端类型 (Python)

**MethodResult** (`python/base.py`):

```python
@dataclass
class MethodResult:
    beta: float
    eta: float
    gamma: float
    r_squared: float
    converged: Union[bool, str] = True
    trace_data: Any = None
```

**WeibullBase** 基类提供:

| 成员 | 说明 |
|------|------|
| `self.data` | 排序后的正值数据 (np.array) |
| `self.n` | 样本量 |
| `self.trace_data` | 追踪数据 (trace=True 时收集) |
| `_median_ranks()` | Bernard / Exact 中位秩 |
| `_calculate_r2()` | R² 拟合优度 |
| `_cdf_3p()` | 三参数 Weibull CDF |
| `log_step()` | 追踪记录 (自动处理 numpy 类型转换) |

---

## 2. 后端 API 端点

| 端点 | 方法 | 用途 | 关键参数 |
|------|------|------|----------|
| `/calculate` | POST | 单次参数估计 | `method, data, trace?, offset?` |
| `/calculate_3d_surface` | POST | MDM 3D 曲面 | `method, data, trace_data?` |
| `/batch_simulation` | POST | 批量蒙特卡洛 (返回 CSV) | `method, true_beta, true_eta, sample_sizes, ...` |
| `/monte_carlo_simulate` | POST | 方法对比用蒙特卡洛 | `method, beta, eta, n, rep, seed, ...` |

### 2.1 请求/响应示例

```json
// POST /calculate
{ "method": "mle", "data": [100, 120, 150, 180, 200], "trace": true }

// Response
{ "beta": 2.5, "eta": 100, "gamma": 0, "rSquared": 0.98,
  "method": "mle", "converged": true, "trace_data": [...] }
```

### 2.2 方法解析规则

`resolve_method()` 在 `python/methods/registry.py` 中实现：

| 分类 | 数量 | 示例 |
|------|------|------|
| IMPLEMENTED | 11 个 | mle, mmle, mps, wmle, lse, mdm, lre, mm, pwm, grey, bayesian |
| ALIASES | 12 个 | wlse→lse, rrx→lre, rry→lre, mde→mdm, gm11→grey, gibbs→bayesian, ... |
| NOT_IMPLEMENTED | 8 个 | construct_stat, mve, lsf, ai, pso, svr, ann, ... |
| 未知方法 | — | 返回 400 错误 |

---

## 3. Next.js API Routes (BFF 层)

| 路由 | 用途 |
|------|------|
| `/api/algorithms` | 读取算法 Markdown 文档 |
| `/api/cases/*` | 案例 CRUD + 分组 + 树状结构 |
| `/api/method-flow/[methodId]` | 程序流程 JSON |
| `/api/studies/[methodId]/chunks` | 适用范围 chunk 文件列表 |
| `/api/studies/simulate` | 适用范围实时模拟 |
| `/api/content` | 通用内容服务 |
| `/api/chat` | AI 聊天 (OpenAI) |
| `/api/case-studies/{methodId}/*` | 可信性验证数据 |

---

## 4. 渲染管线

### Markdown 文档

```
.md 文件 → fetch → gray-matter (提取 YAML) → react-markdown
  ├─ remark: gfm, math
  └─ rehype: raw, slug, katex, autolink-headings
```

### 数学公式

- **算法文档**: `@step` / `@formula` 标记 → `method_flows/*.json` → VariableFlowViewer → KaTeX
- **Markdown 文档**: `$...$` / `$$...$$` → remark-math → rehype-katex → KaTeX (同步渲染，5s 超时保护)

### 图表

- **Plotly.js** — 3D 曲面、等高线、概率图
- **Recharts** — 2D 统计图 (箱型图、热力图、密度图)

### 流程图

- **Mermaid** — 异步渲染
