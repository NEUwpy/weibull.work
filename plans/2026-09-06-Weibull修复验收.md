# Weibull 整体审查修复验收（2026-09-06）

对应 [原审查 F01–F13](2026-09-05-Weibull整体审查.md)。F02–F13 已在本地修复；F01 已完成代码处理，旧密钥在服务商账户中的撤销仍待用户完成。当前不能宣称安全事项全部关闭或线上已经更新。

采用现有架构修复：统一后端估计与指标入口，前端负责展示和请求状态；没有重建研究框架。工作区包含其他任务正在修改的 Study01/02 文稿，本报告只验收本次代码与入口文档修复，不覆盖那些并行改动。验收时未提交、推送或部署；随后按用户要求补充 v3.00 更新日志并保存 Git 提交，提交不代表线上部署。

## 逐项结果

| 发现 | 本次处理 | 状态 |
|---|---|---|
| F01 密钥进入 Git | 删除配置中的密钥，改为服务端 `MOONSHOT_API_KEY`，补空模板和容器注入；缺失时返回 503，构建无需密钥；排除凭证文件进入镜像上下文 | 代码完成；旧密钥撤销未核验 |
| F02 文件读取越界 | 限制 slug/type，以目录文件清单和 realpath 检查阻止路径及外部符号链接 | 已修复 |
| F03 模拟失败后换算法 | 移除浏览器 RRX 回退，结果分析只接收后端原方法行记录及共享统计；错误明确显示 | 已修复 |
| F04 失败写成零 | CSV 缺失估计/Bias 留空，补 converged/status/error/sample_min；JSON 与汇总使用同一有效性判据 | 已修复 |
| F05 静默删观测 | 非有限值、非正观测明确拒绝，保留用户输入语义 | 已修复 |
| F06 旧请求覆盖新输入 | 卡片使用递增请求标识，修改输入/模式/方法或删除卡片即失效；批量逐组请求并校验当前身份 | 已修复 |
| F07 多 offset 仅加载第一项 | 分片请求展开所有维度组合；切换选择时取消旧请求并清空旧数据 | 已修复 |
| F08 同步阻塞及无界工作量 | 计算移至线程池，每进程同时最多两项；忙时 429；现场模拟每请求最多 1000 次估计，n 为 3–500 | 已修复 |
| F09 空实现注册 | PWM/Grey/Bayesian 移到未实现集合，别名同样返回 501；同步方法说明 | 已修复 |
| F10 架构说明滞后 | 更新 registry、失败策略、AI process-optimization 主路径；旧架构决定保留历史标记 | 已修复 |
| F11 研究版本/阶段偏移 | 根入口、Study 地图、规划、待办及路线图对齐当前项目入口；减少重复写死版本号 | 已修复 |
| F12 R04 待办滞后 | 已完成的传统比较、宽度/位置核验从待办移除；保留回退不继续、覆盖验证后置的当前边界 | 已修复 |
| F13 迁移链接失效 | 修正 Study02 前置研究和 AI 当前路线图入口 | 已修复 |

结果分析保留单一样本量、样本量范围、偏移量对比三种模式，分别展示 β/η/γ 的 Bias、SD、RMSE、MAE，以及有效数/总数/失败数。复用已有折线图和 KDE 组件，不再混合不同参数量纲的误差。模拟参数变化后清空旧结果，停止或失败时仅保留已完成组合。

## 验证

- `npm run check:app`：22 个方法缓存一致，44 项 Node 测试通过，TypeScript 检查通过。新增非交互检查入口；不将原来的 ESLint 首次配置向导算作检查通过。
- Python API/runner/simulation/MDM optimizer：76 项通过。
- Python sample/metrics/experiment/MDM 单一来源及六种传统方法回归：107 项通过。以上共 227 项测试，不重复累计复跑次数。
- 13 份当前入口文档的 55 个本地 Markdown 链接：无缺失；本次范围 `git diff --check` 通过。
- 浏览器本地实测：MDM n=7、重复 3 次，单组完成 1/1；δ=0.1、0.2 完成 2/2，各组独立展示；后端不可用时显示错误及完成 0/1，不产生替代统计。
- `npm run build`：exit 0，生产编译、类型校验、34 个静态页面生成及 standalone 打包完成。Browserslist 数据过期提示仍在，不影响本次构建通过。

实测补修了图表聚合导入触发 Plotly 服务端 `self` 错误的问题，改为直接导入实际使用的共享组件。测试仅执行小规模模拟及回归，没有重跑或改写正式研究实验。

## 尚未关闭的事项

旧 Moonshot 密钥可能仍存在于 Git 历史中；删除当前配置不等于撤销。用户需在服务商账户撤销旧密钥，并通过运行环境设置新密钥，操作说明见 [维护指南](../03-维护.md)。本次没有对外试用凭证，也未核验其当前有效性。

原审查附带记录的 Next.js 14.1.0 及依赖安全告警尚未通过框架升级消除；这是独立的兼容性升级工作，不计入 F02–F13 的修复结论。本地构建与测试不等同于线上验收。
