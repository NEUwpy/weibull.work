# 架构决策记录

> 本文档记录影响系统设计的关键架构决策。
> 是 [06-模块.md](06-模块.md) 的补充文档。

---

### AD-1: 前后端分离而非全栈框架

**背景**: 需要复杂的科学计算 (SciPy 优化器) 和丰富的可视化 (Plotly)。
**决策**: Python FastAPI (计算) + Next.js (展示)。
**后果**: 需要维护两套构建管线，但职责清晰，算法可独立测试。

### AD-2: Git-Based Flat File 而非数据库

**背景**: 案例数据量小 (100 篇级)，更新频率低，需要版本控制。
**决策**: 所有数据以 Markdown/CSV/JSON 文件存储在 Git 仓库中。
**后果**: 零运维数据库，数据随代码版本化，但不支持复杂查询。

### AD-3: 纯 React useState 而非全局状态管理

**背景**: 计算器页面状态集中在单个页面组件中。
**决策**: 不引入 Redux/Zustand 等，使用 `useState` + Props drilling。
**后果**: 简单直观，但 Calculator 页面组件较大 (~460 行)。

### AD-4: 方法注册表模式

**背景**: 8 大类 22 种方法需要统一管理和别名支持。
**决策**: `registry.py` 集中管理 IMPLEMENTED / ALIASES / NOT_IMPLEMENTED 三层映射。
**后果**: 添加新方法只需在字典中加一行，但别名关系需手动维护。

### AD-5: 算法 Fallback 到 WMLE

**背景**: 某些算法在特定数据条件下可能失败 (如 MLE 的无界问题)。
**决策**: 任何算法失败时自动降级到 WMLE。
**后果**: 用户始终能得到结果，但可能不是最优方法。

### AD-6: 预计算蒙特卡洛数据 (适用范围)

**背景**: 蒙特卡洛模拟耗时 (数千次迭代)，不适合现场计算。
**决策**: 预生成 CSV 分片文件，前端按需加载。
**后果**: 数据体积大但响应快，参数组合覆盖需提前规划。

### AD-7: Cloudflare Tunnel 部署

**背景**: NAS 无公网 IP，需要外网访问。
**决策**: Cloudflare Tunnel (cloudflared) 提供安全隧道。
**后果**: 零端口暴露，自动 HTTPS，但依赖 Cloudflare 服务可用性。

### AD-8: 统一蒙特卡洛流水线

**背景**: 旧蒙特卡洛流程"用一次写一次"，样本生成、方法调用、结果保存、指标聚合各不统一，横向比较不可信。
**决策**: 收束到 `python/studies/common/` 五模块：sample（确定性样本生成）→ runner（统一方法调用）→ simulation（现场蒙特卡洛）/ experiment（文件型实验）→ metrics（标准指标聚合）。文件型实验统一输出三件套：results.csv / summary.json / manifest.json。
**后果**: 所有研究脚本复用同一套流水线，结果可横向比较；manifest.json 提供完整溯源；但旧脚本需要迁移。

### AD-9: 预计算 + 现场计算双轨

**背景**: 蒙特卡洛模拟耗时（数千次迭代），不适合现场计算；但结果分析需要用户输入数据后实时反馈。
**决策**: 适用范围（Studies）走预计算，结果提前生成 CSV 分片文件，前端按需加载；结果分析（Analysis）走现场计算，用户输入后实时调用 `/monte_carlo_simulate` API。
**后果**: 适用范围响应快但参数组合需提前规划；结果分析灵活但受后端计算时间限制。

### AD-10: AI 模块与传统方法共用指标体系

**背景**: AI 模块（M1/M3）有独立的 MAE/MRE 口径，与传统方法的 Bias/SD/RMSE 口径不一致，无法横向比较。
**决策**: AI 方法必须产出能被 `aggregate_standard_metrics()` 处理的估计行，不能有独立指标体系。旧 AI 数据和旧口径标记为历史原型。
**后果**: 传统方法和 AI 方法可在同一参数空间、同一指标下比较；但旧 AI 页面和旧 public 数据需要迁移。
