# 文件结构

本文档描述项目的目录规范、数据流向及前后端架构。

> **系统模块定义**见 [06-模块.md](06-模块.md)，本文档聚焦物理文件组织。
> **数据模型与接口细节**见 [01-A-数据模型与接口.md](01-A-数据模型与接口.md)。

---

## 1. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      Internet                           │
└────────────────────────┬────────────────────────────────┘
                         │
                   Cloudflare CDN + WAF
                         │
                  Cloudflare Tunnel
                         │
        ┌────────────────┼────────────────┐
        │                                 │
   ┌────┴────┐                      ┌─────┴─────┐
   │ frontend│   REST API / JSON     │  backend  │
   │ Next.js │ ◄──────────────────► │  FastAPI  │
   │  :3000  │                      │   :8001   │
   └─────────┘                      └───────────┘
   展示 + 交互                      计算 + 算法
```

**分层架构**:
- **前端** — 展示层 (Pages) → 组件层 (Components) → 数据访问层 (Hooks + Lib)
- **后端** — API 路由层 (main.py) → 注册层 (registry.py) → 算法实现层 (methods/*.py) → 基础设施层 (base.py)

## 2. 职责边界

### 2.1 后端 (Python / FastAPI)

*   **职责**: 承担所有统计学核心计算。
    *   参数估计 (MLE, LRE, MDM, etc.)
    *   拟合优度计算 (R², Log-Likelihood)
    *   数值优化 (Hessian 矩阵, Nelder-Mead 迭代)
*   **入口**: `python/main.py` (Port: 8001)
*   **规范**: 任何涉及统计推断的逻辑严禁在前端实现。

### 2.2 前端 (TypeScript / Next.js)

*   **职责**: UI 交互、数据展示、图表渲染。
    *   **绘图**: 接收后端返回的分布参数 (β, η, γ)，生成 PDF/CDF 曲线坐标点。
    *   **交互**: 管理用户输入的原始数据，调用后端 API。
*   **入口**: `src/app/page.tsx` (Port: 3000)
*   **例外**: `src/lib/weibull.ts` 仅保留用于绘图平滑处理的数学函数（如 `generatePDFPoints`），不作为计算依据。

### 2.3 前后端数学逻辑分工

| 计算 | 执行位置 | 原因 |
|------|----------|------|
| 参数估计 (MLE, MDM等) | 后端 | 数值优化需要 SciPy |
| 中位秩 (Bernard) | 前端+后端 | 前端用于绘图，后端用于算法 |
| R² 计算 | 后端 | 算法内部需要 |
| PDF/CDF 生成 | 前端 | 仅用于绘图平滑 |
| RRX 线性回归 | 前端 | 快速初始估计/绘图参考 |
| 蒙特卡洛模拟 | 后端 | 批量数值计算 |

---

## 3. 数据层架构

系统不使用传统关系型数据库，采用 **Git-Based Flat File** 存储策略。

### 3.1 数据源映射

| 数据类型 | 存储格式 | 存储路径 | 读取方式 |
| :--- | :--- | :--- | :--- |
| **案例配置 (Cases)** | Markdown + YAML | `public/cases/[methodId]_caseX.md` | API: `/api/cases/[methodId]` 解析 front matter |
| **案例数据 (CSV)** | CSV | `public/cases/[methodId]_caseX_full.csv` | 前端直接 fetch 读取 |
| **算法文档 (Algorithms)** | Markdown + LaTeX | `src/content/algorithms/*.md` | API → gray-matter 解析 |
| **算法流程 (Flows)** | JSON | `src/data/method_flows/*.json` | 精确映射 UI 步骤与 Python 代码行 |
| **文献 (Library)** | Markdown | `src/content/*.md` | API → gray-matter 解析 |
| **适用范围数据** | CSV (chunks) | `public/studies/{method}/chunks/` | 前端按需 fetch |
| **AI 模块数据** | JSON / CSV | `public/ai/data/` | 前端直接 fetch |
| **蒙特卡洛实验产物** | CSV / JSON | `python/output/{experiment}/` | results.csv / summary.json / manifest.json 三件套 |

### 3.2 索引机制

*   算法的分类层级（树状结构）依赖 `src/data/methods.json` 作为静态索引。
*   案例列表通过遍历文件名动态生成。

---

## 4. 系统交互

### 4.1 计算请求流

```
POST /calculate { method, data, trace }
         │
         ▼
  resolve_method()              ← registry.py
  ├─ IMPLEMENTED → 返回算法类
  ├─ ALIASES     → 解析到已实现方法
  └─ 其他        → 400/501 错误
         │
         ▼
  AlgorithmClass(data).run(trace)
         │
         ▼
  _extract_result()
  ├─ MethodResult (dataclass)       ← 新格式
  └─ List [β, η, γ, R², converged] ← 旧格式兼容
         │
         ▼
  { beta, eta, gamma, rSquared, method, converged, trace_data }
```

**Fallback**: 算法执行失败时自动降级到 WMLE。

### 4.1b AI 请求流（旧原型，当前待重构）

> ⚠️ 以下端点为旧实现/原型，不作为当前正式 AI 模块的 API 契约。M1 关系建立当前状态为待重构。

```
POST /ai/relationship/mdm { data: List[float] }
         │
         ▼
  MLP 模型推理 (python/models/mdm_delta/)
         │
         ▼
  { optimal_delta: float, model_n: int, confidence?: str }

POST /ai/relationship/mdm/iterate { data: List[float] }
         │
         ▼
  N₁ 公共模型迭代逼近 (最多 10 步)
         │
         ▼
  { final_delta, final_params?: {beta,eta,gamma}, iterations, converged, convergence_reason }

POST /ai/direct-estimation { data: List[float], scheme: str = 'a1' }
         │
         ▼
  神经网络推理 (python/models/direct_estimation/)
         │
         ▼
  { beta: float, eta: float, gamma: float, model_n: int }
```

### 4.2 前端数据获取

```
组件
  ├─ 静态数据: import '@/data/methods.json'
  ├─ BFF 层:   fetch('/api/cases')              → Next.js API Route
  ├─ 后端直连:  fetch('api.weibull.work/calculate') → FastAPI
  └─ 静态文件:  fetch('/studies/mle/chunks/...')    → public 目录
```

---

## 5. 目录结构

```text
/
├── _archive/             # [忽略] 归档的废弃文件
├── python/               # [后端]
│   ├── main.py           # API 路由入口
│   ├── base.py           # WeibullBase 基类 + MethodResult
│   ├── tests/            # 测试脚本
│   ├── methods/          # 算法实现类 (继承自 WeibullBase)
│   │   ├── registry.py   # 方法注册表 (IMPLEMENTED / ALIASES / NOT_IMPLEMENTED)
│   │   └── *.py          # 各算法实现 (11 个)
│   ├── studies/          # 研究脚本与蒙特卡洛实验
│   │   ├── common/       # 统一流水线 (sample/runner/simulation/experiment/metrics)
│   │   ├── mdm/          # MDM 真值抽样研究 (full-v1 baseline)
│   │   ├── mdm_delta/    # MDM 偏移量研究
│   │   ├── direct_estimation/  # AI 直接估计研究
│   │   ├── mle/          # MLE 研究脚本
│   │   └── wmle/         # WMLE 研究脚本
│   └── models/           # 训练好的模型权重
│       ├── direct_estimation/
│       └── mdm_delta/
├── src/                  # [前端]
│   ├── app/              # Next.js 页面路由
│   │   ├── ai/           # AI 方法页面
│   │   │   ├── relationship/   # M1 关系建立 (R1 直接学习 / R2 迭代逼近)
│   │   │   ├── direct-estimation/  # M3 直接估计
│   │   │   └── optimization/   # M2 优化求解 (待开发)
│   │   ├── methods/      # 传统方法页面
│   │   ├── cases/        # 案例库页面
│   │   ├── library/      # 图书馆页面
│   │   └── help/         # 帮助页面 (manual/changelog/metrics/charts)
│   ├── components/       # UI 组件
│   │   ├── calculator/   # 计算器页面
│   │   ├── methods/      # 方法页面
│   │   │   ├── shared/   # 跨方法共享组件 (compare/studies/verification)
│   │   │   └── {method}/ # 各方法专用目录 (mdm, mle, wmle)
│   │   ├── ai/           # AI 专用图表组件 (charts/)
│   │   ├── chat/         # 对话组件 (ChatDialog)
│   │   ├── library/      # 图书馆页面
│   │   └── shared/       # 跨页面共享组件 (charts/)
│   ├── content/          # Markdown 内容库
│   ├── data/             # 结构化配置 (methods.json, flows)
│   ├── hooks/            # 自定义 Hooks
│   └── lib/              # 工具函数 (metrics.ts 等)
├── public/               # 静态资源
│   ├── ai/data/          # AI 模块前端数据 (JSON/CSV)
│   ├── cases/            # 案例数据
│   ├── studies/          # 适用范围预计算数据 (mdm/mle/wmle chunks)
│   └── *-图片/           # 文献图片
├── coworker/             # 多 agent 任务流转工作区
│   ├── plans/            # 任务计划
│   ├── handoffs/         # 分发记录
│   ├── reports/          # 执行报告
│   ├── reviews/          # 审查结论
│   └── templates/        # 模板
└── docs/                 # 文档
```

### 5.1 方法组件目录结构

每个方法在 `src/components/methods/{method}/` 下按职责组织：

```text
methods/mdm/
├── index.ts
├── visualizers/          # 计算过程可视化
├── charts/               # 方法专用图表
├── studies/              # 方法示例
└── case-studies/         # 可信性验证
    └── caseX/
        └── CaseXViewer.tsx
```

### 5.2 方法页面各部分分工

| 部分 | 职责 |
|------|------|
| **原理文档（默认）** | YAML front matter (简介/公式/变量) + 算法原理正文 |
| **程序流程 Tab** | 代码执行步骤与逻辑，公式与代码一一对应 |
| **计算过程 Tab** | 计算过程量展示（迭代中间值等） |
| **结果分析 Tab** | 结果统计信息 + 现场蒙特卡洛验证 |
| **适用范围 Tab** | 大范围预计算蒙特卡洛分析 |
| **可信性验证 Tab** | 论文复现验证的可视化内容 |
| **方法对比 Tab** | 横向对比不同方法表现（开发中） |
