# 部署与维护指南

## 技术路线

```
用户访问 weibull.work / api.weibull.work
              ↓
       Cloudflare CDN
              ↓
      Cloudflare Tunnel (cloudflared)
              ↓
    ┌─────────┴─────────┐
    ↓                   ↓
前端容器              后端容器
Next.js:3000         FastAPI:8001
```

**关键点：**
- 绿联 NAS 运行 Docker，无公网 IP
- Cloudflare Tunnel 提供外网访问 + HTTPS
- GitHub 托管代码，NAS 通过 git pull 更新

---

## 环境信息

| 项目 | 值 |
|------|-----|
| NAS IP | 192.168.31.148 |
| SSH 用户 | weipengyu |
| SSH 端口 | 22 |
| 项目路径 | /volume1/docker/weibull |
| GitHub | https://github.com/NEUwpy/weibull.work |
| 域名 | weibull.work |
| Tunnel ID | ec0767c7-11b9-4206-94ad-1ba1e2505490 |

---

## 项目构建结构（重要！）

NAS 上有两份 Dockerfile.backend，**只有 `python/` 下的会被使用**：

```
/volume1/docker/weibull/
├── docker-compose.yml          # 编排文件
├── Dockerfile.frontend         # 前端构建（build context: .）
├── Dockerfile.backend          # ⚠️ 根目录副本，不被 compose 使用
├── python/
│   ├── Dockerfile.backend      # ✅ 后端构建（compose 实际使用的）
│   ├── requirements.txt        # Python 依赖
│   ├── main.py                 # FastAPI 入口
│   └── ...                     # 后端代码
└── .env                        # Cloudflare Token（不在 git 中）
```

**docker-compose.yml 构建配置：**
- 前端：`context: .`，`dockerfile: Dockerfile.frontend`
- 后端：`context: ./python`，`dockerfile: Dockerfile.backend`

**因此：修改后端 Dockerfile 后，必须同步 `python/Dockerfile.backend`。**

---

## NAS 特殊网络配置

NAS 的 Docker 默认 bridge 网络无法访问外网（GitHub、PyPI 等）。所有需要在容器内访问外网的命令都必须加 `--network host`：

```bash
# ✅ 正确：加 --network host
sudo docker run --rm --network host -v /volume1/docker/weibull:/git alpine/git -C /git pull origin main

# ❌ 错误：默认 bridge 网络无法连接 GitHub
sudo docker run --rm -v /volume1/docker/weibull:/git alpine/git -C /git pull origin main
```

---

## 首次部署

### 1. Cloudflare 配置

#### 1.1 域名托管
1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
2. 添加站点 `weibull.work`
3. 到域名注册商修改 NS 为 Cloudflare 提供的地址：
   - `demi.ns.cloudflare.com`
   - `toby.ns.cloudflare.com`

#### 1.2 创建 Tunnel
1. **Zero Trust** → **Networks** → **Tunnels** → **Create a tunnel**
2. 选择 **Cloudflared** 连接方式，命名为 `weibull`
3. 选择 **Token** 认证方式，保存 Token

#### 1.3 配置 Public Hostname

| Subdomain | Domain | Path | Type | URL |
|-----------|--------|------|------|-----|
| (留空) | weibull.work | (留空) | HTTP | frontend:3000 |
| api | weibull.work | (留空) | HTTP | backend:8001 |

**注意：** Type 必须选 **HTTP**，URL 填容器名+端口

### 2. NAS 部署

```bash
# SSH 登录
ssh weipengyu@192.168.31.148

# 克隆项目（NAS 未安装 git，用 alpine/git 镜像）
sudo docker run --rm --network host -v /volume1/docker:/git alpine/git clone https://github.com/NEUwpy/weibull.work.git /git/weibull

# 创建 .env 文件（Token 在 docs/凭证信息.md 中）
echo 'CLOUDFLARE_TOKEN=<your-token>' > /volume1/docker/weibull/.env

# 启动（首次约 5 分钟）
cd /volume1/docker/weibull && sudo docker compose up -d --build

# 验证
sudo docker compose ps
```

### 3. 验证部署

- https://weibull.work → 前端页面
- https://api.weibull.work/docs → API 文档

---

## 日常更新（推荐流程）

### 一键更新命令

从本地开发机执行，完成：推送 → 拉取 → 重建：

```bash
# 1. 本地提交推送
git add -A && git commit -m "描述" && git push

# 2. NAS 拉取并重建
ssh weipengyu@192.168.31.148 "sudo docker run --rm --network host -v /volume1/docker/weibull:/git alpine/git -C /git stash && sudo docker run --rm --network host -v /volume1/docker/weibull:/git alpine/git -C /git pull origin main && cd /volume1/docker/weibull && sudo docker compose up -d --build"
```

**注意事项：**
- NAS 未安装 git，使用 `alpine/git` Docker 镜像执行 git 命令
- **必须**加 `--network host`（NAS bridge 网络无法访问外网）
- `git stash` 保存本地修改，避免冲突
- `docker compose up -d --build` 只重建有变更的容器

### 分步执行

```bash
# 1. 本地推送
git add -A && git commit -m "描述" && git push

# 2. SSH 到 NAS
ssh weipengyu@192.168.31.148

# 3. 拉取代码
sudo docker run --rm --network host -v /volume1/docker/weibull:/git alpine/git -C /git stash
sudo docker run --rm --network host -v /volume1/docker/weibull:/git alpine/git -C /git pull origin main

# 4. 重建并启动
cd /volume1/docker/weibull && sudo docker compose up -d --build
```

### 只更新前端/后端

```bash
sudo docker compose up -d --build frontend  # 只重建前端
sudo docker compose up -d --build backend   # 只重建后端
```

---

## 部署钩子：更新版本日志

**每次部署前**，必须更新 `08-更新日志.md`：

1. **提取增量 commits**：读取 `08-更新日志.md` 头部最新日期，从该日期之后提取 git 记录
2. **按天分组总结**：同一天的 commits 合并为 1-3 条简练描述
3. **追加到日志头部**：按格式 `## v{版本号}-YYMMDD (YYYY-MM-DD)` 添加版本块
4. **提交变更**：版本号由 `src/lib/config.ts` 从日志头部自动解析

**版本号规则：**
- 格式：`v{主}.{次}-YYMMDD`
- 功能更新递增次版本号（v1.01 → v1.02）
- 重大架构变更递增主版本号（v1.02 → v2.00）

---

## NAS 上的非 Git 文件

以下文件存在于 NAS 但不在 Git 仓库中，**git pull 不会覆盖它们**，重建时需注意：

| 文件 | 用途 | 恢复方式 |
|------|------|----------|
| `.env` | Cloudflare Tunnel Token | `echo 'CLOUDFLARE_TOKEN=<token>' > .env` |
| `python/Dockerfile.backend` | 后端构建文件（git 管理根目录副本，NAS 上需手动同步） | git pull 已包含，但如手动修改需注意 |

**如果 NAS 上 `.env` 丢失**，cloudflared 容器会反复重启。检查：
```bash
cat /volume1/docker/weibull/.env  # 应包含 CLOUDFLARE_TOKEN=eyJ...
```

---

## 常用命令

| 命令 | 作用 |
|------|------|
| `sudo docker compose ps` | 查看容器状态 |
| `sudo docker compose logs -f` | 查看所有日志 |
| `sudo docker compose logs -f frontend` | 查看前端日志 |
| `sudo docker compose logs -f backend` | 查看后端日志 |
| `sudo docker compose logs -f cloudflared` | 查看 Tunnel 日志 |
| `sudo docker compose restart` | 重启所有服务 |
| `sudo docker compose down` | 停止所有服务 |

---

## 故障排查

### 常见错误

| 现象 | 原因 | 解决方法 |
|------|------|----------|
| cloudflared 反复重启 | `.env` 缺失或 Token 错误 | 创建 `.env` 文件，写入正确的 `CLOUDFLARE_TOKEN` |
| git pull 超时 | Docker 默认 bridge 网络问题 | 命令加 `--network host` |
| pip install 超时/找不到包 | 网络不稳定或镜像源异常 | 检查 `python/Dockerfile.backend`，必要时换源重试 |
| 前端编译失败 | TypeScript 类型错误 | 本地先 `npm run build` 验证，修复后再部署 |
| 1016 错误 | DNS 无法解析 | 检查 DNS 记录 Target |
| 530 错误 | Tunnel 无法连接到源 | 检查 Public Hostname 配置 |
| 502 错误 | 源服务未运行 | 检查 frontend/backend 容器状态 |

### 排查步骤

```bash
# 1. 检查容器状态（所有三个应 Up）
sudo docker ps -a

# 2. 查看 cloudflared 日志
sudo docker logs weibull-cloudflared --tail 50

# 3. 测试本地服务
curl -sI http://localhost:3000
curl -sI http://localhost:8001/docs

# 4. 检查 .env 文件
cat /volume1/docker/weibull/.env
```

### 从本地诊断

```bash
nslookup weibull.work
curl -sI https://weibull.work
curl -sI https://api.weibull.work/docs
```

---

## 安全措施

| 关注点 | 实现方式 |
|--------|----------|
| HTTPS | Cloudflare 自动终止 |
| CORS | 白名单: weibull.work, localhost:3000 |
| DDoS 防护 | Cloudflare WAF |
| 容器安全 | 非root用户运行前端 (nextjs uid 1001) |
| 凭证管理 | `.env` 和 `docs/凭证信息.md` 均不提交到 Git |

---

## 配置文件说明

| 文件 | 用途 |
|------|------|
| `docker-compose.yml` | 容器编排 |
| `Dockerfile.frontend` | Next.js 镜像构建（build context: `.`） |
| `python/Dockerfile.backend` | Python 镜像构建（build context: `./python`）**← 实际使用的** |
| `Dockerfile.backend` | 根目录副本（git 管理，需同步到 `python/`） |
| `next.config.js` | Next.js standalone 输出配置 |

---

## 架构图

```
┌─────────────────────────────────────────────────────────┐
│                      Internet                           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Cloudflare CDN + WAF                       │
│              (HTTPS 终止、DDoS 防护)                     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼ (Cloudflare Tunnel)
┌─────────────────────────────────────────────────────────┐
│              绿联 NAS (192.168.31.148)                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Docker Network                      │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────┐ │   │
│  │  │ cloudflared │  │  frontend   │  │ backend │ │   │
│  │  │   (Tunnel)  │──│  Next.js    │──│ FastAPI │ │   │
│  │  │  host网络   │  │   :3000     │  │  :8001  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────┘ │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```
