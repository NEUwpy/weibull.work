# 部署与维护指南

## 技术路线

```
用户访问 weibull.work / api.weibull.work
              ↓
       Cloudflare CDN + HTTPS
              ↓
      Cloudflare Tunnel (cloudflared)
              ↓
    ┌─────────┴─────────┐
    ↓                   ↓
前端容器              后端容器
Next.js:3000         FastAPI:8001
```

- 绿联 NAS 运行 Docker，无公网 IP
- Cloudflare Tunnel 提供外网访问 + HTTPS
- 代码在 GitHub，NAS 通过 git pull 更新

---

## 容器架构

### 实际运行容器（注意：不止 docker-compose.yml 里的三个）

| 容器 | 来源 | 作用 |
|------|------|------|
| `weibull-frontend` | `/volume1/docker/weibull` | 新环境前端 |
| `weibull-backend` | `/volume1/docker/weibull` | 新环境后端 |
| `weibull-cloudflared` | `/volume1/docker/weibull` | 新环境 Tunnel（当前未使用） |
| `cloudflared` | `/volume1/docker/weibull_old` | **实际在用**：前端 Tunnel |
| `cloudflared-api` | `/volume1/docker/weibull_old` | **实际在用**：API Tunnel |

> 旧环境 `weibull_old` 的 cloudflared 容器仍在使用有效 Token 运行。新环境的 `weibull-cloudflared` Token 已过期，暂未使用。

### 项目构建结构

```
/volume1/docker/weibull/
├── docker-compose.yml          # 编排文件
├── Dockerfile.frontend         # 前端构建（context: .）
├── python/
│   ├── Dockerfile.backend      # ✅ 后端构建（实际使用）
│   ├── requirements.txt        # Python 依赖
│   └── main.py                 # FastAPI 入口
└── .env                        # Cloudflare Token（git 忽略）
```

**前端 `getApiBaseUrl()` 逻辑：**
- 开发环境 → `http://localhost:8001`
- 生产环境 → `https://api.weibull.work`（走 Cloudflare Tunnel）

---

## 日常更新流程

详见 `deploy-nas` skill（Claude Code 中说"部署到 NAS"触发）。

### 手动执行

```bash
# 1. NAS 拉取代码
ssh weipengyu@192.168.31.148 "sudo docker run --rm --network host -v /volume1/docker/weibull:/git -v ~/.ssh:/root/.ssh:ro alpine/git -C /git pull origin main"

# 2. 重建容器
ssh weipengyu@192.168.31.148 "cd /volume1/docker/weibull && sudo docker compose up -d --build"

# 3. 验证
ssh weipengyu@192.168.31.148 "sudo docker compose -f /volume1/docker/weibull/docker-compose.yml ps"
```

### 部署钩子：更新版本日志

每次部署前更新 `08-更新日志.md`，按格式 `## v{版本号}-YYMMDD` 追加版本块。

---

## 踩坑清单

### 1. NAS IP 是 DHCP 动态分配

路由器可能给 NAS 换 IP。部署前先 ping `192.168.31.148`，不通则去路由器 `192.168.31.1` 查终端列表。

### 2. GitHub HTTPS 被 GFW 阻断

NAS 上 git remote 必须用 SSH（`git@github.com:...`），HTTPS 会报 `SSL unexpected EOF`。

NAS 没装 git，必须通过 alpine/git Docker 容器操作，且**必须加 `--network host`**。

### 3. pip 下载慢 → 清华镜像

`python/Dockerfile.backend` 已配置 `pypi.tuna.tsinghua.edu.cn`，pip 安装快 10 倍。

如果清华源不稳定，改回默认：删除 Dockerfile 中的 `pip config set` 行。

### 4. Tunnel origin URL 不能用 localhost

Cloudflare Tunnel Public Hostname 的 URL 必须写 `127.0.0.1:8001`，不能写 `localhost:8001`。

原因：`localhost` 在某些系统上优先解析为 IPv6 `[::1]`，而后端只监听 IPv4 `0.0.0.0`，导致 `connection refused`。

### 5. Token 与 Tunnel 一一对应

Cloudflare Dashboard 上每个 Tunnel 有自己的 Token。如果建了新 Tunnel，必须更新 NAS 上对应 cloudflared 容器的 `.env` 并重启。

Token 嵌入在 `.env` 中，格式：`CLOUDFLARE_TOKEN=eyJ...`

### 6. CDN 缓存会骗人

Cloudflare CDN 可能缓存前端静态页面。即使 Tunnel 挂了，浏览器里仍然能看到旧页面。用 `curl` 或者无痕窗口 + F12 Network 面板确认真实连通性。

---

## 故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| "Failed to fetch" | API Tunnel 不通 | 检查 cloudflared-api 容器 + Tunnel origin URL 是 `127.0.0.1:8001` |
| 前端能看但 API 报错 | CDN 缓存 + API Tunnel 挂了 | 查 `sudo docker logs cloudflared-api` |
| Tunnel `Unauthorized` | Token 过期/Tunnel 已删 | Cloudflare Dashboard 重新生成 Token |
| git pull 超时 | 没用 --network host | 所有 alpine/git 命令加 --network host |
| git pull SSL 错误 | 用了 HTTPS remote | 改用 SSH：`git@github.com:NEUwpy/weibull.work.git` |
| 后端 Restarting | 新依赖未安装 | 查 `docker logs weibull-backend`，ModuleNotFoundError → 加 requirements.txt |
| 后端 `connection refused` | IPv6 问题 | Tunnel origin 用 `127.0.0.1` 而非 `localhost` |

### 诊断命令

```bash
# 容器状态
sudo docker ps -a --format '{{.Names}} {{.Status}}'

# cloudflared 日志
sudo docker logs cloudflared --tail 20
sudo docker logs cloudflared-api --tail 20

# 后端日志
sudo docker logs weibull-backend --tail 20

# 测试本地后端
curl -sI http://localhost:8001/docs
```

---

## 配置文件说明

| 文件 | 用途 |
|------|------|
| `docker-compose.yml` | 容器编排 |
| `Dockerfile.frontend` | Next.js 构建 |
| `python/Dockerfile.backend` | Python 构建（实际使用） |
| `python/requirements.txt` | Python 依赖（含 torch） |
| `.env` | Cloudflare Token（git 忽略，见 `docs/凭证信息.md`） |
| `next.config.js` | Next.js standalone 输出配置 |
