# 适用范围系统规范

> 本文档描述适用范围（Studies）系统的实现规范。

## 概述

适用范围用于展示参数估计方法在不同参数组合下的蒙特卡洛仿真结果，帮助用户了解方法在不同条件下的表现规律。

## 目录结构

```
public/studies/{method}/chunks/
├── b2_e1000_g1000_n7_d0.1_rep1000_seed42_step60.csv
├── b2_e1000_g1000_n10_d0.1_rep1000_seed42_step60.csv
└── ...
```

## 分片文件命名规范

分片文件名格式：`b{beta}_e{eta}_g{gamma}_n{n}_d{d}_rep{rep}_seed{seed}_step{step}.csv`

| 参数 | 简写 | 含义 | 格式化规则 |
|------|------|------|-----------|
| beta | b | 形状参数 | 整数或小数，如 `b2`, `b1.5` |
| eta | e | 尺度参数 | 整数，如 `e200`, `e1000` |
| gamma | g | 位置参数 | 整数，如 `g1000` |
| n | n | 样本量 | 整数，如 `n7`, `n10` |
| d | d | 偏移量 | 保留小数，如 `d0`, `d0.1` |
| rep | rep | 蒙特卡洛重复次数 | 整数，如 `rep1000` |
| seed | seed | 随机种子 | 整数，如 `seed42` |
| step | step | 迭代步长 | 整数，如 `step60` |

示例：`b2_e1000_g1000_n7_d0.1_rep1000_seed42_step60.csv`

## 分片CSV格式

每个分片包含特定参数组合下的所有模拟结果：

```csv
beta_true,eta_true,sample_size,offset_value,sim_id,est_beta,est_eta,est_gamma,bias_beta,bias_eta,bias_gamma,r_squared
2.0,1000,7,0.1,1,1.987,1012.3,998.5,-0.013,12.3,-1.5,0.98
2.0,1000,7,0.1,2,2.045,989.2,1001.2,0.045,-10.8,1.2,0.97
...
```

**列说明**：
- `beta_true`, `eta_true`: 真实参数值
- `sample_size`: 样本量
- `offset_value`: 偏移量
- `sim_id`: 模拟编号（1 到 rep）
- `est_beta`, `est_eta`, `est_gamma`: 估计值（NaN 表示无解）
- `bias_beta`, `bias_eta`, `bias_gamma`: 偏差 = 估计值 - 真实值
- `r_squared`: 拟合优度

## 前端组件结构

### 参数配置

组件采用硬编码方式定义参数：

```typescript
// 参数定义
const PARAM_DEFINITIONS = [
  { id: 'beta', name: '形状参数', symbol: 'β', chunkKey: 'beta', isVariable: true },
  { id: 'eta', name: '尺度参数', symbol: 'η', chunkKey: 'eta', isVariable: true },
  { id: 'gamma', name: '位置参数', symbol: 'γ', chunkKey: 'gamma', isVariable: false },
  { id: 'sampleSize', name: '样本量', symbol: 'n', chunkKey: 'n', isVariable: true },
  { id: 'process', name: '偏移量', symbol: 'δ', chunkKey: 'd', isVariable: true }
]

// 仿真配置
const SIM_CONFIG_DEFINITIONS = [
  { id: 'rep', name: '重复次数', symbol: 'rep', chunkKey: 'rep' },
  { id: 'seed', name: '随机种子', symbol: 'seed', chunkKey: 'seed' }
]

// 计算配置
const CALC_CONFIG_DEFINITIONS = [
  { id: 'step', name: '迭代步长', symbol: 'step', chunkKey: 'step' }
]
```

### Tab 示例配置

每个 Tab 代表一个预设的研究场景：

```typescript
const STUDY_TABS = [
  {
    id: 'demo1',
    name: '示例1: 参数研究',
    description: '研究不同形状参数、尺度参数、样本量、偏移量对估计结果的影响',
    defaultVariables: ['beta', 'sampleSize']
  },
  {
    id: 'demo2',
    name: '示例2: 仿真研究',
    description: '研究不同蒙特卡洛重复次数下估计值的收敛特性',
    defaultVariables: ['sampleSize', 'rep']
  },
  {
    id: 'demo3',
    name: '示例3: 计算研究',
    description: '研究不同迭代步长对计算精度和收敛的影响',
    defaultVariables: ['sampleSize', 'step']
  }
]
```

### 变量维度选择

- 最多同时选择 **2 个变量维度**
- 变量维度决定图表类型：
  - 单变量 → 箱型图
  - 双变量 → 热力图
- 非变量维度使用固定值

### 三色边框逻辑

参数值标签使用三色边框表示状态：

| 颜色 | 含义 | 说明 |
|------|------|------|
| 红色 | 当前选中 | 该值已被选中 |
| 绿色 | 可选择 | 存在对应的分片文件，可点击选择 |
| 白色 | 不可选择 | 不存在对应的分片文件 |

## 页面布局

1. **Tab 导航** - 切换不同研究示例
2. **参数配置** - 选择变量维度和参数值
3. **仿真配置** - 设置蒙特卡洛参数
4. **计算配置** - 设置计算参数
5. **统计结果表格** - 参数估计汇总统计
6. **可视化图表** - 箱型图/热力图/密度图
7. **数据来源表格** - 显示当前统计所用的分片信息

## 数据来源表格

页面底部显示当前统计所用的分片信息：

| 列 | 说明 |
|----|------|
| # | 序号 |
| 文件名 | 分片文件名 |
| 数据规模 | 行数（加载失败显示"加载失败"） |

表格只显示当前参数选择对应的分片，不包括其他分片。

## API 接口

### 获取分片列表

```
GET /api/studies/{methodId}/chunks
```

返回：
```json
{
  "chunks": ["b2_e1000_g1000_n7_d0.1_rep1000_seed42_step60.csv", ...],
  "parsedParams": {
    "beta": [1.5, 2.0, 3, 5, 7],
    "eta": [200, 1000, 5000],
    "n": [3, 5, 7, 10, 20, 30],
    ...
  },
  "total": 150
}
```

### 加载分片数据

```
GET /studies/{methodId}/chunks/{filename}
```

返回原始 CSV 数据。

## 相关文件

- 前端组件：`src/components/methods/{method}/studies/{Method}StudyViewer.tsx`
- API 路由：`src/app/api/studies/[methodId]/chunks/route.ts`
- 分片数据：`public/studies/{method}/chunks/`
- 共享图表：`src/components/shared/charts/`
