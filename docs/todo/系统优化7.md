 看一下gitstatus状态，我之前保存了的git状态弄好了MDM方法，我想推广到所有方法，通过统一组件+方法特殊组件的方式，不仅MDM方法的还是原来这样，  其它所有方法也都是新的状态。如果其它方法不加特殊组件，就是少了一个方法过程量的卡片，其它的应该都是一样的。但是我发现之前的改动（目前未git的  操作）没有做好，甚至原MDM的都改的有些不对劲了，因此需要你检查一下

我现在想完善参数估计方法的方法对比tab，是这样想的，就是  适用范围有，那很自然的，可以对比不同方法的估计效果，在一 个方法的tab内，直接选择1-2个其它方法就行。然后展示效果可 以是竖着分2-3栏。但是有个问题，就是怎么选择参数，这个参  数应该是这2-3个方法共同有的吧。我想把适用范围那个参数、  模拟、计算设置给搬过来，然后可以选择参数，如果这个参数不 
是三个共有的，那就底下有个计算按钮，点一下按照选择的设置 
计算。这个是单选的情况下。如果是多选，那需要这2-3个方法  
已经有的适用范围分析，相当于取交集，多选是这样的，只提供 
单变量的分析，点击后自动提取这个变量的交集，但是一定要是 
剩下的固定参数是一样的组合，如果有好几种组合，那可以设置 
一个小箭头，左右箭头，来切换。现在对于这个开发还有什么疑 
问？

1. 优先适用已有的chunk
2. 单选是固定的一组参数以及计算、模拟设置，多选是这三个设置的一个维度选多个参数，例如想看不同n的计算结果对比，那就是点击设为变量，然后这时候可以选的还是绿色，但是没有交集的变为白色，然后可以全选，选中了还是变为红色，（全选是指有个按钮，这个逻辑完全和适用范围一样），也可以手动选。但是这个和适用范围有一个区别，就是适用范围是没有选择的时候就是有绿的有白的，但是由于可以单选后计算，所以都变成绿的比较好。
3. 对
4. A
5. 和适用范围一样，可以选择


本来有数据的地方，第一次点击的时候显示暂无数据，开始模 拟也不能点，但是我点击完别的再点回来，又有数据了。另外， 需要跟适用范围一样显示各个方法的调用的数据表格  