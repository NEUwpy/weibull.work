我后面打算加入人工智能方法栏，把目前的四大部分变为   五大部分，但是我想先跟你讨论清楚细节，像像是色块起稿一样。一层一层完善、清晰。而不是一下子帮我。把细节全给写清楚，因为这细节我不一定认同。C:\Web\Weibull\docs\todo\人工智能方法规划.md帮我把。每回更新的规划都统一放在这里。然后咱俩经过多轮的对话，最终完善这个规划，然后再开始进行。现在第一轮对话有两个事，第一个就是我发现现在四大部分，包括参数估计方法里头有很多小部分，导致整个代码已经非常的庞大了，那现在经常会几轮对话，就用光上下文窗口，那我如果再加一个的话，会不会直接就超过了能维护的范围？第二个事就是这一个人工智能方法，其实我是想也包括几部分的，一个就是人工智能方法当中的智能优化算法部分，这个是可以和这个产生固定方法当中的优化的这部分来进行交叉，相当于用这些优化来替代参数估计方法当中的优化，然后看效果。这是第一种，然后做的应该也跟参数估计方法当中那么些方法的那个感觉差不多，就是属于方法里头还能再点击进去，然后看一些设置，看一些这个方法里头有一些 Tab。另外一个就是神经网络来达到一些效果，例如说可以是从这个也有两方面，一方面就是从样本直接通过神经网络到参数之前训练，然后后边那个导参数，这个就是其中的包括用什么训练，还有啥这些都也有区别。再一个就是。把这个参数固定方法当中的这个过程量，例如找这些最优的过程量，例如说最好最小差异法当中的那个偏移量设置0.1，但是这个不是说最佳的偏移量，肯定有一个最好的，能让它的偏差达到最小，让它的这个波动估计出来的波动性也最小，但是不知道是多少。所以说需要做一个用神经网络来做一些映射，这个是神经网络的部分的。


智能优化算法就是作为现有方法的优化器选项。神经网络这个数据来源应该是重新弄一个，就是。也不能是重新弄，反正就是针对一个事情弄一个训练的生成样本，生成数据。那这个程序，然后再根据这个程序生成的数据来训练。为啥要整这个人工智能方法？做一个单独的一个，其实就是现在这个研究相当于矩阵式，就是研究一通这个参数估计方法，然后再研究一通人工智能方法，然后他俩就是后一交叉，那就能出来特别多的这个。像矩阵式的吗？但是如果说把哪个作为哪个的子部分的话，那可能也存在问题，我想的就是参数估计方法，这个作为传统的，然后这边这个就是比如说一个这个人工智能方法，就比如是一个什么什么优化方法，它能对应着很多的方法，然后也可以来用这个来。然后最后在这里还能看出来传统和优化这种比较是这样