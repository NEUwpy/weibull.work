针对参数估计方法子页面的方法示例子页面的通用框架，有一个优化的地方，就是参数配置部分不是有五个卡片吗，变量卡片中间有变量值，原则是被选择了被选中的白色部分变成红色，如果是整个维度被选中了，例如形状参数选中了，那就那几个白色部分都变红。同时呢，如果选择了1-2个变量，那其它的不也是有值吗，那那个量当时的值也要变红。总之原则就是虽然说这些量是变量，但是如果是变量的时候那就哪几个量变哪几个量变红，没轮到他变的变量就把其它人变的时候他是多少给标上。



我发现现在这种生成的方式完全就是在一个data.csv里，这样带来了一个问题：如果我想新增分析，例如我想新增β=1.7的，那是不是需要重新运行一整个生成的程序？还是说是否可以拆分生成然后合并运行，要拆分成什么样呢？一个参数组合就一个csv?这个帮我想一想规划一下。


我想做到

我之前进行了案例库的单选和多选逻辑，以及威布尔计算器的多选叠     
  加显示，但是现在参数估计子方法的计算过程子页面的多选叠加逻辑 
  没有实现，帮我完善。以下是我之前的提示词



现在点击案例库按钮已经有单选多选了，也有确认按钮，但是多选的     
  不能写入到分析卡片的样本框里，是因为设计的时候就是没有考虑到  
  会一次叠加分析很多，但是在威布尔计算器里写了叠加对比分析的逻       辑，我认为可以把样本多选定义为一种逻辑，就是在样本栏里，用实     
  线隔开，然后右下角的N=7可以变成N=7×n，例如7×30，这样就能从样       本栏这种底层入手，导入多个案例的话就变成了导入带---的样本，      
  对了，样本栏的语法就变成了---代表横线了。另外，现在样本不是      
  得回车吗，可以是样本之间是；，和空格，回车，都算是正确语法。
这个叠加机制和 overlayCurves                                     
                 
  机制叠加其他曲线是区分开的，当然，可以用后者机制继续往里叠加
其实就是有几个---就多算几个，都在一个图像内显示，并且坐标需      
  要跟着最大的走，这样就都能正常显示了     
方法详情页那块，计算过程子页面，不是通过参数估计可以得到过程     
  量吗，那过程量也应该适应多选，就是有几个多选就显示几个

在参数估计方法子页面的计算过程子页面，我多选了之后还是只显示     
  一个样本的估计结果、图像和寻优过程可视化，我要求寻优过程可视  
  化中的原始视图也是有几个样本就都叠加上
C:\Web\Weibull\docs\     
  任务单-计算器多案例叠加显示.md这个是任务单，先修改任务单，然     
  后用任务单控制任务来完成       


计算过程页面完全没有必要一个方法写一个计算卡片，而是计算卡片     
  这里直接调用计算器的卡片组件就可以了
多选后自动触发批量计算是不对的，应该是点击参数估计按钮触发
为什么不用计算器页面的分析卡片作为组件，然后计算过程和结果分     
  析俩板块共用，不是已经在计算器里面写好了吗我是指的是方法详情页的两个子页面不都用分析卡片吗，而计算器分 
  析卡片的逻辑已经写的正确了，那就应该把计算器的分析卡片写成个     
  组件，这三个地方共同调用就行 现在计算器那里的组件是没有问题的，就按照现在的那个来就行 把计算器分析卡片的逻辑（状态管理 +
  事件处理）提取成一个可复用的组件/钩子，让计算器页面和方法详情页的
  两个子页面共同调用。
先跟我说说你的理解你打算怎么干我确定了之后再干

就是很简单的事情，第一个，案例库点击多选然后点击确定后，会把     
  选择的值都按照样本---样本的形式传入卡片，卡片的样本栏应该也   
  是正常显示，然后根据这里有几个样本，就根据选择的方法算几次。       算完一起显示在图像上。现在威布尔计算器的卡片已经实现了这个功     
  能，但是各个方法的计算过程中不也是需要传入案例然后点击参数估       计得到估计结果和估计过程量吗，但是我发现并没有在样本栏正确显     
  示，并且其它的更没有了。但是这里就存在问题了，因为正确的应该     
  是参数估计方法子页面的卡片跟威布尔计算器中的分析卡片组件用的     
  是一样的组件，那就不应该在操作上有差异，应该一模一样才对。第     
  二， 例如计算过程那主要显示的不是计算过程量吗，这个过程量其      
  实就是根据传入的样本，有几组算几次，然后全部显示在一个图里，     
  就相当于原先的只显示一个，现在的是显示曲线组，就是这样



**我之前在弄案例数据库的案例组建设和样本组进行参数估计，但是现     
  在有俩问题，第一个，最大的问题，为什么威布尔计算器和参数估计       方法中用同样的方法会得到不一样的估计结果？以MDM为例**