针对参数估计方法子页面的方法示例子页面的通用框架，有一个优化的地方，就是参数配置部分不是有五个卡片吗，变量卡片中间有变量值，原则是被选择了被选中的白色部分变成红色，如果是整个维度被选中了，例如形状参数选中了，那就那几个白色部分都变红。同时呢，如果选择了1-2个变量，那其它的不也是有值吗，那那个量当时的值也要变红。总之原则就是虽然说这些量是变量，但是如果是变量的时候那就哪几个量变哪几个量变红，没轮到他变的变量就把其它人变的时候他是多少给标上。



我发现现在这种生成的方式完全就是在一个data.csv里，这样带来了一个问题：如果我想新增分析，例如我想新增β=1.7的，那是不是需要重新运行一整个生成的程序？还是说是否可以拆分生成然后合并运行，要拆分成什么样呢？一个参数组合就一个csv?这个帮我想一想规划一下。


我想做到

