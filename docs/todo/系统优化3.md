对于参数估计方法页面的方法示例1，我要明确一下显示的一致性，就是现在参数配置处，参数的可选值是白色框，已经选中的是红色框，这个已 经选中的就是目前要用于统计用的样本对应的真值。这个红框这个规则我想的是这样的：红框既作为显示，显示目前情况下，对于各自参数，真值  都是什么，例如我现在选择形状参数β为显示维度，那有点相当于给形状参数的变量全选的意思，而其它值是默认的，例如在形状参数为变量的这些 里面，尺度参数应该是默认的1000.样本量应该是7，偏移量应该是0.1，而不是其它的全部，那样的话就乱套了。



现在，要验证wmle.py这个程序应用的正确性，首先C:\Web\Weibull\src\content\182-088-pdf翻译.md这个是原文，[https://github.com/dco usin3/wMLE](https://github.com/dcousin3/wMLE.git)这个是代码库，我希望是先对比一下py程序，证明流程是对的，尤其是py程序里面我希望可以嵌入权重，不用额外  文件去读。其次我要用一样的样本算出一样的估计值，才能证明这个方法是对的
1.其它的wmle xxx的py脚本是之前测试用的，没啥用了
2.原文和代码库的符号含义和目前的不同，但是目前的是和系统一致的，因此都是统一用β表示形状参数，η表示尺度，γ表示位置参数

不要画蛇添足，要用和原文一样的优化、约束等条件，不想发生这种情况：自作聪明，觉得xxx优化比原文的好，擅自采用。

跟我沟通用中文

官方 J3 表有 n=1-100，而当前只有 n=1-16。J3 表有 4999 条数据 (n=1-100, gamma=0.1-5.0)。完整嵌入太大了。让因此可以



现在，需要建设WMLE的参数估计方法页面，主要是原理文档、程序流程、方法示例，参照已经完成了的MDM方法的来弄， 注意：方法示例部分，默认的配置是2，1000，1000，10。也就是如果是要做β=1.5，2，3，5，7的数据，那这个时候不用交叉  ，剩下的默认是1000，1000，10就可以了，不用变化 。其它变量的取值也和mdm一样。例如尺度参数取200，1000，5000。其实就是改一下生成数据的那个程序，还有自己知道，不用生成那么多了而已。原先是x乘y乘z，现在其实就是x+y+z了，大大简化数据

 那既然是偶然，能不能避免。另外，这个东西我想放到参数估计方法子页面的案例展示那块，就叫案例1:n=3时估计效果反而 好，根据目前的案例那块的结构来做。我想的是，1.现在是1000次有偶然，那2000次，3000次会不会把偶然性减小？2.会不会  是因为无解率导致的？3.为什么会无解。案例展示那一部分其实就是是那种动态加载的形式，我想的就是这种不规整的研究，  可以是写一个页面的形式然后做可视化，就把这些数据、表格展示出来，然后用现有案例展示那种规范一样来加载出来，现在  已有的案例展示的案例x其实也都是这样的

 我想在MDM方法的案例部分新建个案例17，这个案例和其它的案例的结构形式一样，也是有一样的字段能被下拉列表读取的 
  ，也是写一个tsx页面用于展示内容的，这个的目的是：我发现一个问题，就是n=3的时候，偏差反倒会比较小（方法示例1   
  里得到的结果）因为正常来说，n越大，估计的应该越准，在n=3的时候，应该会估计的非常偏离 

 先改一下这个py程序的位置，在应该在py的案例展示的文件夹的对应案例的文件夹内，目前我不知道在哪，如果对就没问题 
  。一个样本量一个程序，生成一个csv，而不是所有的一起生成。应该有3、4、5、6、7这五个样本量，而不是更多。先生成  
  3的。并且直接运行5000次就行，然后需要在1000，2000，3000，4000的时候记录各参数估计量的统计量，和5000一起。生   
  成和方法示范1一样的表格，  


现在参数固定方法子页面不是有一个方法示例吗？现在只是有一个示例一，这个里头呢是这些参数加上样本量和方法的内部量作为，无论是变量还是常量，肯定是主角是他们。然后呢，底下有蒙特卡洛仿真的数据展示和就是那些内部计算的计算方法的计算设置，还有这两个设置。我现在比较想的呢，就是他俩给作为变量，一个是示例二，一个是事例三，示例二就是蒙特卡洛这些的这个变量势力。三就是方法设置的点亮。但是这个应该怎么设计界面好呢？我想的就是极大可能的跟这个复用示例一的界面，就基本上都一模一样。只不过呢是它那个示例一，现在不是那些变量了，没有那么能想的了，顶天能选的就是降本量啊，这个没有选。然后呢，这个他选不是说我一下全选，或者说怎么地，我是说我可以点击，比如说这样本量可以取三四、五六七，然后我可以点n等于三，然后n等于四或者n等于四，或者n等于五，这样是像个tab一样。然后这里头的变量就是相当于这个重复次数，可以设置为一千两千三千四千五千。然后其实这样的话就是一个n做5000组，其中1000、2000、3000、4000都是包括在内的，然后就是和示例一一样的表格，这不也是属于单变量吗？然后和示例一一样的概种箱形图和这种箱形图，只不过底下要再多一个以重复次数为变量的。从这边是重复次数从1000到5000的步长是步长以一个以重复次数为横坐标，以估计中位数为纵坐标的一个图。然后这个图可以有一个tab，它可以切换，是中位数还是均值，就是改纵坐标的这个含义。先跟我说你打算怎么完成这个示例2，其它的工作需要等到我看方案可以了才能干


  问题确认：
  1. 样本量选择是多选（可同时看n=3,4,6的对比），还是单选？
  2. 箱形图是每个n一张图，还是所有n在一张图里对比？
  3. 收敛曲线图的纵坐标除了中位数、均值，还需要加"标准差"吗？
❯ 1.我想要布局和示例1基本一样，也就是不进行大的改动，保证能最大化复用。组件应该放到C:\Web\Weibull\src\component s\shared这里，因为横坐标是n纵坐标是估计值的属于通用图表。组件应该放C:\Web\Weibull\src\components\methods\mdm\st udies这里而不是C:\Web\Weibull\src\components\methods\mdm\case-studies这里，前者是方法示例，后者是案例展示。样本 量选择是多选，跟目前方法示例1的设为显示维度一个性质。2.箱型图是所有n在一张图，但是一个参数一张图，概率密度、新  增的n-中位数/均值图表也都一样，这俩是都有β/γ/η切换，这个是和方法示例1的是一样的感觉的。并且概率密度那个图一定要 复用组件而不是新创建。3.加上。图表应该是三个，箱型图，概率密度图（这俩和方法示例1是一模一样的，就用一样的组件） 加上收敛图。样本量选择可以选择某几个，通过点击数字组件，也可以全选，通过设为显示维度。你再更新一下你的理解，跟  我说一下   