现在在弄参数估计方法页面的方法示例的方法示例2，

对于现在这个方法示例2，还存在问题，就是
1. 参数设置和仿真与计算设置两个部分应该是和方法示例1复用一样的结构。也就是模块位置，内容是一样的，只是变量不一样会有调整，而不是现在这样非常不一样。参数设置其它四个是常量，样本数是变量。仿真与计算设置也应该与方法示例1一模一样只不过是在次数那块是有1000，2000，3000，4000，5000的变量显示（跟参数设置那块的变量的小方块是一样的）

我希望是我说哪你改哪


我现在在忙部署的事情，就是是这样的，我通过ssh，部署到NAS的docker上，部署是通过clone下来github上的本项目。用cl oudflare来做的穿透，之前已经能正常的在weibull.work上来显示了。但是我刚才想要在部署的这个也是通过git来控制版本的 新增修改之类的。然后还有一个就是刚才的ai coding告诉我docker安装的位置不对，可能意思是安装到系统目录了，导致存储 空间不足，所以又挪了一下。现在有两个事情，一个是我需要完整的跑通这个部署后重新改完部署位置，git、部署、运行、构 建的全过程。让网站能正常访问。（之前有个问题就是能访问前端但是不能用后端，但是我后端也在tunnel里做了映射了）,第 二就是好好维护一下部署与维护指南。部署部分就不用那么细了，需要把是怎么部署的说清楚就行。维护部分就是后面怎么更  新版本，这块要说清楚。我理想的就是我跟ai说git一个版本是指存到本地，github推送是到云端，更新服务器版本就是用ssh  
操控nas从github上拉下来然后更新一个版本。你现在先ssh吧。用户名和密码啥的是在C:\Web\Weibull\docs\凭证信息.md这里 
。现在首要的就是部署好，全流程也记录下来都是都有啥 


type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh weipengyu@192.168.31.148 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"