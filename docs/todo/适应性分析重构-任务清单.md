# 适应性分析重构 - 任务控制清单

> 进度跟踪文档，完成后打勾 ✅

---

## 阶段 1：Python 生成脚本重构

### 1.1 MDM 方法

| 任务 | 脚本路径 | 状态 |
|------|---------|------|
| 编写 `generate_demo1.py` | `python/studies/mdm/demo1/generate_data.py` | ⬜ |
| 修改 `generate_demo2.py` | `python/studies/mdm/demo2/generate_data.py` | ⬜ |

**MDM demo1 参数配置**：
```yaml
# 参数配置
beta_values: [1.5, 2.0, 3.0, 5.0, 7.0]
eta_values: [200, 1000, 5000]
gamma_values: [1000]  # 固定
sample_sizes: [3, 5, 7, 10, 20, 30]
offset_values: [0, 0.05, 0.1, 0.15, 0.2]  # δ (MDM特有)

# 仿真配置
mc_runs: 1000
seed: 42

# 计算配置 (MDM特有)
step: 60

# 输出命名
# b{beta}_e{eta}_g{gamma}_n{n}_d{offset}_rep{mc_runs}_seed{seed}_step{step}.csv
```

**MDM demo2 参数配置**：
```yaml
# 参数配置
beta: 2.0 (固定)
eta: 1000 (固定)
gamma: 1000 (固定)
sample_sizes: [3, 4, 5, 7, 10, 15, 20, 30]
offset: 0.1 (固定)

# 仿真配置
mc_runs: 5000
seed: 42

# 计算配置
step: 60

# 输出命名 (简化版，只有一个变量)
# n{n}_rep{mc_runs}_seed{seed}_step{step}.csv
```

---

### 1.2 MLE 方法

| 任务 | 脚本路径 | 状态 |
|------|---------|------|
| 编写 `generate_demo1.py` | `python/studies/mle/demo1/generate_data.py` | ⬜ |
| 修改 `generate_demo2.py` | `python/studies/mle/demo2/generate_data.py` | ⬜ |

**MLE 参数配置**：
```yaml
# 通用参数
beta_values: [...]
eta_values: [...]
gamma_values: [...]
sample_sizes: [...]

# 仿真配置
mc_runs: 1000
seed: 42

# 计算配置 (MLE无特有参数)

# 输出命名
# b{beta}_e{eta}_g{gamma}_n{n}_rep{mc_runs}_seed{seed}.csv
```

---

### 1.3 WMLE 方法

| 任务 | 脚本路径 | 状态 |
|------|---------|------|
| 创建 demo1 目录和脚本 | `python/studies/wmle/demo1/generate_data.py` | ⬜ |

---

### 1.4 MMLE 方法

| 任务 | 脚本路径 | 状态 |
|------|---------|------|
| 创建 demo1 目录和脚本 | `python/studies/mmle/demo1/generate_data.py` | ⬜ |

---

## 阶段 2：Chunk 文件重命名

### 2.1 MDM demo1 (453个文件)

| 任务 | 状态 |
|------|------|
| 编写批量重命名脚本 | ⬜ |
| 执行重命名 | ⬜ |
| 验证重命名结果 | ⬜ |

**重命名规则**：
```
旧: b1.5_e1000_n5_d0.csv
新: b1.5_e1000_g1000_n5_d0_rep1000_seed42_step60.csv

补充字段：
- g: 从 config.md 读取 (gamma=1000)
- rep: 默认 1000
- seed: 默认 42
- step: 默认 60
```

---

### 2.2 MDM demo2 (7个文件)

| 任务 | 状态 |
|------|------|
| 重新生成数据（使用新脚本） | ⬜ |
| 或重命名现有文件 | ⬜ |

**重命名规则**：
```
旧: n3.csv
新: n3_rep5000_seed42_step60.csv

补充字段：
- beta=2.0, eta=1000, gamma=1000, d=0.1 (固定值，不写入文件名)
- rep: 默认 5000
- seed: 默认 42
- step: 默认 60
```

---

## 阶段 3：前端 API 开发

| 任务 | API 路径 | 状态 |
|------|---------|------|
| 创建 chunk 列表解析 API | `/api/studies/{methodId}/{studyId}/chunks` | ⬜ |
| 解析文件名提取参数值 | - | ⬜ |
| 返回可用参数组合 | - | ⬜ |

---

## 阶段 4：前端组件重构

| 任务 | 组件 | 状态 |
|------|------|------|
| 重构参数配置组件 | `ParamCard` | ⬜ |
| 实现单值点击切换 | - | ⬜ |
| 实现三色边框逻辑 | - | ⬜ |
| 添加"全选"按钮 | - | ⬜ |
| 仿真配置可选择化 | `SimulationSettingsPanel` | ⬜ |
| 计算配置可选择化 | - | ⬜ |

---

## 执行顺序

```
阶段 1.1 → 阶段 2.1 → 阶段 3 → 阶段 4
           ↓
       阶段 2.2
```

**当前优先级**：
1. P0: MDM demo1 生成脚本 + 重命名
2. P0: MDM demo2 脚本修改
3. P1: API 开发
4. P1: 前端组件重构
5. P2: 其他方法脚本

---

## 文件清单

### 现有文件
- `python/studies/mdm/demo2/generate_data.py` - 需修改
- `python/studies/mle/demo2/generate_data.py` - 需修改
- `public/studies/mdm/demo1/chunks/*.csv` - 453个，需重命名
- `public/studies/mdm/demo2/chunks/*.csv` - 7个，需重命名/重新生成

### 待创建文件
- `python/studies/mdm/demo1/generate_data.py`
- `python/studies/mle/demo1/generate_data.py`
- `python/studies/wmle/demo1/generate_data.py`
- `python/studies/mmle/demo1/generate_data.py`
- `python/studies/scripts/rename_chunks.py` (批量重命名工具)
