不对不对，其实现在2-4都是方法内部，只不过是侧重的方向不同，分别是流程、过程量、蒙特卡洛模拟，而一个案例应该是     
  根据流程（包括公式依赖、步骤、输入输出）、过程量设置（例如MDM中的偏移量）、蒙特卡洛设置，现在情况是，我改变过   
  程量的时候，是在tab3里，这个时候改不了依赖的流程，而且只是一次，例如我蒙特卡洛的时候，也是根据我之前设定好的      
  我想看xx的变化，而整的，有点像x+y=z，现在这个提前算好的z放到案例那个tab里了，有些割裂。你觉得呢，所以我觉得应       该改一下tab4，这里应该是能选择流程+过程量中的1-2个变量，然后可以实时计算蒙特卡洛，然后也可以计算完导出到案例      
  中，存为案例x，这个就是能最大化复用这些模块，并且把这些都连起来了。这存的案例其实就是案例那里我说的常规案例，       也就是有变量，看变量对结果的影响，蒙特卡洛是用来看大量重复下的结果，这样可以看出估计的稳定性，避免了单次估计      
  导致的偶然性


继续改一下，1.2-3的这里，不是说不能有例子，而是想展示，在当然样本进行估计的情况下，tab2应该是展示流程公式、步     
  长等更改的情况下，其它变量和结果的变化。tab3现在这样其实就是我想表达的意思，例如说中间变量偏移量变化，另一个    
  曲线是咋变的，除此之外还有，这俩都是聚焦于这一页的自身的聚焦，2.这些不是针对内部变量的调整吗，那其实后面要做        的是这个其实类似于方法的setting，我在这里设置完保存后的，就是计算器页面或者其它涉及到用了后端进行计算情况下的     
  所用的设置。现在，把咱俩的对话，我的完整保留，你的作简略，保存到C:\Web\Weibull\docs这个文件夹，新建一个todo文       件夹，放到一个md文档中。来表述我的思考流程，为什么要保留你的呢，因为你虽然目前理解的不到位，但是只保留我的没      
  有对话的话，会看不懂，但是你说的一些方案和理解可以不要，因为我还没认可

2.重新规划，现在有几个问题，从严重到轻：结构不清，因为要探索参数估计方法的一些性质，所以采用案例展示tab 
  ，但是这个就打开了潘多拉魔盒，因为每个案例都有网页、数据、md等等，所以导致在越发的臃肿，这样下去会导致  
  无法维护的。现在其实计算器、案例、图书馆这三个子页面都还行，就是要解决参数估计方法子页面的案例展示子页        面的问题，并且除了这个问题，程序流程这里，是展示py程序、变量流转、计算公式的，这个仅仅是静态展示，而其  
  实参数估计方法子页面我认为应该是集原理展示、方法设置、过程量可视化、方法示例为一体的，例如我在程序流程        中，应该可以看到后端用于计算的py代码，有流程、有变量但是也可以调整计算的设置量，例如迭代次数、步长等等  
  ，就跟软件的setting一样，甚至可以看到调整这个之后结果会怎么变，这个就是现在的案例的一部分，就是我案例中 
  就是探讨方法自身，计算时候的这些设置量作为变量是对结果有什么影响，参数估计的三个参数和样本量、过程量有      
  什么影响这就是在计算过程这里了，现在以MDM方法为例已经有点雏形，你理解我的意思吗？就是现在这些tab的功能      
  都是分散的，而且只是个展示，意义缺乏，展示也没展示到位。另外，这些图，很多都是可以复用的，我要表达一样      
  的横纵坐标，那我只是需要调用组件就可以表达对应数据，而不是案例这里是每一个案例都写一个tsx。但是我其实也     
  是有没想明白的地方，例如现在这个案例，其实是在方法中，选择计算方法、样本特征、真值，然后在其中选择1-2个     
  变量，然后选择蒙特卡洛要多少次，最后得到一个能反映规律的图，那其实这个就是可以把这些个tab给利用起来了，     
  就是tab2+3+4=案例展示，并且计算出来的案例展示我不知道该放哪，因为例如我刚才说可以有一部分放程序流程里，     
  例如可以反映以步长为横坐标，得到值的差别，以不同搜索算法为对比的，等等。这个是不是可以？那案例既然能分      
  散到tab里，那案例展示这个tab还有什么意义？


这种的对了，但是要纠正两个事情，1.缺乏计算设置，也就是计算时候的步长、寻优的方法等，另外，开始研究那个      
  不对，应该是仿真设置，这里写蒙特卡洛次数的信息，最底下就是图表区域没有问题，另外，这个方法示例的核心其     
  实就是把需要研究的不用现仿真，就是有一个保存下来的并且清晰告诉了都是在什么参数下的仿真的一个现成的结果        ，所以包括存在md里的参数，还有data.csv，这个就是仿真得到的结果，还有summary.json，这个是根据结果得到的      
  统计量。然后图表区域应该拆分成表区域和图区域，现在其实还差一个很重要的事情，就是这些仿真数据怎么生成，        应该是一个方法有一个仿真的py，这个py一方面是传入计算相关的参数，然后调用方法.py来进行蒙特卡洛，这样就避     
  免一个示例需要一个py程序生成，而是可以根据需求复用。另外，其实现在的方法.py   也是缺乏将计算量例如步长等作为变量的，因此也是后续需要改动的内容，要做到这个仿真.py+方法.py能满足所有的仿
  真需求。你应该把我说的这些列为一个详细的任务单和说明书。这样后面改动也是有据可查。另外纠正你一下，表   
  区域应该显示summary.json的内容，这个是根据data.csv总结的，data数据太多，别在页面上显示，另外，这个生成s       ummary的其实也应该在仿真.py里，或者另一个py也行，中心思想就是模块化，可复用，而不是越堆越多 

现在我要逐渐移植案例展示到方法示例，同时因为以后方法示例里面是同一     
  框架，统一图像、表格的组件嘛，所以我要把案例展示里面不错的图片给移 
  植过来再优化修改，现在方法示例只有1，这里面对应的案例展示是案