# AI 辅助三参数 Weibull 估计当前路线图

> **更新日期**：2026-08-10
>
> **用途**：把博士研究证据转化为可执行的 AI 辅助估计能力；不另立一套研究权威。
>
> **旧版**：[`history/2026-08-10-AI辅助三参数威布尔参数估计重构旧路线图.md`](history/2026-08-10-AI辅助三参数威布尔参数估计重构旧路线图.md)

## 1. 先读哪里

| 需要 | 当前入口 |
|---|---|
| 项目状态和文档路由 | [`../README.md`](../README.md) |
| Study 定义、原则和映射 | [`../Study/README.md`](../Study/README.md) |
| 博士论文总体科学路线 | [`../Study/研究规划v0.2.md`](../Study/研究规划v0.2.md) |
| 小范围验证和孵化方向 | [`../Research/README.md`](../Research/README.md) |
| 产品目标与近期任务 | [`../04-目标与待办.md`](../04-目标与待办.md) |

本文件只回答“AI 能力怎样沿现有科学证据继续建设”。具体结论和数值以各 Study 为准。

## 2. 当前身份

当前主线不是旧的 `M1→M2→M3→M4` 功能堆叠，也不是用端到端网络替代传统方法。目标是形成：

> **仿真信息驱动、任务感知、风险导向，并能在信息不足时回退的三参数 Weibull 自适应估计策略。**

AI 可介入四类位置：选择方法、配置方法内部过程量、对齐工程目标、估计风险与可信度。每次只在受控条件下研究一个主要决策变量。

## 3. 当前科学证据

| 来源 | 已支持 | 尚不支持 |
|---|---|---|
| Study01 | MDM 内部 `delta` 存在样本级选择空间；样本风险曲线预测可恢复部分机会 | 任意样本量、单位、尺度或连续参数空间的通用选择器 |
| Study02 | 同一三参数输出网络中，目标寿命损失相对 `P_equal` 有小而稳定的平均收益，并改变高估/低估风险分布 | 参数整体更准、每个区域都赢、任意任务目标都应直接优化 |
| Research | 表征、稳健求解、传统方法比较和修正方向提供机制、比较或孵化材料 | 未经升级就直接成为产品推荐规则或论文主张 |

旧 M1/M3 页面和模型是工程原型，只能作为迁移材料；其中的旧百分比和“最佳方法”表述不是当前正式证据。

## 4. 当前推进顺序

### A. 收口现有 Study

- Study01 完成论文闭环，保持 MDM 偏移量选择为中心。
- Study02 完成独立复核与 main 合并，保持目标对齐的条件收益、代价和边界。

### B. 建立方法风险地形

已完成 `Research/05 risk_landscape_v1`：在 160 个设计单元和 48,000 个共享样本中，真参数 cell 选择较最佳固定 MDM-0.1 改善 6.07% [4.85%, 7.30%]。这证明存在条件机会空间，但样本量单独没有选择收益，且真参数不可用于部署。

### C. 研究可部署选择

下一步只用当前样本可观测信息，先比较最佳固定规则、少量等变统计量规则和可解释模型，检验能否识别以形状参数为主的风险分区。只有简单规则不足时才引入复杂模型。评价的是风险下降及其恢复的 oracle 比例，不是单独的分类准确率。

### D. 加入校准与回退

输出不仅包括动作，还包括预期收益、不确定性、适用域和回退方案。近似平局、域外或置信不足时，回到经过验证的固定方法。

### E. 最小化综合

只组合已经独立成立的模块；共同基线、单模块和最终组合必须在同一合同下复核。出现实质交互时再补实验，不默认收益相加。

## 5. 从研究到产品的门槛

一项策略进入 `/ai` 页面或生产 API 前，至少满足：

1. 对应一个已定义的 Study 或边界清楚的 Research；
2. 协议、数据身份、损失和失败合同可复核；
3. 超过默认、最佳固定和必要的简单规则；
4. 报告不确定性、失效区域和不应推广的范围；
5. 有明确回退行为，不能只返回“最佳”；
6. 产品文案与当前证据一致，不复活旧原型数字。

## 6. 产品层最小结构

```text
样本 + 工程任务
  → 适用域检查
  → 候选行动及其风险估计
  → 选择 / 拒绝 / 回退
  → 参数与工程量结果
  → 预期收益、不确定性和证据边界
```

平台需要逐步提供的是共同输入合同、候选方法注册、风险评价、决策回执和证据说明；不是先建一个庞大调度平台再寻找科学问题。

## 7. 非目标

- 不以增加网络、智能优化器或页面数量作为进展；
- 不把一个设计域内的结果包装成通用 AI 估计器；
- 不跨 Study 协议拼排名，不把历史原型当正式基线；
- 不提前给未来方向编号，也不为了组合而制造耦合；
- 不在科学证据形成前承诺“自动推荐最佳方法”。

具体近期任务以 [`../04-目标与待办.md`](../04-目标与待办.md) 为准。
