# 新窗口接手提示词

> 复制以下内容发给新工作窗口。

---

你正在接手"AI 辅助三参数威布尔参数估计"研究重构项目。

项目目录：C:\weibull

请按顺序阅读以下文档，不要跳过：

1. `CLAUDE.md` — 项目概况和文档索引
2. `docs/AI辅助三参数威布尔参数估计重构前因后果.md` — 为什么重构
3. `docs/AI辅助三参数威布尔参数估计重构与实验设计总纲.md` — 研究设计
4. `docs/AI辅助三参数威布尔参数估计重构状态控制.md` — 当前状态和接手记录
5. `docs/AI辅助三参数威布尔参数估计S4统一蒙特卡洛框架规划.md` — S4 规划

读完后你会知道：

- S0~S2.5 已完成，S4 框架已实现并通过端到端验证
- S4.5+ 深度诊断完成：MDM failure 确认为理论限制（非搜索网格遗漏），fallback 策略可消除全部 failure 且精度不降
- 当前默认配置：MDM offset=0.1, gamma_steps=20
- 本轮工作目标：进入 S3 Loss 对比实验
- 禁止读取 `_archive/`，不要用旧 AI 结果写正式结论
- 每轮结束前更新状态控制文档的接手记录

**S4.5+/S4.6 关键结论（你需要知道的）：**

MDM 的 32.8% failure rate 不是 bug，是 offset 判据对某些参数组合过于保守。证据链：
1. 394 个 failure 样本 100% 为 grad_min > offset（梯度范围不含 offset）
2. 2000 点高分辨率分析 19/20 个样本仍无交点
3. 梯度曲线在 t_min 附近单调上升（不是下降），解不会藏在 t_min 附近
4. fallback_min_sigma 策略消除全部 failure，NE 从 0.386 降到 0.343

**待外部审查者决定**：fallback 是否应成为 MDM 默认行为。

现在请先读完上述文档，然后报告你对当前状态的理解，再开始实现。
