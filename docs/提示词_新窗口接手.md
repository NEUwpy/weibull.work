# 新窗口接手提示词

> 复制以下内容发给新工作窗口。

---

你正在接手“AI 辅助三参数威布尔参数估计”研究重构项目。

项目目录：`C:\weibull`

请先不要改代码，按顺序阅读：

1. `AGENTS.md`
2. `README.md`
3. `docs/AI辅助三参数威布尔参数估计重构状态控制.md`
4. `docs/S4.9.3_MDM始终有解Brent求解器改造进程控制.md`
5. `docs/research/对MDM方法有解性的证明/对MDM方法有解性的证明/full.md`，重点看第 6.3、8、9.1、9.4 节
6. 如需要查历史，再看 `docs/history/S4.9_MDM默认实现与可视化一致性改造进程控制.md`

禁止读取 `_archive/` 作为当前执行依据。

## 当前主线

S2R 唯一评价指标体系已完成：只保留 MdAPE、方向、IQR、尾部和有效率。前端指标模块、后端指标模块和 `/help/metrics` 页面必须保持双向同口径。

S4.9、S4.9.2 和 S4.9.3 已完成：

- S4.9：默认 MDM 已从旧两段均匀网格改成几何网格，并加入 `gamma >= 0` 下的 `truncated_at_zero` 截断规则；
- S4.9.2：完成一致性整改，旧 AI 数据、旧 case-study 和旧页面文案已标注历史旧口径；
- S4.9.3：默认 MDM 已实现一次探测 + 右端括弧/Brent 定根 + 负根截断到 0 的始终有解求解器；
- 旧 S4.5/S4.7 变体不再作为当前 baseline，后续实验统一使用默认 `mdm`。

S4.9.3 当前结论：

```text
默认 MDM 只保留三种情况：
1. g(0) >= offset：截断到 gamma=0，solution_strategy=truncated_at_zero；
2. g(0) < offset 且右端括弧成立：solution_strategy=brent_root，root_solver=brent；
3. g(0) < offset 且右端锚点仍低于 offset：不返回 failure，不做上边界截断，solution_strategy=brent_root，root_solver=right_edge_fit。

trace_data.grad_gamma_curve 与后端求解器使用同一个 profile_gradient()，并写入实际根点；
前端展示的是本次后端求解过程，不再用另一套离散插值改写最优 gamma。
```

## S4.9.3 已完成内容

- `python/methods/mdm.py`：实现 `profile_gradient()`、Brent 定根、右端补交点、`last_solution_info` 轻量摘要；
- `python/tests/test_mdm_s49.py`：覆盖三种情况、右端补交点、trace 同源性和轻量摘要；
- `python/studies/mdm_delta/generate_curve_study_data.py`：使用轻量摘要扫描 offset，并重新生成 S4.9.3 口径数据；
- `public/case-studies/mdm/curve-study/data.json`：已更新，所有曲线点均有效，右端补交点用 `right_edge_fit_delta` 标记；
- `src/components/methods/mdm/CurvePropertiesViewer.tsx`：不再展示“诊断失败起点”，改为“右端补交点”；
- `src/components/methods/mdm/visualizers/MDMVisualizer.tsx`：最优 γ 来自后端 trace，δ 滑条只调整参考线；
- `src/content/algorithms/mdm.md` 和 `src/data/method_flows/mdm.json`：已同步 S4.9.3 流程。

## 关键文件

| 文件 | 用途 |
|------|------|
| `python/methods/mdm.py` | 默认 MDM，当前 baseline |
| `python/tests/test_mdm_s49.py` | S4.9/S4.9.3 回归测试 |
| `python/studies/mdm_delta/generate_curve_study_data.py` | 曲线性质数据生成脚本 |
| `public/case-studies/mdm/curve-study/data.json` | S4.9.3 口径曲线数据 |
| `src/components/methods/mdm/CurvePropertiesViewer.tsx` | 曲线性质页面 |
| `src/components/methods/mdm/visualizers/MDMVisualizer.tsx` | MDM 计算过程可视化 |
| `src/content/algorithms/mdm.md` | 算法说明 |
| `src/data/method_flows/mdm.json` | 程序流程 Tab |
| `docs/S4.9.3_MDM始终有解Brent求解器改造进程控制.md` | S4.9.3 完成记录 |

## 已建成基础设施

| 模块 | 文件 | 功能 |
|------|------|------|
| S2R 统一指标 | `python/studies/common/metrics.py` | MdAPE / MedRel / RelIQR / 尾部 / Valid Rate / check_status / aggregate |
| 前端指标 | `src/lib/metrics.ts` | 与后端同口径的 S2R 共享函数 |
| 指标规范 | `/help/metrics` | 与前后端模块双向同步的可读规范 |
| 共享样本 | `python/studies/common/sample.py` | hashlib.sha256 种子，可复现 |
| 方法调用 | `python/studies/common/runner.py` | 统一接口，默认 mdm 指向 `python/methods/mdm.py` |
| 实验调度 | `python/studies/common/experiment.py` | 参数网格 × 样本量，CSV + S2R 聚合 JSON |

## 建议下一步

S4.9.3 已完成。下一步建议二选一：

1. 在 S2R 口径下重算 S4.7；
2. 进入 S3 loss 对比实验，使用 S4.9.3 后的默认 MDM baseline。

开始下一步前建议先运行：

```bash
cd python
python -m pytest tests -q

cd ..
npx tsc --noEmit
python -m py_compile python/studies/mdm_delta/generate_curve_study_data.py
git diff --check
```
