就是这个case3，我是为了解决一个问题：在计算的时候发现会有梯度曲线与偏移值无交点的现象，我想看看是什么
  样的样本会导致这样，因此，需要在这个案例3里先跑一个针对2，1000，1000，n=7，偏移量=0.2的程序，一直抽样   
  到出现这种现象为止，然后记录样本、记录当时的曲线。目前就先解决这个问题就行。至于曲线怎么话就可以参考MD  
  M方法以及计算过程那个子页面的两幅图，要的就是那两幅图的。最后生成四幅，两幅是只有无交点曲线时候的状态   
  ，两幅是包括无交点曲线在内加上9个其它抽样形式的曲线簇。 