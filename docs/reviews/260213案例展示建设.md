现在有个问题，就是这个案例1-5，它是方法页面下的子页面下的子页面，但是现在放的位置也不对，命名也不对，还总容易和case弄混，我认为这个应该是存在方法下的具体方法名下的案例展示下的文件夹内，并且现在这些组件乱七八糟的，案例只有两种，一种是常规案例，就是三个参数+样本量+偏移量（这个偏移量是MDM特有，如果是其它方法，那就是他们的过程量）这块实际应该是过程量，然后在MDM中是偏移量，这个是个框架，然后读取对
  应案例的md和组件和csv来渲染出来，这个应该是一个文件夹内放着的，然后就是特殊案例，特殊案例其实就是和常规案例不同，但是我还想展示出来的。这种要是很简单就直接编成一个页面文件了，要不然就是也有对应的md和csv等文件，那也都存进各自的文件夹内，另外，现在可视化组件库内东西太多了，尤其是特殊案例的，如果这样那将越来越臃肿，因此特殊案例的组件也应该放到其文件夹中，总之，记得改一下整个项目的说明文件，说清   
  楚了，就是方法页面中的案例展示，其实就是把每次需要重新计算重新生成或者需要好几个步骤生成的东西，弄了一个脚本，生成了一个数据，给展示出来，这样就不用重复生成甚至重复操作了。另外，生成这个data的或者做其他事情的反正跟生成案例相关的py程序也应该放到其文件夹内。现在，你需要做的事情是生成个这个文件夹，把目前有的案例都构建好，然后写个案例的readme，这样后面的ai以后好看着这个维护增添，并且在总说明文档   
  里面说清楚这部分并且说具体详情在哪看。然后先不删目前有的，而是先构建新的，等构建完新的等验证完新的能达到和旧的一模一样的效果，再删除旧的。最后，你干之前先跟我说一下你对我这一堆话的理解，我看你理解的没有问题后你再干 