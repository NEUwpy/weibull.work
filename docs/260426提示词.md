# 2026-04-26 AI 模块 1 实现衔接提示词（最终版）

> **用途**：在新窗口中用此提示词继续 AI 模块 1 的工作。
> **当前状态**：全部核心流程已完成（数据生成、模型训练、对比数据、前端图表）。

---

## 背景

Weibull 分析平台（weibull.work）的 **AI 模块 1**（MDM 偏移量 δ 优化）已基本实现完成。

**核心文档**（按需阅读）：
- `docs/ai-methods-module1-detail.md` — 完整技术方案
- `docs/ai-module1-status.md` — 当前状态（可能过时，以本文件为准）
- `task_plan.md` — 27 个图表清单

**关键设计决策**：

| 项目 | 决策 |
|------|------|
| 参数空间 | β∈{1,2,5}, η∈{100,1000,5000}, γ=1000, n∈{5,7,15}, MC=500 |
| N₂ 架构 | Linear(n,128)→ReLU→BN→Linear(128,64)→ReLU→BN→Linear(64,1)→Sigmoid |
| N₁ 架构 | Linear(3,32)→ReLU→Linear(32,16)→ReLU→Linear(16,1)→Sigmoid |
| 路线 1 | N₂ 直接从样本预测 δ |
| 路线 2 | δ₀=0.5 → MDM → N₁ → δ₁ → ... → |δ_new-δ_old|<0.001, 最大 10 步 |
| 训练超参 | Adam, lr=0.001, batch=64, ReduceLROnPlateau, patience=30 |

---

## 已完成的工作

### 后端（Python）

| 文件 | 说明 |
|------|------|
| `python/studies/mdm_delta/generate_training_data.py` | 生成 13,442 条训练数据（99.6% 成功率） |
| `python/studies/mdm_delta/train_model.py` | N₁/N₂ 模型训练 |
| `python/studies/mdm_delta/generate_comparison_data.py` | 生成 7 个对比/验证 CSV |
| `python/studies/mdm_delta/copy_data_to_public.py` | 复制数据到 public/ai/data/ |
| `python/main.py` | N₁/N₂ 模型类 + 路线 2 迭代 API |

**API 端点**：
- `POST /ai/relationship/mdm` — 路线 1：样本 → N₂ → δ
- `POST /ai/relationship/mdm/iterate` — 路线 2：迭代逼近

**模型文件**（已训练完成）：
- `python/models/mdm_delta/n5_model.pth` — N₂, n=5, MSE=0.005043
- `python/models/mdm_delta/n7_model.pth` — N₂, n=7, MSE=0.004981
- `python/models/mdm_delta/n15_model.pth` — N₂, n=15, MSE=0.007309
- `python/models/mdm_delta/delta_from_params.pth` — N₁, MSE=0.028033
- `python/models/mdm_delta/*_metrics.json` — 训练指标

### 前端（Next.js）

| 文件 | 说明 |
|------|------|
| `src/app/ai/relationship/page.tsx` | 7 个 Tab 框架 |
| `src/app/ai/relationship/components/DataTab.tsx` | D1-D7 图表 |
| `src/app/ai/relationship/components/TrainingTab.tsx` | T1-T2 损失曲线 |
| `src/app/ai/relationship/components/PerformanceTab.tsx` | P1-P5 预测精度 |
| `src/app/ai/relationship/components/PlaygroundTab.tsx` | 路线 1 + 路线 2 切换 |
| `src/app/ai/relationship/components/CompareTab.tsx` | AI vs 固定 δ 对比 |
| `src/app/ai/relationship/components/VerificationTab.tsx` | 验证案例 + 边界测试 |
| `src/components/ai/charts/*.tsx` | 9 个通用图表组件 |
| `src/lib/ai-data.ts` | 数据加载工具 |
| `src/lib/config.ts` | API 端点配置 |

**TypeScript 编译**：✅ 已通过

### 数据文件（public/ai/data/）

```
training_data_n5.csv, n7.csv, n15.csv, all.csv   # 训练数据
validation_predictions_n5.csv, n7.csv, n15.csv    # 验证预测
comparison_ai_vs_fixed.csv                        # AI vs 固定 δ 对比
comparison_sweep.csv                              # δ sweep MSE 曲线
comparison_improvement.csv                        # 改善百分比
iteration_stats.csv                               # 路线 2 收敛统计
verification_cases.csv                            # 已知参数验证
boundary_tests.csv                                # 边界条件测试
n5_metrics.json, n7_metrics.json, n15_metrics.json, delta_from_params_metrics.json
summary.json, config.json
```

---

## 可能的后续工作

1. **启动前后端验证**：`cd python && python main.py` + `npm run dev`，访问 `/ai/relationship` 逐 Tab 检查
2. **调优模型**：如果精度不满意，可调整超参重新训练
3. **路线 2 迭代逻辑验证**：在 Playground 中测试 Route 2 迭代是否收敛
4. **边界测试**：boundary_tests.csv 中部分极端场景可能 MDM 失败
5. **清理旧文件**：`python/models/mdm_delta/` 下可能有旧原型的 `model_v1.pth` 等文件

---

## 注意事项

1. **Windows GBK 编码**：Python print 语句中不能使用 δ, →, ±, ₂, ₁ 等特殊 Unicode 字符，否则报 `UnicodeEncodeError`。已全部替换为 ASCII。
2. **旧 n=10 文件**：原型遗留的 `training_data_n10.csv` 和 `n10_metrics.json` 已删除，但 `python/models/mdm_delta/` 下可能还有旧模型文件。
3. **PyTorch CPU**：模型推理在 CPU 上运行，无需 GPU。
4. **图表组件**：使用 recharts v2.11.0，遵循"高配版+功能开关"模式。
