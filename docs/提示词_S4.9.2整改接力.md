# S4.9.2 一致性整改复核提示词

> 复制以下内容发给新工作窗口。

---

你正在接手 "S4.9.2 MDM 一致性整改" 的后续复核或延伸任务。

项目目录：C:\weibull

请先阅读：

1. `CLAUDE.md` 或项目根目录的 AI 助手入口文档
2. `docs/S4.9_MDM默认实现与可视化一致性改造进程控制.md` — 重点看第 11 节
3. `docs/提示词_新窗口接手.md` — 当前主线接手说明

## 当前进度

S4.9.2 整改清单已完成并复核：

- P1：曲线性质页面、curve-study 数据和生成脚本已同步为 S4.9 口径。
- P2：AI 旧数据已标注历史旧口径，M1-R2 / M3 对比相关用户可见位置已加提示。
- P3：接口注释、MDM docstring、更新日志、状态控制文档和接手提示词已清理。

## 后续注意

- `truncated_at_zero` 只表示无约束根落在 gamma<0 并被 `gamma >= 0` 约束截断。
- 当 offset 超过可观测梯度上限时，当前默认 MDM 仍可能返回诊断性 `no_intersection` / `no_offset_root`。
- `public/ai/data/` 中的 MDM 相关结果仍是 S4.9 前历史数据；正式 S4/S2R/S3 结论需要用 S4.9 后默认 `mdm` 重算。
- 不要读取 `_archive/` 作为当前执行依据。
