# AI 模块 1 现状梳理：MDM 偏移量 δ 优化

> **更新时间**: 2026-04-27
> **状态**: β 扩展数据生成完成，N₁/N₂ 模型已重新训练，前端数据已同步
> **详细方案**: [ai-methods-module1-detail.md](ai-methods-module1-detail.md)
> **路线 2 结果**: [ai-module1-route2-results.md](ai-module1-route2-results.md)

---

## 〇、当前讨论进展

**已完成的讨论**：
- [x] 研究目的的逻辑链条（现状→问题→尝试→瓶颈→路线）
- [x] 两条研究路线：路线 1（直接学习）和路线 2（迭代逼近）
- [x] 神经网络分支结构：N₁（公共，真值→δ）+ N₂（路线1，样本→δ）
- [x] 指标方案：相对 MSE
- [x] 参数空间：β∈{1,2,5}, η∈{100,1000,5000}, γ=1000, n∈{5,7,10,15,20}, MC=500
- [x] 路线 2 收敛判据：方案 C（δ<0.001 / 最大10步 / MDM失败）
- [x] 路线 2 初始 δ₀ = 0.5
- [x] 可视化方案：27 个图表，按 Tab 分组，组件复用

**待讨论**：
- [ ] 边界过滤问题：~44% 样本因最优 δ 在搜索边界被丢弃，是否去掉边界过滤或调整 delta_min=0
- [ ] 搜索策略优化：粗搜+细搜 31 次 MDM 调用，MSE(δ) 曲线多为单峰，可用 Brent 法减到 ~10-15 次
- [ ] 路线 2 重新评估：N₁ 已重新训练（β 扩展后），需要用新模型重新跑 evaluate_route2.py

---

## 一、模块 1 做了什么（一句话）

训练了一个神经网络，输入一组失效时间样本，直接输出 MDM 方法应使用的最优偏移量 δ，替代人工反复尝试。

---

## 二、整体流程

```
离线（已完成）:
  蒙特卡洛模拟生成 Weibull 样本
    → 对每个样本粗搜+细搜找最优 δ
    → 每个 δ 运行 MDM 得到 est_β, est_η, est_γ
    → 用相对 MSE(β,η,γ) 选出最优 δ*
    → 训练 PyTorch MLP 学习"样本 → δ*"的映射

在线（已部署）:
  用户输入样本 → API 调用 → 模型推理 → 输出最优 δ
```

---

## 三、已确认的设计决策

| 项目 | 决策 |
|------|------|
| 研究路线 | 路线 1（直接学习）→ 路线 2（迭代逼近），网页 Tab 切换 |
| 神经网络 | N₁（公共，真值→δ）供路线 2 使用；N₂（样本→δ）供路线 1 使用 |
| 指标方案 | 相对 MSE：(β̂-β)²/β² + (η̂-η)²/η² + (γ̂-γ)²/γ² |
| 参数空间 | β∈{1,2,5}, η∈{100,1000,5000}, γ=1000, n∈{5,7,10,15,20}, MC=500 |
| 路线 2 初始值 | δ₀ = 0.5 |
| 路线 2 收敛 | 方案 C：|δ_new-δ_old|<0.001 / 最大10步 / MDM失败 |
| 可视化 | 27 个图表，通用组件复用，数据来自训练结果 CSV |
| N₁ 架构 | Linear(3,32)→ReLU→Linear(32,16)→ReLU→Linear(16,1)→Sigmoid |
| N₂ 架构 | Linear(n,128)→ReLU→BN→Linear(128,64)→ReLU→BN→Linear(64,1)→Sigmoid |
| 训练超参 | Adam, lr=0.001, batch=64, ReduceLROnPlateau, patience=30, max_epoch=300 |
| δ 搜索 | 粗搜(步长0.1)+细搜(步长0.01, ±0.1范围)，范围 [0.001, 1.0] |

---

## 四、文件清单

### 4.1 训练数据生成

| 文件 | 说明 |
|------|------|
| `python/studies/mdm_delta/generate_training_data.py` | 蒙特卡洛模拟 + 粗搜+细搜，生成训练数据 |
| `python/studies/mdm_delta/data/training_data_n{5,7,10,15,20}.csv` | 按 n 分文件（供 N₂ 训练） |
| `python/studies/mdm_delta/data/training_data_all.csv` | 全量合并 12,597 条（供 N₁ 训练） |
| `python/studies/mdm_delta/data/summary.json` | 生成统计摘要 |
| `python/studies/mdm_delta/data/mse_curves/` | MSE-δ 曲线分析数据 |

**CSV 格式**: `n, beta, eta, gamma, t1, t2, ..., tn, optimal_delta, best_relative_mse`

### 4.2 模型训练

| 文件 | 说明 |
|------|------|
| `python/studies/mdm_delta/train_model.py` | PyTorch MLP 训练脚本（支持 N₁/N₂） |
| `python/models/mdm_delta/n{5,7,10,15,20}_model.pth` | N₂ 模型权重（路线 1） |
| `python/models/mdm_delta/n{5,7,10,15,20}_metrics.json` | N₂ 训练指标 + 损失历史 |
| `python/models/mdm_delta/delta_from_params.pth` | N₁ 模型权重（路线 2） |
| `python/models/mdm_delta/delta_from_params_metrics.json` | N₁ 训练指标 |

### 4.3 后端 API

| 文件 | 位置 | 说明 |
|------|------|------|
| `python/main.py` | 第 370-491 行 | DeltaMLP 类定义 + 模型加载 + API 端点 |

**API 端点**: `POST /ai/relationship/mdm`
- 请求: `{ "data": [398.3, 520.3, 814.4, 921.3, 2344.0] }`
- 响应: `{ "optimal_delta": 0.1234, "model_n": 5, "confidence": "high" }`

### 4.4 前端页面

| 文件 | 说明 |
|------|------|
| `src/app/ai/page.tsx` | AI 总览页（3 个模块卡片 + 交叉矩阵） |
| `src/app/ai/relationship/page.tsx` | 关系建立子页面（7 个 Tab） |
| `src/lib/ai-data.ts` | 数据加载工具（CSV/JSON） |
| `src/app/ai/relationship/components/DataTab.tsx` | 训练数据可视化 |

### 4.5 前端数据（public/ai/data/）

| 文件 | 说明 |
|------|------|
| `training_data_n{5,7,10,15,20}.csv` | 训练数据（β={1,2,5}，已同步） |
| `training_data_all.csv` | 全量合并（12,597 条） |
| `n{5,7,10,15,20}_metrics.json` | N₂ 模型指标 |
| `delta_from_params_metrics.json` | N₁ 模型指标 |
| `validation_predictions_n{5,7,10,15,20}.csv` | 验证集预测结果 |
| `summary.json` | 数据生成统计 |

---

## 五、模型效果

### 5.1 数据生成统计（β={1,2,5} 扩展后）

| 样本量 | 总模拟数 | 有效数 | 无解数 | 成功率 |
|--------|---------|--------|--------|--------|
| n=5 | 4,500 | 2,415 | 2,085 | 53.7% |
| n=7 | 4,500 | 2,495 | 2,005 | 55.4% |
| n=10 | 4,500 | 2,575 | 1,925 | 57.2% |
| n=15 | 4,500 | 2,543 | 1,957 | 56.5% |
| n=20 | 4,500 | 2,569 | 1,931 | 57.1% |
| **合计** | **22,500** | **12,597** | **9,903** | **56.0%** |

> 注：无解包含两类：(1) MDM 算法本身对某些样本无解；(2) 最优 δ 在搜索边界（δ=0.001 或 δ=1.0），被边界过滤丢弃。约 44% 的样本被过滤，其中大部分是小样本（n≤10）最优 δ 趋近 0 的情况。

### 5.2 N₂ 模型验证指标（路线 1，β={1,2,5} 数据）

| n | 验证 MSE | MAE | RMSE | 验证集大小 | 早停 epoch |
|---|---------|-----|------|-----------|-----------|
| 5 | 0.0279 | 0.104 | 0.167 | 483 | 92 |
| 7 | **0.0275** | **0.100** | **0.166** | 499 | 109 |
| 10 | 0.0340 | 0.113 | 0.184 | 515 | 170 |
| 15 | 0.0335 | 0.110 | 0.183 | 508 | 76 |
| 20 | 0.0305 | 0.105 | 0.175 | 513 | 96 |

> 对比 β=2 only 的旧模型（MSE 0.007-0.019），新模型 MSE 更高，因为数据量更大、参数空间更复杂。但预测范围覆盖 [0.002, 1.0]，未退化为常数预测器。

### 5.3 N₁ 模型验证指标（路线 2，β={1,2,5} 数据）

| 指标 | 值 |
|------|-----|
| 验证 MSE | 0.0520 |
| 验证 MAE | 0.161 |
| 验证 RMSE | 0.228 |
| 验证集大小 | 2,519 |
| 早停 epoch | 29 |
| 预测范围 | [0.036, 0.814] |
| 真实 δ 均值 | 0.363 ± 0.378 |

> 对比 β=2 only 的旧 N₁（预测范围 [0.246, 0.295]，恒定输出），新 N₁ 预测范围 [0.036, 0.814]，已不再是常数预测器。但早停在 epoch 29，模型表达力可能仍不足。

### 5.4 N₁ vs N₂ 对比

| 模型 | MSE | MAE | RMSE | 说明 |
|------|-----|-----|------|------|
| N₂（最佳 n=7） | 0.0275 | 0.100 | 0.166 | 直接看样本，信息量大 |
| N₁（公共模型） | 0.0520 | 0.161 | 0.228 | 只看 (β,η,γ)，信息量少 |

N₂ 效果明显好于 N₁（MSE 低 ~40%），符合预期——N₂ 直接看样本，信号更丰富。

---

## 六、前端页面 Tab 状态

| Tab | 名称 | 状态 | 内容 |
|-----|------|------|------|
| Theory | 原理说明 | ✅ 已完成 | 为什么需要、AI 与传统方法关系、工作流程 |
| Training | 训练算法 | ✅ 已完成 | 网络结构、数据预处理、训练策略、评价标准 |
| Data | 训练数据 | ✅ 已完成 | 生成方式、参数空间表格、数据规模、数据格式 |
| Playground | 在线使用 | ✅ 已完成 | 样本输入 → AI 预测 δ → 置信度评估 |
| Performance | 性能展示 | ⚠️ 部分完成 | 有指标卡片，缺可视化（热力图、箱型图） |
| Verification | 可信性验证 | ❌ 占位 | 空白占位页 |
| Compare | 方法对比 | ⚠️ 部分完成 | 有 Route 2 对比表（C5），缺 AI vs 固定 δ 箱型图 |

---

## 七、端到端调用链路

```
用户浏览器
  ↓ 填写失效时间数据
  ↓ 点击"AI 预测最优 δ"
  ↓
src/app/ai/relationship/page.tsx (PlaygroundTab)
  ↓ fetch POST /ai/relationship/mdm
  ↓
python/main.py → ai_predict_delta()
  ↓ _load_delta_model(n) — 按样本量加载对应 .pth
  ↓ 排序样本 → 标准化 → PyTorch 推理 → 反缩放
  ↓ 置信度评估（基于是否接近边界）
  ↓
返回 { optimal_delta, model_n, confidence }
  ↓
前端展示 δ 值 + 置信度 + 使用提示
```

---

## 八、当前局限性与待解决问题

### 8.1 边界过滤问题（重要）

`generate_training_data.py` 第 328-329 行有边界检查：

```python
if is_boundary:
    optimal_delta = None  # 边界最优 → 当作"无解"丢弃
```

**影响**：
- ~44% 的样本被过滤（9,903/22,500）
- 快速采样测试显示 ~60% 的样本最优 δ 在边界（主要是 δ=0.001）
- 小样本（n≤10）的最优 δ 就是 0（MSE 在 δ=0 最小）
- 只有大样本（n=20）才需要正的 δ

**MSE-δ 曲线分析结果**（`data/mse_curves/summary.csv`）：

| 参数组合 | n | 最优 δ | 说明 |
|----------|---|--------|------|
| 大部分 n=5 | 5 | 0.005 | 接近 0 |
| b5_e100_n5 | 5 | 0.03 | 略正 |
| b1_e5000_n5 | 5 | 0.17 | 中等 |
| b2_e1000_n10 | 10 | 0.005 | 接近 0 |
| b2_e1000_n20 | 20 | 0.42 | 显著正 |
| b1_e1000_n20 | 20 | 0.005 | 接近 0（β=1 时即使 n=20 也接近 0） |

**待决定**：
1. 是否去掉边界过滤，让 δ=0.001 的样本也进入训练
2. 或者将 delta_min 改为 0，搜索从 0 开始
3. 搜索策略优化：MSE(δ) 曲线 11/12 单峰，可用 Brent 法减到 ~10-15 次

### 8.2 路线 2 待重新评估

N₁ 已用 β={1,2,5} 数据重新训练，不再是常数预测器。需要用 `evaluate_route2.py` 重新评估路线 2 的效果。

### 8.3 其他局限

1. **无截尾处理** — 只支持完全样本（Type II censoring 等未处理）
2. **Performance 可视化不完整** — 缺热力图、箱型图、收敛曲线
3. **无验证案例** — Verification Tab 为空
4. **Compare 不完整** — 缺 AI vs 固定 δ 的蒙特卡洛对比

---

## 九、可扩展方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 边界过滤修复 | 去掉过滤或调整 delta_min=0 | 高 |
| 搜索策略优化 | Brent 法替代粗搜+细搜 | 高 |
| 路线 2 重新评估 | 用新 N₁ 模型跑 evaluate_route2.py | 高 |
| Performance 可视化 | 热力图、箱型图、收敛曲线 | 中 |
| 可信性验证 | 已知参数的验证案例 | 中 |
| 方法对比 | AI vs 固定 δ 的蒙特卡洛对比 | 中 |
| 模型改进 | 更深网络、学习率调度、数据增强 | 低 |
| ONNX 导出 | 减少 PyTorch 依赖 | 低 |
| 截尾支持 | 处理不完全样本 | 低 |

---

*本文档记录模块 1 截至 2026-04-27 的完整实现状态。*
