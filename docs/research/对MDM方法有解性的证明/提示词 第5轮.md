这个是我之前弄的报告，我最初的动机链条为：
**我看到原论文 → 觉得 MDM 方法可用 → 按论文思想编程 → 为了简单先用 Bernard 近似和 20 步搜索 → 蒙特卡洛时发现有些样本无解 → 开始怀疑到底是程序问题、网格问题、Bernard 近似问题，还是方法判据本身的边界问题 → 于是做测试和推导。**


报告1的意思就是，除了有解的，剩下的一部分是γ贴近t1导致网格不够，一部分是γ小于0，没有其他的了吧。那我可不可以就在算法中当无解的时候，看看是前者还是后者，是前者γ=t1，是后者γ=0，这样牺牲点精度换来全有解？