# 01~04 研究结论骨架（v3）

> 四大块，每块内按页拆分。每页 = 一个研究目的 + 对应过程 + 对应结果。

> **整体逻辑闭环：** 01 建立双视角评价体系。02 确保 MDM 能算出来。03 搞清楚最优偏移量受什么影响、部署时怎么选。04 用神经网络从样本特征直接预测 δ。

---

# 第一大块：01 用什么指标评价参数估计的准确性

## 第1页：文献里实际用什么指标？

**目的：** 不自创指标，先搞清楚文献主流用什么评价参数估计精度。

**过程：** 对 182 文件夹 68 篇文献逐篇统计精度指标使用频次。

**结果：** Bias（75%）、SD/方差（56%）、MSE/RMSE（40%）构成精度指标主干；相对误差（16%）偏低，MAE（3%）几乎不用。置信区间/敏感性/拟合优度属统计性质范畴，不纳入精度指标。结论：Bias+SD 为绝对主干，MSE/RMSE 为常见补充。

**待做实验：**
- `E01-1` ✅ 已完成：68篇文献笔记扫描，结果见01-04研究内容.md第1页

## 第2页：常见指标的定义与各自能反映什么

**目的：** 梳理常用精度指标的知识体系，明确每个指标"看的是误差的哪个维度"，为后续选择提供依据。

**过程：** 系统列出 Bias、SD、RMSE、MAE、相对误差、置信区间、拟合优度、中位数/分位数等指标的定义，分析其各自反映的信息（中心趋势、离散程度、尾部风险、无量纲化等），讨论它们的互补与冗余关系。

**结果：** Bias 看系统偏移，SD 看离散程度，MSE/RMSE 是两者的综合（MSE=Bias²+SD²），相对误差提供无量纲化视角（但 γ=0 失效），MAE 对尾部不敏感但文献几乎不用。Bias+SD 是最小充分集，MSE/RMSE 为便捷补充。

**待做实验：** 无新实验，纯知识梳理

## 第3页：为什么没有一个统一有效指标？

**目的：** 解释为什么第2页列出了许多指标，而不是直接给出一个"万能指标"。核心原因有两层：不同研究目标关心的估计效果不同；即使目标相同，单个汇总指标也可能被误差分布形态蒙蔽。

**过程：** 先从研究目标出发区分系统偏差、估计稳定性、尾部风险、跨量纲比较和工程寿命预测；再构造五个情景（含真实仿真反例），逐个说明单个指标在特定目标或分布形态下会如何失真。

**结果：**
① RMSE 相同可掩盖完全不同的偏差-方差结构 → 必须成套报告 ✅ E01-3a
② 中位/平均类指标对尾部钝感（P99.9 差 38%，最大误差差 83%）— 待E01-6
③ γ 真值接近 0 时相对指标失真 — 待E01-6
④ 参数指标排序 ≠ 寿命指标排序 — 待E01-6
⑤ 不同量纲参数的 MSE 不可横比 — 待E01-6

**待做实验：**
- `E01-3a` ✅ RMSE陷阱演示
- `E01-3b` ✅ 尾部钝感（P99.9/Median=5.4x）
- `E01-3c` ✅ γ接近0时相对指标失真（RelRMSE≈8800%）
- `E01-3d` ✅ 两视角排序差异实证
- `E01-3e` ✅ 跨量纲MSE不可横比

## 第4页：参数视角推荐方案 + 综合精度指标 J_param

**目的：** 回答"既然指标很多，参数估计到底用哪个评价"。在蒙特卡洛场景下，估计误差形成逐次分布；为了同时表示估计精度和稳定性，Bias 和 SD 是参数视角的最小充分集。若为了 δ 方案排序、神经网络训练等目的必须压缩成一个目标函数，则定义三参数联合指标 J_param。

**过程：** 综合文献调研（第1页）、指标知识（第2页）和单指标失效分析（第3页），先确定 Bias+SD 作为参数诊断主口径；再定义 J_param = √( mean( ((β̂-β)/β)² + ((η̂-η)/η)² + ((γ̂-γ)/η)² ) )，说明其几何含义是三维标准化参数误差空间中的平均欧氏距离，并解释其可同时反映整体精度与稳定性。

**结果：**
- **多指标方案（诊断用）：** 每个参数成套报告 Bias、SD、RMSE。β/η 补充相对偏差和相对 RMSE（%）。γ 只用绝对指标（禁用一切相对指标）。附报 γ̂=0 截断率。至少一张误差分布图（γ 的双峰形态必须看图）。
- **单指标 J_param（排序/训练用）：** 三参数联合相对 RMSE，全部无量纲化，吸收 Bias+SD 体系，可跨参数比较。03-04若需要一个参数视角目标函数，用 J_param；多指标仍保留用于诊断。
- 两个视角不冲突：多指标聚焦单个参数的诊断，J_param 聚焦整体效果的排序。

**待做实验：** 无新实验，综合前3页结论给出参数视角推荐表+J_param定义 ✅ 已完成

## 第5页：工程视角的必要性 + 目标分位点 RMSE

**目的：** 说明为什么参数视角不够，必须补充工程视角：可靠性工程真正关心的不是参数本身，而是指定可靠度下的寿命分位点预测是否准确。

**过程：** 给出寿命分位点公式 x_R = γ + η(-lnR)^{1/β}，说明参数估计准不等于寿命预测准（非线性传递+参数补偿结构）。工程视角按实际任务选择目标分位点：若关心 x₀.₉₅，就报告 x̂₀.₉₅ 的 Bias/SD/RMSE；若关心 x₀.₉₉，就报告 x̂₀.₉₉ 的 Bias/SD/RMSE，不把不同可靠度强行合成。

**结果：**
- **工程诊断指标：** 对目标分位点 x_R 报告 Bias_xR、SD_xR、RMSE_xR。
- **工程排序指标：** 如果只能用一个工程指标，就用目标分位点的 RMSE：RMSE_xR = √(mean((x̂_R − x_R)²))。
- 工程指标采用绝对误差形式，因为寿命偏差的时间量级本身就是工程风险；不同 R 的指标按需求分别报告，不默认合并。

**待做实验：**
- `E01-5` 工程视角必要性论证：用 E01-6 的蒙特卡洛结果，展示参数精度高但寿命精度低的反例 — 待E01-6

## 第6页：两视角不等价的实证 + 三方法蒙特卡洛比较

**目的：** 用蒙特卡洛数据证明参数视角排序 ≠ 工程视角排序，并在两个视角下分别给出三方法的定量对比。

**过程：** β=2.5, η=1000, γ=100；n∈{5,7,10,15,20,50}；每组 2000 次重复；同批样本上 MDM/MLE/LSE 三种方法各自估计。LSE 为研究用本地对照实现。指标口径调用 metrics.py::aggregate_standard_metrics()。

**结果：** 参数视角按 β 的 RMSE：MDM 全样本量最优（n=20: 0.609 < 0.676 < 0.774）。工程视角按 x₀.₉₅ RMSE：LSE ≈ MDM（n=20: 97.8 ≈ 97.7）。**MLE 小样本灾难性失效**：n=5 失败率 54.4%。γ 是最难估的参数。γ̂=0 截断率 MDM 约 20%，LSE 无截断但有近零尖峰（~40%）。n≥50 三法趋同。

**待做实验：**
- `E01-6` ✅ 已完成：β=2.5, η=1000, γ=100；n∈{5,7,10,15,20,50}；2000次/组；MDM/MLE/LSE 配对估计；LSE 为研究用本地对照实现；结果见01-04研究内容.md第6页

## 第7页：第一大块指标选择总结

**目的：** 汇总第一大块，明确后续研究中两个评价视角各用什么指标，避免把参数估计目标和工程寿命目标混在一起。

**过程：** 回顾第1-6页的文献统计、指标知识、单指标失效分析、参数视角指标、工程视角指标和三方法蒙特卡洛对比。

**结果：**
- **参数视角：** 常规报告 Bias+SD+RMSE；需要一个排序/训练目标时用 J_param。
- **工程视角：** 先选目标可靠度 R，再报告 x_R 的 Bias/SD/RMSE；需要一个工程排序指标时用 RMSE_xR。
- **两者的取舍取决于研究目标：** 研究参数估计能力时看参数视角；面向寿命预测或工程决策时看目标分位点视角。04中必须明确神经网络优化目标属于哪一视角。

**待做实验：** 无新实验，纯总结页

---

# 第二大块：02 MDM 方法的有解性证明

## 第1页：无解现象与理论存在性

**目的：** 复刻 MDM 方法，发现"无解"现象；从理论上证明合法根几乎总是存在，从而推断"无解"必有具体原因。

**过程：** 按原文实现 MDM（偏置求根判据 δ=0.1），在多个配置上做蒙特卡洛检验，发现无解现象。然后推导 σ²_η(γ) 的解析形式：σ²_η = C_uu − 2γC_uv + γ²C_vv（开口向上二次函数），包络梯度 g(γ) = (γC_vv − C_uv)/S(γ) 单调上升。存在性判据：s_v > offset 且无约束根 ∈ [0, t₍₁₎) ⟹ 合法根存在且唯一。常用区间 s_v 远大于 0.1，存在性几乎不受限制。

**结果：** 理论预测合法根几乎总存在 → 实际观察到的"无解"必定另有原因 → 只能是：①根落在 γ<0 被截断（非法根）②根贴近 t₍₁₎ 被网格漏检（数值问题）。

**待做实验：** 无。纯论述页。

## 第2页：无解的两类成因

**目的：** 搞清楚"无解"到底是怎么来的，用两张图直观展示两类成因。

**过程：** 对无解样本逐个追因，检查无约束根的位置和网格分辨率。构造两个典型示例并画图。

**结果：** 无解来自两类成因：
- **成因① 下界切除（主因）：** 无约束根落在 γ<0 处，被物理约束 γ≥0 切掉。差异函数本身有良好定义的偏置根，只是不合法。γ₀/η 越大有解率越高——真值离 0 越远，根越不容易落入负区。
- **成因② 网格漏检（次因）：** 根紧贴 t₍₁₎，均匀网格看不到。n 越大时 t₍₁₎ 越贴近 0，漏检更严重。

**待做实验：**
- `E02-1` 负根示例图：构造一个典型的成因①样本，画差异函数曲线，标注无约束根（γ<0）和 γ=0 截断线，直观展示"根为负被切掉"
- `E02-2` 漏检示例图：构造一个典型的成因②样本，用 20 个搜索点画差异函数离散采样，标注根非常靠近 t₁ 导致漏检的情况

## 第3页：工程方案——截断+拟合

**目的：** 给出处理两类无解成因的工程方案。

**过程：** 针对两类成因分别处理：成因①直接截断到 γ̂=0（物理约束，最稳）；成因②不依赖密集网格，改用拟合（解析梯度）直接推算根的位置。

**结果：**（待填充）

**待做实验：** 无独立实验。成因①截断策略在 E01-6 中已验证（MDM γ̂=0 截断率 ~20%，误差可控）。成因②拟合方案在后续 03/04 的求解流程中自然体现。

## 第4页：始终有解方案的精度验证

**目的：** 证明截断+拟合方案不仅提升了可用性（failure_rate → 0%），而且没有以不可接受的精度代价换来的。这是第二大块从"能算"到"能算且可信"的关键一步。

**过程：** 同一批样本、同一 δ=0.1，对比两组策略：
- **对照组：** 标准网格搜索（20点），无解直接标记失败，指标只算 valid 样本
- **实验组：** 截断（γ<0→γ̂=0）+ 拟合（近右端根用解析梯度推算），100% 给出估计

报告指标：
- valid_rate / failure_rate
- J_param
- RMSE_β / RMSE_η / RMSE_γ
- RMSE_x₀.₉₅ / RMSE_x₀.₉₉
- γ̂=0 截断率与 near-zero 率

三层次对比：
1. 可用性提升：failure_rate 从 X% → 0%
2. valid-only 子集精度：同一组 valid 样本上两组的 J_param 对比
3. 全样本精度：实验组 vs 对照组 valid-only，看"补回来的那些样本"质量如何

**结果：**（待填充——目标：始终有解方案显著提升可用性，且主精度指标未出现不可接受退化）

**待做实验：**
- `E02-3` 始终有解方案精度验证：β=2.5, η=1000, γ=100；n∈{5,7,10,20}；2000次/组；δ=0.1 固定；对照组（标准网格，valid-only）vs 实验组（截断+拟合，100%有解）；报告上述全部指标

---

# 第三大块：03 最优偏移量 δ 的选取

> 核心任务：重新定义并重做第三大块实验，回答“最优偏移量 δ 到底由什么决定、在部署时能不能被可靠选择”。当前 `程序/`、`实验数据/` 中的第三大块结果属于旧骨架产物；旧图像已移至 `图像/历史/`。本块写作上只保留“第x页”页码；实验编号只出现在“待做实验”中。

## 第1页：最优 δ 的研究目标与评价口径

**研究目标：** 明确第三大块到底优化什么。这里不再泛泛说“找最优偏移量”，而是先确定：δ 的优劣按三参数估计误差评价，主口径使用第一大块确定的参数视角综合指标；工程寿命分位点指标可以作为补充诊断，但不混入主排序目标。

**过程：** 定义单样本平方损失 `loss_i(δ)` 和层级聚合方式 → 给出 L0~L5 各层级最优 δ 的统一公式 → 建立实验数据契约（样本级明细 → 配置级汇总 → 层级汇总 → manifest）。确保后续任何 δ 扫描、层级 oracle、部署策略或神经网络标签都追溯到同一套 `loss_i(δ)` 和同一套统计口径。

**预期结果：** 形成 L0~L5 层级定义表和统一风险函数公式，后续实验以此为合同。旧实验中 `mean(j_i)` 与 `J_param` 混用问题在新合同里消除。**第1页仅需层级定义表格，不需要图表。**

**待做实验：**
- 第1页实验：建立新版 δ 扫描数据契约，输出样本级明细、配置级汇总、层级汇总和 manifest。
- 第1页复核：确认主排序指标、诊断指标、图表指标分别写清，不再把稳定性/精度/工程寿命指标混成一个结论。

## 第2页：δ 扫描与最优 δ 的影响因素

**研究目标：** 在统一 `J_param` 口径下，展示各层级如何根据风险曲线选择最优 δ，并比较层级间的单样本误差分布差异。

**过程：** 生成 L0~L5 共 7 组图表（详见研究内容第2页图表清单）：
- L0：全局 δ-J_param 曲线，标注 δ=0.1 和 δ*₀
- L1：δ-J_param 按 n 分组曲线（3条），附 n→δ*_n 柱状图
- L2：δ-J_param 按 β 分组曲线（5条），附 β→δ*_β 散点图
- L3：β×n 热力图（δ*_{β,n}）
- L4：按 γ/η=0.1/0.5/1.0 三张 β×n 热力图（δ*_{β,n,γ/η}）
- L5：δ*_i 分布直方图 + 按 β 分组的小提琴图
- 误差分布：L0~L4 每层级选 3 个代表性配置，画 j_i 分布直方图

统计分析同时报告效应大小：不同 β 分组的 `δ*` 跨度、不同 n 带来的增益、γ/η 是否改变主趋势。

**预期结果：** 获得各层级 δ-J_param 关系图和最优 δ 选择图，直观显示 β 是主驱动、逐样本异质性显著。另外获得各层级单样本误差分布对比。影响因素排序为待验证假设，最终以新实验结果为准。

**待做实验：**
- 第2页实验：新版 δ 扫描，生成每个配置的 `δ-R(δ)` 曲线。
- 第2页统计：按 β、n、γ/η 分组计算 `δ*`、最小风险、相对 Default 改善率和效应排序。
- 第2页图表：按研究内容第2页图表清单生成全部 7 组图像。

## 第3页：信息层级与 oracle 阶梯

**研究目标：** 将 L0~L5 信息层级定义和 J_param 数值合并为单一统一阶梯表，回答"如果能知道更多信息，风险上界能下降多少"。这一页只讨论信息层级，不把可部署策略和 oracle 上界混写。

**过程：** 基于第2页新版样本级明细，重新计算各层级最优 δ：L0 全局固定，L1 按 n，L2 按 β，L3 按 β,n，L4 按 β,n,γ/η，L5 逐样本事后最优。每个层级都用相同的样本全集和相同的 `J_param` 风险口径报告。合并层级定义和 J_param 数值为单一阶梯表，列：层级 | δ使用方式 | 可部署性 | J_param | vs Default | 边际增益。

**预期结果：** 形成单一统一口径阶梯表，逐级标注信息增益。清楚区分：Default/L0/L1 是部署可接近的基线，L2~L4 是真值 oracle，L5 是事后 oracle 上界。**第3页只保留此统一阶梯表，不重复嵌入分布图（所有分布图已归第2页）。**

**待做实验：**
- 第3页实验：新版 L0~L5 层级最优计算。
- 第3页统计：计算各层级 `J_param`、相对 Default 改善率、边际收益和分项 Bias/SD/RMSE。

## 第4页：真值未知时的可部署 δ 选择方案

**研究目标：** 检验两个方向上不依赖真值的 δ 选择能否可靠部署：方向1（MDM 自迭代）、方向2（外部 β 预估）。以"能否部署"为核心判据——方法不仅要在 valid-only 子集上好看，还要在全样本、失败样本 fallback、不同配置分组上都可解释。

**过程：** 分两个方向实施：
- **方向1（MDM 自迭代）：** δ=0.1 → MDM 估 β̂ → 查 δ*(β̂,n) → 迭代至收敛
- **方向2（外部 β 预估）：** L 矩/WMLE/双δ/MDM0 等方法估 β̂ → 查 δ*(β̂,n) → MDM 拟合

所有方法必须报告全样本性能、有效率、失败样本处理、fallback 后性能、按配置分组的改善/恶化热力图。valid-only 结果只能作为诊断，不能作为部署性能结论。

**预期结果：** 旧实验提示两条路线均可能无法在全参数空间稳定超越 Default δ=0.1，最终以新口径重算结果为准。若新实验确认显式 β 估计路线仍不稳健，则第三大块自然导向第四大块：神经网络不应直接使用真值，而应从可观测样本特征端到端学习 δ 策略。

**待做实验：**
- 第4页实验：重新实现并评估方向1（自迭代）和方向2（外部 β 预估：L 矩/WMLE/双δ/MDM0）。
- 第4页统计：每个策略报告全样本 `J_param`、valid-only `J_param`、有效率、fallback 后 `J_param`、相对 Default 改善率。
- 第4页图表：方案阶梯表、按配置改善热力图、失败率/有效率图。

## 第5页：第三大块到第四大块的实验交接

**研究目标：** 把第三大块结论转化为第四大块的训练路线。明确两条路线（L4 配置级最优映射、L5 逐样本最优映射），以及为何神经网络能做成而显式 β 估计做不成。

**过程：** 
- 论述神经网络 vs L 矩的本质差异：多维样本统计特征 vs 单统计量 τ₃，信息利用率有质的差别
- 定义两条训练路线：L4（22,500 样本 → 45 个 δ* 标签）和 L5（22,500 样本 → 22,500 个 δ*_i 标签）
- 输入特征：样本次序统计量、间距比、偏度、变异系数、t₁/tₙ、n
- 训练方式：统计特征作为固定维度输入（MLP），而非原始样本序列

**预期结果：** 
- **Baseline：** Default 0.6353（L 矩因有失败率不作为强制 baseline）
- **路线1 目标：** J ≤ L4 oracle 0.5701
- **路线2 目标：** J ≤ L5 oracle 0.4941
- **04 成功标准：** 全样本有效且 J 优于 Default 0.6353，逼近 oracle 上界

**待做实验：**
- 第5页整理：从新版第三大块结果生成第四大块所需训练标签表、baseline 表和数据切分方案。
- 第5页复核：检查第四大块不使用部署时不可见的真值特征，训练标签与模型输入严格分离。

---

### 03 工程视角复算：以 x₀.₉₅ RMSE 重新评价

**研究目标：** 检验参数视角（J_param）最优 δ 是否也适用于工程目标 x₀.₉₅。

**过程：** `RMSE_x95 = √mean((x̂₀.₉₅ − x₀.₉₅_true)²)`，绝对寿命误差。重算 L0-L5 阶梯、方向1 自迭代、视角错配 penalty。所有文件 `_x095` 后缀。

**结果：**
- Default RMSE_x95 = 0.1407，L0 δ* = 0.00 仅改善 -0.7%
- L0→L4 配置级收益仅 1.8%（vs J_param 的 10.3%）——δ 选择对 x₀.₉₅ 几乎无影响
- L5 oracle -13.2%，逐样本仍有空间但远小于 J_param（-22.2%）
- 视角错配 penalty 均值 2.4%：β=1.5 ~7.3%，β=2.0 ~2.8%，β=2.5 ~1.4%，β=4/5 <1%
- 自迭代改善 +0.4%，无工程意义

**对 04 的影响：** 完整 NN 训练在工程视角下成本/收益不匹配。04 仅说明此负结果，不展开完整实验。

---

# 第四大块：04 基于神经网络的MDM偏移量自适应选取

> 核心问题：训练时可用真值生成 oracle 标签，使用时只有样本可观测特征。神经网络要证明仅凭样本特征能逼近 oracle 层级，且 J_param 优于 Default 0.6353。

## 第1页：03→04 实验契约

**研究目标：** 建立从第三大块到第四大块的完整实验合同——明确哪些信息训练时可用、部署时禁用，定义数据流、标签来源、特征来源和 train/test 划分。

**过程：**
- 定义三条训练路线的标签来源：L4 δ*_{β,n,γ/η}（45个值）、L5 δ*_i（22,500个值）、26-grid 风险曲线（每样本 26 维 loss_i(δ)）
- 特征来源：从 22,500 个样本提取统计特征（次序统计量、间距比、偏度、CV、t₁/median、tₙ/median、log(tₙ/t₁)、n）。真 β/η/γ 禁止作为输入
- 数据划分：同配置内 rep 7:3 随机划分（同分布检查）、blocked split 用于泛化页
- 损失函数：MSE（回归 δ*）、交叉熵（分类到 26 个 δ）、KL/JS（风险曲线）、或直接优化 J_param

**预期结果：** 形成实验合同文档，列出完整的输入/输出/标签规格、数据流、禁止泄漏项。

**待做实验：**
- `E04-1` 特征提取：22,500 样本 → 统计特征矩阵 + 三种标签向量

## 第3页：为什么 Risk curve 优于硬标签

**研究目标：** 从理论和实验两方面解释 Risk curve（26维 loss 预测）为何优于 L4/L5 硬标签回归。

**过程：**
- 论证硬标签的三种先天性缺陷：平谷底惩罚、argmin 噪声过拟合、逐样本信息丢失
- 解释 Risk curve 的优势：容忍近似等价的 δ、不拟合 argmin 随机波动、保留逐样本异质性
- 用已完成的 E04-2 实验结果定量对比 Risk vs L5 vs L4

**预期结果：** 阐明 Risk curve 提升的机理，为后续实验选择 Risk curve 作为主路线提供理论支撑。

## 第2页：特征工程 + 标签设计 + 网络架构

**研究目标：** 详细定义输入特征、标签格式、损失函数和 MLP 架构。

**过程：**
- 特征清单（尺度归一化）：次序统计量均值/方差、间距比均值/方差、偏度、变异系数 CV、t₁/median、tₙ/median、log(tₙ/t₁)、n。全部 z-score 标准化
- 三条标签路线：L4 hard label（回归 δ*）、L5 hard argmin（回归 δ*_i）、risk curve（26 维 soft label，KL 散度或 MSE 损失）
- MLP 架构：输入层（特征维度）→ 隐藏层（256→128→64）→ 输出层（1 或 26），ReLU + Dropout(0.2) + BatchNorm。AdamW，cosine annealing，early stopping
- 训练超参数：batch size 256，lr 1e-3，weight decay 1e-4，epochs 200

**预期结果：** 形成特征/标签/架构规格表。

**待做实验：**
- `E04-2` 基线训练：三条路线在 7:3 split 上训练，记录 loss curve、val J_param

## 第3页：为什么 Risk curve 优于硬标签

**研究目标：** 同配置内 rep 7:3 划分，对比三条路线 vs Default/L4/L5 oracle。

**结果（已完成）：**
| 方案 | J_param | vs Default(0.6337) |
|------|---------|:---:|
| Risk curve | 0.5332 | -15.9% |
| L5 hard | 0.5434 | -14.2% |
| L4 hard | 0.5891 | -7.0% |

Risk curve 逼近 L5 Oracle (0.4941)，已超越 L4 Oracle (0.5701)。逐样本标签优于配置标签。三路线均显著优于 Default。

## 第4页：同分布主结果

**结果（已完成）：**
| 泛化 | Risk J | vs Default | gap |
|------|--------|:---:|:---:|
| 跨 β | 0.5389 | -12.9% | +0.006 |
| 跨 n | 0.5474 | -14.9% | +0.014 |
| 跨 γ/η | 0.6392 | -0.9% | +0.106 |

跨 β/n 泛化良好，跨 γ/η 退化至接近 Default（γ/η 信息弱）。模型学到了 β 规律而非记忆。

## 第5页：泛化能力

**结果（已完成）：**
- 标签：Risk > L5 > L4（+1.6pp / +10.5pp vs Risk）
- 特征：尾部特征贡献最大（+0.023），其次形态（+0.008），n 最小（+0.006）
- 架构：所有变体差距 < 0.005，不敏感

## 第6页：消融实验

**NN Risk curve J=0.5332（-16.1% vs Default），跨 β 泛化稳定，推荐替代 Default δ=0.1。**
