我现在要从头捋一下01-04骨架，给出一个骨架调整建议



**第一大块：同意，但有个小问题**

把 J_param 融入第4页、新增 J_eng 到第5页、第7页做极简总结——逻辑很顺。

问题：当前第4页已经有"参数视角推荐方案"的表格（Bias/SD/RMSE/相对误差那张），J_param 是在那张表**之外**加的综合指标。融进去后，这页会变成：先给多指标方案表，再给 J_param
单指标定义。内容密度会不会太高？还是你觉得 J_param 应该**替代**那张表？

另外 J_eng 的定义需要确认——`((x̂₀.₉₅ - x₀.₉₅)/x₀.₉₅)²` 这种相对形式，还是绝对形式？考虑到 x₀.₉₅ 的量级（E01-6 里约 400~500），用相对形式更合理，和 J_param 口径一致。

---

**第二大块：E02-3 是关键缺口，但实验设计要注意**

缺的精度验证页我完全同意。但 E02-3 的实验设计有一个细节：你说"同一批样本、同一 δ=0.1，对比两种策略"——这里的"历史可能失败/漏检策略"具体指什么？是直接丢弃无解样本（valid-only），还是用旧的网格搜索（20 点）？

我建议这样定义：
- **对照组**：标准网格搜索（20 点），无解直接标记失败，指标只算 valid 样本
- **实验组**：截断+拟合方案，100% 给出估计

这样对比能同时看到：
1. 可用性提升（failure rate 从 X% → 0%）
2. 精度代价（valid-only 子集上两组的 J_param 对比）
3. 全样本精度（实验组 vs 对照组 valid-only，看"补回来的那些样本"质量如何）

---

**第三大块：第2页的三个方案很实际，但方案2需要措辞小心**

方案2"外部β预估"你说用 LSE——但 LSE 在我们的代码里是"研究用本地对照实现"，不是平台正式方法。这里有两个选择：
1. 用 LSE 作为示例，但标注"研究用对照"
2. 用 MLE（n≥10 时失败率低），但 n 小了又不行

或者更中性地说：**"任意能稳定估计 β 的方法"**，不具体绑定哪个方法，LSE 只是验证用的一个实例。

方案阶梯表的想法很好，但"是否需要真值"列需要细化——查表方案不需要真值，但需要预估 β；Oracle 需要真值但只做研究参照。

---

**第四大块：重构后逻辑更稳，但有几页旧内容要处理**

旧骨架里引用了具体实验编号（E04-2a/b/c/d, E04-3, E04-4a/b/c, E04-5a/b/c/d, E04-6a~e），这些实验设计是否还适用？特别是：

- 旧第2页"特征设计"里的"多偏移读出曲线"——这个在新架构里放在第3页"候选映射方案"还是第2页"可部署输入"？
- 旧第4页的"架构确定"和旧第5页的"双损失对比"——在新架构里合并成了第4页"训练目标与损失函数"，那具体的架构消融实验（GBDT vs MLP vs 函数型）放哪里？

建议：旧实验编号先不删，等新骨架定稿后再重新分配。

---

**还有一个整体问题：03→04 的桥怎么搭？**

新第3页（候选映射方案）里的选项有查表、迭代、外部β、样本特征模型、NN。如果03的结论是"L3 查表已经够好"，那04就只做查表验证，NN 变成附录。但如果03结论是"L5 明显更好"，04就要全力做NN。

这意味着04的页数和深度**取决于03的结果**。骨架里要不要预留分支？比如：

```
如果 L3 够好 → 04 只做3页（查表实现 + 迭代验证 + 方案阶梯）
如果 L5 明显好 → 04 做完整6页
```

还是你倾向于04先按完整6页写，等03结果出来后再砍？

---

你先回应这几个点，我再动手改骨架。