**交接提示词：**

你好，你接手的是博士研究项目"基于神经网络的MDM偏移量自适应选取"（Research 04）。工作目录：

```
D:/weibull/docs/research/04基于神经网络的MDM偏移量自适应选取/
```

**第一步必读三个文件：**
1. `进度控制.md` — 当前状态、已完成清单、代码调用约定
2. `01-04研究骨架.md` — 整体框架（四大块，每页=研究目的+过程+结果）
3. `01-04研究内容.md` — 已完成的实验结论

**当前进度：**
- ✅ 第一大块（01）：7页全部完成（E01-1~E01-6，含文献统计、指标陷阱、三方法蒙特卡洛等）
- ✅ 第二大块（02）：4页全部完成（无解现象、两类成因、工程方案、精度验证待做）
- 🔄 第三大块（03）：**只写了第1页**（层级探索，L0~L5 J_param 对比）。第2页（真值未知时的可部署δ选择方案：自迭代/外部β/样本特征映射三种方案）还没写。
- ❌ 第四大块（04）：未开始

**03 第1页已完成的实验数据（都在 实验数据/ 文件夹）：**
- E03-1：27个分片CSV（β∈{2,2.5,4}×n∈{7,10,20}×γ/η∈{0.1,0.5,1.0}，每配置26个δ×500rep = 351,000次MDM拟合）
- E03-2：`E03-2_level_optimal_jparam.csv` — L0~L4各层级最优δ*（J_param口径）
- E03-3：`E03-3_L5_oracle_jparam.csv`（13,500行）— 逐样本Oracle最优δ*
- `E03-3_jparam_by_config.csv`（702行）— 每个(β,n,γ/η,δ)配置的J_param
- `E03-3_summary.csv`（702行）— 每个配置的Bias/SD汇总

**关键指标定义：**
- J_param = √( mean( ((β̂-β)/β)² + ((η̂-η)/η)² + ((γ̂-γ)/η)² ) ) — 参数视角综合指标
- J_eng = √( mean( (x̂₀.₉₅ − x₀.₉₅)² + (x̂₀.₉₉ − x₀.₉₉)² ) ) — 工程视角综合指标（绝对形式）

**工作规范：**
- 画图用 `scientific-visualization` skill 的 Nature 样式（`configure_for_journal('nature')`，中文用 SimHei）
- 程序存 `程序/`，图像存 `图像/`，数据存 `实验数据/`
- 结论写入 `01-04研究内容.md`，进度更新 `进度控制.md`
- 不改 `D:/weibull/python/` 下的平台代码，需要改动先问用户
- 代码调用：`sys.path.insert(0, "D:/weibull/python")` 然后 `from methods.mdm import MDM` 等
- 每步闭环：设定→实验→数据→图→结论→汇报