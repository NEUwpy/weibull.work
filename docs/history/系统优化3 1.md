❯ 现在参数固定方法子页面不是有一个方法示例吗？现在只是有一个示例一，这个里头呢是这些参数加上样本量和方法的内部  
量作为，无论是变量还是常量，肯定是主角是他们。然后呢，底下有蒙特卡洛仿真的数据展示和就是那些内部计算的计算方法  
的计算设置，还有这两个设置。我现在比较想的呢，就是他俩给作为变量，一个是示例二，一个是事例三，示例二就是蒙特卡  
洛这些的这个变量势力。三就是方法设置的点亮。但是这个应该怎么设计界面好呢？我想的就是极大可能的跟这个复用示例一  
的界面，就基本上都一模一样。只不过呢是它那个示例一，现在不是那些变量了，没有那么能想的了，顶天能选的就是降本量  
啊，这个没有选。然后呢，这个他选不是说我一下全选，或者说怎么地，我是说我可以点击，比如说这样本量可以取三四、五  
六七，然后我可以点n等于三，然后n等于四或者n等于四，或者n等于五，这样是像个tab一样。然后这里头的变量就是相当于这 
个重复次数，可以设置为一千两千三千四千五千。然后其实这样的话就是一个n做5000组，其中1000、2000、3000、4000都是包 
括在内的，然后就是和示例一一样的表格，这不也是属于单变量吗？然后和示例一一样的概种箱形图和这种箱形图，只不过底  
下要再多一个以重复次数为变量的。从这边是重复次数从1000到5000的步长是步长以一个以重复次数为横坐标，以估计中位数  
为纵坐标的一个图。然后这个图可以有一个tab，它可以切换，是中位数还是均值，就是改纵坐标的这个含义。先跟我说你打算 
怎么完成这个示例2，其它的工作需要等到我看方案可以了才能干

● 我来分析一下示例2的设计方案。 

  示例2 设计方案                                                                                                   
  核心思路                                                                                                      
  
  - 固定：参数（β, η, γ, δ）、方法设置
  - 变量：样本量 n（多选） + 蒙特卡洛重复次数（单选查看）

  界面布局

  ┌─────────────────────────────────────────────────────────┐
  │ 固定参数区（只读显示）                                    │
  │ β=2  η=1000  γ=1000  δ=0.1                              │
  ├─────────────────────────────────────────────────────────┤
  │ 样本量选择（Tab多选）                                     │
  │ [✓ n=3] [✓ n=4] [ n=5] [✓ n=6] [ n=7]                  │
  ├─────────────────────────────────────────────────────────┤
  │ 重复次数选择（Tab单选）- 控制下方表格和箱形图              │
  │ [1000] [2000] [3000] [4000] [5000]                      │
  ├─────────────────────────────────────────────────────────┤
  │ 统计表格（复用示例1）                                     │
  │ ┌─────┬────────┬────────┬────────┬────────┐            │
  │ │  n  │ β均值  │ β中位数│ 无解率 │ ...    │            │
  │ ├─────┼────────┼────────┼────────┼────────┤            │
  │ │  3  │ 1.85   │ 1.92   │ 10.8%  │        │            │
  │ │  4  │ 1.91   │ 1.95   │ 9.2%   │        │            │
  │ │  6  │ 1.96   │ 1.98   │ 7.5%   │        │            │
  │ └─────┴────────┴────────┴────────┴────────┘            │
  ├─────────────────────────────────────────────────────────┤
  │ 箱形图（复用示例1，按选中的n分组显示）                     │
  │ ┌─────────────────────────────────────┐                │
  │ │     ┌──┐   ┌──┐     ┌──┐           │                │
  │ │  ├──┤  ├───┤  ├───┬─┤  ├───        │                │
  │ │  │  └──┘   └──┘   │ └──┘           │                │
  │ │  n=3      n=4     n=6              │                │
  │ └─────────────────────────────────────┘                │
  ├─────────────────────────────────────────────────────────┤
  │ 收敛曲线图（新增）                                        │
  │ 纵坐标: [中位数] [均值] [标准差] ← Tab切换                │
  │ ┌─────────────────────────────────────┐                │
  │ │ β均值                                │                │
  │ │ 2.1 ─┐                              │                │
  │ │     │   ╭─────n=3                   │                │
  │ │ 2.0 ─┤  ╱   ╭───n=4                 │                │
  │ │     │ ╱   ╱                        │                │
  │ │ 1.9 ─╱───╱────n=6                   │                │
  │ │     └──────────────────────────     │                │
  │ │      1000  2000 3000 4000 5000      │                │
  │ └─────────────────────────────────────┘                │
  └─────────────────────────────────────────────────────────┘

  数据生成策略

  一次运行，多层切片：
  # 对每个选中的n，运行5000次仿真
  mc_runs = 5000

  # 统计时切片计算
  for mc in [1000, 2000, 3000, 4000, 5000]:
      stats = calculate_stats(results[:mc])  # 取前mc个

  这样选中3个样本量只需运行 3×5000=15000 次 MDM，而不是 3×(1000+2000+...+5000)=45000 次。

  文件结构

  public/case-studies/mdm/demo2/
  ├── config.md          # 配置（固定参数 + 样本量选项 + 重复次数选项）
  └── data.json          # 嵌套结构 {n: {mc_runs: stats}}

  src/components/methods/mdm/case-studies/
  ├── shared/            # 新增：抽取公共组件
  │   ├── StatsTable.tsx
  │   └── BoxPlot.tsx
  └── demo2/
      └── Demo2Viewer.tsx

  ---
  问题确认：
  1. 样本量选择是多选（可同时看n=3,4,6的对比），还是单选？
  2. 箱形图是每个n一张图，还是所有n在一张图里对比？
  3. 收敛曲线图的纵坐标除了中位数、均值，还需要加"标准差"吗？

✻ Baked for 45s

❯ 1.我想要布局和示例1基本一样，也就是不进行大的改动，保证能最大化复用。组件应该放到C:\Web\Weibull\src\component s\shared这里，因为横坐标是n纵坐标是估计值的属于通用图表。组件应该放C:\Web\Weibull\src\components\methods\mdm\st udies这里而不是C:\Web\Weibull\src\components\methods\mdm\case-studies这里，前者是方法示例，后者是案例展示。样本 量选择是多选，跟目前方法示例1的设为显示维度一个性质。2.箱型图是所有n在一张图，但是一个参数一张图，概率密度、新  增的n-中位数/均值图表也都一样，这俩是都有β/γ/η切换，这个是和方法示例1的是一样的感觉的。并且概率密度那个图一定要 复用组件而不是新创建。3.加上。图表应该是三个，箱型图，概率密度图（这俩和方法示例1是一模一样的，就用一样的组件） 加上收敛图。样本量选择可以选择某几个，通过点击数字组件，也可以全选，通过设为显示维度。你再更新一下你的理解，跟  我说一下                                                                                                        
                                                                                                                
● 更新后的理解                                                                                                                                                                                                                  
  文件位置                                                                                                         
  ┌──────────────┬───────────────────────────────────────────┐                                                  
  │     内容     │                   路径                    │
  ├──────────────┼───────────────────────────────────────────┤
  │ 通用图表组件 │ src/components/shared/                    │
  ├──────────────┼───────────────────────────────────────────┤
  │ 示例2组件    │ src/components/methods/mdm/studies/demo2/ │
  ├──────────────┼───────────────────────────────────────────┤
  │ 配置和数据   │ public/studies/mdm/demo2/                 │
  └──────────────┴───────────────────────────────────────────┘

  界面布局（与示例1高度一致）

  ┌─────────────────────────────────────────────────────────┐
  │ 固定参数区（只读，β/η/γ/δ 固定）                          │
  ├─────────────────────────────────────────────────────────┤
  │ 样本量选择                                               │
  │ [n=3] [n=4] [n=5] [n=6] [n=7]    [全选] ← 设为显示维度   │
  ├─────────────────────────────────────────────────────────┤
  │ 重复次数选择（Tab单选）                                   │
  │ [1000] [2000] [3000] [4000] [5000]                      │
  ├─────────────────────────────────────────────────────────┤
  │ 统计表格（复用）                                          │
  ├─────────────────────────────────────────────────────────┤
  │ 图表区 - Tab切换参数 [β] [η] [γ]                         │
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ 箱型图（复用示例1组件）                               ││
  │ │ 一张图显示所有选中的n                                 ││
  │ └─────────────────────────────────────────────────────┘│
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ 概率密度图（复用示例1组件，完全复用）                   ││
  │ │ 一张图显示所有选中的n                                 ││
  │ └─────────────────────────────────────────────────────┘│
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ 收敛图（新增通用组件 → shared/）                       ││
  │ │ Tab: [中位数] [均值] [标准差]                         ││
  │ │ 横坐标: 1000→5000，多条曲线（每个n一条）              ││
  │ └─────────────────────────────────────────────────────┘│
  └─────────────────────────────────────────────────────────┘

  三个图表总结

  ┌────────────┬────────────────┬────────────────────────────────┬─────────────────────┐
  │    图表    │      来源      │            参数切换            │      数据维度       │
  ├────────────┼────────────────┼────────────────────────────────┼─────────────────────┤
  │ 箱型图     │ 复用示例1      │ β/η/γ Tab                      │ 选中的所有n，当前mc │
  ├────────────┼────────────────┼────────────────────────────────┼─────────────────────┤
  │ 概率密度图 │ 复用示例1      │ β/η/γ Tab                      │ 选中的所有n，当前mc │
  ├────────────┼────────────────┼────────────────────────────────┼─────────────────────┤
  │ 收敛图     │ 新建(放shared) │ 中位数/均值/标准差 + β/η/γ Tab │ 横轴mc，每个n一条线 │
  └────────────┴────────────────┴────────────────────────────────┴─────────────────────┘

  数据结构

  {
    "fixed_params": {"beta": 2, "eta": 1000, "gamma": 1000, "offset": 0.1},
    "sample_sizes": [3, 4, 5, 6, 7],
    "mc_runs": [1000, 2000, 3000, 4000, 5000],
    "data": {
      "3": {
        "1000": {"beta": {...}, "eta": {...}, "gamma": {...}},
        "2000": {...},
        ...
      },
      "4": {...}
    }
  }

  复用策略

  1. 箱型图 + 概率密度图：直接复用示例1的组件，传入不同的数据
  2. 收敛图：新建通用组件 ConvergenceChart.tsx 放 shared/
  3. 样本量选择器：复用示例1的多选逻辑

  ---
  确认问题：
  4. 表格要保留吗？还是示例2不需要表格？
  5. "设为显示维度"在示例1是单选（热力图的一个维度），示例2是作为"全选样本量"的按钮，对吗？

❯ Tab切换参数 [β] [η] [γ]应该是每个图都有控制的，例如方法示例1，概率密度是自己控制tab的。1.表格需要保留，也是复 用示例1的样式和形式。2.对。这个示例2的样本量相当于示例1不是由四个变量吗，这回示例2还复用示例1的形式，只不过只有 样本量是变量了。就是这样。然后参数、蒙特卡洛和计算配置的组件位置样式大小都是复用方法示例1的                                                                                                                                     
● 最终理解                                                                                                      

  界面布局（完全复用示例1的形式）                                                                                                                                                                                               
  ┌─────────────────────────────────────────────────────────┐                                                     │ 参数区（固定，只读，样式同示例1）                          │                                                
  │ β=2  η=1000  γ=1000  δ=0.1                              │
  ├─────────────────────────────────────────────────────────┤
  │ 蒙特卡洛设置区（样式同示例1，内容改为重复次数选择）         │
  │ 样本量: [n=3] [n=4] [n=5] [n=6] [n=7]    [设为显示维度]  │
  │ 重复次数: [1000] [2000] [3000] [4000] [5000]            │
  ├─────────────────────────────────────────────────────────┤
  │ 计算配置区（固定，只读，样式同示例1）                      │
  │ 方法: MDM | 排序: Bernard | ...                         │
  ├─────────────────────────────────────────────────────────┤
  │ 统计表格（复用示例1组件）                                 │
  ├─────────────────────────────────────────────────────────┤
  │ 箱型图                                                   │
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ [β] [η] [γ] ← 每个图独立的Tab                         ││
  │ │                                                     ││
  │ │  ┌──┐   ┌──┐     ┌──┐                              ││
  │ │  │  │   │  │     │  │  （所有选中的n在一张图）        ││
  │ │  n=3    n=4      n=6                               ││
  │ └─────────────────────────────────────────────────────┘│
  ├─────────────────────────────────────────────────────────┤
  │ 概率密度图（复用示例1组件）                               │
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ [β] [η] [γ] ← 每个图独立的Tab                         ││
  │ │                                                     ││
  │ │  概率密度曲线（所有选中的n在一张图）                   ││
  │ └─────────────────────────────────────────────────────┘│
  ├─────────────────────────────────────────────────────────┤
  │ 收敛图（新增通用组件）                                    │
  │ ┌─────────────────────────────────────────────────────┐│
  │ │ [中位数] [均值] [标准差] [β] [η] [γ] ← 两个Tab组      ││
  │ │                                                     ││
  │ │  横轴: 1000→5000，每个选中的n一条曲线                 ││
  │ │     ╭─── n=3                                        ││
  │ │    ╱╭─── n=4                                        ││
  │ │   ╱╱─── n=6                                         ││
  │ │  ─────────────────────                              ││
  │ │  1000    3000    5000                               ││
  │ └─────────────────────────────────────────────────────┘│
  └─────────────────────────────────────────────────────────┘

  变量对照

  ┌───────────────────────────┬───────────────────────────────────────────┐
  │      示例1（四变量）      │              示例2（单变量）              │
  ├───────────────────────────┼───────────────────────────────────────────┤
  │ β, η, δ, n 都是变量       │ 只有 n 是变量                             │
  ├───────────────────────────┼───────────────────────────────────────────┤
  │ 参数区可调                │ 参数区只读                                │
  ├───────────────────────────┼───────────────────────────────────────────┤
  │ 蒙特卡洛: 样本量+重复次数 │ 蒙特卡洛: 样本量(多选)+重复次数(切片选择) │
  ├───────────────────────────┼───────────────────────────────────────────┤
  │ 计算配置可调              │ 计算配置只读                              │
  └───────────────────────────┴───────────────────────────────────────────┘

  复用清单

  ┌────────────────┬────────────────┬───────────────┐
  │      组件      │      来源      │     修改      │
  ├────────────────┼────────────────┼───────────────┤
  │ 参数区         │ 示例1          │ 改为只读      │
  ├────────────────┼────────────────┼───────────────┤
  │ 样本量选择器   │ 示例1          │ 复用          │
  ├────────────────┼────────────────┼───────────────┤
  │ 重复次数选择器 │ 新建           │ 样式复用示例1 │
  ├────────────────┼────────────────┼───────────────┤
  │ 计算配置区     │ 示例1          │ 改为只读      │
  ├────────────────┼────────────────┼───────────────┤
  │ 统计表格       │ 示例1          │ 完全复用      │
  ├────────────────┼────────────────┼───────────────┤
  │ 箱型图         │ 示例1          │ 完全复用      │
  ├────────────────┼────────────────┼───────────────┤
  │ 概率密度图     │ 示例1          │ 完全复用      │
  ├────────────────┼────────────────┼───────────────┤
  │ 收敛图         │ 新建 → shared/ │ 新组件        │
  └────────────────┴────────────────┴───────────────┘

  数据生成

  每个样本量 n 运行 5000 次，统计时按 [1000, 2000, 3000, 4000, 5000] 切片计算。

  ---
  这个理解对吗？确认后我开始实现。

❯ 开始实现。最后一个更改，就是箱型图的tab是跟表格一起控制的，跟方法示例1一样的。开始实现吧。   

脚本应该生成csv格式的文件，一个样本量一个csv，怎么命名参考方法示例1的生成的程序