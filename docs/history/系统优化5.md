readme里不用有文档导航了，claude.md里已经有了。就是告诉架构在哪，维护信息在哪，然后我每次让ai了解一下文件夹肯定会先读claude.md，然后在这个里面告诉架构是啥，想要是两个原则1.渐进式披露，大概的可以很少上下文知道，具体想知道啥可以细了解。每次加载尽量少的上下文了解项目。2.这几个文件分工明确，不能有内容重叠，例如架构文档里有扩展开发指南 (Extension Guide)，现在打算是这样。claude.md是最先读取的，这里面是有其它说明文档的位置，还有功能，包括什么时候该读什么。肯定要读的是readme，读完就能了解这是个什么系统，然后剩下的我觉得需要有规则、结构、部署与维护、目标这几个文档，并且要标上01开始的序号排列。先帮我重构吧


还得改一下相关说明文档，就是我在参数估计方法-

B，核心是各列应该统一显示形式，需要的列有：
名称，这里包括序号和名称，例如[c1] MDM方法验证样本 [182-046]
类型 类型也应该统一，就几个类型，一个是抽样样本，一个是真实样本，就分成这两个就行   
数据规模，应该统一，点×组，例如15点×1组 7点×30组。
描述也要统一，就是如果是抽样样本那应该是样本来源 β=2 η=1000 γ=1000（举例），用于xx的验证。就完事了。如果是真实样本就写来源，是什么数据就行。然后tag包括：方法、三/两参数Weibull，以及样本性质（大样本、小样本）如果是真实样本那还要包括完全样本、截断样本等性质。并且把这个统一格式放到该页面代码顶部注释，保证以后新增也是这样的


统一详情页逻辑，就是无论是单组还是多组，都能点进详情页，多组的详情页子案例列表不用有标签 
了，直接给出具体样本的值，操作和去计算按钮和上一级一个风格。单组的话也可以点进详情页，展示 
样本值